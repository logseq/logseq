// Generated by Melange
'use strict';

const Caml_array = require("melange.js/caml_array.js");
const Caml_option = require("melange.js/caml_option.js");
const Curry = require("melange.js/curry.js");
const Stdlib__Array = require("melange/array.js");
const Stdlib__Option = require("melange/option.js");
const Stdlib__String = require("melange/string.js");

function compile(s) {
  return new RegExp(s, "i");
}

function test(t, s) {
  const match = t.exec(s);
  return match !== null;
}

function replace(t, f, s) {
  const result = t.exec(s);
  if (result === null) {
    return s;
  }
  const groups = Stdlib__Array.map((function (prim) {
    if (prim == null) {
      return;
    } else {
      return Caml_option.some(prim);
    }
  }), result);
  const match_ = groups.length ? Stdlib__Option.value(Caml_array.get(groups, 0), "") : "";
  const offset = result.index;
  const input = result.input;
  const rep = Curry._4(f, match_, groups, offset, input);
  return Stdlib__String.sub(s, 0, offset) + (rep + Stdlib__String.sub(s, offset + match_.length | 0, (s.length - offset | 0) - match_.length | 0));
}

module.exports = {
  compile,
  test,
  replace,
}
/* No side effect */
