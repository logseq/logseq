// Generated by Melange
'use strict';

const Caml_string = require("melange.js/caml_string.js");
const Datascript_melange_storage = require("datascript-ocaml-melange.storage/datascript_melange_storage.js");
const Stdlib__List = require("melange/list.js");
const Stdlib__String = require("melange/string.js");

const encode = Datascript_melange_storage.encode;

const decode = Datascript_melange_storage.decode;

function encode_addresses(addrs) {
  return "[" + (Stdlib__String.concat(",", addrs) + "]");
}

function decode_addresses(json) {
  const len = json.length;
  const skip_ws = function (_i) {
    while (true) {
      const i = _i;
      if (!(i < len && (Caml_string.get(json, i) === /* ' ' */32 || Caml_string.get(json, i) === /* '\t' */9 || Caml_string.get(json, i) === /* '\n' */10))) {
        return i;
      }
      _i = i + 1 | 0;
      continue;
    };
  };
  const parse_int = function (_i, _acc) {
    while (true) {
      const acc = _acc;
      const i = _i;
      if (!(i < len && Caml_string.get(json, i) >= /* '0' */48 && Caml_string.get(json, i) <= /* '9' */57)) {
        return [
          acc,
          i
        ];
      }
      _acc = Math.imul(acc, 10) + (Caml_string.get(json, i) - /* '0' */48 | 0) | 0;
      _i = i + 1 | 0;
      continue;
    };
  };
  let _i = 0;
  let _acc = /* [] */ 0;
  while (true) {
    const acc = _acc;
    const i = _i;
    const i$1 = skip_ws(i);
    if (i$1 >= len || Caml_string.get(json, i$1) === /* ']' */93) {
      return Stdlib__List.rev(acc);
    }
    if (Caml_string.get(json, i$1) === /* ',' */44) {
      _i = i$1 + 1 | 0;
      continue;
    }
    const match = parse_int(i$1, 0);
    _acc = {
      hd: String(match[0]),
      tl: acc
    };
    _i = match[1];
    continue;
  };
}

module.exports = {
  encode,
  decode,
  encode_addresses,
  decode_addresses,
}
/* Datascript_melange_storage Not a pure module */
