// Generated by Melange
'use strict';

const Caml_js_exceptions = require("melange.js/caml_js_exceptions.js");
const Caml_option = require("melange.js/caml_option.js");
const Db_worker_effect = require("./db_worker_effect.js");
const Stdlib = require("melange/stdlib.js");
const Stdlib__List = require("melange/list.js");

function get(key) {
  const ls = globalThis.localStorage;
  if (ls !== undefined) {
    return Db_worker_effect.pure(Caml_option.null_to_opt(ls.getItem(key)));
  } else {
    return Db_worker_effect.error(new Caml_js_exceptions.MelangeError(Stdlib.Failure, {
        MEL_EXN_ID: Stdlib.Failure,
        _1: "Idb: localStorage unavailable in this runtime"
      }));
  }
}

function set(key, value) {
  const ls = globalThis.localStorage;
  if (ls !== undefined) {
    ls.setItem(key, value);
    return Db_worker_effect.pure();
  } else {
    return Db_worker_effect.error(new Caml_js_exceptions.MelangeError(Stdlib.Failure, {
        MEL_EXN_ID: Stdlib.Failure,
        _1: "Idb: localStorage unavailable in this runtime"
      }));
  }
}

function $$delete(key) {
  const ls = globalThis.localStorage;
  if (ls !== undefined) {
    ls.removeItem(key);
    return Db_worker_effect.pure();
  } else {
    return Db_worker_effect.error(new Caml_js_exceptions.MelangeError(Stdlib.Failure, {
        MEL_EXN_ID: Stdlib.Failure,
        _1: "Idb: localStorage unavailable in this runtime"
      }));
  }
}

function keys(param) {
  const ls = globalThis.localStorage;
  if (ls === undefined) {
    return Db_worker_effect.error(new Caml_js_exceptions.MelangeError(Stdlib.Failure, {
        MEL_EXN_ID: Stdlib.Failure,
        _1: "Idb: localStorage unavailable in this runtime"
      }));
  }
  const n = ls.length;
  const go = function (_i, _acc) {
    while (true) {
      const acc = _acc;
      const i = _i;
      if (i >= n) {
        return Stdlib__List.rev(acc);
      }
      const k = ls.key(i);
      _acc = k !== null ? ({
          hd: k,
          tl: acc
        }) : acc;
      _i = i + 1 | 0;
      continue;
    };
  };
  return Db_worker_effect.pure(go(0, /* [] */ 0));
}

module.exports = {
  get,
  set,
  $$delete,
  keys,
}
/* Db_worker_effect Not a pure module */
