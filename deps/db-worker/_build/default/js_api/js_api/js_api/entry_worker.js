// Generated by Melange
'use strict';

const Db_worker_effect = require("../runtime/melange/db_worker_effect.js");
const Dispatcher = require("../lib/dispatcher.js");
const Worker_core = require("../lib/worker_core.js");

function init(param) {
  Worker_core.init();
}

function invoke(name, transit_args) {
  Worker_core.init();
  return new Promise((function (resolve, reject) {
      Db_worker_effect.on_any(Worker_core.invoke(name, transit_args), (function (result) {
        resolve(result);
      }), (function (exn) {
        reject(exn);
      }));
    }));
}

const registered = Dispatcher.registered;

module.exports = {
  init,
  invoke,
  registered,
}
/* Db_worker_effect Not a pure module */
