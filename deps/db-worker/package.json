{
  "name": "logseq-db-worker",
  "private": true,
  "description": "npm dependencies for running melange-emitted node tests (see test/dune).",
  "dependencies": {
    "transit-js": "^0.8.874"
  },
  "scripts": {
    "build": "dune build && vite build --mode node && vite build --mode browser"
  }
}