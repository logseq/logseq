# Keep a flatter node_modules layout for tools that still assume hoisted bins and packages.
shamefully-hoist=true

# Match the lockfile import behavior and avoid peer resolution drift across environments.
auto-install-peers=true
