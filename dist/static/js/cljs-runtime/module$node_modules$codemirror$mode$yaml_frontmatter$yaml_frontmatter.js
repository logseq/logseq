shadow$provide.module$node_modules$codemirror$mode$yaml_frontmatter$yaml_frontmatter = function(global, require, module, exports) {
  (function(mod) {
    "object" == typeof exports && "object" == typeof module ? mod(require("module$node_modules$codemirror$lib$codemirror"), require("module$node_modules$codemirror$mode$yaml$yaml")) : "function" == typeof define && define.amd ? define(["../../lib/codemirror", "../yaml/yaml"], mod) : mod(CodeMirror);
  })(function(CodeMirror) {
    CodeMirror.defineMode("yaml-frontmatter", function(config, parserConfig) {
      function localMode(state) {
        return 1 == state.state ? {mode:yamlMode, state:state.yaml} : {mode:innerMode, state:state.inner};
      }
      var yamlMode = CodeMirror.getMode(config, "yaml"), innerMode = CodeMirror.getMode(config, parserConfig && parserConfig.base || "gfm");
      return {startState:function() {
        return {state:0, yaml:null, inner:CodeMirror.startState(innerMode)};
      }, copyState:function(state) {
        return {state:state.state, yaml:state.yaml && CodeMirror.copyState(yamlMode, state.yaml), inner:CodeMirror.copyState(innerMode, state.inner)};
      }, token:function(stream, state) {
        if (0 == state.state) {
          if (stream.match("---", !1)) {
            return state.state = 1, state.yaml = CodeMirror.startState(yamlMode), yamlMode.token(stream, state.yaml);
          }
          state.state = 2;
          return innerMode.token(stream, state.inner);
        }
        if (1 == state.state) {
          var end = stream.sol() && stream.match(/(---|\.\.\.)/, !1);
          stream = yamlMode.token(stream, state.yaml);
          end && (state.state = 2, state.yaml = null);
          return stream;
        }
        return innerMode.token(stream, state.inner);
      }, innerMode:localMode, indent:function(state, a, b) {
        state = localMode(state);
        return state.mode.indent ? state.mode.indent(state.state, a, b) : CodeMirror.Pass;
      }, blankLine:function(state) {
        state = localMode(state);
        if (state.mode.blankLine) {
          return state.mode.blankLine(state.state);
        }
      }};
    });
  });
};

//# sourceMappingURL=module$node_modules$codemirror$mode$yaml_frontmatter$yaml_frontmatter.js.map
