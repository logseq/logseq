shadow$provide.module$node_modules$jszip$lib$crc32 = function(global, require, module, exports) {
  var utils = require("module$node_modules$jszip$lib$utils"), crcTable = function() {
    for (var c, table = [], n = 0; 256 > n; n++) {
      c = n;
      for (var k = 0; 8 > k; k++) {
        c = c & 1 ? 3988292384 ^ c >>> 1 : c >>> 1;
      }
      table[n] = c;
    }
    return table;
  }();
  module.exports = function(input, crc) {
    if ("undefined" === typeof input || !input.length) {
      return 0;
    }
    if ("string" !== utils.getTypeOf(input)) {
      var end = 0 + input.length;
      crc = (crc | 0) ^ -1;
      for (var i = 0; i < end; i++) {
        crc = crc >>> 8 ^ crcTable[(crc ^ input[i]) & 255];
      }
      return crc ^ -1;
    }
    end = 0 + input.length;
    crc = (crc | 0) ^ -1;
    for (i = 0; i < end; i++) {
      crc = crc >>> 8 ^ crcTable[(crc ^ input.charCodeAt(i)) & 255];
    }
    return crc ^ -1;
  };
};

//# sourceMappingURL=module$node_modules$jszip$lib$crc32.js.map
