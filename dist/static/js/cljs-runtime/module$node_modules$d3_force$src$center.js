shadow$provide.module$node_modules$d3_force$src$center = function(global, require, module, exports) {
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, default:{enumerable:!0, get:function() {
    return $$default;
  }}});
  const $$default = function(x, y) {
    function force() {
      var i, n = nodes.length, sx = 0, sy = 0;
      for (i = 0; i < n; ++i) {
        var node = nodes[i];
        sx += node.x;
        sy += node.y;
      }
      sx = (sx / n - x) * strength;
      sy = (sy / n - y) * strength;
      for (i = 0; i < n; ++i) {
        node = nodes[i], node.x -= sx, node.y -= sy;
      }
    }
    var nodes, strength = 1;
    null == x && (x = 0);
    null == y && (y = 0);
    force.initialize = function(_) {
      nodes = _;
    };
    force.x = function(_) {
      return arguments.length ? (x = +_, force) : x;
    };
    force.y = function(_) {
      return arguments.length ? (y = +_, force) : y;
    };
    force.strength = function(_) {
      return arguments.length ? (strength = +_, force) : strength;
    };
    return force;
  };
};

//# sourceMappingURL=module$node_modules$d3_force$src$center.js.map
