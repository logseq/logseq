shadow$provide.module$node_modules$fuse_DOT_js$dist$fuse_common = function(global, require, module, exports) {
  function _typeof(obj) {
    "@babel/helpers - typeof";
    _typeof = "function" === typeof Symbol && "symbol" === typeof Symbol.iterator ? function(obj) {
      return typeof obj;
    } : function(obj) {
      return obj && "function" === typeof Symbol && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj;
    };
    return _typeof(obj);
  }
  function _classCallCheck(instance, Constructor) {
    if (!(instance instanceof Constructor)) {
      throw new TypeError("Cannot call a class as a function");
    }
  }
  function _defineProperties(target, props) {
    for (var i = 0; i < props.length; i++) {
      var descriptor = props[i];
      descriptor.enumerable = descriptor.enumerable || !1;
      descriptor.configurable = !0;
      "value" in descriptor && (descriptor.writable = !0);
      Object.defineProperty(target, descriptor.key, descriptor);
    }
  }
  function _createClass(Constructor, protoProps, staticProps) {
    protoProps && _defineProperties(Constructor.prototype, protoProps);
    staticProps && _defineProperties(Constructor, staticProps);
    return Constructor;
  }
  function _defineProperty(obj, key, value) {
    key in obj ? Object.defineProperty(obj, key, {value, enumerable:!0, configurable:!0, writable:!0}) : obj[key] = value;
    return obj;
  }
  function ownKeys(object, enumerableOnly) {
    var keys = Object.keys(object);
    if (Object.getOwnPropertySymbols) {
      var symbols = Object.getOwnPropertySymbols(object);
      enumerableOnly && (symbols = symbols.filter(function(sym) {
        return Object.getOwnPropertyDescriptor(object, sym).enumerable;
      }));
      keys.push.apply(keys, symbols);
    }
    return keys;
  }
  function _objectSpread2(target) {
    for (var i = 1; i < arguments.length; i++) {
      var source = null != arguments[i] ? arguments[i] : {};
      i % 2 ? ownKeys(Object(source), !0).forEach(function(key) {
        _defineProperty(target, key, source[key]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(target, Object.getOwnPropertyDescriptors(source)) : ownKeys(Object(source)).forEach(function(key) {
        Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key));
      });
    }
    return target;
  }
  function _inherits(subClass, superClass) {
    if ("function" !== typeof superClass && null !== superClass) {
      throw new TypeError("Super expression must either be null or a function");
    }
    subClass.prototype = Object.create(superClass && superClass.prototype, {constructor:{value:subClass, writable:!0, configurable:!0}});
    superClass && _setPrototypeOf(subClass, superClass);
  }
  function _getPrototypeOf(o) {
    _getPrototypeOf = Object.setPrototypeOf ? Object.getPrototypeOf : function(o) {
      return o.__proto__ || Object.getPrototypeOf(o);
    };
    return _getPrototypeOf(o);
  }
  function _setPrototypeOf(o, p) {
    _setPrototypeOf = Object.setPrototypeOf || function(o, p) {
      o.__proto__ = p;
      return o;
    };
    return _setPrototypeOf(o, p);
  }
  function _isNativeReflectConstruct() {
    if ("undefined" === typeof Reflect || !Reflect.construct || Reflect.construct.sham) {
      return !1;
    }
    if ("function" === typeof Proxy) {
      return !0;
    }
    try {
      return Date.prototype.toString.call(Reflect.construct(Date, [], function() {
      })), !0;
    } catch (e) {
      return !1;
    }
  }
  function _createSuper(Derived) {
    var hasNativeReflectConstruct = _isNativeReflectConstruct();
    return function() {
      var Super = _getPrototypeOf(Derived);
      if (hasNativeReflectConstruct) {
        var NewTarget = _getPrototypeOf(this).constructor;
        Super = Reflect.construct(Super, arguments, NewTarget);
      } else {
        Super = Super.apply(this, arguments);
      }
      if (!Super || "object" !== typeof Super && "function" !== typeof Super) {
        if (void 0 === this) {
          throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
        }
        Super = this;
      }
      return Super;
    };
  }
  function _toConsumableArray(arr) {
    var JSCompiler_inline_result = Array.isArray(arr) ? _arrayLikeToArray(arr) : void 0;
    JSCompiler_inline_result || (JSCompiler_inline_result = "undefined" !== typeof Symbol && Symbol.iterator in Object(arr) ? Array.from(arr) : void 0);
    if (!JSCompiler_inline_result) {
      a: {
        if (arr) {
          if ("string" === typeof arr) {
            JSCompiler_inline_result = _arrayLikeToArray(arr, void 0);
            break a;
          }
          JSCompiler_inline_result = Object.prototype.toString.call(arr).slice(8, -1);
          "Object" === JSCompiler_inline_result && arr.constructor && (JSCompiler_inline_result = arr.constructor.name);
          if ("Map" === JSCompiler_inline_result || "Set" === JSCompiler_inline_result) {
            JSCompiler_inline_result = Array.from(arr);
            break a;
          }
          if ("Arguments" === JSCompiler_inline_result || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(JSCompiler_inline_result)) {
            JSCompiler_inline_result = _arrayLikeToArray(arr, void 0);
            break a;
          }
        }
        JSCompiler_inline_result = void 0;
      }
    }
    if (!(arr = JSCompiler_inline_result)) {
      throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
    }
    return arr;
  }
  function _arrayLikeToArray(arr, len) {
    if (null == len || len > arr.length) {
      len = arr.length;
    }
    for (var i = 0, arr2 = Array(len); i < len; i++) {
      arr2[i] = arr[i];
    }
    return arr2;
  }
  function isArray(value) {
    return Array.isArray ? Array.isArray(value) : "[object Array]" === getTag(value);
  }
  function isString(value) {
    return "string" === typeof value;
  }
  function isDefined(value) {
    return void 0 !== value && null !== value;
  }
  function getTag(value) {
    return null == value ? void 0 === value ? "[object Undefined]" : "[object Null]" : Object.prototype.toString.call(value);
  }
  function createKey(key) {
    var weight = 1;
    if (isString(key) || isArray(key)) {
      var src = key;
      var path = createKeyPath(key);
      key = createKeyId(key);
    } else {
      if (!hasOwn.call(key, "name")) {
        throw Error("Missing ".concat("name", " property in key"));
      }
      var name = key.name;
      src = name;
      if (hasOwn.call(key, "weight") && (weight = key.weight, 0 >= weight)) {
        throw Error("Property 'weight' in key '".concat(name, "' must be a positive integer"));
      }
      path = createKeyPath(name);
      key = createKeyId(name);
    }
    return {path, id:key, weight, src};
  }
  function createKeyPath(key) {
    return isArray(key) ? key : key.split(".");
  }
  function createKeyId(key) {
    return isArray(key) ? key.join(".") : key;
  }
  function norm() {
    var mantissa = 0 < arguments.length && void 0 !== arguments[0] ? arguments[0] : 3, cache = new Map(), m = Math.pow(10, mantissa);
    return {get:function(value) {
      value = value.match(SPACE).length;
      if (cache.has(value)) {
        return cache.get(value);
      }
      var n = parseFloat(Math.round(1 / Math.sqrt(value) * m) / m);
      cache.set(value, n);
      return n;
    }, clear:function() {
      cache.clear();
    }};
  }
  function createIndex(keys, docs) {
    var _ref2$getFn = (2 < arguments.length && void 0 !== arguments[2] ? arguments[2] : {}).getFn;
    _ref2$getFn = new FuseIndex({getFn:void 0 === _ref2$getFn ? Config.getFn : _ref2$getFn});
    _ref2$getFn.setKeys(keys.map(createKey));
    _ref2$getFn.setSources(docs);
    _ref2$getFn.create();
    return _ref2$getFn;
  }
  function computeScore(pattern) {
    var _ref = 1 < arguments.length && void 0 !== arguments[1] ? arguments[1] : {}, _ref$errors = _ref.errors, _ref$currentLocation = _ref.currentLocation, _ref$expectedLocation = _ref.expectedLocation, _ref$distance = _ref.distance;
    _ref$distance = void 0 === _ref$distance ? Config.distance : _ref$distance;
    _ref = _ref.ignoreLocation;
    _ref$errors = (void 0 === _ref$errors ? 0 : _ref$errors) / pattern.length;
    if (void 0 === _ref ? Config.ignoreLocation : _ref) {
      return _ref$errors;
    }
    _ref$currentLocation = Math.abs((void 0 === _ref$expectedLocation ? 0 : _ref$expectedLocation) - (void 0 === _ref$currentLocation ? 0 : _ref$currentLocation));
    return _ref$distance ? _ref$errors + _ref$currentLocation / _ref$distance : _ref$currentLocation ? 1.0 : _ref$errors;
  }
  function convertMaskToIndices() {
    for (var matchmask = 0 < arguments.length && void 0 !== arguments[0] ? arguments[0] : [], minMatchCharLength = 1 < arguments.length && void 0 !== arguments[1] ? arguments[1] : Config.minMatchCharLength, indices = [], start = -1, end, i = 0, len = matchmask.length; i < len; i += 1) {
      (end = matchmask[i]) && -1 === start ? start = i : end || -1 === start || (end = i - 1, end - start + 1 >= minMatchCharLength && indices.push([start, end]), start = -1);
    }
    matchmask[i - 1] && i - start >= minMatchCharLength && indices.push([start, i - 1]);
    return indices;
  }
  function search(text, pattern, patternAlphabet) {
    var _ref = 3 < arguments.length && void 0 !== arguments[3] ? arguments[3] : {}, _ref$location = _ref.location, location = void 0 === _ref$location ? Config.location : _ref$location;
    _ref$location = _ref.distance;
    _ref$location = void 0 === _ref$location ? Config.distance : _ref$location;
    var _ref$threshold = _ref.threshold, threshold = void 0 === _ref$threshold ? Config.threshold : _ref$threshold;
    _ref$threshold = _ref.findAllMatches;
    _ref$threshold = void 0 === _ref$threshold ? Config.findAllMatches : _ref$threshold;
    var _ref$minMatchCharLeng = _ref.minMatchCharLength;
    _ref$minMatchCharLeng = void 0 === _ref$minMatchCharLeng ? Config.minMatchCharLength : _ref$minMatchCharLeng;
    var _ref$includeMatches = _ref.includeMatches;
    _ref$includeMatches = void 0 === _ref$includeMatches ? Config.includeMatches : _ref$includeMatches;
    _ref = _ref.ignoreLocation;
    _ref = void 0 === _ref ? Config.ignoreLocation : _ref;
    if (32 < pattern.length) {
      throw Error("Pattern length exceeds max of ".concat(32, "."));
    }
    var patternLen = pattern.length, textLen = text.length;
    location = Math.max(0, Math.min(location, textLen));
    for (var currentThreshold = threshold, bestLocation = location, matchMask = (threshold = 1 < _ref$minMatchCharLeng || _ref$includeMatches) ? Array(textLen) : [], index; -1 < (index = text.indexOf(pattern, bestLocation));) {
      if (bestLocation = computeScore(pattern, {currentLocation:index, expectedLocation:location, distance:_ref$location, ignoreLocation:_ref}), currentThreshold = Math.min(bestLocation, currentThreshold), bestLocation = index + patternLen, threshold) {
        for (var i = 0; i < patternLen;) {
          matchMask[index + i] = 1, i += 1;
        }
      }
    }
    bestLocation = -1;
    index = [];
    i = 1;
    for (var binMax = patternLen + textLen, mask = 1 << patternLen - 1, _i = 0; _i < patternLen; _i += 1) {
      for (var binMin = 0, binMid = binMax; binMin < binMid;) {
        computeScore(pattern, {errors:_i, currentLocation:location + binMid, expectedLocation:location, distance:_ref$location, ignoreLocation:_ref}) <= currentThreshold ? binMin = binMid : binMax = binMid, binMid = Math.floor((binMax - binMin) / 2 + binMin);
      }
      binMax = binMid;
      binMin = Math.max(1, location - binMid + 1);
      var finish = _ref$threshold ? textLen : Math.min(location + binMid, textLen) + patternLen;
      binMid = Array(finish + 2);
      for (binMid[finish + 1] = (1 << _i) - 1; finish >= binMin; --finish) {
        var currentLocation = finish - 1, charMatch = patternAlphabet[text.charAt(currentLocation)];
        threshold && (matchMask[currentLocation] = +!!charMatch);
        binMid[finish] = (binMid[finish + 1] << 1 | 1) & charMatch;
        _i && (binMid[finish] = binMid[finish] | (index[finish + 1] | index[finish]) << 1 | 1 | index[finish + 1]);
        if (binMid[finish] & mask && (i = computeScore(pattern, {errors:_i, currentLocation, expectedLocation:location, distance:_ref$location, ignoreLocation:_ref}), i <= currentThreshold)) {
          currentThreshold = i;
          bestLocation = currentLocation;
          if (bestLocation <= location) {
            break;
          }
          binMin = Math.max(1, 2 * location - bestLocation);
        }
      }
      if (computeScore(pattern, {errors:_i + 1, currentLocation:location, expectedLocation:location, distance:_ref$location, ignoreLocation:_ref}) > currentThreshold) {
        break;
      }
      index = binMid;
    }
    _ref$location = {isMatch:0 <= bestLocation, score:Math.max(0.001, i)};
    threshold && ((_ref$threshold = convertMaskToIndices(matchMask, _ref$minMatchCharLeng), _ref$threshold.length) ? _ref$includeMatches && (_ref$location.indices = _ref$threshold) : _ref$location.isMatch = !1);
    return _ref$location;
  }
  function getMatch(pattern, exp) {
    return (pattern = pattern.match(exp)) ? pattern[1] : null;
  }
  function parseQuery(pattern) {
    var options = 1 < arguments.length && void 0 !== arguments[1] ? arguments[1] : {};
    return pattern.split("|").map(function(item) {
      item = item.trim().split(SPACE_RE).filter(function(item) {
        return item && !!item.trim();
      });
      for (var results = [], i = 0, len = item.length; i < len; i += 1) {
        for (var queryItem = item[i], found = !1, idx = -1; !found && ++idx < searchersLen;) {
          var searcher = searchers[idx], token = searcher.isMultiMatch(queryItem);
          token && (results.push(new searcher(token, options)), found = !0);
        }
        if (!found) {
          for (idx = -1; ++idx < searchersLen;) {
            if (found = searchers[idx], searcher = found.isSingleMatch(queryItem)) {
              results.push(new found(searcher, options));
              break;
            }
          }
        }
      }
      return results;
    });
  }
  function createSearcher(pattern, options) {
    for (var i = 0, len = registeredSearchers.length; i < len; i += 1) {
      var searcherClass = registeredSearchers[i];
      if (searcherClass.condition(pattern, options)) {
        return new searcherClass(pattern, options);
      }
    }
    return new BitapSearch(pattern, options);
  }
  function parse(query, options) {
    var _ref3$auto = (2 < arguments.length && void 0 !== arguments[2] ? arguments[2] : {}).auto, auto = void 0 === _ref3$auto ? !0 : _ref3$auto;
    query[JSCompiler_object_inline_AND_5451] || query[JSCompiler_object_inline_OR_5452] || (query = convertToExplicit(query));
    return function next(query) {
      var keys = Object.keys(query), isQueryPath = !!query[KeyType.PATH];
      if (!isQueryPath && 1 < keys.length && !query[JSCompiler_object_inline_AND_5451] && !query[JSCompiler_object_inline_OR_5452]) {
        return next(convertToExplicit(query));
      }
      if (isLeaf(query)) {
        keys = isQueryPath ? query[KeyType.PATH] : keys[0];
        isQueryPath = isQueryPath ? query[KeyType.PATTERN] : query[keys];
        if (!isString(isQueryPath)) {
          throw Error("Invalid value for key ".concat(keys));
        }
        keys = {keyId:createKeyId(keys), pattern:isQueryPath};
        auto && (keys.searcher = createSearcher(isQueryPath, options));
        return keys;
      }
      var node = {children:[], operator:keys[0]};
      keys.forEach(function(key) {
        key = query[key];
        isArray(key) && key.forEach(function(item) {
          node.children.push(next(item));
        });
      });
      return node;
    }(query);
  }
  function computeScore$1(results, _ref) {
    _ref = _ref.ignoreFieldNorm;
    var ignoreFieldNorm = void 0 === _ref ? Config.ignoreFieldNorm : _ref;
    results.forEach(function(result) {
      var totalScore = 1;
      result.matches.forEach(function(_ref2) {
        var key = _ref2.key, norm = _ref2.norm;
        _ref2 = _ref2.score;
        key = key ? key.weight : null;
        totalScore *= Math.pow(0 === _ref2 && key ? Number.EPSILON : _ref2, (key || 1) * (ignoreFieldNorm ? 1 : norm));
      });
      result.score = totalScore;
    });
  }
  function transformMatches(result, data) {
    result = result.matches;
    data.matches = [];
    isDefined(result) && result.forEach(function(match) {
      if (isDefined(match.indices) && match.indices.length) {
        var obj = {indices:match.indices, value:match.value};
        match.key && (obj.key = match.key.src);
        -1 < match.idx && (obj.refIndex = match.idx);
        data.matches.push(obj);
      }
    });
  }
  function transformScore(result, data) {
    data.score = result.score;
  }
  function format(results, docs) {
    var _ref = 2 < arguments.length && void 0 !== arguments[2] ? arguments[2] : {}, _ref$includeMatches = _ref.includeMatches;
    _ref = _ref.includeScore;
    _ref = void 0 === _ref ? Config.includeScore : _ref;
    var transformers = [];
    (void 0 === _ref$includeMatches ? Config.includeMatches : _ref$includeMatches) && transformers.push(transformMatches);
    _ref && transformers.push(transformScore);
    return results.map(function(result) {
      var idx = result.idx, data = {item:docs[idx], refIndex:idx};
      transformers.length && transformers.forEach(function(transformer) {
        transformer(result, data);
      });
      return data;
    });
  }
  var INFINITY = 1 / 0, hasOwn = Object.prototype.hasOwnProperty, KeyStore = function() {
    function KeyStore(keys) {
      var _this = this;
      _classCallCheck(this, KeyStore);
      this._keys = [];
      this._keyMap = {};
      var totalWeight = 0;
      keys.forEach(function(key) {
        key = createKey(key);
        totalWeight += key.weight;
        _this._keys.push(key);
        _this._keyMap[key.id] = key;
        totalWeight += key.weight;
      });
      this._keys.forEach(function(key) {
        key.weight /= totalWeight;
      });
    }
    _createClass(KeyStore, [{key:"get", value:function(keyId) {
      return this._keyMap[keyId];
    }}, {key:"keys", value:function() {
      return this._keys;
    }}, {key:"toJSON", value:function() {
      return JSON.stringify(this._keys);
    }}]);
    return KeyStore;
  }(), Config = _objectSpread2({}, {isCaseSensitive:!1, includeScore:!1, keys:[], shouldSort:!0, sortFn:function(a, b) {
    return a.score === b.score ? a.idx < b.idx ? -1 : 1 : a.score < b.score ? -1 : 1;
  }}, {}, {includeMatches:!1, findAllMatches:!1, minMatchCharLength:1}, {}, {location:0, threshold:0.6, distance:100}, {}, {useExtendedSearch:!1, getFn:function(obj, path) {
    var list = [], arr = !1;
    (function deepGet(obj, path, index) {
      if (isDefined(obj)) {
        if (path[index]) {
          if (obj = obj[path[index]], isDefined(obj)) {
            var JSCompiler_temp;
            if (JSCompiler_temp = index === path.length - 1) {
              (JSCompiler_temp = isString(obj) || "number" === typeof obj) || (JSCompiler_temp = !0 === obj || !1 === obj) || (JSCompiler_temp = "object" === _typeof(obj) && null !== obj && "[object Boolean]" == getTag(obj));
            }
            if (JSCompiler_temp) {
              path = list.push, null == obj ? obj = "" : "string" != typeof obj && (index = obj + "", obj = "0" == index && 1 / obj == -INFINITY ? "-0" : index), path.call(list, obj);
            } else if (isArray(obj)) {
              arr = !0;
              JSCompiler_temp = 0;
              for (var len = obj.length; JSCompiler_temp < len; JSCompiler_temp += 1) {
                deepGet(obj[JSCompiler_temp], path, index + 1);
              }
            } else {
              path.length && deepGet(obj, path, index + 1);
            }
          }
        } else {
          list.push(obj);
        }
      }
    })(obj, isString(path) ? path.split(".") : path, 0);
    return arr ? list : list[0];
  }, ignoreLocation:!1, ignoreFieldNorm:!1}), SPACE = /[^ ]+/g, FuseIndex = function() {
    function FuseIndex() {
      var _ref$getFn = (0 < arguments.length && void 0 !== arguments[0] ? arguments[0] : {}).getFn;
      _ref$getFn = void 0 === _ref$getFn ? Config.getFn : _ref$getFn;
      _classCallCheck(this, FuseIndex);
      this.norm = norm(3);
      this.getFn = _ref$getFn;
      this.isCreated = !1;
      this.setIndexRecords();
    }
    _createClass(FuseIndex, [{key:"setSources", value:function() {
      this.docs = 0 < arguments.length && void 0 !== arguments[0] ? arguments[0] : [];
    }}, {key:"setIndexRecords", value:function() {
      this.records = 0 < arguments.length && void 0 !== arguments[0] ? arguments[0] : [];
    }}, {key:"setKeys", value:function() {
      var _this = this, keys = 0 < arguments.length && void 0 !== arguments[0] ? arguments[0] : [];
      this.keys = keys;
      this._keysMap = {};
      keys.forEach(function(key, idx) {
        _this._keysMap[key.id] = idx;
      });
    }}, {key:"create", value:function() {
      var _this2 = this;
      !this.isCreated && this.docs.length && (this.isCreated = !0, isString(this.docs[0]) ? this.docs.forEach(function(doc, docIndex) {
        _this2._addString(doc, docIndex);
      }) : this.docs.forEach(function(doc, docIndex) {
        _this2._addObject(doc, docIndex);
      }), this.norm.clear());
    }}, {key:"add", value:function(doc) {
      var idx = this.size();
      isString(doc) ? this._addString(doc, idx) : this._addObject(doc, idx);
    }}, {key:"removeAt", value:function(idx) {
      this.records.splice(idx, 1);
      for (var len = this.size(); idx < len; idx += 1) {
        --this.records[idx].i;
      }
    }}, {key:"getValueForItemAtKeyId", value:function(item, keyId) {
      return item[this._keysMap[keyId]];
    }}, {key:"size", value:function() {
      return this.records.length;
    }}, {key:"_addString", value:function(doc, docIndex) {
      isDefined(doc) && doc.trim().length && (doc = {v:doc, i:docIndex, n:this.norm.get(doc)}, this.records.push(doc));
    }}, {key:"_addObject", value:function(doc, docIndex) {
      var _this3 = this, record = {i:docIndex, $:{}};
      this.keys.forEach(function(key, keyIndex) {
        var value = _this3.getFn(doc, key.path);
        isDefined(value) && (isArray(value) ? function() {
          for (var subRecords = [], stack = [{nestedArrIndex:-1, value}]; stack.length;) {
            var _stack$pop = stack.pop(), nestedArrIndex = _stack$pop.nestedArrIndex;
            _stack$pop = _stack$pop.value;
            isDefined(_stack$pop) && (isString(_stack$pop) && _stack$pop.trim().length ? (nestedArrIndex = {v:_stack$pop, i:nestedArrIndex, n:_this3.norm.get(_stack$pop)}, subRecords.push(nestedArrIndex)) : isArray(_stack$pop) && _stack$pop.forEach(function(item, k) {
              stack.push({nestedArrIndex:k, value:item});
            }));
          }
          record.$[keyIndex] = subRecords;
        }() : value.trim().length && (key = {v:value, n:_this3.norm.get(value)}, record.$[keyIndex] = key));
      });
      this.records.push(record);
    }}, {key:"toJSON", value:function() {
      return {keys:this.keys, records:this.records};
    }}]);
    return FuseIndex;
  }(), BitapSearch = function() {
    function BitapSearch(pattern) {
      var _this = this, _ref = 1 < arguments.length && void 0 !== arguments[1] ? arguments[1] : {}, _ref$location = _ref.location;
      _ref$location = void 0 === _ref$location ? Config.location : _ref$location;
      var _ref$threshold = _ref.threshold;
      _ref$threshold = void 0 === _ref$threshold ? Config.threshold : _ref$threshold;
      var _ref$distance = _ref.distance;
      _ref$distance = void 0 === _ref$distance ? Config.distance : _ref$distance;
      var _ref$includeMatches = _ref.includeMatches;
      _ref$includeMatches = void 0 === _ref$includeMatches ? Config.includeMatches : _ref$includeMatches;
      var _ref$findAllMatches = _ref.findAllMatches;
      _ref$findAllMatches = void 0 === _ref$findAllMatches ? Config.findAllMatches : _ref$findAllMatches;
      var _ref$minMatchCharLeng = _ref.minMatchCharLength;
      _ref$minMatchCharLeng = void 0 === _ref$minMatchCharLeng ? Config.minMatchCharLength : _ref$minMatchCharLeng;
      var _ref$isCaseSensitive = _ref.isCaseSensitive;
      _ref$isCaseSensitive = void 0 === _ref$isCaseSensitive ? Config.isCaseSensitive : _ref$isCaseSensitive;
      _ref = _ref.ignoreLocation;
      _ref = void 0 === _ref ? Config.ignoreLocation : _ref;
      _classCallCheck(this, BitapSearch);
      this.options = {location:_ref$location, threshold:_ref$threshold, distance:_ref$distance, includeMatches:_ref$includeMatches, findAllMatches:_ref$findAllMatches, minMatchCharLength:_ref$minMatchCharLeng, isCaseSensitive:_ref$isCaseSensitive, ignoreLocation:_ref};
      this.pattern = _ref$isCaseSensitive ? pattern : pattern.toLowerCase();
      this.chunks = [];
      if (this.pattern.length) {
        if (_ref$location = function(pattern, startIndex) {
          for (var JSCompiler_temp_const = _this.chunks, JSCompiler_temp_const$jscomp$0 = JSCompiler_temp_const.push, mask = {}, i = 0, len = pattern.length; i < len; i += 1) {
            var char = pattern.charAt(i);
            mask[char] = (mask[char] || 0) | 1 << len - i - 1;
          }
          JSCompiler_temp_const$jscomp$0.call(JSCompiler_temp_const, {pattern, alphabet:mask, startIndex});
        }, _ref = this.pattern.length, 32 < _ref) {
          _ref$threshold = 0;
          _ref$distance = _ref % 32;
          for (_ref$includeMatches = _ref - _ref$distance; _ref$threshold < _ref$includeMatches;) {
            _ref$location(this.pattern.substr(_ref$threshold, 32), _ref$threshold), _ref$threshold += 32;
          }
          _ref$distance && (_ref -= 32, _ref$location(this.pattern.substr(_ref), _ref));
        } else {
          _ref$location(this.pattern, 0);
        }
      }
    }
    _createClass(BitapSearch, [{key:"searchIn", value:function(text) {
      var _this$options = this.options, includeMatches = _this$options.includeMatches;
      _this$options.isCaseSensitive || (text = text.toLowerCase());
      if (this.pattern === text) {
        return _this$options = {isMatch:!0, score:0}, includeMatches && (_this$options.indices = [[0, text.length - 1]]), _this$options;
      }
      _this$options = this.options;
      var location = _this$options.location, distance = _this$options.distance, threshold = _this$options.threshold, findAllMatches = _this$options.findAllMatches, minMatchCharLength = _this$options.minMatchCharLength, ignoreLocation = _this$options.ignoreLocation, allIndices = [], totalScore = 0, hasMatches = !1;
      this.chunks.forEach(function(_ref2) {
        var _search = search(text, _ref2.pattern, _ref2.alphabet, {location:location + _ref2.startIndex, distance, threshold, findAllMatches, minMatchCharLength, includeMatches, ignoreLocation});
        _ref2 = _search.isMatch;
        var score = _search.score;
        _search = _search.indices;
        _ref2 && (hasMatches = !0);
        totalScore += score;
        _ref2 && _search && (allIndices = [].concat(_toConsumableArray(allIndices), _toConsumableArray(_search)));
      });
      _this$options = {isMatch:hasMatches, score:hasMatches ? totalScore / this.chunks.length : 1};
      hasMatches && includeMatches && (_this$options.indices = allIndices);
      return _this$options;
    }}]);
    return BitapSearch;
  }(), BaseMatch = function() {
    function BaseMatch(pattern) {
      _classCallCheck(this, BaseMatch);
      this.pattern = pattern;
    }
    _createClass(BaseMatch, [{key:"search", value:function() {
    }}], [{key:"isMultiMatch", value:function(pattern) {
      return getMatch(pattern, this.multiRegex);
    }}, {key:"isSingleMatch", value:function(pattern) {
      return getMatch(pattern, this.singleRegex);
    }}]);
    return BaseMatch;
  }();
  global = function(_BaseMatch) {
    function ExactMatch(pattern) {
      _classCallCheck(this, ExactMatch);
      return _super.call(this, pattern);
    }
    _inherits(ExactMatch, _BaseMatch);
    var _super = _createSuper(ExactMatch);
    _createClass(ExactMatch, [{key:"search", value:function(text) {
      text = text === this.pattern;
      return {isMatch:text, score:text ? 0 : 1, indices:[0, this.pattern.length - 1]};
    }}], [{key:"type", get:function() {
      return "exact";
    }}, {key:"multiRegex", get:function() {
      return /^="(.*)"$/;
    }}, {key:"singleRegex", get:function() {
      return /^=(.*)$/;
    }}]);
    return ExactMatch;
  }(BaseMatch);
  require = function(_BaseMatch) {
    function InverseExactMatch(pattern) {
      _classCallCheck(this, InverseExactMatch);
      return _super.call(this, pattern);
    }
    _inherits(InverseExactMatch, _BaseMatch);
    var _super = _createSuper(InverseExactMatch);
    _createClass(InverseExactMatch, [{key:"search", value:function(text) {
      var isMatch = -1 === text.indexOf(this.pattern);
      return {isMatch, score:isMatch ? 0 : 1, indices:[0, text.length - 1]};
    }}], [{key:"type", get:function() {
      return "inverse-exact";
    }}, {key:"multiRegex", get:function() {
      return /^!"(.*)"$/;
    }}, {key:"singleRegex", get:function() {
      return /^!(.*)$/;
    }}]);
    return InverseExactMatch;
  }(BaseMatch);
  exports = function(_BaseMatch) {
    function PrefixExactMatch(pattern) {
      _classCallCheck(this, PrefixExactMatch);
      return _super.call(this, pattern);
    }
    _inherits(PrefixExactMatch, _BaseMatch);
    var _super = _createSuper(PrefixExactMatch);
    _createClass(PrefixExactMatch, [{key:"search", value:function(text) {
      text = text.startsWith(this.pattern);
      return {isMatch:text, score:text ? 0 : 1, indices:[0, this.pattern.length - 1]};
    }}], [{key:"type", get:function() {
      return "prefix-exact";
    }}, {key:"multiRegex", get:function() {
      return /^\^"(.*)"$/;
    }}, {key:"singleRegex", get:function() {
      return /^\^(.*)$/;
    }}]);
    return PrefixExactMatch;
  }(BaseMatch);
  var InversePrefixExactMatch = function(_BaseMatch) {
    function InversePrefixExactMatch(pattern) {
      _classCallCheck(this, InversePrefixExactMatch);
      return _super.call(this, pattern);
    }
    _inherits(InversePrefixExactMatch, _BaseMatch);
    var _super = _createSuper(InversePrefixExactMatch);
    _createClass(InversePrefixExactMatch, [{key:"search", value:function(text) {
      var isMatch = !text.startsWith(this.pattern);
      return {isMatch, score:isMatch ? 0 : 1, indices:[0, text.length - 1]};
    }}], [{key:"type", get:function() {
      return "inverse-prefix-exact";
    }}, {key:"multiRegex", get:function() {
      return /^!\^"(.*)"$/;
    }}, {key:"singleRegex", get:function() {
      return /^!\^(.*)$/;
    }}]);
    return InversePrefixExactMatch;
  }(BaseMatch), SuffixExactMatch = function(_BaseMatch) {
    function SuffixExactMatch(pattern) {
      _classCallCheck(this, SuffixExactMatch);
      return _super.call(this, pattern);
    }
    _inherits(SuffixExactMatch, _BaseMatch);
    var _super = _createSuper(SuffixExactMatch);
    _createClass(SuffixExactMatch, [{key:"search", value:function(text) {
      var isMatch = text.endsWith(this.pattern);
      return {isMatch, score:isMatch ? 0 : 1, indices:[text.length - this.pattern.length, text.length - 1]};
    }}], [{key:"type", get:function() {
      return "suffix-exact";
    }}, {key:"multiRegex", get:function() {
      return /^"(.*)"\$$/;
    }}, {key:"singleRegex", get:function() {
      return /^(.*)\$$/;
    }}]);
    return SuffixExactMatch;
  }(BaseMatch), InverseSuffixExactMatch = function(_BaseMatch) {
    function InverseSuffixExactMatch(pattern) {
      _classCallCheck(this, InverseSuffixExactMatch);
      return _super.call(this, pattern);
    }
    _inherits(InverseSuffixExactMatch, _BaseMatch);
    var _super = _createSuper(InverseSuffixExactMatch);
    _createClass(InverseSuffixExactMatch, [{key:"search", value:function(text) {
      var isMatch = !text.endsWith(this.pattern);
      return {isMatch, score:isMatch ? 0 : 1, indices:[0, text.length - 1]};
    }}], [{key:"type", get:function() {
      return "inverse-suffix-exact";
    }}, {key:"multiRegex", get:function() {
      return /^!"(.*)"\$$/;
    }}, {key:"singleRegex", get:function() {
      return /^!(.*)\$$/;
    }}]);
    return InverseSuffixExactMatch;
  }(BaseMatch), FuzzyMatch = function(_BaseMatch) {
    function FuzzyMatch(pattern) {
      var _this = 1 < arguments.length && void 0 !== arguments[1] ? arguments[1] : {};
      var _ref$location = _this.location;
      _ref$location = void 0 === _ref$location ? Config.location : _ref$location;
      var _ref$threshold = _this.threshold;
      _ref$threshold = void 0 === _ref$threshold ? Config.threshold : _ref$threshold;
      var _ref$distance = _this.distance;
      _ref$distance = void 0 === _ref$distance ? Config.distance : _ref$distance;
      var _ref$includeMatches = _this.includeMatches;
      _ref$includeMatches = void 0 === _ref$includeMatches ? Config.includeMatches : _ref$includeMatches;
      var _ref$findAllMatches = _this.findAllMatches;
      _ref$findAllMatches = void 0 === _ref$findAllMatches ? Config.findAllMatches : _ref$findAllMatches;
      var _ref$minMatchCharLeng = _this.minMatchCharLength;
      _ref$minMatchCharLeng = void 0 === _ref$minMatchCharLeng ? Config.minMatchCharLength : _ref$minMatchCharLeng;
      var _ref$isCaseSensitive = _this.isCaseSensitive;
      _ref$isCaseSensitive = void 0 === _ref$isCaseSensitive ? Config.isCaseSensitive : _ref$isCaseSensitive;
      _this = _this.ignoreLocation;
      var ignoreLocation = void 0 === _this ? Config.ignoreLocation : _this;
      _classCallCheck(this, FuzzyMatch);
      _this = _super.call(this, pattern);
      _this._bitapSearch = new BitapSearch(pattern, {location:_ref$location, threshold:_ref$threshold, distance:_ref$distance, includeMatches:_ref$includeMatches, findAllMatches:_ref$findAllMatches, minMatchCharLength:_ref$minMatchCharLeng, isCaseSensitive:_ref$isCaseSensitive, ignoreLocation});
      return _this;
    }
    _inherits(FuzzyMatch, _BaseMatch);
    var _super = _createSuper(FuzzyMatch);
    _createClass(FuzzyMatch, [{key:"search", value:function(text) {
      return this._bitapSearch.searchIn(text);
    }}], [{key:"type", get:function() {
      return "fuzzy";
    }}, {key:"multiRegex", get:function() {
      return /^"(.*)"$/;
    }}, {key:"singleRegex", get:function() {
      return /^(.*)$/;
    }}]);
    return FuzzyMatch;
  }(BaseMatch);
  BaseMatch = function(_BaseMatch) {
    function IncludeMatch(pattern) {
      _classCallCheck(this, IncludeMatch);
      return _super.call(this, pattern);
    }
    _inherits(IncludeMatch, _BaseMatch);
    var _super = _createSuper(IncludeMatch);
    _createClass(IncludeMatch, [{key:"search", value:function(text) {
      for (var location = 0, index, indices = [], patternLen = this.pattern.length; -1 < (index = text.indexOf(this.pattern, location));) {
        location = index + patternLen, indices.push([index, location - 1]);
      }
      text = !!indices.length;
      return {isMatch:text, score:text ? 0 : 1, indices};
    }}], [{key:"type", get:function() {
      return "include";
    }}, {key:"multiRegex", get:function() {
      return /^'"(.*)"$/;
    }}, {key:"singleRegex", get:function() {
      return /^'(.*)$/;
    }}]);
    return IncludeMatch;
  }(BaseMatch);
  var searchers = [global, BaseMatch, exports, InversePrefixExactMatch, InverseSuffixExactMatch, SuffixExactMatch, require, FuzzyMatch], searchersLen = searchers.length, SPACE_RE = / +(?=([^"]*"[^"]*")*[^"]*$)/, MultiMatchSet = new Set([FuzzyMatch.type, BaseMatch.type]);
  global = function() {
    function ExtendedSearch(pattern) {
      var _ref = 1 < arguments.length && void 0 !== arguments[1] ? arguments[1] : {}, _ref$isCaseSensitive = _ref.isCaseSensitive;
      _ref$isCaseSensitive = void 0 === _ref$isCaseSensitive ? Config.isCaseSensitive : _ref$isCaseSensitive;
      var _ref$includeMatches = _ref.includeMatches;
      _ref$includeMatches = void 0 === _ref$includeMatches ? Config.includeMatches : _ref$includeMatches;
      var _ref$minMatchCharLeng = _ref.minMatchCharLength;
      _ref$minMatchCharLeng = void 0 === _ref$minMatchCharLeng ? Config.minMatchCharLength : _ref$minMatchCharLeng;
      var _ref$ignoreLocation = _ref.ignoreLocation;
      _ref$ignoreLocation = void 0 === _ref$ignoreLocation ? Config.ignoreLocation : _ref$ignoreLocation;
      var _ref$findAllMatches = _ref.findAllMatches;
      _ref$findAllMatches = void 0 === _ref$findAllMatches ? Config.findAllMatches : _ref$findAllMatches;
      var _ref$location = _ref.location;
      _ref$location = void 0 === _ref$location ? Config.location : _ref$location;
      var _ref$threshold = _ref.threshold;
      _ref$threshold = void 0 === _ref$threshold ? Config.threshold : _ref$threshold;
      _ref = _ref.distance;
      _ref = void 0 === _ref ? Config.distance : _ref;
      _classCallCheck(this, ExtendedSearch);
      this.query = null;
      this.options = {isCaseSensitive:_ref$isCaseSensitive, includeMatches:_ref$includeMatches, minMatchCharLength:_ref$minMatchCharLeng, findAllMatches:_ref$findAllMatches, ignoreLocation:_ref$ignoreLocation, location:_ref$location, threshold:_ref$threshold, distance:_ref};
      this.pattern = _ref$isCaseSensitive ? pattern : pattern.toLowerCase();
      this.query = parseQuery(this.pattern, this.options);
    }
    _createClass(ExtendedSearch, [{key:"searchIn", value:function(text) {
      var query = this.query;
      if (!query) {
        return {isMatch:!1, score:1};
      }
      var _this$options = this.options, includeMatches = _this$options.includeMatches;
      text = _this$options.isCaseSensitive ? text : text.toLowerCase();
      var numMatches;
      _this$options = [];
      for (var totalScore = 0, i = 0, qLen = query.length; i < qLen; i += 1) {
        for (var searchers = query[i], j = numMatches = _this$options.length = 0, pLen = searchers.length; j < pLen; j += 1) {
          var searcher = searchers[j], _searcher$search = searcher.search(text), indices = _searcher$search.indices, score = _searcher$search.score;
          if (_searcher$search.isMatch) {
            numMatches += 1, totalScore += score, includeMatches && (MultiMatchSet.has(searcher.constructor.type) ? _this$options = [].concat(_toConsumableArray(_this$options), _toConsumableArray(indices)) : _this$options.push(indices));
          } else {
            numMatches = totalScore = 0;
            _this$options.length = 0;
            break;
          }
        }
        if (numMatches) {
          return text = {isMatch:!0, score:totalScore / numMatches}, includeMatches && (text.indices = _this$options), text;
        }
      }
      return {isMatch:!1, score:1};
    }}], [{key:"condition", value:function(_, options) {
      return options.useExtendedSearch;
    }}]);
    return ExtendedSearch;
  }();
  var registeredSearchers = [], JSCompiler_object_inline_AND_5451 = "$and", JSCompiler_object_inline_OR_5452 = "$or", KeyType = {PATH:"$path", PATTERN:"$val"}, isLeaf = function(query) {
    return !isArray(query) && "object" === _typeof(query) && !(query[JSCompiler_object_inline_AND_5451] || query[JSCompiler_object_inline_OR_5452]);
  }, convertToExplicit = function(query) {
    return _defineProperty({}, JSCompiler_object_inline_AND_5451, Object.keys(query).map(function(key) {
      return _defineProperty({}, key, query[key]);
    }));
  };
  require = function() {
    function Fuse(docs) {
      var options = 1 < arguments.length && void 0 !== arguments[1] ? arguments[1] : {}, index = 2 < arguments.length ? arguments[2] : void 0;
      _classCallCheck(this, Fuse);
      this.options = _objectSpread2({}, Config, {}, options);
      this._keyStore = new KeyStore(this.options.keys);
      this.setCollection(docs, index);
    }
    _createClass(Fuse, [{key:"setCollection", value:function(docs, index) {
      this._docs = docs;
      if (index && !(index instanceof FuseIndex)) {
        throw Error("Incorrect 'index' type");
      }
      this._myIndex = index || createIndex(this.options.keys, this._docs, {getFn:this.options.getFn});
    }}, {key:"add", value:function(doc) {
      isDefined(doc) && (this._docs.push(doc), this._myIndex.add(doc));
    }}, {key:"remove", value:function() {
      for (var predicate = 0 < arguments.length && void 0 !== arguments[0] ? arguments[0] : function() {
        return !1;
      }, results = [], i = 0, len = this._docs.length; i < len; i += 1) {
        var doc = this._docs[i];
        predicate(doc, i) && (this.removeAt(i), --i, --len, results.push(doc));
      }
      return results;
    }}, {key:"removeAt", value:function(idx) {
      this._docs.splice(idx, 1);
      this._myIndex.removeAt(idx);
    }}, {key:"getIndex", value:function() {
      return this._myIndex;
    }}, {key:"search", value:function(query) {
      var _ref$limit = (1 < arguments.length && void 0 !== arguments[1] ? arguments[1] : {}).limit;
      _ref$limit = void 0 === _ref$limit ? -1 : _ref$limit;
      var _this$options = this.options, includeMatches = _this$options.includeMatches, includeScore = _this$options.includeScore, shouldSort = _this$options.shouldSort, sortFn = _this$options.sortFn;
      _this$options = _this$options.ignoreFieldNorm;
      var results = isString(query) ? isString(this._docs[0]) ? this._searchStringList(query) : this._searchObjectList(query) : this._searchLogical(query);
      computeScore$1(results, {ignoreFieldNorm:_this$options});
      shouldSort && results.sort(sortFn);
      "number" === typeof _ref$limit && -1 < _ref$limit && (results = results.slice(0, _ref$limit));
      return format(results, this._docs, {includeMatches, includeScore});
    }}, {key:"_searchStringList", value:function(query) {
      var searcher = createSearcher(query, this.options), results = [];
      this._myIndex.records.forEach(function(_ref2) {
        var text = _ref2.v, idx = _ref2.i;
        _ref2 = _ref2.n;
        if (isDefined(text)) {
          var _searcher$searchIn = searcher.searchIn(text), score = _searcher$searchIn.score, indices = _searcher$searchIn.indices;
          _searcher$searchIn.isMatch && results.push({item:text, idx, matches:[{score, value:text, norm:_ref2, indices}]});
        }
      });
      return results;
    }}, {key:"_searchLogical", value:function(query) {
      var _this = this, expression = parse(query, this.options), evaluate = function evaluate(node, item, idx) {
        if (!node.children) {
          var keyId = node.keyId;
          node = node.searcher;
          return (node = _this._findMatches({key:_this._keyStore.get(keyId), value:_this._myIndex.getValueForItemAtKeyId(item, keyId), searcher:node})) && node.length ? [{idx, item, matches:node}] : [];
        }
        switch(node.operator) {
          case JSCompiler_object_inline_AND_5451:
            keyId = [];
            for (var i = 0, len = node.children.length; i < len; i += 1) {
              var result = evaluate(node.children[i], item, idx);
              if (result.length) {
                keyId.push.apply(keyId, _toConsumableArray(result));
              } else {
                return [];
              }
            }
            return keyId;
          case JSCompiler_object_inline_OR_5452:
            keyId = [];
            i = 0;
            for (len = node.children.length; i < len; i += 1) {
              if (result = evaluate(node.children[i], item, idx), result.length) {
                keyId.push.apply(keyId, _toConsumableArray(result));
                break;
              }
            }
            return keyId;
        }
      }, resultMap = {}, results = [];
      this._myIndex.records.forEach(function(_ref3) {
        var item = _ref3.$, idx = _ref3.i;
        isDefined(item) && (_ref3 = evaluate(expression, item, idx), _ref3.length && (resultMap[idx] || (resultMap[idx] = {idx, item, matches:[]}, results.push(resultMap[idx])), _ref3.forEach(function(_ref4) {
          var _resultMap$idx$matche;
          _ref4 = _ref4.matches;
          (_resultMap$idx$matche = resultMap[idx].matches).push.apply(_resultMap$idx$matche, _toConsumableArray(_ref4));
        })));
      });
      return results;
    }}, {key:"_searchObjectList", value:function(query) {
      var _this2 = this, searcher = createSearcher(query, this.options);
      query = this._myIndex;
      var keys = query.keys, results = [];
      query.records.forEach(function(_ref5) {
        var item = _ref5.$;
        _ref5 = _ref5.i;
        if (isDefined(item)) {
          var matches = [];
          keys.forEach(function(key, keyIndex) {
            matches.push.apply(matches, _toConsumableArray(_this2._findMatches({key, value:item[keyIndex], searcher})));
          });
          matches.length && results.push({idx:_ref5, item, matches});
        }
      });
      return results;
    }}, {key:"_findMatches", value:function(_ref6) {
      var key = _ref6.key, value = _ref6.value, searcher = _ref6.searcher;
      if (!isDefined(value)) {
        return [];
      }
      var matches = [];
      if (isArray(value)) {
        value.forEach(function(_ref7) {
          var text = _ref7.v, idx = _ref7.i;
          _ref7 = _ref7.n;
          if (isDefined(text)) {
            var _searcher$searchIn2 = searcher.searchIn(text), score = _searcher$searchIn2.score, indices = _searcher$searchIn2.indices;
            _searcher$searchIn2.isMatch && matches.push({score, key, value:text, idx, norm:_ref7, indices});
          }
        });
      } else {
        _ref6 = value.v;
        value = value.n;
        var _searcher$searchIn3 = searcher.searchIn(_ref6), score = _searcher$searchIn3.score, indices = _searcher$searchIn3.indices;
        _searcher$searchIn3.isMatch && matches.push({score, key, value:_ref6, norm:value, indices});
      }
      return matches;
    }}]);
    return Fuse;
  }();
  require.version = "6.4.6";
  require.createIndex = createIndex;
  require.parseIndex = function(data) {
    var _ref3$getFn = (1 < arguments.length && void 0 !== arguments[1] ? arguments[1] : {}).getFn, keys = data.keys, records = data.records;
    _ref3$getFn = new FuseIndex({getFn:void 0 === _ref3$getFn ? Config.getFn : _ref3$getFn});
    _ref3$getFn.setKeys(keys);
    _ref3$getFn.setIndexRecords(records);
    return _ref3$getFn;
  };
  require.config = Config;
  require.parseQuery = parse;
  (function() {
    registeredSearchers.push.apply(registeredSearchers, arguments);
  })(global);
  module.exports = require;
};

//# sourceMappingURL=module$node_modules$fuse_DOT_js$dist$fuse_common.js.map
