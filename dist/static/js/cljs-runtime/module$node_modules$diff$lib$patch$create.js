shadow$provide.module$node_modules$diff$lib$patch$create = function(global, require, module, exports) {
  function _toConsumableArray(arr) {
    var JSCompiler_temp = Array.isArray(arr) ? _arrayLikeToArray(arr) : void 0;
    JSCompiler_temp || (JSCompiler_temp = "undefined" !== typeof Symbol && Symbol.iterator in Object(arr) ? Array.from(arr) : void 0);
    if (!JSCompiler_temp) {
      a: {
        if (arr) {
          if ("string" === typeof arr) {
            JSCompiler_temp = _arrayLikeToArray(arr, void 0);
            break a;
          }
          JSCompiler_temp = Object.prototype.toString.call(arr).slice(8, -1);
          "Object" === JSCompiler_temp && arr.constructor && (JSCompiler_temp = arr.constructor.name);
          if ("Map" === JSCompiler_temp || "Set" === JSCompiler_temp) {
            JSCompiler_temp = Array.from(arr);
            break a;
          }
          if ("Arguments" === JSCompiler_temp || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(JSCompiler_temp)) {
            JSCompiler_temp = _arrayLikeToArray(arr, void 0);
            break a;
          }
        }
        JSCompiler_temp = void 0;
      }
    }
    if (!(arr = JSCompiler_temp)) {
      throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
    }
    return arr;
  }
  function _arrayLikeToArray(arr, len) {
    if (null == len || len > arr.length) {
      len = arr.length;
    }
    for (var i = 0, arr2 = Array(len); i < len; i++) {
      arr2[i] = arr[i];
    }
    return arr2;
  }
  function structuredPatch(oldFileName, newFileName, oldStr, newStr, oldHeader, newHeader, options) {
    function contextLines(lines) {
      return lines.map(function(entry) {
        return " " + entry;
      });
    }
    options ||= {};
    "undefined" === typeof options.context && (options.context = 4);
    var diff = (0,_line.diffLines)(oldStr, newStr, options);
    diff.push({value:"", lines:[]});
    for (var hunks = [], oldRangeStart = 0, newRangeStart = 0, curRange = [], oldLine = 1, newLine = 1, _loop = function(i) {
      var current = diff[i], lines = current.lines || current.value.replace(/\n$/, "").split("\n");
      current.lines = lines;
      if (current.added || current.removed) {
        var _curRange;
        oldRangeStart || (i = diff[i - 1], oldRangeStart = oldLine, newRangeStart = newLine, i && (curRange = 0 < options.context ? contextLines(i.lines.slice(-options.context)) : [], oldRangeStart -= curRange.length, newRangeStart -= curRange.length));
        (_curRange = curRange).push.apply(_curRange, _toConsumableArray(lines.map(function(entry) {
          return (current.added ? "+" : "-") + entry;
        })));
        current.added ? newLine += lines.length : oldLine += lines.length;
      } else {
        if (oldRangeStart) {
          if (lines.length <= 2 * options.context && i < diff.length - 2) {
            var _curRange2;
            (_curRange2 = curRange).push.apply(_curRange2, _toConsumableArray(contextLines(lines)));
          } else {
            var _curRange3;
            _curRange = Math.min(lines.length, options.context);
            (_curRange3 = curRange).push.apply(_curRange3, _toConsumableArray(contextLines(lines.slice(0, _curRange))));
            _curRange = {oldStart:oldRangeStart, oldLines:oldLine - oldRangeStart + _curRange, newStart:newRangeStart, newLines:newLine - newRangeStart + _curRange, lines:curRange};
            i >= diff.length - 2 && lines.length <= options.context && (i = /\n$/.test(oldStr), _curRange3 = /\n$/.test(newStr), _curRange2 = 0 == lines.length && curRange.length > _curRange.oldLines, !i && _curRange2 && 0 < oldStr.length && curRange.splice(_curRange.oldLines, 0, "\\ No newline at end of file"), (i || _curRange2) && _curRange3 || curRange.push("\\ No newline at end of file"));
            hunks.push(_curRange);
            newRangeStart = oldRangeStart = 0;
            curRange = [];
          }
        }
        oldLine += lines.length;
        newLine += lines.length;
      }
    }, i = 0; i < diff.length; i++) {
      _loop(i);
    }
    return {oldFileName, newFileName, oldHeader, newHeader, hunks};
  }
  function formatPatch(diff) {
    var ret = [];
    diff.oldFileName == diff.newFileName && ret.push("Index: " + diff.oldFileName);
    ret.push("");
    ret.push("--- " + diff.oldFileName + ("undefined" === typeof diff.oldHeader ? "" : "\t" + diff.oldHeader));
    ret.push("+++ " + diff.newFileName + ("undefined" === typeof diff.newHeader ? "" : "\t" + diff.newHeader));
    for (var i = 0; i < diff.hunks.length; i++) {
      var hunk = diff.hunks[i];
      0 === hunk.oldLines && --hunk.oldStart;
      0 === hunk.newLines && --hunk.newStart;
      ret.push("@@ -" + hunk.oldStart + "," + hunk.oldLines + " +" + hunk.newStart + "," + hunk.newLines + " @@");
      ret.push.apply(ret, hunk.lines);
    }
    return ret.join("\n") + "\n";
  }
  function createTwoFilesPatch(oldFileName, newFileName, oldStr, newStr, oldHeader, newHeader, options) {
    return formatPatch(structuredPatch(oldFileName, newFileName, oldStr, newStr, oldHeader, newHeader, options));
  }
  Object.defineProperty(exports, "__esModule", {value:!0});
  exports.structuredPatch = structuredPatch;
  exports.formatPatch = formatPatch;
  exports.createTwoFilesPatch = createTwoFilesPatch;
  exports.createPatch = function(fileName, oldStr, newStr, oldHeader, newHeader, options) {
    return createTwoFilesPatch(fileName, fileName, oldStr, newStr, oldHeader, newHeader, options);
  };
  var _line = require("module$node_modules$diff$lib$diff$line");
};

//# sourceMappingURL=module$node_modules$diff$lib$patch$create.js.map
