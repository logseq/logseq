shadow$provide.module$node_modules$chrono_node$dist$common$refiners$UnlikelyFormatFilter = function(global, require, module, exports) {
  Object.defineProperty(exports, "__esModule", {value:!0});
  global = require("module$node_modules$chrono_node$dist$common$abstractRefiners");
  class UnlikelyFormatFilter extends global.Filter {
    constructor(strictMode) {
      super();
      this.strictMode = strictMode;
    }
    isValid(context, result) {
      return result.text.replace(" ", "").match(/^\d*(\.\d*)?$/) ? (context.debug(() => {
        console.log(`Removing unlikely result '${result.text}'`);
      }), !1) : result.start.isValidDate() ? result.end && !result.end.isValidDate() ? (context.debug(() => {
        console.log(`Removing invalid result: ${result} (${result.end})`);
      }), !1) : this.strictMode ? this.isStrictModeValid(context, result) : !0 : (context.debug(() => {
        console.log(`Removing invalid result: ${result} (${result.start})`);
      }), !1);
    }
    isStrictModeValid(context, result) {
      return result.start.isOnlyWeekdayComponent() ? (context.debug(() => {
        console.log(`(Strict) Removing weekday only component: ${result} (${result.end})`);
      }), !1) : !result.start.isOnlyTime() || result.start.isCertain("hour") && result.start.isCertain("minute") ? !0 : (context.debug(() => {
        console.log(`(Strict) Removing uncertain time component: ${result} (${result.end})`);
      }), !1);
    }
  }
  exports.default = UnlikelyFormatFilter;
};

//# sourceMappingURL=module$node_modules$chrono_node$dist$common$refiners$UnlikelyFormatFilter.js.map
