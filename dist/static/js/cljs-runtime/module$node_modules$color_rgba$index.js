shadow$provide.module$node_modules$color_rgba$index = function(global, require, module, exports) {
  var parse = require("module$node_modules$color_parse$index");
  module.exports = function(color) {
    Array.isArray(color) && color.raw && (color = String.raw.apply(null, arguments));
    var parsed = parse(color);
    if (!parsed.space) {
      return [];
    }
    var min = [0, 0, 0], max = "h" === parsed.space[0] ? [360, 100, 100] : [255, 255, 255];
    var values = Array(3);
    values[0] = Math.min(Math.max(parsed.values[0], min[0]), max[0]);
    values[1] = Math.min(Math.max(parsed.values[1], min[1]), max[1]);
    values[2] = Math.min(Math.max(parsed.values[2], min[2]), max[2]);
    if ("h" === parsed.space[0]) {
      min = values[0] / 360;
      max = values[1] / 100;
      var l = values[2] / 100, t3, rgb;
      values = 0;
      if (0 === max) {
        values = (t3 = 255 * l, [t3, t3, t3]);
      } else {
        max = 0.5 > l ? l * (1 + max) : l + max - l * max;
        l = 2 * l - max;
        for (rgb = [0, 0, 0]; 3 > values;) {
          t3 = min + 1 / 3 * -(values - 1), 0 > t3 ? t3++ : 1 < t3 && t3--, t3 = 1 > 6 * t3 ? l + 6 * (max - l) * t3 : 1 > 2 * t3 ? max : 2 > 3 * t3 ? l + (max - l) * (2 / 3 - t3) * 6 : l, rgb[values++] = 255 * t3;
        }
        values = rgb;
      }
    }
    values.push(Math.min(Math.max(parsed.alpha, 0), 1));
    return values;
  };
};

//# sourceMappingURL=module$node_modules$color_rgba$index.js.map
