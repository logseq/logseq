shadow$provide.module$node_modules$truncate_utf8_bytes$lib$truncate = function(global, require, module, exports) {
  module.exports = function(getLength, string, byteLength) {
    if ("string" !== typeof string) {
      throw Error("Input must be string");
    }
    for (var charLength = string.length, curByteLength = 0, codePoint, segment, i = 0; i < charLength; i += 1) {
      codePoint = string.charCodeAt(i);
      segment = string[i];
      if (codePoint = 55296 <= codePoint && 56319 >= codePoint) {
        codePoint = string.charCodeAt(i + 1), codePoint = 56320 <= codePoint && 57343 >= codePoint;
      }
      codePoint && (i += 1, segment += string[i]);
      curByteLength += getLength(segment);
      if (curByteLength === byteLength) {
        return string.slice(0, i + 1);
      }
      if (curByteLength > byteLength) {
        return string.slice(0, i - segment.length + 1);
      }
    }
    return string;
  };
};

//# sourceMappingURL=module$node_modules$truncate_utf8_bytes$lib$truncate.js.map
