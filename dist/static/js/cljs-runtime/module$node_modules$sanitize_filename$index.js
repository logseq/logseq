shadow$provide.module$node_modules$sanitize_filename$index = function(global, require, module, exports) {
  function sanitize(input, replacement) {
    if ("string" !== typeof input) {
      throw Error("Input must be string");
    }
    input = input.replace(illegalRe, replacement).replace(controlRe, replacement).replace(reservedRe, replacement).replace(windowsReservedRe, replacement).replace(windowsTrailingRe, replacement);
    return truncate(input, 255);
  }
  var truncate = require("module$node_modules$truncate_utf8_bytes$browser"), illegalRe = /[\/\?<>\\:\*\|"]/g, controlRe = /[\x00-\x1f\x80-\x9f]/g, reservedRe = /^\.+$/, windowsReservedRe = /^(con|prn|aux|nul|com[0-9]|lpt[0-9])(\..*)?$/i, windowsTrailingRe = /[\. ]+$/;
  module.exports = function(input, options) {
    options = options && options.replacement || "";
    input = sanitize(input, options);
    return "" === options ? input : sanitize(input, "");
  };
};

//# sourceMappingURL=module$node_modules$sanitize_filename$index.js.map
