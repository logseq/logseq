shadow$provide.module$node_modules$codemirror$mode$troff$troff = function(global, require, module, exports) {
  (function(mod) {
    "object" == typeof exports && "object" == typeof module ? mod(require("module$node_modules$codemirror$lib$codemirror")) : "function" == typeof define && define.amd ? define(["../../lib/codemirror"], mod) : mod(CodeMirror);
  })(function(CodeMirror) {
    CodeMirror.defineMode("troff", function() {
      function tokenBase(stream) {
        if (stream.eatSpace()) {
          return null;
        }
        var sol = stream.sol(), ch = stream.next();
        if ("\\" === ch) {
          if (stream.match("fB") || stream.match("fR") || stream.match("fI") || stream.match("u") || stream.match("d") || stream.match("%") || stream.match("\x26")) {
            return "string";
          }
          if (stream.match("m[")) {
            return stream.skipTo("]"), stream.next(), "string";
          }
          if (stream.match("s+") || stream.match("s-")) {
            return stream.eatWhile(/[\d-]/), "string";
          }
          (stream.match("(") || stream.match("*(")) && stream.eatWhile(/[\w-]/);
          return "string";
        }
        if (sol && ("." === ch || "'" === ch) && stream.eat("\\") && stream.eat('"')) {
          return stream.skipToEnd(), "comment";
        }
        if (sol && "." === ch) {
          if (stream.match("B ") || stream.match("I ") || stream.match("R ")) {
            return "attribute";
          }
          if (stream.match("TH ") || stream.match("SH ") || stream.match("SS ") || stream.match("HP ")) {
            return stream.skipToEnd(), "quote";
          }
          if (stream.match(/[A-Z]/) && stream.match(/[A-Z]/) || stream.match(/[a-z]/) && stream.match(/[a-z]/)) {
            return "attribute";
          }
        }
        stream.eatWhile(/[\w-]/);
        stream = stream.current();
        return words.hasOwnProperty(stream) ? words[stream] : null;
      }
      var words = {};
      return {startState:function() {
        return {tokens:[]};
      }, token:function(stream, state) {
        return (state.tokens[0] || tokenBase)(stream, state);
      }};
    });
    CodeMirror.defineMIME("text/troff", "troff");
    CodeMirror.defineMIME("text/x-troff", "troff");
    CodeMirror.defineMIME("application/x-troff", "troff");
  });
};

//# sourceMappingURL=module$node_modules$codemirror$mode$troff$troff.js.map
