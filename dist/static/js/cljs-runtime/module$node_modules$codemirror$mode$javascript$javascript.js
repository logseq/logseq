shadow$provide.module$node_modules$codemirror$mode$javascript$javascript = function(global, require, module, exports) {
  (function(mod) {
    "object" == typeof exports && "object" == typeof module ? mod(require("module$node_modules$codemirror$lib$codemirror")) : "function" == typeof define && define.amd ? define(["../../lib/codemirror"], mod) : mod(CodeMirror);
  })(function(CodeMirror) {
    CodeMirror.defineMode("javascript", function(config, parserConfig) {
      var JSCompiler_object_inline_state_5401, JSCompiler_object_inline_stream_5402, JSCompiler_object_inline_marked_5403;
      function ret(tp, style, cont) {
        type = tp;
        content = cont;
        return style;
      }
      function tokenBase(stream, state) {
        var ch = stream.next();
        if ('"' == ch || "'" == ch) {
          return state.tokenize = tokenString(ch), state.tokenize(stream, state);
        }
        if ("." == ch && stream.match(/^\d[\d_]*(?:[eE][+\-]?[\d_]+)?/)) {
          return ret("number", "number");
        }
        if ("." == ch && stream.match("..")) {
          return ret("spread", "meta");
        }
        if (/[\[\]{}\(\),;:\.]/.test(ch)) {
          return ret(ch);
        }
        if ("\x3d" == ch && stream.eat("\x3e")) {
          return ret("\x3d\x3e", "operator");
        }
        if ("0" == ch && stream.match(/^(?:x[\dA-Fa-f_]+|o[0-7_]+|b[01_]+)n?/)) {
          return ret("number", "number");
        }
        if (/\d/.test(ch)) {
          return stream.match(/^[\d_]*(?:n|(?:\.[\d_]*)?(?:[eE][+\-]?[\d_]+)?)?/), ret("number", "number");
        }
        if ("/" == ch) {
          if (stream.eat("*")) {
            return state.tokenize = tokenComment, tokenComment(stream, state);
          }
          if (stream.eat("/")) {
            return stream.skipToEnd(), ret("comment", "comment");
          }
          if (expressionAllowed(stream, state, 1)) {
            a: {
              for (var inSet = state = !1; null != (ch = stream.next());) {
                if (!state) {
                  if ("/" == ch && !inSet) {
                    break a;
                  }
                  "[" == ch ? inSet = !0 : inSet && "]" == ch && (inSet = !1);
                }
                state = !state && "\\" == ch;
              }
            }
            stream.match(/^\b(([gimyus])(?![gimyus]*\2))+\b/);
            return ret("regexp", "string-2");
          }
          stream.eat("\x3d");
          return ret("operator", "operator", stream.current());
        }
        if ("`" == ch) {
          return state.tokenize = tokenQuasi, tokenQuasi(stream, state);
        }
        if ("#" == ch && "!" == stream.peek()) {
          return stream.skipToEnd(), ret("meta", "meta");
        }
        if ("#" == ch && stream.eatWhile(wordRE)) {
          return ret("variable", "property");
        }
        if ("\x3c" == ch && stream.match("!--") || "-" == ch && stream.match("-\x3e") && !/\S/.test(stream.string.slice(0, stream.start))) {
          return stream.skipToEnd(), ret("comment", "comment");
        }
        if (isOperatorChar.test(ch)) {
          return "\x3e" == ch && state.lexical && "\x3e" == state.lexical.type || (stream.eat("\x3d") ? "!" != ch && "\x3d" != ch || stream.eat("\x3d") : /[<>*+\-|&?]/.test(ch) && (stream.eat(ch), "\x3e" == ch && stream.eat(ch))), "?" == ch && stream.eat(".") ? ret(".") : ret("operator", "operator", stream.current());
        }
        if (wordRE.test(ch)) {
          stream.eatWhile(wordRE);
          ch = stream.current();
          if ("." != state.lastType) {
            if (keywords.propertyIsEnumerable(ch)) {
              return stream = keywords[ch], ret(stream.type, stream.style, ch);
            }
            if ("async" == ch && stream.match(/^(\s|\/\*([^*]|\*(?!\/))*?\*\/)*[\[\(\w]/, !1)) {
              return ret("async", "keyword", ch);
            }
          }
          return ret("variable", "variable", ch);
        }
      }
      function tokenString(quote) {
        return function(stream, state) {
          var escaped = !1, next;
          if (jsonldMode && "@" == stream.peek() && stream.match(isJsonldKeyword)) {
            return state.tokenize = tokenBase, ret("jsonld-keyword", "meta");
          }
          for (; null != (next = stream.next()) && (next != quote || escaped);) {
            escaped = !escaped && "\\" == next;
          }
          escaped || (state.tokenize = tokenBase);
          return ret("string", "string");
        };
      }
      function tokenComment(stream, state) {
        for (var maybeEnd = !1, ch; ch = stream.next();) {
          if ("/" == ch && maybeEnd) {
            state.tokenize = tokenBase;
            break;
          }
          maybeEnd = "*" == ch;
        }
        return ret("comment", "comment");
      }
      function tokenQuasi(stream, state) {
        for (var escaped = !1, next; null != (next = stream.next());) {
          if (!escaped && ("`" == next || "$" == next && stream.eat("{"))) {
            state.tokenize = tokenBase;
            break;
          }
          escaped = !escaped && "\\" == next;
        }
        return ret("quasi", "string-2", stream.current());
      }
      function findFatArrow(stream, state) {
        state.fatArrowAt && (state.fatArrowAt = null);
        var arrow = stream.string.indexOf("\x3d\x3e", stream.start);
        if (!(0 > arrow)) {
          if (isTS) {
            var m = /:\s*(?:\w+(?:<[^>]*>|\[\])?|\{[^}]*\})\s*$/.exec(stream.string.slice(stream.start, arrow));
            m && (arrow = m.index);
          }
          m = 0;
          var sawSomething = !1;
          for (--arrow; 0 <= arrow; --arrow) {
            var ch = stream.string.charAt(arrow), bracket = "([{}])".indexOf(ch);
            if (0 <= bracket && 3 > bracket) {
              if (!m) {
                ++arrow;
                break;
              }
              if (0 == --m) {
                "(" == ch && (sawSomething = !0);
                break;
              }
            } else if (3 <= bracket && 6 > bracket) {
              ++m;
            } else if (wordRE.test(ch)) {
              sawSomething = !0;
            } else if (/["'\/`]/.test(ch)) {
              for (;; --arrow) {
                if (0 == arrow) {
                  return;
                }
                if (stream.string.charAt(arrow - 1) == ch && "\\" != stream.string.charAt(arrow - 2)) {
                  arrow--;
                  break;
                }
              }
            } else if (sawSomething && !m) {
              ++arrow;
              break;
            }
          }
          sawSomething && !m && (state.fatArrowAt = arrow);
        }
      }
      function JSLexical(indented, column, type, align, prev, info) {
        this.indented = indented;
        this.column = column;
        this.type = type;
        this.prev = prev;
        this.info = info;
        null != align && (this.align = align);
      }
      function parseJS(state, style, type, content, stream) {
        var cc = state.cc;
        JSCompiler_object_inline_state_5401 = state;
        JSCompiler_object_inline_stream_5402 = stream;
        JSCompiler_object_inline_marked_5403 = null;
        JSCompiler_object_inline_cc_5404 = cc;
        JSCompiler_object_inline_style_5405 = style;
        state.lexical.hasOwnProperty("align") || (state.lexical.align = !0);
        for (;;) {
          if ((cc.length ? cc.pop() : jsonMode ? expression : statement)(type, content)) {
            for (; cc.length && cc[cc.length - 1].lex;) {
              cc.pop()();
            }
            if (JSCompiler_object_inline_marked_5403) {
              return JSCompiler_object_inline_marked_5403;
            }
            if (type = "variable" == type) {
              a: {
                if (trackScope) {
                  for (type = state.localVars; type; type = type.next) {
                    if (type.name == content) {
                      type = !0;
                      break a;
                    }
                  }
                  for (state = state.context; state; state = state.prev) {
                    for (type = state.vars; type; type = type.next) {
                      if (type.name == content) {
                        type = !0;
                        break a;
                      }
                    }
                  }
                  type = void 0;
                } else {
                  type = !1;
                }
              }
            }
            return type ? "variable-2" : style;
          }
        }
      }
      function pass() {
        for (var i = arguments.length - 1; 0 <= i; i--) {
          JSCompiler_object_inline_cc_5404.push(arguments[i]);
        }
      }
      function cont() {
        pass.apply(null, arguments);
        return !0;
      }
      function inList(name, list) {
        for (; list; list = list.next) {
          if (list.name == name) {
            return !0;
          }
        }
        return !1;
      }
      function register(varname) {
        var state = JSCompiler_object_inline_state_5401;
        JSCompiler_object_inline_marked_5403 = "def";
        if (trackScope) {
          if (state.context) {
            if ("var" == state.lexical.info && state.context && state.context.block) {
              var newContext = registerVarScoped(varname, state.context);
              if (null != newContext) {
                state.context = newContext;
                return;
              }
            } else if (!inList(varname, state.localVars)) {
              state.localVars = new Var(varname, state.localVars);
              return;
            }
          }
          parserConfig.globalVars && !inList(varname, state.globalVars) && (state.globalVars = new Var(varname, state.globalVars));
        }
      }
      function registerVarScoped(varname, context) {
        return context ? context.block ? (varname = registerVarScoped(varname, context.prev)) ? varname == context.prev ? context : new Context(varname, context.vars, !0) : null : inList(varname, context.vars) ? context : new Context(context.prev, new Var(varname, context.vars), !1) : null;
      }
      function isModifier(name) {
        return "public" == name || "private" == name || "protected" == name || "abstract" == name || "readonly" == name;
      }
      function Context(prev, vars, block) {
        this.prev = prev;
        this.vars = vars;
        this.block = block;
      }
      function Var(name, next) {
        this.name = name;
        this.next = next;
      }
      function pushcontext() {
        JSCompiler_object_inline_state_5401.context = new Context(JSCompiler_object_inline_state_5401.context, JSCompiler_object_inline_state_5401.localVars, !1);
        JSCompiler_object_inline_state_5401.localVars = defaultVars;
      }
      function pushblockcontext() {
        JSCompiler_object_inline_state_5401.context = new Context(JSCompiler_object_inline_state_5401.context, JSCompiler_object_inline_state_5401.localVars, !0);
        JSCompiler_object_inline_state_5401.localVars = null;
      }
      function popcontext() {
        JSCompiler_object_inline_state_5401.localVars = JSCompiler_object_inline_state_5401.context.vars;
        JSCompiler_object_inline_state_5401.context = JSCompiler_object_inline_state_5401.context.prev;
      }
      function pushlex(type, info) {
        var result = function() {
          var state = JSCompiler_object_inline_state_5401, indent = state.indented;
          if ("stat" == state.lexical.type) {
            indent = state.lexical.indented;
          } else {
            for (var outer = state.lexical; outer && ")" == outer.type && outer.align; outer = outer.prev) {
              indent = outer.indented;
            }
          }
          state.lexical = new JSLexical(indent, JSCompiler_object_inline_stream_5402.column(), type, null, state.lexical, info);
        };
        result.lex = !0;
        return result;
      }
      function poplex() {
        var state = JSCompiler_object_inline_state_5401;
        state.lexical.prev && (")" == state.lexical.type && (state.indented = state.lexical.indented), state.lexical = state.lexical.prev);
      }
      function expect(wanted) {
        function exp(type) {
          return type == wanted ? cont() : ";" == wanted || "}" == type || ")" == type || "]" == type ? pass() : cont(exp);
        }
        return exp;
      }
      function statement(type, value) {
        return "var" == type ? cont(pushlex("vardef", value), vardef, expect(";"), poplex) : "keyword a" == type ? cont(pushlex("form"), parenExpr, statement, poplex) : "keyword b" == type ? cont(pushlex("form"), statement, poplex) : "keyword d" == type ? JSCompiler_object_inline_stream_5402.match(/^\s*$/, !1) ? cont() : cont(pushlex("stat"), maybeexpression, expect(";"), poplex) : "debugger" == type ? cont(expect(";")) : "{" == type ? cont(pushlex("}"), pushblockcontext, block, poplex, popcontext) : 
        ";" == type ? cont() : "if" == type ? ("else" == JSCompiler_object_inline_state_5401.lexical.info && JSCompiler_object_inline_state_5401.cc[JSCompiler_object_inline_state_5401.cc.length - 1] == poplex && JSCompiler_object_inline_state_5401.cc.pop()(), cont(pushlex("form"), parenExpr, statement, poplex, maybeelse)) : "function" == type ? cont(functiondef) : "for" == type ? cont(pushlex("form"), pushblockcontext, forspec, statement, popcontext, poplex) : "class" == type || isTS && "interface" == 
        value ? (JSCompiler_object_inline_marked_5403 = "keyword", cont(pushlex("form", "class" == type ? type : value), className, poplex)) : "variable" == type ? isTS && "declare" == value ? (JSCompiler_object_inline_marked_5403 = "keyword", cont(statement)) : isTS && ("module" == value || "enum" == value || "type" == value) && JSCompiler_object_inline_stream_5402.match(/^\s*\w/, !1) ? (JSCompiler_object_inline_marked_5403 = "keyword", "enum" == value ? cont(enumdef) : "type" == value ? cont(typename, 
        expect("operator"), typeexpr, expect(";")) : cont(pushlex("form"), pattern, expect("{"), pushlex("}"), block, poplex, poplex)) : isTS && "namespace" == value ? (JSCompiler_object_inline_marked_5403 = "keyword", cont(pushlex("form"), expression, statement, poplex)) : isTS && "abstract" == value ? (JSCompiler_object_inline_marked_5403 = "keyword", cont(statement)) : cont(pushlex("stat"), maybelabel) : "switch" == type ? cont(pushlex("form"), parenExpr, expect("{"), pushlex("}", "switch"), pushblockcontext, 
        block, poplex, poplex, popcontext) : "case" == type ? cont(expression, expect(":")) : "default" == type ? cont(expect(":")) : "catch" == type ? cont(pushlex("form"), pushcontext, maybeCatchBinding, statement, poplex, popcontext) : "export" == type ? cont(pushlex("stat"), afterExport, poplex) : "import" == type ? cont(pushlex("stat"), afterImport, poplex) : "async" == type ? cont(statement) : "@" == value ? cont(expression, statement) : pass(pushlex("stat"), expression, expect(";"), poplex);
      }
      function maybeCatchBinding(type) {
        if ("(" == type) {
          return cont(funarg, expect(")"));
        }
      }
      function expression(type, value) {
        return expressionInner(type, value, !1);
      }
      function expressionNoComma(type, value) {
        return expressionInner(type, value, !0);
      }
      function parenExpr(type) {
        return "(" != type ? pass() : cont(pushlex(")"), maybeexpression, expect(")"), poplex);
      }
      function expressionInner(type, value, noComma) {
        if (JSCompiler_object_inline_state_5401.fatArrowAt == JSCompiler_object_inline_stream_5402.start) {
          var body = noComma ? arrowBodyNoComma : arrowBody;
          if ("(" == type) {
            return cont(pushcontext, pushlex(")"), commasep(funarg, ")"), poplex, expect("\x3d\x3e"), body, popcontext);
          }
          if ("variable" == type) {
            return pass(pushcontext, pattern, expect("\x3d\x3e"), body, popcontext);
          }
        }
        body = noComma ? maybeoperatorNoComma : maybeoperatorComma;
        return atomicTypes.hasOwnProperty(type) ? cont(body) : "function" == type ? cont(functiondef, body) : "class" == type || isTS && "interface" == value ? (JSCompiler_object_inline_marked_5403 = "keyword", cont(pushlex("form"), classExpression, poplex)) : "keyword c" == type || "async" == type ? cont(noComma ? expressionNoComma : expression) : "(" == type ? cont(pushlex(")"), maybeexpression, expect(")"), poplex, body) : "operator" == type || "spread" == type ? cont(noComma ? expressionNoComma : 
        expression) : "[" == type ? cont(pushlex("]"), arrayLiteral, poplex, body) : "{" == type ? contCommasep(objprop, "}", null, body) : "quasi" == type ? pass(quasi, body) : "new" == type ? cont(maybeTarget(noComma)) : cont();
      }
      function maybeexpression(type) {
        return type.match(/[;\}\)\],]/) ? pass() : pass(expression);
      }
      function maybeoperatorComma(type, value) {
        return "," == type ? cont(maybeexpression) : maybeoperatorNoComma(type, value, !1);
      }
      function maybeoperatorNoComma(type, value, noComma) {
        var me = 0 == noComma ? maybeoperatorComma : maybeoperatorNoComma, expr = 0 == noComma ? expression : expressionNoComma;
        if ("\x3d\x3e" == type) {
          return cont(pushcontext, noComma ? arrowBodyNoComma : arrowBody, popcontext);
        }
        if ("operator" == type) {
          return /\+\+|--/.test(value) || isTS && "!" == value ? cont(me) : isTS && "\x3c" == value && JSCompiler_object_inline_stream_5402.match(/^([^<>]|<[^<>]*>)*>\s*\(/, !1) ? cont(pushlex("\x3e"), commasep(typeexpr, "\x3e"), poplex, me) : "?" == value ? cont(expression, expect(":"), expr) : cont(expr);
        }
        if ("quasi" == type) {
          return pass(quasi, me);
        }
        if (";" != type) {
          if ("(" == type) {
            return contCommasep(expressionNoComma, ")", "call", me);
          }
          if ("." == type) {
            return cont(property, me);
          }
          if ("[" == type) {
            return cont(pushlex("]"), maybeexpression, expect("]"), poplex, me);
          }
          if (isTS && "as" == value) {
            return JSCompiler_object_inline_marked_5403 = "keyword", cont(typeexpr, me);
          }
          if ("regexp" == type) {
            return JSCompiler_object_inline_state_5401.lastType = JSCompiler_object_inline_marked_5403 = "operator", JSCompiler_object_inline_stream_5402.backUp(JSCompiler_object_inline_stream_5402.pos - JSCompiler_object_inline_stream_5402.start - 1), cont(expr);
          }
        }
      }
      function quasi(type, value) {
        return "quasi" != type ? pass() : "${" != value.slice(value.length - 2) ? cont(quasi) : cont(maybeexpression, continueQuasi);
      }
      function continueQuasi(type) {
        if ("}" == type) {
          return JSCompiler_object_inline_marked_5403 = "string-2", JSCompiler_object_inline_state_5401.tokenize = tokenQuasi, cont(quasi);
        }
      }
      function arrowBody(type) {
        findFatArrow(JSCompiler_object_inline_stream_5402, JSCompiler_object_inline_state_5401);
        return pass("{" == type ? statement : expression);
      }
      function arrowBodyNoComma(type) {
        findFatArrow(JSCompiler_object_inline_stream_5402, JSCompiler_object_inline_state_5401);
        return pass("{" == type ? statement : expressionNoComma);
      }
      function maybeTarget(noComma) {
        return function(type) {
          return "." == type ? cont(noComma ? targetNoComma : target) : "variable" == type && isTS ? cont(maybeTypeArgs, noComma ? maybeoperatorNoComma : maybeoperatorComma) : pass(noComma ? expressionNoComma : expression);
        };
      }
      function target(_, value) {
        if ("target" == value) {
          return JSCompiler_object_inline_marked_5403 = "keyword", cont(maybeoperatorComma);
        }
      }
      function targetNoComma(_, value) {
        if ("target" == value) {
          return JSCompiler_object_inline_marked_5403 = "keyword", cont(maybeoperatorNoComma);
        }
      }
      function maybelabel(type) {
        return ":" == type ? cont(poplex, statement) : pass(maybeoperatorComma, expect(";"), poplex);
      }
      function property(type) {
        if ("variable" == type) {
          return JSCompiler_object_inline_marked_5403 = "property", cont();
        }
      }
      function objprop(type, value) {
        if ("async" == type) {
          return JSCompiler_object_inline_marked_5403 = "property", cont(objprop);
        }
        if ("variable" == type || "keyword" == JSCompiler_object_inline_style_5405) {
          JSCompiler_object_inline_marked_5403 = "property";
          if ("get" == value || "set" == value) {
            return cont(getterSetter);
          }
          var m;
          isTS && JSCompiler_object_inline_state_5401.fatArrowAt == JSCompiler_object_inline_stream_5402.start && (m = JSCompiler_object_inline_stream_5402.match(/^\s*:\s*/, !1)) && (JSCompiler_object_inline_state_5401.fatArrowAt = JSCompiler_object_inline_stream_5402.pos + m[0].length);
          return cont(afterprop);
        }
        if ("number" == type || "string" == type) {
          return JSCompiler_object_inline_marked_5403 = jsonldMode ? "property" : JSCompiler_object_inline_style_5405 + " property", cont(afterprop);
        }
        if ("jsonld-keyword" == type) {
          return cont(afterprop);
        }
        if (isTS && isModifier(value)) {
          return JSCompiler_object_inline_marked_5403 = "keyword", cont(objprop);
        }
        if ("[" == type) {
          return cont(expression, maybetype, expect("]"), afterprop);
        }
        if ("spread" == type) {
          return cont(expressionNoComma, afterprop);
        }
        if ("*" == value) {
          return JSCompiler_object_inline_marked_5403 = "keyword", cont(objprop);
        }
        if (":" == type) {
          return pass(afterprop);
        }
      }
      function getterSetter(type) {
        if ("variable" != type) {
          return pass(afterprop);
        }
        JSCompiler_object_inline_marked_5403 = "property";
        return cont(functiondef);
      }
      function afterprop(type) {
        if (":" == type) {
          return cont(expressionNoComma);
        }
        if ("(" == type) {
          return pass(functiondef);
        }
      }
      function commasep(what, end, sep) {
        function proceed(type, value) {
          return (sep ? -1 < sep.indexOf(type) : "," == type) ? (type = JSCompiler_object_inline_state_5401.lexical, "call" == type.info && (type.pos = (type.pos || 0) + 1), cont(function(type, value) {
            return type == end || value == end ? pass() : pass(what);
          }, proceed)) : type == end || value == end ? cont() : sep && -1 < sep.indexOf(";") ? pass(what) : cont(expect(end));
        }
        return function(type, value) {
          return type == end || value == end ? cont() : pass(what, proceed);
        };
      }
      function contCommasep(what, end, info) {
        for (var i = 3; i < arguments.length; i++) {
          JSCompiler_object_inline_cc_5404.push(arguments[i]);
        }
        return cont(pushlex(end, info), commasep(what, end), poplex);
      }
      function block(type) {
        return "}" == type ? cont() : pass(statement, block);
      }
      function maybetype(type, value) {
        if (isTS) {
          if (":" == type) {
            return cont(typeexpr);
          }
          if ("?" == value) {
            return cont(maybetype);
          }
        }
      }
      function maybetypeOrIn(type, value) {
        if (isTS && (":" == type || "in" == value)) {
          return cont(typeexpr);
        }
      }
      function mayberettype(type) {
        if (isTS && ":" == type) {
          return JSCompiler_object_inline_stream_5402.match(/^\s*\w+\s+is\b/, !1) ? cont(expression, isKW, typeexpr) : cont(typeexpr);
        }
      }
      function isKW(_, value) {
        if ("is" == value) {
          return JSCompiler_object_inline_marked_5403 = "keyword", cont();
        }
      }
      function typeexpr(type, value) {
        if ("keyof" == value || "typeof" == value || "infer" == value || "readonly" == value) {
          return JSCompiler_object_inline_marked_5403 = "keyword", cont("typeof" == value ? expressionNoComma : typeexpr);
        }
        if ("variable" == type || "void" == value) {
          return JSCompiler_object_inline_marked_5403 = "type", cont(afterType);
        }
        if ("|" == value || "\x26" == value) {
          return cont(typeexpr);
        }
        if ("string" == type || "number" == type || "atom" == type) {
          return cont(afterType);
        }
        if ("[" == type) {
          return cont(pushlex("]"), commasep(typeexpr, "]", ","), poplex, afterType);
        }
        if ("{" == type) {
          return cont(pushlex("}"), typeprops, poplex, afterType);
        }
        if ("(" == type) {
          return cont(commasep(typearg, ")"), maybeReturnType, afterType);
        }
        if ("\x3c" == type) {
          return cont(commasep(typeexpr, "\x3e"), typeexpr);
        }
        if ("quasi" == type) {
          return pass(quasiType, afterType);
        }
      }
      function maybeReturnType(type) {
        if ("\x3d\x3e" == type) {
          return cont(typeexpr);
        }
      }
      function typeprops(type) {
        return type.match(/[\}\)\]]/) ? cont() : "," == type || ";" == type ? cont(typeprops) : pass(typeprop, typeprops);
      }
      function typeprop(type, value) {
        if ("variable" == type || "keyword" == JSCompiler_object_inline_style_5405) {
          return JSCompiler_object_inline_marked_5403 = "property", cont(typeprop);
        }
        if ("?" == value || "number" == type || "string" == type) {
          return cont(typeprop);
        }
        if (":" == type) {
          return cont(typeexpr);
        }
        if ("[" == type) {
          return cont(expect("variable"), maybetypeOrIn, expect("]"), typeprop);
        }
        if ("(" == type) {
          return pass(functiondecl, typeprop);
        }
        if (!type.match(/[;\}\)\],]/)) {
          return cont();
        }
      }
      function quasiType(type, value) {
        return "quasi" != type ? pass() : "${" != value.slice(value.length - 2) ? cont(quasiType) : cont(typeexpr, continueQuasiType);
      }
      function continueQuasiType(type) {
        if ("}" == type) {
          return JSCompiler_object_inline_marked_5403 = "string-2", JSCompiler_object_inline_state_5401.tokenize = tokenQuasi, cont(quasiType);
        }
      }
      function typearg(type, value) {
        return "variable" == type && JSCompiler_object_inline_stream_5402.match(/^\s*[?:]/, !1) || "?" == value ? cont(typearg) : ":" == type ? cont(typeexpr) : "spread" == type ? cont(typearg) : pass(typeexpr);
      }
      function afterType(type, value) {
        if ("\x3c" == value) {
          return cont(pushlex("\x3e"), commasep(typeexpr, "\x3e"), poplex, afterType);
        }
        if ("|" == value || "." == type || "\x26" == value) {
          return cont(typeexpr);
        }
        if ("[" == type) {
          return cont(typeexpr, expect("]"), afterType);
        }
        if ("extends" == value || "implements" == value) {
          return JSCompiler_object_inline_marked_5403 = "keyword", cont(typeexpr);
        }
        if ("?" == value) {
          return cont(typeexpr, expect(":"), typeexpr);
        }
      }
      function maybeTypeArgs(_, value) {
        if ("\x3c" == value) {
          return cont(pushlex("\x3e"), commasep(typeexpr, "\x3e"), poplex, afterType);
        }
      }
      function typeparam() {
        return pass(typeexpr, maybeTypeDefault);
      }
      function maybeTypeDefault(_, value) {
        if ("\x3d" == value) {
          return cont(typeexpr);
        }
      }
      function vardef(_, value) {
        return "enum" == value ? (JSCompiler_object_inline_marked_5403 = "keyword", cont(enumdef)) : pass(pattern, maybetype, maybeAssign, vardefCont);
      }
      function pattern(type, value) {
        if (isTS && isModifier(value)) {
          return JSCompiler_object_inline_marked_5403 = "keyword", cont(pattern);
        }
        if ("variable" == type) {
          return register(value), cont();
        }
        if ("spread" == type) {
          return cont(pattern);
        }
        if ("[" == type) {
          return contCommasep(eltpattern, "]");
        }
        if ("{" == type) {
          return contCommasep(proppattern, "}");
        }
      }
      function proppattern(type, value) {
        if ("variable" == type && !JSCompiler_object_inline_stream_5402.match(/^\s*:/, !1)) {
          return register(value), cont(maybeAssign);
        }
        "variable" == type && (JSCompiler_object_inline_marked_5403 = "property");
        return "spread" == type ? cont(pattern) : "}" == type ? pass() : "[" == type ? cont(expression, expect("]"), expect(":"), proppattern) : cont(expect(":"), pattern, maybeAssign);
      }
      function eltpattern() {
        return pass(pattern, maybeAssign);
      }
      function maybeAssign(_type, value) {
        if ("\x3d" == value) {
          return cont(expressionNoComma);
        }
      }
      function vardefCont(type) {
        if ("," == type) {
          return cont(vardef);
        }
      }
      function maybeelse(type, value) {
        if ("keyword b" == type && "else" == value) {
          return cont(pushlex("form", "else"), statement, poplex);
        }
      }
      function forspec(type, value) {
        if ("await" == value) {
          return cont(forspec);
        }
        if ("(" == type) {
          return cont(pushlex(")"), forspec1, poplex);
        }
      }
      function forspec1(type) {
        return "var" == type ? cont(vardef, forspec2) : "variable" == type ? cont(forspec2) : pass(forspec2);
      }
      function forspec2(type, value) {
        return ")" == type ? cont() : ";" == type ? cont(forspec2) : "in" == value || "of" == value ? (JSCompiler_object_inline_marked_5403 = "keyword", cont(expression, forspec2)) : pass(expression, forspec2);
      }
      function functiondef(type, value) {
        if ("*" == value) {
          return JSCompiler_object_inline_marked_5403 = "keyword", cont(functiondef);
        }
        if ("variable" == type) {
          return register(value), cont(functiondef);
        }
        if ("(" == type) {
          return cont(pushcontext, pushlex(")"), commasep(funarg, ")"), poplex, mayberettype, statement, popcontext);
        }
        if (isTS && "\x3c" == value) {
          return cont(pushlex("\x3e"), commasep(typeparam, "\x3e"), poplex, functiondef);
        }
      }
      function functiondecl(type, value) {
        if ("*" == value) {
          return JSCompiler_object_inline_marked_5403 = "keyword", cont(functiondecl);
        }
        if ("variable" == type) {
          return register(value), cont(functiondecl);
        }
        if ("(" == type) {
          return cont(pushcontext, pushlex(")"), commasep(funarg, ")"), poplex, mayberettype, popcontext);
        }
        if (isTS && "\x3c" == value) {
          return cont(pushlex("\x3e"), commasep(typeparam, "\x3e"), poplex, functiondecl);
        }
      }
      function typename(type, value) {
        if ("keyword" == type || "variable" == type) {
          return JSCompiler_object_inline_marked_5403 = "type", cont(typename);
        }
        if ("\x3c" == value) {
          return cont(pushlex("\x3e"), commasep(typeparam, "\x3e"), poplex);
        }
      }
      function funarg(type, value) {
        "@" == value && cont(expression, funarg);
        return "spread" == type ? cont(funarg) : isTS && isModifier(value) ? (JSCompiler_object_inline_marked_5403 = "keyword", cont(funarg)) : isTS && "this" == type ? cont(maybetype, maybeAssign) : pass(pattern, maybetype, maybeAssign);
      }
      function classExpression(type, value) {
        return "variable" == type ? className(type, value) : classNameAfter(type, value);
      }
      function className(type, value) {
        if ("variable" == type) {
          return register(value), cont(classNameAfter);
        }
      }
      function classNameAfter(type, value) {
        if ("\x3c" == value) {
          return cont(pushlex("\x3e"), commasep(typeparam, "\x3e"), poplex, classNameAfter);
        }
        if ("extends" == value || "implements" == value || isTS && "," == type) {
          return "implements" == value && (JSCompiler_object_inline_marked_5403 = "keyword"), cont(isTS ? typeexpr : expression, classNameAfter);
        }
        if ("{" == type) {
          return cont(pushlex("}"), classBody, poplex);
        }
      }
      function classBody(type, value) {
        if ("async" == type || "variable" == type && ("static" == value || "get" == value || "set" == value || isTS && isModifier(value)) && JSCompiler_object_inline_stream_5402.match(/^\s+#?[\w$\xa1-\uffff]/, !1)) {
          return JSCompiler_object_inline_marked_5403 = "keyword", cont(classBody);
        }
        if ("variable" == type || "keyword" == JSCompiler_object_inline_style_5405) {
          return JSCompiler_object_inline_marked_5403 = "property", cont(classfield, classBody);
        }
        if ("number" == type || "string" == type) {
          return cont(classfield, classBody);
        }
        if ("[" == type) {
          return cont(expression, maybetype, expect("]"), classfield, classBody);
        }
        if ("*" == value) {
          return JSCompiler_object_inline_marked_5403 = "keyword", cont(classBody);
        }
        if (isTS && "(" == type) {
          return pass(functiondecl, classBody);
        }
        if (";" == type || "," == type) {
          return cont(classBody);
        }
        if ("}" == type) {
          return cont();
        }
        if ("@" == value) {
          return cont(expression, classBody);
        }
      }
      function classfield(type, value) {
        if ("!" == value || "?" == value) {
          return cont(classfield);
        }
        if (":" == type) {
          return cont(typeexpr, maybeAssign);
        }
        if ("\x3d" == value) {
          return cont(expressionNoComma);
        }
        type = JSCompiler_object_inline_state_5401.lexical.prev;
        return pass(type && "interface" == type.info ? functiondecl : functiondef);
      }
      function afterExport(type, value) {
        return "*" == value ? (JSCompiler_object_inline_marked_5403 = "keyword", cont(maybeFrom, expect(";"))) : "default" == value ? (JSCompiler_object_inline_marked_5403 = "keyword", cont(expression, expect(";"))) : "{" == type ? cont(commasep(exportField, "}"), maybeFrom, expect(";")) : pass(statement);
      }
      function exportField(type, value) {
        if ("as" == value) {
          return JSCompiler_object_inline_marked_5403 = "keyword", cont(expect("variable"));
        }
        if ("variable" == type) {
          return pass(expressionNoComma, exportField);
        }
      }
      function afterImport(type) {
        return "string" == type ? cont() : "(" == type ? pass(expression) : "." == type ? pass(maybeoperatorComma) : pass(importSpec, maybeMoreImports, maybeFrom);
      }
      function importSpec(type, value) {
        if ("{" == type) {
          return contCommasep(importSpec, "}");
        }
        "variable" == type && register(value);
        "*" == value && (JSCompiler_object_inline_marked_5403 = "keyword");
        return cont(maybeAs);
      }
      function maybeMoreImports(type) {
        if ("," == type) {
          return cont(importSpec, maybeMoreImports);
        }
      }
      function maybeAs(_type, value) {
        if ("as" == value) {
          return JSCompiler_object_inline_marked_5403 = "keyword", cont(importSpec);
        }
      }
      function maybeFrom(_type, value) {
        if ("from" == value) {
          return JSCompiler_object_inline_marked_5403 = "keyword", cont(expression);
        }
      }
      function arrayLiteral(type) {
        return "]" == type ? cont() : pass(commasep(expressionNoComma, "]"));
      }
      function enumdef() {
        return pass(pushlex("form"), pattern, expect("{"), pushlex("}"), commasep(enummember, "}"), poplex, poplex);
      }
      function enummember() {
        return pass(pattern, maybeAssign);
      }
      function expressionAllowed(stream, state, backUp) {
        return state.tokenize == tokenBase && /^(?:operator|sof|keyword [bcd]|case|new|export|default|spread|[\[{}\(,;:]|=>)$/.test(state.lastType) || "quasi" == state.lastType && /\{\s*$/.test(stream.string.slice(0, stream.pos - (backUp || 0)));
      }
      var indentUnit = config.indentUnit, statementIndent = parserConfig.statementIndent, jsonldMode = parserConfig.jsonld, jsonMode = parserConfig.json || jsonldMode, trackScope = !1 !== parserConfig.trackScope, isTS = parserConfig.typescript, wordRE = parserConfig.wordCharacters || /[\w$\xa1-\uffff]/, keywords = function() {
        function kw(type) {
          return {type, style:"keyword"};
        }
        var A = kw("keyword a"), B = kw("keyword b"), C = kw("keyword c"), D = kw("keyword d"), operator = kw("operator"), atom = {type:"atom", style:"atom"};
        return {"if":kw("if"), "while":A, "with":A, "else":B, "do":B, "try":B, "finally":B, "return":D, "break":D, "continue":D, "new":kw("new"), "delete":C, "void":C, "throw":C, "debugger":kw("debugger"), "var":kw("var"), "const":kw("var"), let:kw("var"), "function":kw("function"), "catch":kw("catch"), "for":kw("for"), "switch":kw("switch"), "case":kw("case"), "default":kw("default"), "in":operator, "typeof":operator, "instanceof":operator, "true":atom, "false":atom, "null":atom, undefined:atom, 
        NaN:atom, Infinity:atom, "this":kw("this"), "class":kw("class"), "super":kw("atom"), yield:C, "export":kw("export"), "import":kw("import"), "extends":C, await:C};
      }(), isOperatorChar = /[+\-*&%=<>!?|~^@]/, isJsonldKeyword = /^@(context|id|value|language|type|container|list|set|reverse|index|base|vocab|graph)"/, type, content, atomicTypes = {atom:!0, number:!0, variable:!0, string:!0, regexp:!0, "this":!0, "import":!0, "jsonld-keyword":!0};
      var JSCompiler_object_inline_cc_5404 = JSCompiler_object_inline_marked_5403 = JSCompiler_object_inline_state_5401 = null;
      var JSCompiler_object_inline_style_5405 = JSCompiler_object_inline_stream_5402 = void 0;
      var defaultVars = new Var("this", new Var("arguments", null));
      pushcontext.lex = pushblockcontext.lex = !0;
      popcontext.lex = !0;
      poplex.lex = !0;
      return {startState:function(basecolumn) {
        basecolumn = {tokenize:tokenBase, lastType:"sof", cc:[], lexical:new JSLexical((basecolumn || 0) - indentUnit, 0, "block", !1), localVars:parserConfig.localVars, context:parserConfig.localVars && new Context(null, null, !1), indented:basecolumn || 0};
        parserConfig.globalVars && "object" == typeof parserConfig.globalVars && (basecolumn.globalVars = parserConfig.globalVars);
        return basecolumn;
      }, token:function(stream, state) {
        stream.sol() && (state.lexical.hasOwnProperty("align") || (state.lexical.align = !1), state.indented = stream.indentation(), findFatArrow(stream, state));
        if (state.tokenize != tokenComment && stream.eatSpace()) {
          return null;
        }
        var style = state.tokenize(stream, state);
        if ("comment" == type) {
          return style;
        }
        state.lastType = "operator" != type || "++" != content && "--" != content ? type : "incdec";
        return parseJS(state, style, type, content, stream);
      }, indent:function(state, textAfter) {
        if (state.tokenize == tokenComment || state.tokenize == tokenQuasi) {
          return CodeMirror.Pass;
        }
        if (state.tokenize != tokenBase) {
          return 0;
        }
        var firstChar = textAfter && textAfter.charAt(0), lexical = state.lexical, top;
        if (!/^\s*else\b/.test(textAfter)) {
          for (var i = state.cc.length - 1; 0 <= i; --i) {
            var c = state.cc[i];
            if (c == poplex) {
              lexical = lexical.prev;
            } else if (c != maybeelse && c != popcontext) {
              break;
            }
          }
        }
        for (; !("stat" != lexical.type && "form" != lexical.type || "}" != firstChar && (!(top = state.cc[state.cc.length - 1]) || top != maybeoperatorComma && top != maybeoperatorNoComma || /^[,\.=+\-*:?[\(]/.test(textAfter)));) {
          lexical = lexical.prev;
        }
        statementIndent && ")" == lexical.type && "stat" == lexical.prev.type && (lexical = lexical.prev);
        top = lexical.type;
        i = firstChar == top;
        return "vardef" == top ? lexical.indented + ("operator" == state.lastType || "," == state.lastType ? lexical.info.length + 1 : 0) : "form" == top && "{" == firstChar ? lexical.indented : "form" == top ? lexical.indented + indentUnit : "stat" == top ? (firstChar = lexical.indented, state = "operator" == state.lastType || "," == state.lastType || isOperatorChar.test(textAfter.charAt(0)) || /[,.]/.test(textAfter.charAt(0)), firstChar + (state ? statementIndent || indentUnit : 0)) : "switch" != 
        lexical.info || i || 0 == parserConfig.doubleIndentSwitch ? lexical.align ? lexical.column + (i ? 0 : 1) : lexical.indented + (i ? 0 : indentUnit) : lexical.indented + (/^(?:case|default)\b/.test(textAfter) ? indentUnit : 2 * indentUnit);
      }, electricInput:/^\s*(?:case .*?:|default:|\{|\})$/, blockCommentStart:jsonMode ? null : "/*", blockCommentEnd:jsonMode ? null : "*/", blockCommentContinue:jsonMode ? null : " * ", lineComment:jsonMode ? null : "//", fold:"brace", closeBrackets:"()[]{}''\"\"``", helperType:jsonMode ? "json" : "javascript", jsonldMode, jsonMode, expressionAllowed, skipExpression:function(state) {
        parseJS(state, "atom", "atom", "true", new CodeMirror.StringStream("", 2, null));
      }};
    });
    CodeMirror.registerHelper("wordChars", "javascript", /[\w$]/);
    CodeMirror.defineMIME("text/javascript", "javascript");
    CodeMirror.defineMIME("text/ecmascript", "javascript");
    CodeMirror.defineMIME("application/javascript", "javascript");
    CodeMirror.defineMIME("application/x-javascript", "javascript");
    CodeMirror.defineMIME("application/ecmascript", "javascript");
    CodeMirror.defineMIME("application/json", {name:"javascript", json:!0});
    CodeMirror.defineMIME("application/x-json", {name:"javascript", json:!0});
    CodeMirror.defineMIME("application/manifest+json", {name:"javascript", json:!0});
    CodeMirror.defineMIME("application/ld+json", {name:"javascript", jsonld:!0});
    CodeMirror.defineMIME("text/typescript", {name:"javascript", typescript:!0});
    CodeMirror.defineMIME("application/typescript", {name:"javascript", typescript:!0});
  });
};

//# sourceMappingURL=module$node_modules$codemirror$mode$javascript$javascript.js.map
