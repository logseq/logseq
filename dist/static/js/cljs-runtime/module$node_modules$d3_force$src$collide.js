shadow$provide.module$node_modules$d3_force$src$collide = function(global, require, module, exports) {
  function x(d) {
    return d.x + d.vx;
  }
  function y(d) {
    return d.y + d.vy;
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, default:{enumerable:!0, get:function() {
    return $$default;
  }}});
  var require$d3_quadtree = require("module$node_modules$d3_quadtree$src$index");
  global = require("module$node_modules$d3_force$src$constant");
  module = require("module$node_modules$d3_force$src$jiggle");
  var default$$require$_DOT__SLASH_jiggle_DOT_js = require.esmDefault(module), default$$require$_DOT__SLASH_constant_DOT_js = require.esmDefault(global);
  const $$default = function(radius) {
    function force() {
      function apply(quad, x0, y0, x1, y1) {
        var data = quad.data;
        quad = quad.r;
        var r = ri + quad;
        if (data) {
          data.index > node.index && (x0 = xi - data.x - data.vx, y0 = yi - data.y - data.vy, x1 = x0 * x0 + y0 * y0, x1 < r * r && (0 === x0 && (x0 = (0,default$$require$_DOT__SLASH_jiggle_DOT_js.default)(random), x1 += x0 * x0), 0 === y0 && (y0 = (0,default$$require$_DOT__SLASH_jiggle_DOT_js.default)(random), x1 += y0 * y0), x1 = (r - (x1 = Math.sqrt(x1))) / x1 * strength, node.vx += (x0 *= x1) * (r = (quad *= quad) / (ri2 + quad)), node.vy += (y0 *= x1) * r, data.vx -= x0 * (r = 1 - r), data.vy -= 
          y0 * r));
        } else {
          return x0 > xi + r || x1 < xi - r || y0 > yi + r || y1 < yi - r;
        }
      }
      for (var i, n = nodes.length, tree, node, xi, yi, ri, ri2, k = 0; k < iterations; ++k) {
        for (tree = (0,require$d3_quadtree.quadtree)(nodes, x, y).visitAfter(prepare), i = 0; i < n; ++i) {
          node = nodes[i], ri = radii[node.index], ri2 = ri * ri, xi = node.x + node.vx, yi = node.y + node.vy, tree.visit(apply);
        }
      }
    }
    function prepare(quad) {
      if (quad.data) {
        return quad.r = radii[quad.data.index];
      }
      for (var i = quad.r = 0; 4 > i; ++i) {
        quad[i] && quad[i].r > quad.r && (quad.r = quad[i].r);
      }
    }
    function initialize() {
      if (nodes) {
        var i, n = nodes.length;
        radii = Array(n);
        for (i = 0; i < n; ++i) {
          var node = nodes[i];
          radii[node.index] = +radius(node, i, nodes);
        }
      }
    }
    var nodes, radii, random, strength = 1, iterations = 1;
    "function" !== typeof radius && (radius = (0,default$$require$_DOT__SLASH_constant_DOT_js.default)(null == radius ? 1 : +radius));
    force.initialize = function(_nodes, _random) {
      nodes = _nodes;
      random = _random;
      initialize();
    };
    force.iterations = function(_) {
      return arguments.length ? (iterations = +_, force) : iterations;
    };
    force.strength = function(_) {
      return arguments.length ? (strength = +_, force) : strength;
    };
    force.radius = function(_) {
      return arguments.length ? (radius = "function" === typeof _ ? _ : (0,default$$require$_DOT__SLASH_constant_DOT_js.default)(+_), initialize(), force) : radius;
    };
    return force;
  };
};

//# sourceMappingURL=module$node_modules$d3_force$src$collide.js.map
