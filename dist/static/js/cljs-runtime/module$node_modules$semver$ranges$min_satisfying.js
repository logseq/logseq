shadow$provide.module$node_modules$semver$ranges$min_satisfying = function(global, require, module, exports) {
  const SemVer = require("module$node_modules$semver$classes$semver"), Range = require("module$node_modules$semver$classes$range");
  module.exports = (versions, range, options) => {
    let min = null, minSV = null, rangeObj = null;
    try {
      rangeObj = new Range(range, options);
    } catch (er) {
      return null;
    }
    versions.forEach(v => {
      !rangeObj.test(v) || min && 1 !== minSV.compare(v) || (min = v, minSV = new SemVer(min, options));
    });
    return min;
  };
};

//# sourceMappingURL=module$node_modules$semver$ranges$min_satisfying.js.map
