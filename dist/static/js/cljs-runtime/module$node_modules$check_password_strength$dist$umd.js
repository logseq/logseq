shadow$provide.module$node_modules$check_password_strength$dist$umd = function(global, require, module, exports) {
  (function(factory) {
    "function" === typeof define && define.amd ? define(factory) : factory();
  })(function() {
    const defaultOptions = [{id:0, value:"Too weak", minDiversity:0, minLength:0}, {id:1, value:"Weak", minDiversity:2, minLength:6}, {id:2, value:"Medium", minDiversity:4, minLength:8}, {id:3, value:"Strong", minDiversity:4, minLength:10}];
    module.exports = {passwordStrength:(password, options = defaultOptions, allowedSymbols = "!\"#$%\x26'()*+,-./:;\x3c\x3d\x3e?@[\\\\\\]^_`{|}~") => {
      let passwordCopy = password || "";
      options[0].minDiversity = 0;
      options[0].minLength = 0;
      password = [{regex:"[a-z]", message:"lowercase"}, {regex:"[A-Z]", message:"uppercase"}, {regex:"[0-9]", message:"number"}];
      allowedSymbols && password.push({regex:`[${allowedSymbols}]`, message:"symbol"});
      let strength = {};
      strength.contains = password.filter(rule => (new RegExp(`${rule.regex}`)).test(passwordCopy)).map(rule => rule.message);
      strength.length = passwordCopy.length;
      options = options.filter(option => strength.contains.length >= option.minDiversity).filter(option => strength.length >= option.minLength).sort((o1, o2) => o2.id - o1.id).map(option => ({id:option.id, value:option.value}));
      Object.assign(strength, options[0]);
      return strength;
    }, defaultOptions};
  });
};

//# sourceMappingURL=module$node_modules$check_password_strength$dist$umd.js.map
