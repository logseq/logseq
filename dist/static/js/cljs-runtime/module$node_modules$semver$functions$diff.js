shadow$provide.module$node_modules$semver$functions$diff = function(global, require, module, exports) {
  const parse = require("module$node_modules$semver$functions$parse");
  module.exports = (version1, version2) => {
    version1 = parse(version1, null, !0);
    version2 = parse(version2, null, !0);
    var comparison = version1.compare(version2);
    if (0 === comparison) {
      return null;
    }
    var v1Higher = 0 < comparison;
    comparison = v1Higher ? version1 : version2;
    v1Higher = v1Higher ? version2 : version1;
    const highHasPre = !!comparison.prerelease.length;
    if (v1Higher.prerelease.length && !highHasPre) {
      return v1Higher.patch || v1Higher.minor ? comparison.patch ? "patch" : comparison.minor ? "minor" : "major" : "major";
    }
    comparison = highHasPre ? "pre" : "";
    return version1.major !== version2.major ? comparison + "major" : version1.minor !== version2.minor ? comparison + "minor" : version1.patch !== version2.patch ? comparison + "patch" : "prerelease";
  };
};

//# sourceMappingURL=module$node_modules$semver$functions$diff.js.map
