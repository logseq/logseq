shadow$provide.module$node_modules$codemirror$mode$commonlisp$commonlisp = function(global, require, module, exports) {
  (function(mod) {
    "object" == typeof exports && "object" == typeof module ? mod(require("module$node_modules$codemirror$lib$codemirror")) : "function" == typeof define && define.amd ? define(["../../lib/codemirror"], mod) : mod(CodeMirror);
  })(function(CodeMirror) {
    CodeMirror.defineMode("commonlisp", function(config) {
      function readSym(stream) {
        for (var ch; ch = stream.next();) {
          if ("\\" == ch) {
            stream.next();
          } else if (!symbol.test(ch)) {
            stream.backUp(1);
            break;
          }
        }
        return stream.current();
      }
      function base(stream, state) {
        if (stream.eatSpace()) {
          return type = "ws", null;
        }
        if (stream.match(numLiteral)) {
          return "number";
        }
        var ch = stream.next();
        "\\" == ch && (ch = stream.next());
        if ('"' == ch) {
          return (state.tokenize = inString)(stream, state);
        }
        if ("(" == ch) {
          return type = "open", "bracket";
        }
        if (")" == ch || "]" == ch) {
          return type = "close", "bracket";
        }
        if (";" == ch) {
          return stream.skipToEnd(), type = "ws", "comment";
        }
        if (/['`,@]/.test(ch)) {
          return null;
        }
        if ("|" == ch) {
          if (stream.skipTo("|")) {
            return stream.next(), "symbol";
          }
          stream.skipToEnd();
          return "error";
        }
        if ("#" == ch) {
          return ch = stream.next(), "(" == ch ? (type = "open", "bracket") : /[+\-=\.']/.test(ch) || /\d/.test(ch) && stream.match(/^\d*#/) ? null : "|" == ch ? (state.tokenize = inComment)(stream, state) : ":" == ch ? (readSym(stream), "meta") : "\\" == ch ? (stream.next(), readSym(stream), "string-2") : "error";
        }
        stream = readSym(stream);
        if ("." == stream) {
          return null;
        }
        type = "symbol";
        return "nil" == stream || "t" == stream || ":" == stream.charAt(0) ? "atom" : "open" == state.lastType && (specialForm.test(stream) || assumeBody.test(stream)) ? "keyword" : "\x26" == stream.charAt(0) ? "variable-2" : "variable";
      }
      function inString(stream, state) {
        for (var escaped = !1, next; next = stream.next();) {
          if ('"' == next && !escaped) {
            state.tokenize = base;
            break;
          }
          escaped = !escaped && "\\" == next;
        }
        return "string";
      }
      function inComment(stream, state) {
        for (var next, last; next = stream.next();) {
          if ("#" == next && "|" == last) {
            state.tokenize = base;
            break;
          }
          last = next;
        }
        type = "ws";
        return "comment";
      }
      var specialForm = /^(block|let*|return-from|catch|load-time-value|setq|eval-when|locally|symbol-macrolet|flet|macrolet|tagbody|function|multiple-value-call|the|go|multiple-value-prog1|throw|if|progn|unwind-protect|labels|progv|let|quote)$/, assumeBody = /^with|^def|^do|^prog|case$|^cond$|bind$|when$|unless$/, numLiteral = /^(?:[+\-]?(?:\d+|\d*\.\d+)(?:[efd][+\-]?\d+)?|[+\-]?\d+(?:\/[+\-]?\d+)?|#b[+\-]?[01]+|#o[+\-]?[0-7]+|#x[+\-]?[\da-f]+)/, symbol = /[^\s'`,@()\[\]";]/, type;
      return {startState:function() {
        return {ctx:{prev:null, start:0, indentTo:0}, lastType:null, tokenize:base};
      }, token:function(stream, state) {
        stream.sol() && "number" != typeof state.ctx.indentTo && (state.ctx.indentTo = state.ctx.start + 1);
        type = null;
        var style = state.tokenize(stream, state);
        "ws" != type && (null == state.ctx.indentTo ? "symbol" == type && assumeBody.test(stream.current()) ? state.ctx.indentTo = state.ctx.start + config.indentUnit : state.ctx.indentTo = "next" : "next" == state.ctx.indentTo && (state.ctx.indentTo = stream.column()), state.lastType = type);
        "open" == type ? state.ctx = {prev:state.ctx, start:stream.column(), indentTo:null} : "close" == type && (state.ctx = state.ctx.prev || state.ctx);
        return style;
      }, indent:function(state, _textAfter) {
        _textAfter = state.ctx.indentTo;
        return "number" == typeof _textAfter ? _textAfter : state.ctx.start + 1;
      }, closeBrackets:{pairs:'()[]{}""'}, lineComment:";;", fold:"brace-paren", blockCommentStart:"#|", blockCommentEnd:"|#"};
    });
    CodeMirror.defineMIME("text/x-common-lisp", "commonlisp");
  });
};

//# sourceMappingURL=module$node_modules$codemirror$mode$commonlisp$commonlisp.js.map
