shadow$provide.module$node_modules$semver$ranges$max_satisfying = function(global, require, module, exports) {
  const SemVer = require("module$node_modules$semver$classes$semver"), Range = require("module$node_modules$semver$classes$range");
  module.exports = (versions, range, options) => {
    let max = null, maxSV = null, rangeObj = null;
    try {
      rangeObj = new Range(range, options);
    } catch (er) {
      return null;
    }
    versions.forEach(v => {
      !rangeObj.test(v) || max && -1 !== maxSV.compare(v) || (max = v, maxSV = new SemVer(max, options));
    });
    return max;
  };
};

//# sourceMappingURL=module$node_modules$semver$ranges$max_satisfying.js.map
