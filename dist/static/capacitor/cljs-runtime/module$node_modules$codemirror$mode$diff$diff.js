shadow$provide.module$node_modules$codemirror$mode$diff$diff = function(global, require, module, exports) {
  (function(mod) {
    "object" == typeof exports && "object" == typeof module ? mod(require("module$node_modules$codemirror$lib$codemirror")) : "function" == typeof define && define.amd ? define(["../../lib/codemirror"], mod) : mod(CodeMirror);
  })(function(CodeMirror) {
    CodeMirror.defineMode("diff", function() {
      var TOKEN_NAMES = {"+":"positive", "-":"negative", "@":"meta"};
      return {token:function(stream) {
        var tw_pos = stream.string.search(/[\t ]+?$/);
        if (!stream.sol() || 0 === tw_pos) {
          return stream.skipToEnd(), ("error " + (TOKEN_NAMES[stream.string.charAt(0)] || "")).replace(/ $/, "");
        }
        var token_name = TOKEN_NAMES[stream.peek()] || stream.skipToEnd();
        -1 === tw_pos ? stream.skipToEnd() : stream.pos = tw_pos;
        return token_name;
      }};
    });
    CodeMirror.defineMIME("text/x-diff", "diff");
  });
};

//# sourceMappingURL=module$node_modules$codemirror$mode$diff$diff.js.map
