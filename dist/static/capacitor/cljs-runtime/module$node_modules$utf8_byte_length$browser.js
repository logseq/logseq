shadow$provide.module$node_modules$utf8_byte_length$browser = function(global, require, module, exports) {
  module.exports = function(string) {
    if ("string" !== typeof string) {
      throw Error("Input must be string");
    }
    for (var charLength = string.length, byteLength = 0, codePoint, prevCodePoint = null, i = 0; i < charLength; i++) {
      codePoint = string.charCodeAt(i), 56320 <= codePoint && 57343 >= codePoint ? byteLength = null != prevCodePoint && 55296 <= prevCodePoint && 56319 >= prevCodePoint ? byteLength + 1 : byteLength + 3 : 127 >= codePoint ? byteLength += 1 : 128 <= codePoint && 2047 >= codePoint ? byteLength += 2 : 2048 <= codePoint && 65535 >= codePoint && (byteLength += 3), prevCodePoint = codePoint;
    }
    return byteLength;
  };
};

//# sourceMappingURL=module$node_modules$utf8_byte_length$browser.js.map
