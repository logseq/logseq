shadow$provide.module$node_modules$jszip$lib$flate = function(global, require, module, exports) {
  function FlateWorker(action, options) {
    GenericWorker.call(this, "FlateWorker/" + action);
    this._pako = null;
    this._pakoAction = action;
    this._pakoOptions = options;
    this.meta = {};
  }
  global = "undefined" !== typeof Uint8Array && "undefined" !== typeof Uint16Array && "undefined" !== typeof Uint32Array;
  var pako = require("module$node_modules$pako$index"), utils = require("module$node_modules$jszip$lib$utils"), GenericWorker = require("module$node_modules$jszip$lib$stream$GenericWorker"), ARRAY_TYPE = global ? "uint8array" : "array";
  exports.magic = "\b\x00";
  utils.inherits(FlateWorker, GenericWorker);
  FlateWorker.prototype.processChunk = function(chunk) {
    this.meta = chunk.meta;
    null === this._pako && this._createPako();
    this._pako.push(utils.transformTo(ARRAY_TYPE, chunk.data), !1);
  };
  FlateWorker.prototype.flush = function() {
    GenericWorker.prototype.flush.call(this);
    null === this._pako && this._createPako();
    this._pako.push([], !0);
  };
  FlateWorker.prototype.cleanUp = function() {
    GenericWorker.prototype.cleanUp.call(this);
    this._pako = null;
  };
  FlateWorker.prototype._createPako = function() {
    this._pako = new pako[this._pakoAction]({raw:!0, level:this._pakoOptions.level || -1});
    var self = this;
    this._pako.onData = function(data) {
      self.push({data, meta:self.meta});
    };
  };
  exports.compressWorker = function(compressionOptions) {
    return new FlateWorker("Deflate", compressionOptions);
  };
  exports.uncompressWorker = function() {
    return new FlateWorker("Inflate", {});
  };
};

//# sourceMappingURL=module$node_modules$jszip$lib$flate.js.map
