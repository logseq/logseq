shadow$provide.module$node_modules$codemirror$mode$webidl$webidl = function(global, require, module, exports) {
  (function(mod) {
    "object" == typeof exports && "object" == typeof module ? mod(require("module$node_modules$codemirror$lib$codemirror")) : "function" == typeof define && define.amd ? define(["../../lib/codemirror"], mod) : mod(CodeMirror);
  })(function(CodeMirror) {
    function wordRegexp(words) {
      return new RegExp("^((" + words.join(")|(") + "))\\b");
    }
    function readToken(stream, state) {
      if (stream.eatSpace()) {
        return null;
      }
      if (state.inComment) {
        if (stream.match(multilineCommentsEnd)) {
          return state.inComment = !1, "comment";
        }
        stream.skipToEnd();
        return "comment";
      }
      if (stream.match("//")) {
        return stream.skipToEnd(), "comment";
      }
      if (stream.match(multilineComments)) {
        return "comment";
      }
      if (stream.match(multilineCommentsStart)) {
        return state.inComment = !0, "comment";
      }
      if (stream.match(/^-?[0-9\.]/, !1) && (stream.match(integers) || stream.match(floats))) {
        return "number";
      }
      if (stream.match(strings)) {
        return "string";
      }
      if (state.startDef && stream.match(identifiers)) {
        return "def";
      }
      if (state.endDef && stream.match(identifiersEnd)) {
        return state.endDef = !1, "def";
      }
      if (stream.match(keywords)) {
        return "keyword";
      }
      if (stream.match(types)) {
        return state = state.lastToken, stream = (stream.match(/^\s*(.+?)\b/, !1) || [])[1], ":" === state || "implements" === state || "implements" === stream || "\x3d" === stream ? "builtin" : "variable-3";
      }
      if (stream.match(builtins)) {
        return "builtin";
      }
      if (stream.match(atoms)) {
        return "atom";
      }
      if (stream.match(identifiers)) {
        return "variable";
      }
      if (stream.match(singleOperators)) {
        return "operator";
      }
      stream.next();
      return null;
    }
    var builtinArray = "Clamp Constructor EnforceRange Exposed ImplicitThis Global PrimaryGlobal LegacyArrayClass LegacyUnenumerableNamedProperties LenientThis NamedConstructor NewObject NoInterfaceObject OverrideBuiltins PutForwards Replaceable SameObject TreatNonObjectAsNull TreatNullAs EmptyString Unforgeable Unscopeable".split(" "), builtins = wordRegexp(builtinArray), typeArray = "unsigned short long unrestricted float double boolean byte octet Promise ArrayBuffer DataView Int8Array Int16Array Int32Array Uint8Array Uint16Array Uint32Array Uint8ClampedArray Float32Array Float64Array ByteString DOMString USVString sequence object RegExp Error DOMException FrozenArray any void".split(" "), 
    types = wordRegexp(typeArray), keywordArray = "attribute callback const deleter dictionary enum getter implements inherit interface iterable legacycaller maplike partial required serializer setlike setter static stringifier typedef optional readonly or".split(" "), keywords = wordRegexp(keywordArray), atomArray = ["true", "false", "Infinity", "NaN", "null"], atoms = wordRegexp(atomArray);
    CodeMirror.registerHelper("hintWords", "webidl", builtinArray.concat(typeArray).concat(keywordArray).concat(atomArray));
    var startDefs = wordRegexp(["callback", "dictionary", "enum", "interface"]), endDefs = wordRegexp(["typedef"]), singleOperators = /^[:<=>?]/, integers = /^-?([1-9][0-9]*|0[Xx][0-9A-Fa-f]+|0[0-7]*)/, floats = /^-?(([0-9]+\.[0-9]*|[0-9]*\.[0-9]+)([Ee][+-]?[0-9]+)?|[0-9]+[Ee][+-]?[0-9]+)/, identifiers = /^_?[A-Za-z][0-9A-Z_a-z-]*/, identifiersEnd = /^_?[A-Za-z][0-9A-Z_a-z-]*(?=\s*;)/, strings = /^"[^"]*"/, multilineComments = /^\/\*.*?\*\//, multilineCommentsStart = /^\/\*.*/, multilineCommentsEnd = 
    /^.*?\*\//;
    CodeMirror.defineMode("webidl", function() {
      return {startState:function() {
        return {inComment:!1, lastToken:"", startDef:!1, endDef:!1};
      }, token:function(stream, state) {
        var style = readToken(stream, state);
        style && (stream = stream.current(), state.lastToken = stream, "keyword" === style ? (state.startDef = startDefs.test(stream), state.endDef = state.endDef || endDefs.test(stream)) : state.startDef = !1);
        return style;
      }};
    });
    CodeMirror.defineMIME("text/x-webidl", "webidl");
  });
};

//# sourceMappingURL=module$node_modules$codemirror$mode$webidl$webidl.js.map
