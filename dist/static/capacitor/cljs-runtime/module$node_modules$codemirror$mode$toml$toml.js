shadow$provide.module$node_modules$codemirror$mode$toml$toml = function(global, require, module, exports) {
  (function(mod) {
    "object" == typeof exports && "object" == typeof module ? mod(require("module$node_modules$codemirror$lib$codemirror")) : "function" == typeof define && define.amd ? define(["../../lib/codemirror"], mod) : mod(CodeMirror);
  })(function(CodeMirror) {
    CodeMirror.defineMode("toml", function() {
      return {startState:function() {
        return {inString:!1, stringType:"", lhs:!0, inArray:0};
      }, token:function(stream, state) {
        state.inString || '"' != stream.peek() && "'" != stream.peek() || (state.stringType = stream.peek(), stream.next(), state.inString = !0);
        stream.sol() && 0 === state.inArray && (state.lhs = !0);
        if (state.inString) {
          for (; state.inString && !stream.eol();) {
            stream.peek() === state.stringType ? (stream.next(), state.inString = !1) : "\\" === stream.peek() ? (stream.next(), stream.next()) : stream.match(/^.[^\\"']*/);
          }
          return state.lhs ? "property string" : "string";
        }
        if (state.inArray && "]" === stream.peek()) {
          return stream.next(), state.inArray--, "bracket";
        }
        if (state.lhs && "[" === stream.peek() && stream.skipTo("]")) {
          return stream.next(), "]" === stream.peek() && stream.next(), "atom";
        }
        if ("#" === stream.peek()) {
          return stream.skipToEnd(), "comment";
        }
        if (!stream.eatSpace()) {
          if (state.lhs && stream.eatWhile(function(c) {
            return "\x3d" != c && " " != c;
          })) {
            return "property";
          }
          if (state.lhs && "\x3d" === stream.peek()) {
            stream.next(), state.lhs = !1;
          } else {
            if (!state.lhs && stream.match(/^\d\d\d\d[\d\-:\.T]*Z/) || !state.lhs && (stream.match("true") || stream.match("false"))) {
              return "atom";
            }
            if (state.lhs || "[" !== stream.peek()) {
              if (!state.lhs && stream.match(/^\-?\d+(?:\.\d+)?/)) {
                return "number";
              }
              stream.eatSpace() || stream.next();
            } else {
              return state.inArray++, stream.next(), "bracket";
            }
          }
        }
        return null;
      }};
    });
    CodeMirror.defineMIME("text/x-toml", "toml");
  });
};

//# sourceMappingURL=module$node_modules$codemirror$mode$toml$toml.js.map
