shadow$provide.module$node_modules$$capacitor$filesystem$dist$plugin_cjs = function(global, require, module, exports) {
  function resolve(path) {
    const newPosix = [];
    path.split("/").filter(item => "." !== item).forEach(item => {
      ".." === item && 0 < newPosix.length && ".." !== newPosix[newPosix.length - 1] ? newPosix.pop() : newPosix.push(item);
    });
    return newPosix.join("/");
  }
  function isPathParent(parent, children) {
    parent = resolve(parent);
    children = resolve(children);
    const pathsA = parent.split("/"), pathsB = children.split("/");
    return parent !== children && pathsA.every((value, index) => value === pathsB[index]);
  }
  var core = require("module$node_modules$$capacitor$core$dist$index_cjs");
  exports.Directory = void 0;
  (function(Directory) {
    Directory.Documents = "DOCUMENTS";
    Directory.Data = "DATA";
    Directory.Library = "LIBRARY";
    Directory.Cache = "CACHE";
    Directory.External = "EXTERNAL";
    Directory.ExternalStorage = "EXTERNAL_STORAGE";
  })(exports.Directory || (exports.Directory = {}));
  exports.Encoding = void 0;
  (function(Encoding) {
    Encoding.UTF8 = "utf8";
    Encoding.ASCII = "ascii";
    Encoding.UTF16 = "utf16";
  })(exports.Encoding || (exports.Encoding = {}));
  global = exports.Directory;
  require = exports.Encoding;
  module = core.registerPlugin("Filesystem", {web:() => Promise.resolve().then(function() {
    return web;
  }).then(m => new m.FilesystemWeb())});
  class FilesystemWeb extends core.WebPlugin {
    constructor() {
      super(...arguments);
      this.DB_VERSION = 1;
      this.DB_NAME = "Disc";
      this._writeCmds = ["add", "put", "delete"];
      this.downloadFile = async options => {
        var _a, _b, requestInit = core.buildRequestInit(options, options.webFetchExtra), response = await fetch(options.url, requestInit);
        if (options.progress) {
          if (null === response || void 0 === response ? 0 : response.body) {
            var reader = response.body.getReader(), bytes = 0;
            requestInit = [];
            const contentType = response.headers.get("content-type");
            for (response = parseInt(response.headers.get("content-length") || "0", 10);;) {
              const {done, value} = await reader.read();
              if (done) {
                break;
              }
              requestInit.push(value);
              bytes += (null === value || void 0 === value ? void 0 : value.length) || 0;
              this.notifyListeners("progress", {url:options.url, bytes, contentLength:response});
            }
            reader = new Uint8Array(bytes);
            bytes = 0;
            for (blob of requestInit) {
              "undefined" !== typeof blob && (reader.set(blob, bytes), bytes += blob.length);
            }
            var blob = new Blob([reader.buffer], {type:contentType || void 0});
          } else {
            blob = new Blob();
          }
        } else {
          blob = await response.blob();
        }
        return {path:(await this.writeFile({path:options.path, directory:null !== (_a = options.directory) && void 0 !== _a ? _a : void 0, recursive:null !== (_b = options.recursive) && void 0 !== _b ? _b : !1, data:blob})).uri, blob};
      };
    }
    async initDb() {
      if (void 0 !== this._db) {
        return this._db;
      }
      if (!("indexedDB" in window)) {
        throw this.unavailable("This browser doesn't support IndexedDB");
      }
      return new Promise((resolve, reject) => {
        const request = indexedDB.open(this.DB_NAME, this.DB_VERSION);
        request.onupgradeneeded = FilesystemWeb.doUpgrade;
        request.onsuccess = () => {
          this._db = request.result;
          resolve(request.result);
        };
        request.onerror = () => reject(request.error);
        request.onblocked = () => {
          console.warn("db blocked");
        };
      });
    }
    static doUpgrade(event) {
      event = event.target.result;
      event.objectStoreNames.contains("FileStorage") && event.deleteObjectStore("FileStorage");
      event.createObjectStore("FileStorage", {keyPath:"path"}).createIndex("by_folder", "folder");
    }
    async dbRequest(cmd, args) {
      const readFlag = -1 !== this._writeCmds.indexOf(cmd) ? "readwrite" : "readonly";
      return this.initDb().then(conn => new Promise((resolve, reject) => {
        const req = conn.transaction(["FileStorage"], readFlag).objectStore("FileStorage")[cmd](...args);
        req.onsuccess = () => resolve(req.result);
        req.onerror = () => reject(req.error);
      }));
    }
    async dbIndexRequest(indexName, cmd, args) {
      const readFlag = -1 !== this._writeCmds.indexOf(cmd) ? "readwrite" : "readonly";
      return this.initDb().then(conn => new Promise((resolve, reject) => {
        const req = conn.transaction(["FileStorage"], readFlag).objectStore("FileStorage").index(indexName)[cmd](...args);
        req.onsuccess = () => resolve(req.result);
        req.onerror = () => reject(req.error);
      }));
    }
    getPath(directory, uriPath) {
      const cleanedUriPath = void 0 !== uriPath ? uriPath.replace(/^[/]+|[/]+$/g, "") : "";
      let fsPath = "";
      void 0 !== directory && (fsPath += "/" + directory);
      "" !== uriPath && (fsPath += "/" + cleanedUriPath);
      return fsPath;
    }
    async clear() {
      (await this.initDb()).transaction(["FileStorage"], "readwrite").objectStore("FileStorage").clear();
    }
    async readFile(options) {
      options = this.getPath(options.directory, options.path);
      options = await this.dbRequest("get", [options]);
      if (void 0 === options) {
        throw Error("File does not exist.");
      }
      return {data:options.content ? options.content : ""};
    }
    async writeFile(options) {
      var path = this.getPath(options.directory, options.path);
      let data = options.data;
      const encoding = options.encoding, doRecursive = options.recursive;
      var occupiedEntry = await this.dbRequest("get", [path]);
      if (occupiedEntry && "directory" === occupiedEntry.type) {
        throw Error("The supplied path is a directory.");
      }
      occupiedEntry = path.substr(0, path.lastIndexOf("/"));
      if (void 0 === await this.dbRequest("get", [occupiedEntry])) {
        var subDirIndex = occupiedEntry.indexOf("/", 1);
        -1 !== subDirIndex && (subDirIndex = occupiedEntry.substr(subDirIndex), await this.mkdir({path:subDirIndex, directory:options.directory, recursive:doRecursive}));
      }
      if (!(encoding || data instanceof Blob || (data = 0 <= data.indexOf(",") ? data.split(",")[1] : data, this.isBase64String(data)))) {
        throw Error("The supplied data is not valid base64 content.");
      }
      options = Date.now();
      path = {path, folder:occupiedEntry, type:"file", size:data instanceof Blob ? data.size : data.length, ctime:options, mtime:options, content:data};
      await this.dbRequest("put", [path]);
      return {uri:path.path};
    }
    async appendFile(options) {
      const path = this.getPath(options.directory, options.path);
      let data = options.data;
      const encoding = options.encoding, parentPath = path.substr(0, path.lastIndexOf("/")), now = Date.now();
      let ctime = now;
      const occupiedEntry = await this.dbRequest("get", [path]);
      if (occupiedEntry && "directory" === occupiedEntry.type) {
        throw Error("The supplied path is a directory.");
      }
      if (void 0 === await this.dbRequest("get", [parentPath])) {
        var subDirIndex = parentPath.indexOf("/", 1);
        -1 !== subDirIndex && (subDirIndex = parentPath.substr(subDirIndex), await this.mkdir({path:subDirIndex, directory:options.directory, recursive:!0}));
      }
      if (!encoding && !this.isBase64String(data)) {
        throw Error("The supplied data is not valid base64 content.");
      }
      if (void 0 !== occupiedEntry) {
        if (occupiedEntry.content instanceof Blob) {
          throw Error("The occupied entry contains a Blob object which cannot be appended to.");
        }
        data = void 0 === occupiedEntry.content || encoding ? occupiedEntry.content + data : btoa(atob(occupiedEntry.content) + atob(data));
        ctime = occupiedEntry.ctime;
      }
      await this.dbRequest("put", [{path, folder:parentPath, type:"file", size:data.length, ctime, mtime:now, content:data}]);
    }
    async deleteFile(options) {
      options = this.getPath(options.directory, options.path);
      if (void 0 === await this.dbRequest("get", [options])) {
        throw Error("File does not exist.");
      }
      if (0 !== (await this.dbIndexRequest("by_folder", "getAllKeys", [IDBKeyRange.only(options)])).length) {
        throw Error("Folder is not empty.");
      }
      await this.dbRequest("delete", [options]);
    }
    async mkdir(options) {
      const path = this.getPath(options.directory, options.path), doRecursive = options.recursive, parentPath = path.substr(0, path.lastIndexOf("/"));
      var depth = (path.match(/\//g) || []).length;
      const parentEntry = await this.dbRequest("get", [parentPath]), occupiedEntry = await this.dbRequest("get", [path]);
      if (1 === depth) {
        throw Error("Cannot create Root directory");
      }
      if (void 0 !== occupiedEntry) {
        throw Error("Current directory does already exist.");
      }
      if (!doRecursive && 2 !== depth && void 0 === parentEntry) {
        throw Error("Parent directory must exist");
      }
      doRecursive && 2 !== depth && void 0 === parentEntry && (depth = parentPath.substr(parentPath.indexOf("/", 1)), await this.mkdir({path:depth, directory:options.directory, recursive:doRecursive}));
      options = Date.now();
      await this.dbRequest("put", [{path, folder:parentPath, type:"directory", size:0, ctime:options, mtime:options}]);
    }
    async rmdir(options) {
      const {path, directory, recursive} = options;
      options = this.getPath(directory, path);
      var entry$jscomp$0 = await this.dbRequest("get", [options]);
      if (void 0 === entry$jscomp$0) {
        throw Error("Folder does not exist.");
      }
      if ("directory" !== entry$jscomp$0.type) {
        throw Error("Requested path is not a directory");
      }
      entry$jscomp$0 = await this.readdir({path, directory});
      if (0 !== entry$jscomp$0.files.length && !recursive) {
        throw Error("Folder is not empty");
      }
      for (const entry of entry$jscomp$0.files) {
        entry$jscomp$0 = `${path}/${entry.name}`, "file" === (await this.stat({path:entry$jscomp$0, directory})).type ? await this.deleteFile({path:entry$jscomp$0, directory}) : await this.rmdir({path:entry$jscomp$0, directory, recursive});
      }
      await this.dbRequest("delete", [options]);
    }
    async readdir(options) {
      const path = this.getPath(options.directory, options.path), entry = await this.dbRequest("get", [path]);
      if ("" !== options.path && void 0 === entry) {
        throw Error("Folder does not exist.");
      }
      options = await this.dbIndexRequest("by_folder", "getAllKeys", [IDBKeyRange.only(path)]);
      return {files:await Promise.all(options.map(async e => {
        let subEntry = await this.dbRequest("get", [e]);
        void 0 === subEntry && (subEntry = await this.dbRequest("get", [e + "/"]));
        return {name:e.substring(path.length + 1), type:subEntry.type, size:subEntry.size, ctime:subEntry.ctime, mtime:subEntry.mtime, uri:subEntry.path};
      }))};
    }
    async getUri(options) {
      options = this.getPath(options.directory, options.path);
      let entry = await this.dbRequest("get", [options]);
      void 0 === entry && (entry = await this.dbRequest("get", [options + "/"]));
      return {uri:(null === entry || void 0 === entry ? void 0 : entry.path) || options};
    }
    async stat(options) {
      options = this.getPath(options.directory, options.path);
      let entry = await this.dbRequest("get", [options]);
      void 0 === entry && (entry = await this.dbRequest("get", [options + "/"]));
      if (void 0 === entry) {
        throw Error("Entry does not exist.");
      }
      return {type:entry.type, size:entry.size, ctime:entry.ctime, mtime:entry.mtime, uri:entry.path};
    }
    async rename(options) {
      await this._copy(options, !0);
    }
    async copy(options) {
      return this._copy(options, !1);
    }
    async requestPermissions() {
      return {publicStorage:"granted"};
    }
    async checkPermissions() {
      return {publicStorage:"granted"};
    }
    async _copy(options, doRename = !1) {
      let {toDirectory} = options;
      const {to, from, directory:fromDirectory} = options;
      if (!to || !from) {
        throw Error("Both to and from must be provided");
      }
      toDirectory ||= fromDirectory;
      var fromPath = this.getPath(fromDirectory, from);
      options = this.getPath(toDirectory, to);
      if (fromPath === options) {
        return {uri:options};
      }
      if (isPathParent(fromPath, options)) {
        throw Error("To path cannot contain the from path");
      }
      let toObj;
      try {
        toObj = await this.stat({path:to, directory:toDirectory});
      } catch (e) {
        fromPath = to.split("/");
        fromPath.pop();
        var toPath = fromPath.join("/");
        if (0 < fromPath.length && "directory" !== (await this.stat({path:toPath, directory:toDirectory})).type) {
          throw Error("Parent directory of the to path is a file");
        }
      }
      if (toObj && "directory" === toObj.type) {
        throw Error("Cannot overwrite a directory with a file");
      }
      fromPath = await this.stat({path:from, directory:fromDirectory});
      toPath = async(path, ctime, mtime) => {
        path = this.getPath(toDirectory, path);
        path = await this.dbRequest("get", [path]);
        path.ctime = ctime;
        path.mtime = mtime;
        await this.dbRequest("put", [path]);
      };
      const ctime = fromPath.ctime ? fromPath.ctime : Date.now();
      switch(fromPath.type) {
        case "file":
          var file = await this.readFile({path:from, directory:fromDirectory});
          doRename && await this.deleteFile({path:from, directory:fromDirectory});
          if (!(file.data instanceof Blob || this.isBase64String(file.data))) {
            var encoding = exports.Encoding.UTF8;
          }
          encoding = await this.writeFile({path:to, directory:toDirectory, data:file.data, encoding});
          doRename && await toPath(to, ctime, fromPath.mtime);
          return encoding;
        case "directory":
          if (toObj) {
            throw Error("Cannot move a directory over an existing object");
          }
          try {
            await this.mkdir({path:to, directory:toDirectory, recursive:!1}), doRename && await toPath(to, ctime, fromPath.mtime);
          } catch (e) {
          }
          encoding = (await this.readdir({path:from, directory:fromDirectory})).files;
          for (file of encoding) {
            await this._copy({from:`${from}/${file.name}`, to:`${to}/${file.name}`, directory:fromDirectory, toDirectory}, doRename);
          }
          doRename && await this.rmdir({path:from, directory:fromDirectory});
      }
      return {uri:options};
    }
    isBase64String(str) {
      try {
        return btoa(atob(str)) == str;
      } catch (err) {
        return !1;
      }
    }
  }
  FilesystemWeb._debug = !0;
  var web = Object.freeze({__proto__:null, FilesystemWeb});
  exports.Filesystem = module;
  exports.FilesystemDirectory = global;
  exports.FilesystemEncoding = require;
};

//# sourceMappingURL=module$node_modules$$capacitor$filesystem$dist$plugin_cjs.js.map
