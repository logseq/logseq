shadow$provide.module$node_modules$readable_stream$lib$internal$streams$BufferList = function(global, require, module, exports) {
  var Buffer = require("module$node_modules$readable_stream$node_modules$safe_buffer$index").Buffer, util = require("shadow$empty");
  module.exports = function() {
    function BufferList() {
      if (!(this instanceof BufferList)) {
        throw new TypeError("Cannot call a class as a function");
      }
      this.tail = this.head = null;
      this.length = 0;
    }
    BufferList.prototype.push = function(v) {
      v = {data:v, next:null};
      0 < this.length ? this.tail.next = v : this.head = v;
      this.tail = v;
      ++this.length;
    };
    BufferList.prototype.unshift = function(v) {
      v = {data:v, next:this.head};
      0 === this.length && (this.tail = v);
      this.head = v;
      ++this.length;
    };
    BufferList.prototype.shift = function() {
      if (0 !== this.length) {
        var ret = this.head.data;
        this.head = 1 === this.length ? this.tail = null : this.head.next;
        --this.length;
        return ret;
      }
    };
    BufferList.prototype.clear = function() {
      this.head = this.tail = null;
      this.length = 0;
    };
    BufferList.prototype.join = function(s) {
      if (0 === this.length) {
        return "";
      }
      for (var p = this.head, ret = "" + p.data; p = p.next;) {
        ret += s + p.data;
      }
      return ret;
    };
    BufferList.prototype.concat = function(n) {
      if (0 === this.length) {
        return Buffer.alloc(0);
      }
      n = Buffer.allocUnsafe(n >>> 0);
      for (var p = this.head, i = 0; p;) {
        p.data.copy(n, i), i += p.data.length, p = p.next;
      }
      return n;
    };
    return BufferList;
  }();
  util && util.inspect && util.inspect.custom && (module.exports.prototype[util.inspect.custom] = function() {
    var obj = util.inspect({length:this.length});
    return this.constructor.name + " " + obj;
  });
};

//# sourceMappingURL=module$node_modules$readable_stream$lib$internal$streams$BufferList.js.map
