shadow$provide.module$node_modules$semver$internal$identifiers = function(global, require, module, exports) {
  const numeric = /^[0-9]+$/, compareIdentifiers = (a, b) => {
    const anum = numeric.test(a), bnum = numeric.test(b);
    anum && bnum && (a = +a, b = +b);
    return a === b ? 0 : anum && !bnum ? -1 : bnum && !anum ? 1 : a < b ? -1 : 1;
  };
  module.exports = {compareIdentifiers, rcompareIdentifiers:(a, b) => compareIdentifiers(b, a)};
};

//# sourceMappingURL=module$node_modules$semver$internal$identifiers.js.map
