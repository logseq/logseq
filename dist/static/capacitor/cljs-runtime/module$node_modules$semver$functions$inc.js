shadow$provide.module$node_modules$semver$functions$inc = function(global, require, module, exports) {
  const SemVer = require("module$node_modules$semver$classes$semver");
  module.exports = (version, release, options, identifier, identifierBase) => {
    "string" === typeof options && (identifierBase = identifier, identifier = options, options = void 0);
    try {
      return (new SemVer(version instanceof SemVer ? version.version : version, options)).inc(release, identifier, identifierBase).version;
    } catch (er) {
      return null;
    }
  };
};

//# sourceMappingURL=module$node_modules$semver$functions$inc.js.map
