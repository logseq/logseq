shadow$provide.module$node_modules$codemirror$mode$properties$properties = function(global, require, module, exports) {
  (function(mod) {
    "object" == typeof exports && "object" == typeof module ? mod(require("module$node_modules$codemirror$lib$codemirror")) : "function" == typeof define && define.amd ? define(["../../lib/codemirror"], mod) : mod(CodeMirror);
  })(function(CodeMirror) {
    CodeMirror.defineMode("properties", function() {
      return {token:function(stream, state) {
        var sol = stream.sol() || state.afterSection, eol = stream.eol();
        state.afterSection = !1;
        sol && (state.nextMultiline ? (state.inMultiline = !0, state.nextMultiline = !1) : state.position = "def");
        eol && !state.nextMultiline && (state.inMultiline = !1, state.position = "def");
        if (sol) {
          for (; stream.eatSpace();) {
          }
        }
        eol = stream.next();
        if (!sol || "#" !== eol && "!" !== eol && ";" !== eol) {
          if (sol && "[" === eol) {
            return state.afterSection = !0, stream.skipTo("]"), stream.eat("]"), "header";
          }
          if ("\x3d" === eol || ":" === eol) {
            return state.position = "quote", null;
          }
          "\\" === eol && "quote" === state.position && stream.eol() && (state.nextMultiline = !0);
        } else {
          return state.position = "comment", stream.skipToEnd(), "comment";
        }
        return state.position;
      }, startState:function() {
        return {position:"def", nextMultiline:!1, inMultiline:!1, afterSection:!1};
      }};
    });
    CodeMirror.defineMIME("text/x-properties", "properties");
    CodeMirror.defineMIME("text/x-ini", "properties");
  });
};

//# sourceMappingURL=module$node_modules$codemirror$mode$properties$properties.js.map
