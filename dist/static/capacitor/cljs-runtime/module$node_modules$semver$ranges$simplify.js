shadow$provide.module$node_modules$semver$ranges$simplify = function(global, require, module, exports) {
  const satisfies = require("module$node_modules$semver$functions$satisfies"), compare = require("module$node_modules$semver$functions$compare");
  module.exports = (versions, range, options) => {
    var set = [], first = null;
    let prev = null;
    versions = versions.sort((a, b) => compare(a, b, options));
    for (const version of versions) {
      satisfies(version, range, options) ? (prev = version, first ||= version) : (prev && set.push([first, prev]), first = prev = null);
    }
    first && set.push([first, null]);
    first = [];
    for (const [min, max] of set) {
      min === max ? first.push(min) : max || min !== versions[0] ? max ? min === versions[0] ? first.push(`<=${max}`) : first.push(`${min} - ${max}`) : first.push(`>=${min}`) : first.push("*");
    }
    set = first.join(" || ");
    return set.length < ("string" === typeof range.raw ? range.raw : String(range)).length ? set : range;
  };
};

//# sourceMappingURL=module$node_modules$semver$ranges$simplify.js.map
