shadow$provide.module$node_modules$lru_cache$index = function(global, require, module, exports) {
  const Yallist = require("module$node_modules$yallist$yallist"), MAX = Symbol("max"), LENGTH = Symbol("length"), LENGTH_CALCULATOR = Symbol("lengthCalculator"), ALLOW_STALE = Symbol("allowStale"), MAX_AGE = Symbol("maxAge"), DISPOSE = Symbol("dispose"), NO_DISPOSE_ON_SET = Symbol("noDisposeOnSet"), LRU_LIST = Symbol("lruList"), CACHE = Symbol("cache"), UPDATE_AGE_ON_GET = Symbol("updateAgeOnGet"), naiveLength = () => 1;
  class LRUCache {
    constructor(options) {
      "number" === typeof options && (options = {max:options});
      options ||= {};
      if (options.max && ("number" !== typeof options.max || 0 > options.max)) {
        throw new TypeError("max must be a non-negative number");
      }
      this[MAX] = options.max || Infinity;
      const lc = options.length || naiveLength;
      this[LENGTH_CALCULATOR] = "function" !== typeof lc ? naiveLength : lc;
      this[ALLOW_STALE] = options.stale || !1;
      if (options.maxAge && "number" !== typeof options.maxAge) {
        throw new TypeError("maxAge must be a number");
      }
      this[MAX_AGE] = options.maxAge || 0;
      this[DISPOSE] = options.dispose;
      this[NO_DISPOSE_ON_SET] = options.noDisposeOnSet || !1;
      this[UPDATE_AGE_ON_GET] = options.updateAgeOnGet || !1;
      this.reset();
    }
    set max(mL) {
      if ("number" !== typeof mL || 0 > mL) {
        throw new TypeError("max must be a non-negative number");
      }
      this[MAX] = mL || Infinity;
      trim(this);
    }
    get max() {
      return this[MAX];
    }
    set allowStale(allowStale) {
      this[ALLOW_STALE] = !!allowStale;
    }
    get allowStale() {
      return this[ALLOW_STALE];
    }
    set maxAge(mA) {
      if ("number" !== typeof mA) {
        throw new TypeError("maxAge must be a non-negative number");
      }
      this[MAX_AGE] = mA;
      trim(this);
    }
    get maxAge() {
      return this[MAX_AGE];
    }
    set lengthCalculator(lC) {
      "function" !== typeof lC && (lC = naiveLength);
      lC !== this[LENGTH_CALCULATOR] && (this[LENGTH_CALCULATOR] = lC, this[LENGTH] = 0, this[LRU_LIST].forEach(hit => {
        hit.length = this[LENGTH_CALCULATOR](hit.value, hit.key);
        this[LENGTH] += hit.length;
      }));
      trim(this);
    }
    get lengthCalculator() {
      return this[LENGTH_CALCULATOR];
    }
    get length() {
      return this[LENGTH];
    }
    get itemCount() {
      return this[LRU_LIST].length;
    }
    rforEach(fn, thisp) {
      thisp = thisp || this;
      for (let walker = this[LRU_LIST].tail; null !== walker;) {
        const prev = walker.prev;
        forEachStep(this, fn, walker, thisp);
        walker = prev;
      }
    }
    forEach(fn, thisp) {
      thisp = thisp || this;
      for (let walker = this[LRU_LIST].head; null !== walker;) {
        const next = walker.next;
        forEachStep(this, fn, walker, thisp);
        walker = next;
      }
    }
    keys() {
      return this[LRU_LIST].toArray().map(k => k.key);
    }
    values() {
      return this[LRU_LIST].toArray().map(k => k.value);
    }
    reset() {
      this[DISPOSE] && this[LRU_LIST] && this[LRU_LIST].length && this[LRU_LIST].forEach(hit => this[DISPOSE](hit.key, hit.value));
      this[CACHE] = new Map();
      this[LRU_LIST] = new Yallist();
      this[LENGTH] = 0;
    }
    dump() {
      return this[LRU_LIST].map(hit => isStale(this, hit) ? !1 : {k:hit.key, v:hit.value, e:hit.now + (hit.maxAge || 0)}).toArray().filter(h => h);
    }
    dumpLru() {
      return this[LRU_LIST];
    }
    set(key, value, maxAge) {
      if ((maxAge = maxAge || this[MAX_AGE]) && "number" !== typeof maxAge) {
        throw new TypeError("maxAge must be a number");
      }
      const now = maxAge ? Date.now() : 0, len = this[LENGTH_CALCULATOR](value, key);
      if (this[CACHE].has(key)) {
        if (len > this[MAX]) {
          return del(this, this[CACHE].get(key)), !1;
        }
        const item = this[CACHE].get(key).value;
        if (this[DISPOSE] && !this[NO_DISPOSE_ON_SET]) {
          this[DISPOSE](key, item.value);
        }
        item.now = now;
        item.maxAge = maxAge;
        item.value = value;
        this[LENGTH] += len - item.length;
        item.length = len;
        this.get(key);
        trim(this);
        return !0;
      }
      maxAge = new Entry(key, value, len, now, maxAge);
      if (maxAge.length > this[MAX]) {
        if (this[DISPOSE]) {
          this[DISPOSE](key, value);
        }
        return !1;
      }
      this[LENGTH] += maxAge.length;
      this[LRU_LIST].unshift(maxAge);
      this[CACHE].set(key, this[LRU_LIST].head);
      trim(this);
      return !0;
    }
    has(key) {
      if (!this[CACHE].has(key)) {
        return !1;
      }
      key = this[CACHE].get(key).value;
      return !isStale(this, key);
    }
    get(key) {
      return get(this, key, !0);
    }
    peek(key) {
      return get(this, key, !1);
    }
    pop() {
      const node = this[LRU_LIST].tail;
      if (!node) {
        return null;
      }
      del(this, node);
      return node.value;
    }
    del(key) {
      del(this, this[CACHE].get(key));
    }
    load(arr) {
      this.reset();
      const now = Date.now();
      for (let l = arr.length - 1; 0 <= l; l--) {
        const hit = arr[l];
        var expiresAt = hit.e || 0;
        0 === expiresAt ? this.set(hit.k, hit.v) : (expiresAt -= now, 0 < expiresAt && this.set(hit.k, hit.v, expiresAt));
      }
    }
    prune() {
      this[CACHE].forEach((value, key) => get(this, key, !1));
    }
  }
  const get = (self, key, doUse) => {
    if (key = self[CACHE].get(key)) {
      const hit = key.value;
      if (isStale(self, hit)) {
        if (del(self, key), !self[ALLOW_STALE]) {
          return;
        }
      } else {
        doUse && (self[UPDATE_AGE_ON_GET] && (key.value.now = Date.now()), self[LRU_LIST].unshiftNode(key));
      }
      return hit.value;
    }
  }, isStale = (self, hit) => {
    if (!hit || !hit.maxAge && !self[MAX_AGE]) {
      return !1;
    }
    const diff = Date.now() - hit.now;
    return hit.maxAge ? diff > hit.maxAge : self[MAX_AGE] && diff > self[MAX_AGE];
  }, trim = self => {
    if (self[LENGTH] > self[MAX]) {
      for (let walker = self[LRU_LIST].tail; self[LENGTH] > self[MAX] && null !== walker;) {
        const prev = walker.prev;
        del(self, walker);
        walker = prev;
      }
    }
  }, del = (self, node) => {
    if (node) {
      const hit = node.value;
      if (self[DISPOSE]) {
        self[DISPOSE](hit.key, hit.value);
      }
      self[LENGTH] -= hit.length;
      self[CACHE].delete(hit.key);
      self[LRU_LIST].removeNode(node);
    }
  };
  class Entry {
    constructor(key, value, length, now, maxAge) {
      this.key = key;
      this.value = value;
      this.length = length;
      this.now = now;
      this.maxAge = maxAge || 0;
    }
  }
  const forEachStep = (self, fn, node, thisp) => {
    let hit = node.value;
    isStale(self, hit) && (del(self, node), self[ALLOW_STALE] || (hit = void 0));
    hit && fn.call(thisp, hit.value, hit.key, self);
  };
  module.exports = LRUCache;
};

//# sourceMappingURL=module$node_modules$lru_cache$index.js.map
