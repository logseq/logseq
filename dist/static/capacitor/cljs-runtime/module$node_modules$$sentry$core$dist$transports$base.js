shadow$provide.module$node_modules$$sentry$core$dist$transports$base = function(global, require, module, exports) {
  function getRateLimitReason(rateLimits, category) {
    return "Too many " + category + " requests, backing off until: " + (new Date(utils_1.disabledUntil(rateLimits, category))).toISOString();
  }
  Object.defineProperty(exports, "__esModule", {value:!0});
  var utils_1 = require("module$node_modules$$sentry$utils$dist$index");
  exports.ERROR_TRANSPORT_CATEGORY = "error";
  exports.TRANSACTION_TRANSPORT_CATEGORY = "transaction";
  exports.ATTACHMENT_TRANSPORT_CATEGORY = "attachment";
  exports.SESSION_TRANSPORT_CATEGORY = "session";
  exports.DEFAULT_TRANSPORT_BUFFER_SIZE = 30;
  exports.createTransport = function(options, makeRequest, buffer) {
    void 0 === buffer && (buffer = utils_1.makePromiseBuffer(options.bufferSize || exports.DEFAULT_TRANSPORT_BUFFER_SIZE));
    var rateLimits = {};
    return {send:function(envelope) {
      var envCategory = utils_1.getEnvelopeType(envelope), category = "event" === envCategory ? "error" : envCategory, request = {category, body:utils_1.serializeEnvelope(envelope)};
      return utils_1.isRateLimited(rateLimits, category) ? utils_1.rejectedSyncPromise({status:"rate_limit", reason:getRateLimitReason(rateLimits, category)}) : buffer.add(function() {
        return makeRequest(request).then(function(_a) {
          var body = _a.body, headers = _a.headers, reason = _a.reason;
          _a = utils_1.eventStatusFromHttpCode(_a.statusCode);
          headers && (rateLimits = utils_1.updateRateLimits(rateLimits, headers));
          return "success" === _a ? utils_1.resolvedSyncPromise({status:_a, reason}) : utils_1.rejectedSyncPromise({status:_a, reason:reason || body || ("rate_limit" === _a ? getRateLimitReason(rateLimits, category) : "Unknown transport error")});
        });
      });
    }, flush:function(timeout) {
      return buffer.drain(timeout);
    }};
  };
};

//# sourceMappingURL=module$node_modules$$sentry$core$dist$transports$base.js.map
