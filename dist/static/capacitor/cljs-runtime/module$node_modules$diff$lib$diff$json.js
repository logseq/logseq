shadow$provide.module$node_modules$diff$lib$diff$json = function(global, require, module, exports) {
  function _typeof(obj) {
    "@babel/helpers - typeof";
    _typeof = "function" === typeof Symbol && "symbol" === typeof Symbol.iterator ? function(obj) {
      return typeof obj;
    } : function(obj) {
      return obj && "function" === typeof Symbol && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj;
    };
    return _typeof(obj);
  }
  function canonicalize(obj, stack, replacementStack, replacer, key) {
    stack = stack || [];
    replacementStack = replacementStack || [];
    replacer && (obj = replacer(key, obj));
    var i;
    for (i = 0; i < stack.length; i += 1) {
      if (stack[i] === obj) {
        return replacementStack[i];
      }
    }
    if ("[object Array]" === objectPrototypeToString.call(obj)) {
      stack.push(obj);
      var canonicalizedObj = Array(obj.length);
      replacementStack.push(canonicalizedObj);
      for (i = 0; i < obj.length; i += 1) {
        canonicalizedObj[i] = canonicalize(obj[i], stack, replacementStack, replacer, key);
      }
      stack.pop();
      replacementStack.pop();
      return canonicalizedObj;
    }
    obj && obj.toJSON && (obj = obj.toJSON());
    if ("object" === _typeof(obj) && null !== obj) {
      stack.push(obj);
      canonicalizedObj = {};
      replacementStack.push(canonicalizedObj);
      key = [];
      for (var _key in obj) {
        obj.hasOwnProperty(_key) && key.push(_key);
      }
      key.sort();
      for (i = 0; i < key.length; i += 1) {
        _key = key[i], canonicalizedObj[_key] = canonicalize(obj[_key], stack, replacementStack, replacer, _key);
      }
      stack.pop();
      replacementStack.pop();
    } else {
      canonicalizedObj = obj;
    }
    return canonicalizedObj;
  }
  Object.defineProperty(exports, "__esModule", {value:!0});
  exports.diffJson = function(oldObj, newObj, options) {
    return jsonDiff.diff(oldObj, newObj, options);
  };
  exports.canonicalize = canonicalize;
  exports.jsonDiff = void 0;
  var _base = function(obj) {
    return obj && obj.__esModule ? obj : {"default":obj};
  }(require("module$node_modules$diff$lib$diff$base"));
  global = require("module$node_modules$diff$lib$diff$line");
  var objectPrototypeToString = Object.prototype.toString, jsonDiff = new _base["default"]();
  exports.jsonDiff = jsonDiff;
  jsonDiff.useLongestToken = !0;
  jsonDiff.tokenize = global.lineDiff.tokenize;
  jsonDiff.castInput = function(value) {
    var _this$options = this.options, undefinedReplacement = _this$options.undefinedReplacement;
    _this$options = _this$options.stringifyReplacer;
    _this$options = void 0 === _this$options ? function(k, v) {
      return "undefined" === typeof v ? undefinedReplacement : v;
    } : _this$options;
    return "string" === typeof value ? value : JSON.stringify(canonicalize(value, null, null, _this$options), _this$options, "  ");
  };
  jsonDiff.equals = function(left, right) {
    return _base["default"].prototype.equals.call(jsonDiff, left.replace(/,([\r\n])/g, "$1"), right.replace(/,([\r\n])/g, "$1"));
  };
};

//# sourceMappingURL=module$node_modules$diff$lib$diff$json.js.map
