shadow$provide.module$node_modules$codemirror$mode$eiffel$eiffel = function(global, require, module, exports) {
  (function(mod) {
    "object" == typeof exports && "object" == typeof module ? mod(require("module$node_modules$codemirror$lib$codemirror")) : "function" == typeof define && define.amd ? define(["../../lib/codemirror"], mod) : mod(CodeMirror);
  })(function(CodeMirror) {
    CodeMirror.defineMode("eiffel", function() {
      function wordObj(words) {
        for (var o = {}, i = 0, e = words.length; i < e; ++i) {
          o[words[i]] = !0;
        }
        return o;
      }
      function tokenBase(stream, state) {
        if (stream.eatSpace()) {
          return null;
        }
        var ch = stream.next();
        return '"' == ch || "'" == ch ? (ch = readQuoted(ch, "string"), state.tokenize.push(ch), ch(stream, state)) : "-" == ch && stream.eat("-") ? (stream.skipToEnd(), "comment") : ":" == ch && stream.eat("\x3d") ? "operator" : /[0-9]/.test(ch) ? (stream.eatWhile(/[xXbBCc0-9\.]/), stream.eat(/[\?!]/), "ident") : /[a-zA-Z_0-9]/.test(ch) ? (stream.eatWhile(/[a-zA-Z_0-9]/), stream.eat(/[\?!]/), "ident") : /[=+\-\/*^%<>~]/.test(ch) ? (stream.eatWhile(/[=+\-\/*^%<>~]/), "operator") : null;
      }
      function readQuoted(quote, style, unescaped) {
        return function(stream, state) {
          for (var escaped = !1, ch; null != (ch = stream.next());) {
            if (ch == quote && (unescaped || !escaped)) {
              state.tokenize.pop();
              break;
            }
            escaped = !escaped && "%" == ch;
          }
          return style;
        };
      }
      var keywords = wordObj("note across when variant until unique undefine then strip select retry rescue require rename reference redefine prefix once old obsolete loop local like is inspect infix include if frozen from external export ensure end elseif else do creation create check alias agent separate invariant inherit indexing feature expanded deferred class Void True Result Precursor False Current create attached detachable as and implies not or".split(" ")), operators = wordObj(":\x3d;and then;and;or;\x3c\x3c;\x3e\x3e".split(";"));
      return {startState:function() {
        return {tokenize:[tokenBase]};
      }, token:function(stream, state) {
        state = state.tokenize[state.tokenize.length - 1](stream, state);
        "ident" == state && (state = stream.current(), state = keywords.propertyIsEnumerable(stream.current()) ? "keyword" : operators.propertyIsEnumerable(stream.current()) ? "operator" : /^[A-Z][A-Z_0-9]*$/g.test(state) ? "tag" : /^0[bB][0-1]+$/g.test(state) ? "number" : /^0[cC][0-7]+$/g.test(state) ? "number" : /^0[xX][a-fA-F0-9]+$/g.test(state) ? "number" : /^([0-9]+\.[0-9]*)|([0-9]*\.[0-9]+)$/g.test(state) ? "number" : /^[0-9]+$/g.test(state) ? "number" : "variable");
        return state;
      }, lineComment:"--"};
    });
    CodeMirror.defineMIME("text/x-eiffel", "eiffel");
  });
};

//# sourceMappingURL=module$node_modules$codemirror$mode$eiffel$eiffel.js.map
