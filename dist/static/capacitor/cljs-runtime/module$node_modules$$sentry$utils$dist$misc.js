shadow$provide.module$node_modules$$sentry$utils$dist$misc = function(global, require, module, exports) {
  Object.defineProperty(exports, "__esModule", {value:!0});
  var tslib_1 = require("module$node_modules$$sentry$utils$node_modules$tslib$tslib"), global_1 = require("module$node_modules$$sentry$utils$dist$global"), object_1 = require("module$node_modules$$sentry$utils$dist$object"), string_1 = require("module$node_modules$$sentry$utils$dist$string");
  exports.uuid4 = function() {
    var global = global_1.getGlobalObject(), crypto = global.crypto || global.msCrypto;
    return void 0 !== crypto && crypto.getRandomValues ? (global = new Uint16Array(8), crypto.getRandomValues(global), global[3] = global[3] & 4095 | 16384, global[4] = global[4] & 16383 | 32768, crypto = function(num) {
      for (num = num.toString(16); 4 > num.length;) {
        num = "0" + num;
      }
      return num;
    }, crypto(global[0]) + crypto(global[1]) + crypto(global[2]) + crypto(global[3]) + crypto(global[4]) + crypto(global[5]) + crypto(global[6]) + crypto(global[7])) : "xxxxxxxxxxxx4xxxyxxxxxxxxxxxxxxx".replace(/[xy]/g, function(c) {
      var r = 16 * Math.random() | 0;
      return ("x" === c ? r : r & 3 | 8).toString(16);
    });
  };
  exports.parseUrl = function(url) {
    return url ? (url = url.match(/^(([^:/?#]+):)?(\/\/([^/?#]*))?([^?#]*)(\?([^#]*))?(#(.*))?$/)) ? {host:url[4], path:url[5], protocol:url[2], relative:url[5] + (url[6] || "") + (url[8] || "")} : {} : {};
  };
  exports.getEventDescription = function(event) {
    var message = event.message, eventId = event.event_id;
    return message ? message : (event = event.exception && event.exception.values ? event.exception.values[0] : void 0) ? event.type && event.value ? event.type + ": " + event.value : event.type || event.value || eventId || "\x3cunknown\x3e" : eventId || "\x3cunknown\x3e";
  };
  exports.addExceptionTypeValue = function(event, value, type) {
    event = event.exception = event.exception || {};
    event = event.values = event.values || [];
    event = event[0] = event[0] || {};
    event.value || (event.value = value || "");
    event.type || (event.type = type || "Error");
  };
  exports.addExceptionMechanism = function(event, newMechanism) {
    if (event = event.exception && event.exception.values ? event.exception.values[0] : void 0) {
      var currentMechanism = event.mechanism;
      event.mechanism = tslib_1.__assign(tslib_1.__assign(tslib_1.__assign({}, {type:"generic", handled:!0}), currentMechanism), newMechanism);
      newMechanism && "data" in newMechanism && (newMechanism = tslib_1.__assign(tslib_1.__assign({}, currentMechanism && currentMechanism.data), newMechanism.data), event.mechanism.data = newMechanism);
    }
  };
  var SEMVER_REGEXP = /^(0|[1-9]\d*)\.(0|[1-9]\d*)\.(0|[1-9]\d*)(?:-((?:0|[1-9]\d*|\d*[a-zA-Z-][0-9a-zA-Z-]*)(?:\.(?:0|[1-9]\d*|\d*[a-zA-Z-][0-9a-zA-Z-]*))*))?(?:\+([0-9a-zA-Z-]+(?:\.[0-9a-zA-Z-]+)*))?$/;
  exports.parseSemver = function(input) {
    input = input.match(SEMVER_REGEXP) || [];
    var major = parseInt(input[1], 10), minor = parseInt(input[2], 10), patch = parseInt(input[3], 10);
    return {buildmetadata:input[5], major:isNaN(major) ? void 0 : major, minor:isNaN(minor) ? void 0 : minor, patch:isNaN(patch) ? void 0 : patch, prerelease:input[4]};
  };
  exports.addContextToFrame = function(lines, frame, linesOfContext) {
    void 0 === linesOfContext && (linesOfContext = 5);
    var maxLines = lines.length, sourceLine = Math.max(Math.min(maxLines, (frame.lineno || 0) - 1), 0);
    frame.pre_context = lines.slice(Math.max(0, sourceLine - linesOfContext), sourceLine).map(function(line) {
      return string_1.snipLine(line, 0);
    });
    frame.context_line = string_1.snipLine(lines[Math.min(maxLines - 1, sourceLine)], frame.colno || 0);
    frame.post_context = lines.slice(Math.min(sourceLine + 1, maxLines), sourceLine + 1 + linesOfContext).map(function(line) {
      return string_1.snipLine(line, 0);
    });
  };
  exports.stripUrlQueryAndFragment = function(urlPath) {
    return urlPath.split(/[\?#]/, 1)[0];
  };
  exports.checkOrSetAlreadyCaught = function(exception) {
    if (exception && exception.__sentry_captured__) {
      return !0;
    }
    try {
      object_1.addNonEnumerableProperty(exception, "__sentry_captured__", !0);
    } catch (err) {
    }
    return !1;
  };
};

//# sourceMappingURL=module$node_modules$$sentry$utils$dist$misc.js.map
