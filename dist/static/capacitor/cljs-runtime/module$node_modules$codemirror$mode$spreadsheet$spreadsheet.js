shadow$provide.module$node_modules$codemirror$mode$spreadsheet$spreadsheet = function(global, require, module, exports) {
  (function(mod) {
    "object" == typeof exports && "object" == typeof module ? mod(require("module$node_modules$codemirror$lib$codemirror")) : "function" == typeof define && define.amd ? define(["../../lib/codemirror"], mod) : mod(CodeMirror);
  })(function(CodeMirror) {
    CodeMirror.defineMode("spreadsheet", function() {
      return {startState:function() {
        return {stringType:null, stack:[]};
      }, token:function(stream, state) {
        if (stream) {
          0 !== state.stack.length || '"' != stream.peek() && "'" != stream.peek() || (state.stringType = stream.peek(), stream.next(), state.stack.unshift("string"));
          switch(state.stack[0]) {
            case "string":
              for (; "string" === state.stack[0] && !stream.eol();) {
                stream.peek() === state.stringType ? (stream.next(), state.stack.shift()) : "\\" === stream.peek() ? (stream.next(), stream.next()) : stream.match(/^.[^\\"']*/);
              }
              return "string";
            case "characterClass":
              for (; "characterClass" === state.stack[0] && !stream.eol();) {
                stream.match(/^[^\]\\]+/) || stream.match(/^\\./) || state.stack.shift();
              }
              return "operator";
          }
          var peek = stream.peek();
          switch(peek) {
            case "[":
              return stream.next(), state.stack.unshift("characterClass"), "bracket";
            case ":":
              return stream.next(), "operator";
            case "\\":
              if (stream.match(/\\[a-z]+/)) {
                return "string-2";
              }
              stream.next();
              return "atom";
            case ".":
            case ",":
            case ";":
            case "*":
            case "-":
            case "+":
            case "^":
            case "\x3c":
            case "/":
            case "\x3d":
              return stream.next(), "atom";
            case "$":
              return stream.next(), "builtin";
          }
          if (stream.match(/\d+/)) {
            return stream.match(/^\w+/) ? "error" : "number";
          }
          if (stream.match(/^[a-zA-Z_]\w*/)) {
            return stream.match(/(?=[\(.])/, !1) ? "keyword" : "variable-2";
          }
          if (-1 != "[](){}".split("").indexOf(peek)) {
            return stream.next(), "bracket";
          }
          stream.eatSpace() || stream.next();
          return null;
        }
      }};
    });
    CodeMirror.defineMIME("text/x-spreadsheet", "spreadsheet");
  });
};

//# sourceMappingURL=module$node_modules$codemirror$mode$spreadsheet$spreadsheet.js.map
