shadow$provide.module$node_modules$chrono_node$dist$calculation$years = function(global, require, module, exports) {
  global = this && this.__importDefault || function(mod) {
    return mod && mod.__esModule ? mod : {"default":mod};
  };
  Object.defineProperty(exports, "__esModule", {value:!0});
  exports.findYearClosestToRef = exports.findMostLikelyADYear = void 0;
  const dayjs_1 = global(require("module$node_modules$dayjs$dayjs_min"));
  exports.findMostLikelyADYear = function(yearNumber) {
    100 > yearNumber && (yearNumber = 50 < yearNumber ? yearNumber + 1900 : yearNumber + 2000);
    return yearNumber;
  };
  exports.findYearClosestToRef = function(refDate, day, month) {
    refDate = dayjs_1.default(refDate);
    month = refDate.month(month - 1);
    month = month.date(day);
    month = month.year(refDate.year());
    day = month.add(1, "y");
    const lastYear = month.add(-1, "y");
    Math.abs(day.diff(refDate)) < Math.abs(month.diff(refDate)) ? month = day : Math.abs(lastYear.diff(refDate)) < Math.abs(month.diff(refDate)) && (month = lastYear);
    return month.year();
  };
};

//# sourceMappingURL=module$node_modules$chrono_node$dist$calculation$years.js.map
