shadow$provide.module$node_modules$codemirror$addon$edit$closebrackets = function(global, require, module, exports) {
  (function(mod) {
    "object" == typeof exports && "object" == typeof module ? mod(require("module$node_modules$codemirror$lib$codemirror")) : "function" == typeof define && define.amd ? define(["../../lib/codemirror"], mod) : mod(CodeMirror);
  })(function(CodeMirror) {
    function getOption(conf, name) {
      return "pairs" == name && "string" == typeof conf ? conf : "object" == typeof conf && null != conf[name] ? conf[name] : defaults[name];
    }
    function ensureBound(chars) {
      for (var i = 0; i < chars.length; i++) {
        var ch = chars.charAt(i), key = "'" + ch + "'";
        keyMap[key] || (keyMap[key] = handler(ch));
      }
    }
    function handler(ch) {
      return function(cm) {
        return handleChar(cm, ch);
      };
    }
    function getConfig(cm) {
      var deflt = cm.state.closeBrackets;
      return !deflt || deflt.override ? deflt : cm.getModeAt(cm.getCursor()).closeBrackets || deflt;
    }
    function moveSel(cm, dir) {
      for (var newRanges = [], ranges = cm.listSelections(), primary = 0, i = 0; i < ranges.length; i++) {
        var range = ranges[i];
        range.head == cm.getCursor() && (primary = i);
        range = range.head.ch || 0 < dir ? {line:range.head.line, ch:range.head.ch + dir} : {line:range.head.line - 1};
        newRanges.push({anchor:range, head:range});
      }
      cm.setSelections(newRanges, primary);
    }
    function handleChar(cm, ch) {
      var conf = getConfig(cm);
      if (!conf || cm.getOption("disableInput")) {
        return CodeMirror.Pass;
      }
      var pairs = getOption(conf, "pairs"), pos = pairs.indexOf(ch);
      if (-1 == pos) {
        return CodeMirror.Pass;
      }
      var closeBefore = getOption(conf, "closeBefore");
      conf = getOption(conf, "triples");
      for (var identical = pairs.charAt(pos + 1) == ch, ranges = cm.listSelections(), opening = 0 == pos % 2, type, i = 0; i < ranges.length; i++) {
        var range = ranges[i], cur = range.head;
        var curType = cm.getRange(cur, Pos(cur.line, cur.ch + 1));
        if (opening && !range.empty()) {
          curType = "surround";
        } else if (!identical && opening || curType != ch) {
          if (identical && 1 < cur.ch && 0 <= conf.indexOf(ch) && cm.getRange(Pos(cur.line, cur.ch - 2), cur) == ch + ch) {
            if (2 < cur.ch && /\bstring/.test(cm.getTokenTypeAt(Pos(cur.line, cur.ch - 2)))) {
              return CodeMirror.Pass;
            }
            curType = "addFour";
          } else if (identical) {
            range = 0 == cur.ch ? " " : cm.getRange(Pos(cur.line, cur.ch - 1), cur);
            if (CodeMirror.isWordChar(curType) || range == ch || CodeMirror.isWordChar(range)) {
              return CodeMirror.Pass;
            }
            curType = "both";
          } else if (opening && (0 === curType.length || /\s/.test(curType) || -1 < closeBefore.indexOf(curType))) {
            curType = "both";
          } else {
            return CodeMirror.Pass;
          }
        } else {
          curType = identical && stringStartsAfter(cm, cur) ? "both" : 0 <= conf.indexOf(ch) && cm.getRange(cur, Pos(cur.line, cur.ch + 3)) == ch + ch + ch ? "skipThree" : "skip";
        }
        if (!type) {
          type = curType;
        } else if (type != curType) {
          return CodeMirror.Pass;
        }
      }
      var left = pos % 2 ? pairs.charAt(pos - 1) : ch, right = pos % 2 ? ch : pairs.charAt(pos + 1);
      cm.operation(function() {
        if ("skip" == type) {
          moveSel(cm, 1);
        } else if ("skipThree" == type) {
          moveSel(cm, 3);
        } else if ("surround" == type) {
          for (var sels = cm.getSelections(), i = 0; i < sels.length; i++) {
            sels[i] = left + sels[i] + right;
          }
          cm.replaceSelections(sels, "around");
          sels = cm.listSelections().slice();
          for (i = 0; i < sels.length; i++) {
            var JSCompiler_temp_const = sels, JSCompiler_temp_const$jscomp$0 = i;
            var JSCompiler_inline_result = sels[i];
            var inverted = 0 < CodeMirror.cmpPos(JSCompiler_inline_result.anchor, JSCompiler_inline_result.head);
            JSCompiler_inline_result = {anchor:new Pos(JSCompiler_inline_result.anchor.line, JSCompiler_inline_result.anchor.ch + (inverted ? -1 : 1)), head:new Pos(JSCompiler_inline_result.head.line, JSCompiler_inline_result.head.ch + (inverted ? 1 : -1))};
            JSCompiler_temp_const[JSCompiler_temp_const$jscomp$0] = JSCompiler_inline_result;
          }
          cm.setSelections(sels);
        } else {
          "both" == type ? (cm.replaceSelection(left + right, null), cm.triggerElectric(left + right), moveSel(cm, -1)) : "addFour" == type && (cm.replaceSelection(left + left + left + left, "before"), moveSel(cm, 1));
        }
      });
    }
    function charsAround(cm, pos) {
      cm = cm.getRange(Pos(pos.line, pos.ch - 1), Pos(pos.line, pos.ch + 1));
      return 2 == cm.length ? cm : null;
    }
    function stringStartsAfter(cm, pos) {
      var token = cm.getTokenAt(Pos(pos.line, pos.ch + 1));
      return /\bstring/.test(token.type) && token.start == pos.ch && (0 == pos.ch || !/\bstring/.test(cm.getTokenTypeAt(pos)));
    }
    var defaults = {pairs:"()[]{}''\"\"", closeBefore:")]}'\":;\x3e", triples:"", explode:"[]{}"}, Pos = CodeMirror.Pos;
    CodeMirror.defineOption("autoCloseBrackets", !1, function(cm, val, old) {
      old && old != CodeMirror.Init && (cm.removeKeyMap(keyMap), cm.state.closeBrackets = null);
      val && (ensureBound(getOption(val, "pairs")), cm.state.closeBrackets = val, cm.addKeyMap(keyMap));
    });
    var keyMap = {Backspace:function(cm) {
      var conf = getConfig(cm);
      if (!conf || cm.getOption("disableInput")) {
        return CodeMirror.Pass;
      }
      var pairs = getOption(conf, "pairs");
      conf = cm.listSelections();
      for (var i = 0; i < conf.length; i++) {
        if (!conf[i].empty()) {
          return CodeMirror.Pass;
        }
        var around = charsAround(cm, conf[i].head);
        if (!around || 0 != pairs.indexOf(around) % 2) {
          return CodeMirror.Pass;
        }
      }
      for (i = conf.length - 1; 0 <= i; i--) {
        pairs = conf[i].head, cm.replaceRange("", Pos(pairs.line, pairs.ch - 1), Pos(pairs.line, pairs.ch + 1), "+delete");
      }
    }, Enter:function(cm) {
      var conf = getConfig(cm);
      conf = conf && getOption(conf, "explode");
      if (!conf || cm.getOption("disableInput")) {
        return CodeMirror.Pass;
      }
      for (var ranges = cm.listSelections(), i = 0; i < ranges.length; i++) {
        if (!ranges[i].empty()) {
          return CodeMirror.Pass;
        }
        var around = charsAround(cm, ranges[i].head);
        if (!around || 0 != conf.indexOf(around) % 2) {
          return CodeMirror.Pass;
        }
      }
      cm.operation(function() {
        var linesep = cm.lineSeparator() || "\n";
        cm.replaceSelection(linesep + linesep, null);
        moveSel(cm, -1);
        ranges = cm.listSelections();
        for (linesep = 0; linesep < ranges.length; linesep++) {
          var line = ranges[linesep].head.line;
          cm.indentLine(line, null, !0);
          cm.indentLine(line + 1, null, !0);
        }
      });
    }};
    ensureBound(defaults.pairs + "`");
  });
};

//# sourceMappingURL=module$node_modules$codemirror$addon$edit$closebrackets.js.map
