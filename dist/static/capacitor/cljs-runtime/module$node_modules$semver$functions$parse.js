shadow$provide.module$node_modules$semver$functions$parse = function(global, require, module, exports) {
  const SemVer = require("module$node_modules$semver$classes$semver");
  module.exports = (version, options, throwErrors = !1) => {
    if (version instanceof SemVer) {
      return version;
    }
    try {
      return new SemVer(version, options);
    } catch (er) {
      if (!throwErrors) {
        return null;
      }
      throw er;
    }
  };
};

//# sourceMappingURL=module$node_modules$semver$functions$parse.js.map
