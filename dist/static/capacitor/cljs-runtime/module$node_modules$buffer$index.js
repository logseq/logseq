shadow$provide.module$node_modules$buffer$index = function(global, require, module, exports) {
  function typedArraySupport() {
    try {
      var arr = new Uint8Array(1);
      arr.__proto__ = {__proto__:Uint8Array.prototype, foo:function() {
        return 42;
      }};
      return 42 === arr.foo() && "function" === typeof arr.subarray && 0 === arr.subarray(1, 1).byteLength;
    } catch (e) {
      return !1;
    }
  }
  function createBuffer(that, length) {
    if ((Buffer.TYPED_ARRAY_SUPPORT ? 2147483647 : 1073741823) < length) {
      throw new RangeError("Invalid typed array length");
    }
    Buffer.TYPED_ARRAY_SUPPORT ? (that = new Uint8Array(length), that.__proto__ = Buffer.prototype) : (null === that && (that = new Buffer(length)), that.length = length);
    return that;
  }
  function Buffer(arg, encodingOrOffset, length) {
    if (!(Buffer.TYPED_ARRAY_SUPPORT || this instanceof Buffer)) {
      return new Buffer(arg, encodingOrOffset, length);
    }
    if ("number" === typeof arg) {
      if ("string" === typeof encodingOrOffset) {
        throw Error("If encoding is specified then the first argument must be a string");
      }
      return allocUnsafe(this, arg);
    }
    return from(this, arg, encodingOrOffset, length);
  }
  function from(that, value, encodingOrOffset, length) {
    if ("number" === typeof value) {
      throw new TypeError('"value" argument must not be a number');
    }
    if ("undefined" !== typeof ArrayBuffer && value instanceof ArrayBuffer) {
      value.byteLength;
      if (0 > encodingOrOffset || value.byteLength < encodingOrOffset) {
        throw new RangeError("'offset' is out of bounds");
      }
      if (value.byteLength < encodingOrOffset + (length || 0)) {
        throw new RangeError("'length' is out of bounds");
      }
      value = void 0 === encodingOrOffset && void 0 === length ? new Uint8Array(value) : void 0 === length ? new Uint8Array(value, encodingOrOffset) : new Uint8Array(value, encodingOrOffset, length);
      Buffer.TYPED_ARRAY_SUPPORT ? (that = value, that.__proto__ = Buffer.prototype) : that = fromArrayLike(that, value);
      return that;
    }
    if ("string" === typeof value) {
      length = that;
      that = encodingOrOffset;
      if ("string" !== typeof that || "" === that) {
        that = "utf8";
      }
      if (!Buffer.isEncoding(that)) {
        throw new TypeError('"encoding" must be a valid string encoding');
      }
      encodingOrOffset = byteLength(value, that) | 0;
      length = createBuffer(length, encodingOrOffset);
      value = length.write(value, that);
      value !== encodingOrOffset && (length = length.slice(0, value));
      return length;
    }
    return fromObject(that, value);
  }
  function assertSize(size) {
    if ("number" !== typeof size) {
      throw new TypeError('"size" argument must be a number');
    }
    if (0 > size) {
      throw new RangeError('"size" argument must not be negative');
    }
  }
  function allocUnsafe(that, size) {
    assertSize(size);
    that = createBuffer(that, 0 > size ? 0 : checked(size) | 0);
    if (!Buffer.TYPED_ARRAY_SUPPORT) {
      for (var i = 0; i < size; ++i) {
        that[i] = 0;
      }
    }
    return that;
  }
  function fromArrayLike(that, array) {
    var length = 0 > array.length ? 0 : checked(array.length) | 0;
    that = createBuffer(that, length);
    for (var i = 0; i < length; i += 1) {
      that[i] = array[i] & 255;
    }
    return that;
  }
  function fromObject(that, obj) {
    if (Buffer.isBuffer(obj)) {
      var len = checked(obj.length) | 0;
      that = createBuffer(that, len);
      if (0 === that.length) {
        return that;
      }
      obj.copy(that, 0, 0, len);
      return that;
    }
    if (obj) {
      if ("undefined" !== typeof ArrayBuffer && obj.buffer instanceof ArrayBuffer || "length" in obj) {
        return (len = "number" !== typeof obj.length) || (len = obj.length, len = len !== len), len ? createBuffer(that, 0) : fromArrayLike(that, obj);
      }
      if ("Buffer" === obj.type && isArray(obj.data)) {
        return fromArrayLike(that, obj.data);
      }
    }
    throw new TypeError("First argument must be a string, Buffer, ArrayBuffer, Array, or array-like object.");
  }
  function checked(length) {
    if (length >= (Buffer.TYPED_ARRAY_SUPPORT ? 2147483647 : 1073741823)) {
      throw new RangeError("Attempt to allocate Buffer larger than maximum size: 0x" + (Buffer.TYPED_ARRAY_SUPPORT ? 2147483647 : 1073741823).toString(16) + " bytes");
    }
    return length | 0;
  }
  function byteLength(string, encoding) {
    if (Buffer.isBuffer(string)) {
      return string.length;
    }
    if ("undefined" !== typeof ArrayBuffer && "function" === typeof ArrayBuffer.isView && (ArrayBuffer.isView(string) || string instanceof ArrayBuffer)) {
      return string.byteLength;
    }
    "string" !== typeof string && (string = "" + string);
    var len = string.length;
    if (0 === len) {
      return 0;
    }
    for (var loweredCase = !1;;) {
      switch(encoding) {
        case "ascii":
        case "latin1":
        case "binary":
          return len;
        case "utf8":
        case "utf-8":
        case void 0:
          return utf8ToBytes(string).length;
        case "ucs2":
        case "ucs-2":
        case "utf16le":
        case "utf-16le":
          return 2 * len;
        case "hex":
          return len >>> 1;
        case "base64":
          return base64ToBytes(string).length;
        default:
          if (loweredCase) {
            return utf8ToBytes(string).length;
          }
          encoding = ("" + encoding).toLowerCase();
          loweredCase = !0;
      }
    }
  }
  function slowToString(encoding, start, end) {
    var loweredCase = !1;
    if (void 0 === start || 0 > start) {
      start = 0;
    }
    if (start > this.length) {
      return "";
    }
    if (void 0 === end || end > this.length) {
      end = this.length;
    }
    if (0 >= end) {
      return "";
    }
    end >>>= 0;
    start >>>= 0;
    if (end <= start) {
      return "";
    }
    for (encoding ||= "utf8";;) {
      switch(encoding) {
        case "hex":
          encoding = start;
          start = this.length;
          if (!encoding || 0 > encoding) {
            encoding = 0;
          }
          if (!end || 0 > end || end > start) {
            end = start;
          }
          for (start = ""; encoding < end; ++encoding) {
            loweredCase = this[encoding], loweredCase = 16 > loweredCase ? "0" + loweredCase.toString(16) : loweredCase.toString(16), start += loweredCase;
          }
          return start;
        case "utf8":
        case "utf-8":
          return utf8Slice(this, start, end);
        case "ascii":
          encoding = start;
          start = "";
          for (end = Math.min(this.length, end); encoding < end; ++encoding) {
            start += String.fromCharCode(this[encoding] & 127);
          }
          return start;
        case "latin1":
        case "binary":
          encoding = start;
          start = "";
          for (end = Math.min(this.length, end); encoding < end; ++encoding) {
            start += String.fromCharCode(this[encoding]);
          }
          return start;
        case "base64":
          return encoding = start, end = 0 === encoding && end === this.length ? base64.fromByteArray(this) : base64.fromByteArray(this.slice(encoding, end)), end;
        case "ucs2":
        case "ucs-2":
        case "utf16le":
        case "utf-16le":
          end = this.slice(start, end);
          encoding = "";
          for (start = 0; start < end.length; start += 2) {
            encoding += String.fromCharCode(end[start] + 256 * end[start + 1]);
          }
          return encoding;
        default:
          if (loweredCase) {
            throw new TypeError("Unknown encoding: " + encoding);
          }
          encoding = (encoding + "").toLowerCase();
          loweredCase = !0;
      }
    }
  }
  function swap(b, n, m) {
    var i = b[n];
    b[n] = b[m];
    b[m] = i;
  }
  function bidirectionalIndexOf(buffer, val, byteOffset, encoding, dir) {
    if (0 === buffer.length) {
      return -1;
    }
    "string" === typeof byteOffset ? (encoding = byteOffset, byteOffset = 0) : 2147483647 < byteOffset ? byteOffset = 2147483647 : -2147483648 > byteOffset && (byteOffset = -2147483648);
    byteOffset = +byteOffset;
    isNaN(byteOffset) && (byteOffset = dir ? 0 : buffer.length - 1);
    0 > byteOffset && (byteOffset = buffer.length + byteOffset);
    if (byteOffset >= buffer.length) {
      if (dir) {
        return -1;
      }
      byteOffset = buffer.length - 1;
    } else if (0 > byteOffset) {
      if (dir) {
        byteOffset = 0;
      } else {
        return -1;
      }
    }
    "string" === typeof val && (val = Buffer.from(val, encoding));
    if (Buffer.isBuffer(val)) {
      return 0 === val.length ? -1 : arrayIndexOf(buffer, val, byteOffset, encoding, dir);
    }
    if ("number" === typeof val) {
      return val &= 255, Buffer.TYPED_ARRAY_SUPPORT && "function" === typeof Uint8Array.prototype.indexOf ? dir ? Uint8Array.prototype.indexOf.call(buffer, val, byteOffset) : Uint8Array.prototype.lastIndexOf.call(buffer, val, byteOffset) : arrayIndexOf(buffer, [val], byteOffset, encoding, dir);
    }
    throw new TypeError("val must be string, number or Buffer");
  }
  function arrayIndexOf(arr, val, byteOffset, encoding, dir) {
    function read(buf, i) {
      return 1 === indexSize ? buf[i] : buf.readUInt16BE(i * indexSize);
    }
    var indexSize = 1, arrLength = arr.length, valLength = val.length;
    if (void 0 !== encoding && (encoding = String(encoding).toLowerCase(), "ucs2" === encoding || "ucs-2" === encoding || "utf16le" === encoding || "utf-16le" === encoding)) {
      if (2 > arr.length || 2 > val.length) {
        return -1;
      }
      indexSize = 2;
      arrLength /= 2;
      valLength /= 2;
      byteOffset /= 2;
    }
    if (dir) {
      for (encoding = -1; byteOffset < arrLength; byteOffset++) {
        if (read(arr, byteOffset) === read(val, -1 === encoding ? 0 : byteOffset - encoding)) {
          if (-1 === encoding && (encoding = byteOffset), byteOffset - encoding + 1 === valLength) {
            return encoding * indexSize;
          }
        } else {
          -1 !== encoding && (byteOffset -= byteOffset - encoding), encoding = -1;
        }
      }
    } else {
      for (byteOffset + valLength > arrLength && (byteOffset = arrLength - valLength); 0 <= byteOffset; byteOffset--) {
        arrLength = !0;
        for (encoding = 0; encoding < valLength; encoding++) {
          if (read(arr, byteOffset + encoding) !== read(val, encoding)) {
            arrLength = !1;
            break;
          }
        }
        if (arrLength) {
          return byteOffset;
        }
      }
    }
    return -1;
  }
  function utf8Slice(buf, start, end) {
    end = Math.min(buf.length, end);
    for (var res = []; start < end;) {
      var firstByte = buf[start], codePoint = null, bytesPerSequence = 239 < firstByte ? 4 : 223 < firstByte ? 3 : 191 < firstByte ? 2 : 1;
      if (start + bytesPerSequence <= end) {
        switch(bytesPerSequence) {
          case 1:
            128 > firstByte && (codePoint = firstByte);
            break;
          case 2:
            var secondByte = buf[start + 1];
            128 === (secondByte & 192) && (firstByte = (firstByte & 31) << 6 | secondByte & 63, 127 < firstByte && (codePoint = firstByte));
            break;
          case 3:
            secondByte = buf[start + 1];
            var thirdByte = buf[start + 2];
            128 === (secondByte & 192) && 128 === (thirdByte & 192) && (firstByte = (firstByte & 15) << 12 | (secondByte & 63) << 6 | thirdByte & 63, 2047 < firstByte && (55296 > firstByte || 57343 < firstByte) && (codePoint = firstByte));
            break;
          case 4:
            secondByte = buf[start + 1];
            thirdByte = buf[start + 2];
            var fourthByte = buf[start + 3];
            128 === (secondByte & 192) && 128 === (thirdByte & 192) && 128 === (fourthByte & 192) && (firstByte = (firstByte & 15) << 18 | (secondByte & 63) << 12 | (thirdByte & 63) << 6 | fourthByte & 63, 65535 < firstByte && 1114112 > firstByte && (codePoint = firstByte));
        }
      }
      null === codePoint ? (codePoint = 65533, bytesPerSequence = 1) : 65535 < codePoint && (codePoint -= 65536, res.push(codePoint >>> 10 & 1023 | 55296), codePoint = 56320 | codePoint & 1023);
      res.push(codePoint);
      start += bytesPerSequence;
    }
    buf = res.length;
    if (buf <= MAX_ARGUMENTS_LENGTH) {
      res = String.fromCharCode.apply(String, res);
    } else {
      end = "";
      for (start = 0; start < buf;) {
        end += String.fromCharCode.apply(String, res.slice(start, start += MAX_ARGUMENTS_LENGTH));
      }
      res = end;
    }
    return res;
  }
  function checkOffset(offset, ext, length) {
    if (0 !== offset % 1 || 0 > offset) {
      throw new RangeError("offset is not uint");
    }
    if (offset + ext > length) {
      throw new RangeError("Trying to access beyond buffer length");
    }
  }
  function checkInt(buf, value, offset, ext, max, min) {
    if (!Buffer.isBuffer(buf)) {
      throw new TypeError('"buffer" argument must be a Buffer instance');
    }
    if (value > max || value < min) {
      throw new RangeError('"value" argument is out of bounds');
    }
    if (offset + ext > buf.length) {
      throw new RangeError("Index out of range");
    }
  }
  function objectWriteUInt16(buf, value, offset, littleEndian) {
    0 > value && (value = 65535 + value + 1);
    for (var i = 0, j = Math.min(buf.length - offset, 2); i < j; ++i) {
      buf[offset + i] = (value & 255 << 8 * (littleEndian ? i : 1 - i)) >>> 8 * (littleEndian ? i : 1 - i);
    }
  }
  function objectWriteUInt32(buf, value, offset, littleEndian) {
    0 > value && (value = 4294967295 + value + 1);
    for (var i = 0, j = Math.min(buf.length - offset, 4); i < j; ++i) {
      buf[offset + i] = value >>> 8 * (littleEndian ? i : 3 - i) & 255;
    }
  }
  function checkIEEE754(buf, value, offset, ext, max, min) {
    if (offset + ext > buf.length) {
      throw new RangeError("Index out of range");
    }
    if (0 > offset) {
      throw new RangeError("Index out of range");
    }
  }
  function writeFloat(buf, value, offset, littleEndian, noAssert) {
    noAssert || checkIEEE754(buf, value, offset, 4, 3.4028234663852886e+38, -3.4028234663852886e+38);
    ieee754.write(buf, value, offset, littleEndian, 23, 4);
    return offset + 4;
  }
  function writeDouble(buf, value, offset, littleEndian, noAssert) {
    noAssert || checkIEEE754(buf, value, offset, 8, 1.7976931348623157E+308, -1.7976931348623157E+308);
    ieee754.write(buf, value, offset, littleEndian, 52, 8);
    return offset + 8;
  }
  function utf8ToBytes(string, units) {
    units = units || Infinity;
    for (var codePoint, length = string.length, leadSurrogate = null, bytes = [], i = 0; i < length; ++i) {
      codePoint = string.charCodeAt(i);
      if (55295 < codePoint && 57344 > codePoint) {
        if (!leadSurrogate) {
          if (56319 < codePoint) {
            -1 < (units -= 3) && bytes.push(239, 191, 189);
            continue;
          } else if (i + 1 === length) {
            -1 < (units -= 3) && bytes.push(239, 191, 189);
            continue;
          }
          leadSurrogate = codePoint;
          continue;
        }
        if (56320 > codePoint) {
          -1 < (units -= 3) && bytes.push(239, 191, 189);
          leadSurrogate = codePoint;
          continue;
        }
        codePoint = (leadSurrogate - 55296 << 10 | codePoint - 56320) + 65536;
      } else {
        leadSurrogate && -1 < (units -= 3) && bytes.push(239, 191, 189);
      }
      leadSurrogate = null;
      if (128 > codePoint) {
        if (0 > --units) {
          break;
        }
        bytes.push(codePoint);
      } else if (2048 > codePoint) {
        if (0 > (units -= 2)) {
          break;
        }
        bytes.push(codePoint >> 6 | 192, codePoint & 63 | 128);
      } else if (65536 > codePoint) {
        if (0 > (units -= 3)) {
          break;
        }
        bytes.push(codePoint >> 12 | 224, codePoint >> 6 & 63 | 128, codePoint & 63 | 128);
      } else if (1114112 > codePoint) {
        if (0 > (units -= 4)) {
          break;
        }
        bytes.push(codePoint >> 18 | 240, codePoint >> 12 & 63 | 128, codePoint >> 6 & 63 | 128, codePoint & 63 | 128);
      } else {
        throw Error("Invalid code point");
      }
    }
    return bytes;
  }
  function asciiToBytes(str) {
    for (var byteArray = [], i = 0; i < str.length; ++i) {
      byteArray.push(str.charCodeAt(i) & 255);
    }
    return byteArray;
  }
  function base64ToBytes(str) {
    var JSCompiler_temp_const = base64, JSCompiler_temp_const$jscomp$0 = JSCompiler_temp_const.toByteArray;
    str = (str.trim ? str.trim() : str.replace(/^\s+|\s+$/g, "")).replace(INVALID_BASE64_RE, "");
    if (2 > str.length) {
      str = "";
    } else {
      for (; 0 !== str.length % 4;) {
        str += "\x3d";
      }
    }
    return JSCompiler_temp_const$jscomp$0.call(JSCompiler_temp_const, str);
  }
  function blitBuffer(src, dst, offset, length) {
    for (var i = 0; i < length && !(i + offset >= dst.length || i >= src.length); ++i) {
      dst[i + offset] = src[i];
    }
    return i;
  }
  var base64 = require("module$node_modules$base64_js$index"), ieee754 = require("module$node_modules$ieee754$index"), isArray = require("module$node_modules$isarray$index");
  exports.Buffer = Buffer;
  exports.SlowBuffer = function(length) {
    +length != length && (length = 0);
    return Buffer.alloc(+length);
  };
  exports.INSPECT_MAX_BYTES = 50;
  Buffer.TYPED_ARRAY_SUPPORT = void 0 !== global.TYPED_ARRAY_SUPPORT ? global.TYPED_ARRAY_SUPPORT : typedArraySupport();
  exports.kMaxLength = Buffer.TYPED_ARRAY_SUPPORT ? 2147483647 : 1073741823;
  Buffer.poolSize = 8192;
  Buffer._augment = function(arr) {
    arr.__proto__ = Buffer.prototype;
    return arr;
  };
  Buffer.from = function(value, encodingOrOffset, length) {
    return from(null, value, encodingOrOffset, length);
  };
  Buffer.TYPED_ARRAY_SUPPORT && (Buffer.prototype.__proto__ = Uint8Array.prototype, Buffer.__proto__ = Uint8Array, "undefined" !== typeof Symbol && Symbol.species && Buffer[Symbol.species] === Buffer && Object.defineProperty(Buffer, Symbol.species, {value:null, configurable:!0}));
  Buffer.alloc = function(size, fill, encoding) {
    assertSize(size);
    size = 0 >= size ? createBuffer(null, size) : void 0 !== fill ? "string" === typeof encoding ? createBuffer(null, size).fill(fill, encoding) : createBuffer(null, size).fill(fill) : createBuffer(null, size);
    return size;
  };
  Buffer.allocUnsafe = function(size) {
    return allocUnsafe(null, size);
  };
  Buffer.allocUnsafeSlow = function(size) {
    return allocUnsafe(null, size);
  };
  Buffer.isBuffer = function(b) {
    return !(null == b || !b._isBuffer);
  };
  Buffer.compare = function(a, b) {
    if (!Buffer.isBuffer(a) || !Buffer.isBuffer(b)) {
      throw new TypeError("Arguments must be Buffers");
    }
    if (a === b) {
      return 0;
    }
    for (var x = a.length, y = b.length, i = 0, len = Math.min(x, y); i < len; ++i) {
      if (a[i] !== b[i]) {
        x = a[i];
        y = b[i];
        break;
      }
    }
    return x < y ? -1 : y < x ? 1 : 0;
  };
  Buffer.isEncoding = function(encoding) {
    switch(String(encoding).toLowerCase()) {
      case "hex":
      case "utf8":
      case "utf-8":
      case "ascii":
      case "latin1":
      case "binary":
      case "base64":
      case "ucs2":
      case "ucs-2":
      case "utf16le":
      case "utf-16le":
        return !0;
      default:
        return !1;
    }
  };
  Buffer.concat = function(list, length) {
    if (!isArray(list)) {
      throw new TypeError('"list" argument must be an Array of Buffers');
    }
    if (0 === list.length) {
      return Buffer.alloc(0);
    }
    var i;
    if (void 0 === length) {
      for (i = length = 0; i < list.length; ++i) {
        length += list[i].length;
      }
    }
    length = Buffer.allocUnsafe(length);
    var pos = 0;
    for (i = 0; i < list.length; ++i) {
      var buf = list[i];
      if (!Buffer.isBuffer(buf)) {
        throw new TypeError('"list" argument must be an Array of Buffers');
      }
      buf.copy(length, pos);
      pos += buf.length;
    }
    return length;
  };
  Buffer.byteLength = byteLength;
  Buffer.prototype._isBuffer = !0;
  Buffer.prototype.swap16 = function() {
    var len = this.length;
    if (0 !== len % 2) {
      throw new RangeError("Buffer size must be a multiple of 16-bits");
    }
    for (var i = 0; i < len; i += 2) {
      swap(this, i, i + 1);
    }
    return this;
  };
  Buffer.prototype.swap32 = function() {
    var len = this.length;
    if (0 !== len % 4) {
      throw new RangeError("Buffer size must be a multiple of 32-bits");
    }
    for (var i = 0; i < len; i += 4) {
      swap(this, i, i + 3), swap(this, i + 1, i + 2);
    }
    return this;
  };
  Buffer.prototype.swap64 = function() {
    var len = this.length;
    if (0 !== len % 8) {
      throw new RangeError("Buffer size must be a multiple of 64-bits");
    }
    for (var i = 0; i < len; i += 8) {
      swap(this, i, i + 7), swap(this, i + 1, i + 6), swap(this, i + 2, i + 5), swap(this, i + 3, i + 4);
    }
    return this;
  };
  Buffer.prototype.toString = function() {
    var length = this.length | 0;
    return 0 === length ? "" : 0 === arguments.length ? utf8Slice(this, 0, length) : slowToString.apply(this, arguments);
  };
  Buffer.prototype.equals = function(b) {
    if (!Buffer.isBuffer(b)) {
      throw new TypeError("Argument must be a Buffer");
    }
    return this === b ? !0 : 0 === Buffer.compare(this, b);
  };
  Buffer.prototype.inspect = function() {
    var str = "", max = exports.INSPECT_MAX_BYTES;
    0 < this.length && (str = this.toString("hex", 0, max).match(/.{2}/g).join(" "), this.length > max && (str += " ... "));
    return "\x3cBuffer " + str + "\x3e";
  };
  Buffer.prototype.compare = function(target, start, end, thisStart, thisEnd) {
    if (!Buffer.isBuffer(target)) {
      throw new TypeError("Argument must be a Buffer");
    }
    void 0 === start && (start = 0);
    void 0 === end && (end = target ? target.length : 0);
    void 0 === thisStart && (thisStart = 0);
    void 0 === thisEnd && (thisEnd = this.length);
    if (0 > start || end > target.length || 0 > thisStart || thisEnd > this.length) {
      throw new RangeError("out of range index");
    }
    if (thisStart >= thisEnd && start >= end) {
      return 0;
    }
    if (thisStart >= thisEnd) {
      return -1;
    }
    if (start >= end) {
      return 1;
    }
    start >>>= 0;
    end >>>= 0;
    thisStart >>>= 0;
    thisEnd >>>= 0;
    if (this === target) {
      return 0;
    }
    var x = thisEnd - thisStart, y = end - start, len = Math.min(x, y);
    thisStart = this.slice(thisStart, thisEnd);
    target = target.slice(start, end);
    for (start = 0; start < len; ++start) {
      if (thisStart[start] !== target[start]) {
        x = thisStart[start];
        y = target[start];
        break;
      }
    }
    return x < y ? -1 : y < x ? 1 : 0;
  };
  Buffer.prototype.includes = function(val, byteOffset, encoding) {
    return -1 !== this.indexOf(val, byteOffset, encoding);
  };
  Buffer.prototype.indexOf = function(val, byteOffset, encoding) {
    return bidirectionalIndexOf(this, val, byteOffset, encoding, !0);
  };
  Buffer.prototype.lastIndexOf = function(val, byteOffset, encoding) {
    return bidirectionalIndexOf(this, val, byteOffset, encoding, !1);
  };
  Buffer.prototype.write = function(string, offset, length, encoding) {
    if (void 0 === offset) {
      encoding = "utf8", length = this.length, offset = 0;
    } else if (void 0 === length && "string" === typeof offset) {
      encoding = offset, length = this.length, offset = 0;
    } else if (isFinite(offset)) {
      offset |= 0, isFinite(length) ? (length |= 0, void 0 === encoding && (encoding = "utf8")) : (encoding = length, length = void 0);
    } else {
      throw Error("Buffer.write(string, encoding, offset[, length]) is no longer supported");
    }
    var remaining = this.length - offset;
    if (void 0 === length || length > remaining) {
      length = remaining;
    }
    if (0 < string.length && (0 > length || 0 > offset) || offset > this.length) {
      throw new RangeError("Attempt to write outside buffer bounds");
    }
    encoding ||= "utf8";
    for (remaining = !1;;) {
      switch(encoding) {
        case "hex":
          a: {
            offset = Number(offset) || 0;
            encoding = this.length - offset;
            length ? (length = Number(length), length > encoding && (length = encoding)) : length = encoding;
            encoding = string.length;
            if (0 !== encoding % 2) {
              throw new TypeError("Invalid hex string");
            }
            length > encoding / 2 && (length = encoding / 2);
            for (encoding = 0; encoding < length; ++encoding) {
              remaining = parseInt(string.substr(2 * encoding, 2), 16);
              if (isNaN(remaining)) {
                string = encoding;
                break a;
              }
              this[offset + encoding] = remaining;
            }
            string = encoding;
          }
          return string;
        case "utf8":
        case "utf-8":
          return blitBuffer(utf8ToBytes(string, this.length - offset), this, offset, length);
        case "ascii":
          return blitBuffer(asciiToBytes(string), this, offset, length);
        case "latin1":
        case "binary":
          return blitBuffer(asciiToBytes(string), this, offset, length);
        case "base64":
          return blitBuffer(base64ToBytes(string), this, offset, length);
        case "ucs2":
        case "ucs-2":
        case "utf16le":
        case "utf-16le":
          encoding = string;
          remaining = this.length - offset;
          for (var byteArray = [], i = 0; i < encoding.length && !(0 > (remaining -= 2)); ++i) {
            var lo = encoding.charCodeAt(i);
            string = lo >> 8;
            lo %= 256;
            byteArray.push(lo);
            byteArray.push(string);
          }
          return blitBuffer(byteArray, this, offset, length);
        default:
          if (remaining) {
            throw new TypeError("Unknown encoding: " + encoding);
          }
          encoding = ("" + encoding).toLowerCase();
          remaining = !0;
      }
    }
  };
  Buffer.prototype.toJSON = function() {
    return {type:"Buffer", data:Array.prototype.slice.call(this._arr || this, 0)};
  };
  var MAX_ARGUMENTS_LENGTH = 4096;
  Buffer.prototype.slice = function(start, end) {
    var len = this.length;
    start = ~~start;
    end = void 0 === end ? len : ~~end;
    0 > start ? (start += len, 0 > start && (start = 0)) : start > len && (start = len);
    0 > end ? (end += len, 0 > end && (end = 0)) : end > len && (end = len);
    end < start && (end = start);
    if (Buffer.TYPED_ARRAY_SUPPORT) {
      end = this.subarray(start, end), end.__proto__ = Buffer.prototype;
    } else {
      len = end - start;
      end = new Buffer(len, void 0);
      for (var i = 0; i < len; ++i) {
        end[i] = this[i + start];
      }
    }
    return end;
  };
  Buffer.prototype.readUIntLE = function(offset, byteLength, noAssert) {
    offset |= 0;
    byteLength |= 0;
    noAssert || checkOffset(offset, byteLength, this.length);
    noAssert = this[offset];
    for (var mul = 1, i = 0; ++i < byteLength && (mul *= 256);) {
      noAssert += this[offset + i] * mul;
    }
    return noAssert;
  };
  Buffer.prototype.readUIntBE = function(offset, byteLength, noAssert) {
    offset |= 0;
    byteLength |= 0;
    noAssert || checkOffset(offset, byteLength, this.length);
    noAssert = this[offset + --byteLength];
    for (var mul = 1; 0 < byteLength && (mul *= 256);) {
      noAssert += this[offset + --byteLength] * mul;
    }
    return noAssert;
  };
  Buffer.prototype.readUInt8 = function(offset, noAssert) {
    noAssert || checkOffset(offset, 1, this.length);
    return this[offset];
  };
  Buffer.prototype.readUInt16LE = function(offset, noAssert) {
    noAssert || checkOffset(offset, 2, this.length);
    return this[offset] | this[offset + 1] << 8;
  };
  Buffer.prototype.readUInt16BE = function(offset, noAssert) {
    noAssert || checkOffset(offset, 2, this.length);
    return this[offset] << 8 | this[offset + 1];
  };
  Buffer.prototype.readUInt32LE = function(offset, noAssert) {
    noAssert || checkOffset(offset, 4, this.length);
    return (this[offset] | this[offset + 1] << 8 | this[offset + 2] << 16) + 16777216 * this[offset + 3];
  };
  Buffer.prototype.readUInt32BE = function(offset, noAssert) {
    noAssert || checkOffset(offset, 4, this.length);
    return 16777216 * this[offset] + (this[offset + 1] << 16 | this[offset + 2] << 8 | this[offset + 3]);
  };
  Buffer.prototype.readIntLE = function(offset, byteLength, noAssert) {
    offset |= 0;
    byteLength |= 0;
    noAssert || checkOffset(offset, byteLength, this.length);
    noAssert = this[offset];
    for (var mul = 1, i = 0; ++i < byteLength && (mul *= 256);) {
      noAssert += this[offset + i] * mul;
    }
    noAssert >= 128 * mul && (noAssert -= Math.pow(2, 8 * byteLength));
    return noAssert;
  };
  Buffer.prototype.readIntBE = function(offset, byteLength, noAssert) {
    offset |= 0;
    byteLength |= 0;
    noAssert || checkOffset(offset, byteLength, this.length);
    noAssert = byteLength;
    for (var mul = 1, val = this[offset + --noAssert]; 0 < noAssert && (mul *= 256);) {
      val += this[offset + --noAssert] * mul;
    }
    val >= 128 * mul && (val -= Math.pow(2, 8 * byteLength));
    return val;
  };
  Buffer.prototype.readInt8 = function(offset, noAssert) {
    noAssert || checkOffset(offset, 1, this.length);
    return this[offset] & 128 ? -1 * (255 - this[offset] + 1) : this[offset];
  };
  Buffer.prototype.readInt16LE = function(offset, noAssert) {
    noAssert || checkOffset(offset, 2, this.length);
    offset = this[offset] | this[offset + 1] << 8;
    return offset & 32768 ? offset | 4294901760 : offset;
  };
  Buffer.prototype.readInt16BE = function(offset, noAssert) {
    noAssert || checkOffset(offset, 2, this.length);
    offset = this[offset + 1] | this[offset] << 8;
    return offset & 32768 ? offset | 4294901760 : offset;
  };
  Buffer.prototype.readInt32LE = function(offset, noAssert) {
    noAssert || checkOffset(offset, 4, this.length);
    return this[offset] | this[offset + 1] << 8 | this[offset + 2] << 16 | this[offset + 3] << 24;
  };
  Buffer.prototype.readInt32BE = function(offset, noAssert) {
    noAssert || checkOffset(offset, 4, this.length);
    return this[offset] << 24 | this[offset + 1] << 16 | this[offset + 2] << 8 | this[offset + 3];
  };
  Buffer.prototype.readFloatLE = function(offset, noAssert) {
    noAssert || checkOffset(offset, 4, this.length);
    return ieee754.read(this, offset, !0, 23, 4);
  };
  Buffer.prototype.readFloatBE = function(offset, noAssert) {
    noAssert || checkOffset(offset, 4, this.length);
    return ieee754.read(this, offset, !1, 23, 4);
  };
  Buffer.prototype.readDoubleLE = function(offset, noAssert) {
    noAssert || checkOffset(offset, 8, this.length);
    return ieee754.read(this, offset, !0, 52, 8);
  };
  Buffer.prototype.readDoubleBE = function(offset, noAssert) {
    noAssert || checkOffset(offset, 8, this.length);
    return ieee754.read(this, offset, !1, 52, 8);
  };
  Buffer.prototype.writeUIntLE = function(value, offset, byteLength, noAssert) {
    value = +value;
    offset |= 0;
    byteLength |= 0;
    noAssert || checkInt(this, value, offset, byteLength, Math.pow(2, 8 * byteLength) - 1, 0);
    noAssert = 1;
    var i = 0;
    for (this[offset] = value & 255; ++i < byteLength && (noAssert *= 256);) {
      this[offset + i] = value / noAssert & 255;
    }
    return offset + byteLength;
  };
  Buffer.prototype.writeUIntBE = function(value, offset, byteLength, noAssert) {
    value = +value;
    offset |= 0;
    byteLength |= 0;
    noAssert || checkInt(this, value, offset, byteLength, Math.pow(2, 8 * byteLength) - 1, 0);
    noAssert = byteLength - 1;
    var mul = 1;
    for (this[offset + noAssert] = value & 255; 0 <= --noAssert && (mul *= 256);) {
      this[offset + noAssert] = value / mul & 255;
    }
    return offset + byteLength;
  };
  Buffer.prototype.writeUInt8 = function(value, offset, noAssert) {
    value = +value;
    offset |= 0;
    noAssert || checkInt(this, value, offset, 1, 255, 0);
    Buffer.TYPED_ARRAY_SUPPORT || (value = Math.floor(value));
    this[offset] = value & 255;
    return offset + 1;
  };
  Buffer.prototype.writeUInt16LE = function(value, offset, noAssert) {
    value = +value;
    offset |= 0;
    noAssert || checkInt(this, value, offset, 2, 65535, 0);
    Buffer.TYPED_ARRAY_SUPPORT ? (this[offset] = value & 255, this[offset + 1] = value >>> 8) : objectWriteUInt16(this, value, offset, !0);
    return offset + 2;
  };
  Buffer.prototype.writeUInt16BE = function(value, offset, noAssert) {
    value = +value;
    offset |= 0;
    noAssert || checkInt(this, value, offset, 2, 65535, 0);
    Buffer.TYPED_ARRAY_SUPPORT ? (this[offset] = value >>> 8, this[offset + 1] = value & 255) : objectWriteUInt16(this, value, offset, !1);
    return offset + 2;
  };
  Buffer.prototype.writeUInt32LE = function(value, offset, noAssert) {
    value = +value;
    offset |= 0;
    noAssert || checkInt(this, value, offset, 4, 4294967295, 0);
    Buffer.TYPED_ARRAY_SUPPORT ? (this[offset + 3] = value >>> 24, this[offset + 2] = value >>> 16, this[offset + 1] = value >>> 8, this[offset] = value & 255) : objectWriteUInt32(this, value, offset, !0);
    return offset + 4;
  };
  Buffer.prototype.writeUInt32BE = function(value, offset, noAssert) {
    value = +value;
    offset |= 0;
    noAssert || checkInt(this, value, offset, 4, 4294967295, 0);
    Buffer.TYPED_ARRAY_SUPPORT ? (this[offset] = value >>> 24, this[offset + 1] = value >>> 16, this[offset + 2] = value >>> 8, this[offset + 3] = value & 255) : objectWriteUInt32(this, value, offset, !1);
    return offset + 4;
  };
  Buffer.prototype.writeIntLE = function(value, offset, byteLength, noAssert) {
    value = +value;
    offset |= 0;
    noAssert || (noAssert = Math.pow(2, 8 * byteLength - 1), checkInt(this, value, offset, byteLength, noAssert - 1, -noAssert));
    noAssert = 0;
    var mul = 1, sub = 0;
    for (this[offset] = value & 255; ++noAssert < byteLength && (mul *= 256);) {
      0 > value && 0 === sub && 0 !== this[offset + noAssert - 1] && (sub = 1), this[offset + noAssert] = (value / mul >> 0) - sub & 255;
    }
    return offset + byteLength;
  };
  Buffer.prototype.writeIntBE = function(value, offset, byteLength, noAssert) {
    value = +value;
    offset |= 0;
    noAssert || (noAssert = Math.pow(2, 8 * byteLength - 1), checkInt(this, value, offset, byteLength, noAssert - 1, -noAssert));
    noAssert = byteLength - 1;
    var mul = 1, sub = 0;
    for (this[offset + noAssert] = value & 255; 0 <= --noAssert && (mul *= 256);) {
      0 > value && 0 === sub && 0 !== this[offset + noAssert + 1] && (sub = 1), this[offset + noAssert] = (value / mul >> 0) - sub & 255;
    }
    return offset + byteLength;
  };
  Buffer.prototype.writeInt8 = function(value, offset, noAssert) {
    value = +value;
    offset |= 0;
    noAssert || checkInt(this, value, offset, 1, 127, -128);
    Buffer.TYPED_ARRAY_SUPPORT || (value = Math.floor(value));
    0 > value && (value = 255 + value + 1);
    this[offset] = value & 255;
    return offset + 1;
  };
  Buffer.prototype.writeInt16LE = function(value, offset, noAssert) {
    value = +value;
    offset |= 0;
    noAssert || checkInt(this, value, offset, 2, 32767, -32768);
    Buffer.TYPED_ARRAY_SUPPORT ? (this[offset] = value & 255, this[offset + 1] = value >>> 8) : objectWriteUInt16(this, value, offset, !0);
    return offset + 2;
  };
  Buffer.prototype.writeInt16BE = function(value, offset, noAssert) {
    value = +value;
    offset |= 0;
    noAssert || checkInt(this, value, offset, 2, 32767, -32768);
    Buffer.TYPED_ARRAY_SUPPORT ? (this[offset] = value >>> 8, this[offset + 1] = value & 255) : objectWriteUInt16(this, value, offset, !1);
    return offset + 2;
  };
  Buffer.prototype.writeInt32LE = function(value, offset, noAssert) {
    value = +value;
    offset |= 0;
    noAssert || checkInt(this, value, offset, 4, 2147483647, -2147483648);
    Buffer.TYPED_ARRAY_SUPPORT ? (this[offset] = value & 255, this[offset + 1] = value >>> 8, this[offset + 2] = value >>> 16, this[offset + 3] = value >>> 24) : objectWriteUInt32(this, value, offset, !0);
    return offset + 4;
  };
  Buffer.prototype.writeInt32BE = function(value, offset, noAssert) {
    value = +value;
    offset |= 0;
    noAssert || checkInt(this, value, offset, 4, 2147483647, -2147483648);
    0 > value && (value = 4294967295 + value + 1);
    Buffer.TYPED_ARRAY_SUPPORT ? (this[offset] = value >>> 24, this[offset + 1] = value >>> 16, this[offset + 2] = value >>> 8, this[offset + 3] = value & 255) : objectWriteUInt32(this, value, offset, !1);
    return offset + 4;
  };
  Buffer.prototype.writeFloatLE = function(value, offset, noAssert) {
    return writeFloat(this, value, offset, !0, noAssert);
  };
  Buffer.prototype.writeFloatBE = function(value, offset, noAssert) {
    return writeFloat(this, value, offset, !1, noAssert);
  };
  Buffer.prototype.writeDoubleLE = function(value, offset, noAssert) {
    return writeDouble(this, value, offset, !0, noAssert);
  };
  Buffer.prototype.writeDoubleBE = function(value, offset, noAssert) {
    return writeDouble(this, value, offset, !1, noAssert);
  };
  Buffer.prototype.copy = function(target, targetStart, start, end) {
    start ||= 0;
    end || 0 === end || (end = this.length);
    targetStart >= target.length && (targetStart = target.length);
    targetStart ||= 0;
    0 < end && end < start && (end = start);
    if (end === start || 0 === target.length || 0 === this.length) {
      return 0;
    }
    if (0 > targetStart) {
      throw new RangeError("targetStart out of bounds");
    }
    if (0 > start || start >= this.length) {
      throw new RangeError("sourceStart out of bounds");
    }
    if (0 > end) {
      throw new RangeError("sourceEnd out of bounds");
    }
    end > this.length && (end = this.length);
    target.length - targetStart < end - start && (end = target.length - targetStart + start);
    var len = end - start;
    if (this === target && start < targetStart && targetStart < end) {
      for (end = len - 1; 0 <= end; --end) {
        target[end + targetStart] = this[end + start];
      }
    } else if (1000 > len || !Buffer.TYPED_ARRAY_SUPPORT) {
      for (end = 0; end < len; ++end) {
        target[end + targetStart] = this[end + start];
      }
    } else {
      Uint8Array.prototype.set.call(target, this.subarray(start, start + len), targetStart);
    }
    return len;
  };
  Buffer.prototype.fill = function(val, start, end, encoding) {
    if ("string" === typeof val) {
      "string" === typeof start ? (encoding = start, start = 0, end = this.length) : "string" === typeof end && (encoding = end, end = this.length);
      if (1 === val.length) {
        var code = val.charCodeAt(0);
        256 > code && (val = code);
      }
      if (void 0 !== encoding && "string" !== typeof encoding) {
        throw new TypeError("encoding must be a string");
      }
      if ("string" === typeof encoding && !Buffer.isEncoding(encoding)) {
        throw new TypeError("Unknown encoding: " + encoding);
      }
    } else {
      "number" === typeof val && (val &= 255);
    }
    if (0 > start || this.length < start || this.length < end) {
      throw new RangeError("Out of range index");
    }
    if (end <= start) {
      return this;
    }
    start >>>= 0;
    end = void 0 === end ? this.length : end >>> 0;
    val ||= 0;
    if ("number" === typeof val) {
      for (encoding = start; encoding < end; ++encoding) {
        this[encoding] = val;
      }
    } else {
      for (val = Buffer.isBuffer(val) ? val : utf8ToBytes((new Buffer(val, encoding)).toString()), code = val.length, encoding = 0; encoding < end - start; ++encoding) {
        this[encoding + start] = val[encoding % code];
      }
    }
    return this;
  };
  var INVALID_BASE64_RE = /[^+\/0-9A-Za-z-_]/g;
};

//# sourceMappingURL=module$node_modules$buffer$index.js.map
