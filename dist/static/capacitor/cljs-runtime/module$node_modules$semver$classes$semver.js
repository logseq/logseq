shadow$provide.module$node_modules$semver$classes$semver = function(global, require, module, exports) {
  const debug = require("module$node_modules$semver$internal$debug"), {MAX_LENGTH, MAX_SAFE_INTEGER} = require("module$node_modules$semver$internal$constants"), {safeRe:re, t} = require("module$node_modules$semver$internal$re"), parseOptions = require("module$node_modules$semver$internal$parse_options"), {compareIdentifiers} = require("module$node_modules$semver$internal$identifiers");
  class SemVer {
    constructor(version, options) {
      options = parseOptions(options);
      if (version instanceof SemVer) {
        if (version.loose === !!options.loose && version.includePrerelease === !!options.includePrerelease) {
          return version;
        }
        version = version.version;
      } else if ("string" !== typeof version) {
        throw new TypeError(`Invalid version. Must be a string. Got type "${typeof version}".`);
      }
      if (version.length > MAX_LENGTH) {
        throw new TypeError(`version is longer than ${MAX_LENGTH} characters`);
      }
      debug("SemVer", version, options);
      this.options = options;
      this.loose = !!options.loose;
      this.includePrerelease = !!options.includePrerelease;
      options = version.trim().match(options.loose ? re[t.LOOSE] : re[t.FULL]);
      if (!options) {
        throw new TypeError(`Invalid Version: ${version}`);
      }
      this.raw = version;
      this.major = +options[1];
      this.minor = +options[2];
      this.patch = +options[3];
      if (this.major > MAX_SAFE_INTEGER || 0 > this.major) {
        throw new TypeError("Invalid major version");
      }
      if (this.minor > MAX_SAFE_INTEGER || 0 > this.minor) {
        throw new TypeError("Invalid minor version");
      }
      if (this.patch > MAX_SAFE_INTEGER || 0 > this.patch) {
        throw new TypeError("Invalid patch version");
      }
      this.prerelease = options[4] ? options[4].split(".").map(id => {
        if (/^[0-9]+$/.test(id)) {
          const num = +id;
          if (0 <= num && num < MAX_SAFE_INTEGER) {
            return num;
          }
        }
        return id;
      }) : [];
      this.build = options[5] ? options[5].split(".") : [];
      this.format();
    }
    format() {
      this.version = `${this.major}.${this.minor}.${this.patch}`;
      this.prerelease.length && (this.version += `-${this.prerelease.join(".")}`);
      return this.version;
    }
    toString() {
      return this.version;
    }
    compare(other) {
      debug("SemVer.compare", this.version, this.options, other);
      if (!(other instanceof SemVer)) {
        if ("string" === typeof other && other === this.version) {
          return 0;
        }
        other = new SemVer(other, this.options);
      }
      return other.version === this.version ? 0 : this.compareMain(other) || this.comparePre(other);
    }
    compareMain(other) {
      other instanceof SemVer || (other = new SemVer(other, this.options));
      return compareIdentifiers(this.major, other.major) || compareIdentifiers(this.minor, other.minor) || compareIdentifiers(this.patch, other.patch);
    }
    comparePre(other) {
      other instanceof SemVer || (other = new SemVer(other, this.options));
      if (this.prerelease.length && !other.prerelease.length) {
        return -1;
      }
      if (!this.prerelease.length && other.prerelease.length) {
        return 1;
      }
      if (!this.prerelease.length && !other.prerelease.length) {
        return 0;
      }
      let i = 0;
      do {
        const a = this.prerelease[i], b = other.prerelease[i];
        debug("prerelease compare", i, a, b);
        if (void 0 === a && void 0 === b) {
          return 0;
        }
        if (void 0 === b) {
          return 1;
        }
        if (void 0 === a) {
          return -1;
        }
        if (a !== b) {
          return compareIdentifiers(a, b);
        }
      } while (++i);
    }
    compareBuild(other) {
      other instanceof SemVer || (other = new SemVer(other, this.options));
      let i = 0;
      do {
        const a = this.build[i], b = other.build[i];
        debug("prerelease compare", i, a, b);
        if (void 0 === a && void 0 === b) {
          return 0;
        }
        if (void 0 === b) {
          return 1;
        }
        if (void 0 === a) {
          return -1;
        }
        if (a !== b) {
          return compareIdentifiers(a, b);
        }
      } while (++i);
    }
    inc(release, identifier, identifierBase) {
      switch(release) {
        case "premajor":
          this.minor = this.patch = this.prerelease.length = 0;
          this.major++;
          this.inc("pre", identifier, identifierBase);
          break;
        case "preminor":
          this.patch = this.prerelease.length = 0;
          this.minor++;
          this.inc("pre", identifier, identifierBase);
          break;
        case "prepatch":
          this.prerelease.length = 0;
          this.inc("patch", identifier, identifierBase);
          this.inc("pre", identifier, identifierBase);
          break;
        case "prerelease":
          0 === this.prerelease.length && this.inc("patch", identifier, identifierBase);
          this.inc("pre", identifier, identifierBase);
          break;
        case "major":
          0 === this.minor && 0 === this.patch && 0 !== this.prerelease.length || this.major++;
          this.patch = this.minor = 0;
          this.prerelease = [];
          break;
        case "minor":
          0 === this.patch && 0 !== this.prerelease.length || this.minor++;
          this.patch = 0;
          this.prerelease = [];
          break;
        case "patch":
          0 === this.prerelease.length && this.patch++;
          this.prerelease = [];
          break;
        case "pre":
          release = Number(identifierBase) ? 1 : 0;
          if (!identifier && !1 === identifierBase) {
            throw Error("invalid increment argument: identifier is empty");
          }
          if (0 === this.prerelease.length) {
            this.prerelease = [release];
          } else {
            let i = this.prerelease.length;
            for (; 0 <= --i;) {
              "number" === typeof this.prerelease[i] && (this.prerelease[i]++, i = -2);
            }
            if (-1 === i) {
              if (identifier === this.prerelease.join(".") && !1 === identifierBase) {
                throw Error("invalid increment argument: identifier already exists");
              }
              this.prerelease.push(release);
            }
          }
          identifier && (release = [identifier, release], !1 === identifierBase && (release = [identifier]), 0 === compareIdentifiers(this.prerelease[0], identifier) ? isNaN(this.prerelease[1]) && (this.prerelease = release) : this.prerelease = release);
          break;
        default:
          throw Error(`invalid increment argument: ${release}`);
      }
      this.raw = this.format();
      this.build.length && (this.raw += `+${this.build.join(".")}`);
      return this;
    }
  }
  module.exports = SemVer;
};

//# sourceMappingURL=module$node_modules$semver$classes$semver.js.map
