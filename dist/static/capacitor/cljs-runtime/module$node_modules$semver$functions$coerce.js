shadow$provide.module$node_modules$semver$functions$coerce = function(global, require, module, exports) {
  const SemVer = require("module$node_modules$semver$classes$semver"), parse = require("module$node_modules$semver$functions$parse"), {safeRe:re, t} = require("module$node_modules$semver$internal$re");
  module.exports = (version, options) => {
    if (version instanceof SemVer) {
      return version;
    }
    "number" === typeof version && (version = String(version));
    if ("string" !== typeof version) {
      return null;
    }
    options = options || {};
    let match = null;
    if (options.rtl) {
      let next;
      for (; (next = re[t.COERCERTL].exec(version)) && (!match || match.index + match[0].length !== version.length);) {
        match && next.index + next[0].length === match.index + match[0].length || (match = next), re[t.COERCERTL].lastIndex = next.index + next[1].length + next[2].length;
      }
      re[t.COERCERTL].lastIndex = -1;
    } else {
      match = version.match(re[t.COERCE]);
    }
    return null === match ? null : parse(`${match[2]}.${match[3] || "0"}.${match[4] || "0"}`, options);
  };
};

//# sourceMappingURL=module$node_modules$semver$functions$coerce.js.map
