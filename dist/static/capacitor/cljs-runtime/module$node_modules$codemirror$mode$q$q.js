shadow$provide.module$node_modules$codemirror$mode$q$q = function(global, require, module, exports) {
  (function(mod) {
    "object" == typeof exports && "object" == typeof module ? mod(require("module$node_modules$codemirror$lib$codemirror")) : "function" == typeof define && define.amd ? define(["../../lib/codemirror"], mod) : mod(CodeMirror);
  })(function(CodeMirror) {
    CodeMirror.defineMode("q", function(config) {
      function tokenBase(stream, state) {
        var sol = stream.sol(), c = stream.next();
        curPunc = null;
        if (sol) {
          if ("/" == c) {
            return (state.tokenize = tokenLineComment)(stream, state);
          }
          if ("\\" == c) {
            return stream.eol() || /\s/.test(stream.peek()) ? (stream.skipToEnd(), /^\\\s*$/.test(stream.current()) ? (state.tokenize = tokenCommentToEOF)(stream) : state.tokenize = tokenBase, "comment") : (state.tokenize = tokenBase, "builtin");
          }
        }
        if (/\s/.test(c)) {
          return "/" == stream.peek() ? (stream.skipToEnd(), "comment") : "whitespace";
        }
        if ('"' == c) {
          return (state.tokenize = tokenString)(stream, state);
        }
        if ("`" == c) {
          return stream.eatWhile(/[A-Za-z\d_:\/.]/), "symbol";
        }
        if ("." == c && /\d/.test(stream.peek()) || /\d/.test(c)) {
          state = null;
          stream.backUp(1);
          if (stream.match(/^\d{4}\.\d{2}(m|\.\d{2}([DT](\d{2}(:\d{2}(:\d{2}(\.\d{1,9})?)?)?)?)?)/) || stream.match(/^\d+D(\d{2}(:\d{2}(:\d{2}(\.\d{1,9})?)?)?)/) || stream.match(/^\d{2}:\d{2}(:\d{2}(\.\d{1,9})?)?/) || stream.match(/^\d+[ptuv]{1}/)) {
            state = "temporal";
          } else if (stream.match(/^0[NwW]{1}/) || stream.match(/^0x[\da-fA-F]*/) || stream.match(/^[01]+[b]{1}/) || stream.match(/^\d+[chijn]{1}/) || stream.match(/-?\d*(\.\d*)?(e[+\-]?\d+)?(e|f)?/)) {
            state = "number";
          }
          return !state || (c = stream.peek()) && !E.test(c) ? (stream.next(), "error") : state;
        }
        return /[A-Za-z]|\./.test(c) ? (stream.eatWhile(/[A-Za-z._\d]/), keywords.test(stream.current()) ? "keyword" : "variable") : /[|/&^!+:\\\-*%$=~#;@><\.,?_']/.test(c) || /[{}\(\[\]\)]/.test(c) ? null : "error";
      }
      function tokenLineComment(stream, state) {
        return stream.skipToEnd(), /\/\s*$/.test(stream.current()) ? (state.tokenize = tokenBlockComment)(stream, state) : state.tokenize = tokenBase, "comment";
      }
      function tokenBlockComment(stream, state) {
        var f = stream.sol() && "\\" == stream.peek();
        stream.skipToEnd();
        f && /^\\\s*$/.test(stream.current()) && (state.tokenize = tokenBase);
        return "comment";
      }
      function tokenCommentToEOF(stream) {
        return stream.skipToEnd(), "comment";
      }
      function tokenString(stream, state) {
        for (var escaped = !1, next, end = !1; next = stream.next();) {
          if ('"' == next && !escaped) {
            end = !0;
            break;
          }
          escaped = !escaped && "\\" == next;
        }
        end && (state.tokenize = tokenBase);
        return "string";
      }
      function pushContext(state, type, col) {
        state.context = {prev:state.context, indent:state.indent, col, type};
      }
      function popContext(state) {
        state.indent = state.context.indent;
        state.context = state.context.prev;
      }
      var indentUnit = config.indentUnit, curPunc, keywords = RegExp("^(abs|acos|aj|aj0|all|and|any|asc|asin|asof|atan|attr|avg|avgs|bin|by|ceiling|cols|cor|cos|count|cov|cross|csv|cut|delete|deltas|desc|dev|differ|distinct|div|do|each|ej|enlist|eval|except|exec|exit|exp|fby|fills|first|fkeys|flip|floor|from|get|getenv|group|gtime|hclose|hcount|hdel|hopen|hsym|iasc|idesc|if|ij|in|insert|inter|inv|key|keys|last|like|list|lj|load|log|lower|lsq|ltime|ltrim|mavg|max|maxs|mcount|md5|mdev|med|meta|min|mins|mmax|mmin|mmu|mod|msum|neg|next|not|null|or|over|parse|peach|pj|plist|prd|prds|prev|prior|rand|rank|ratios|raze|read0|read1|reciprocal|reverse|rload|rotate|rsave|rtrim|save|scan|select|set|setenv|show|signum|sin|sqrt|ss|ssr|string|sublist|sum|sums|sv|system|tables|tan|til|trim|txf|type|uj|ungroup|union|update|upper|upsert|value|var|view|views|vs|wavg|where|where|while|within|wj|wj1|wsum|xasc|xbar|xcol|xcols|xdesc|xexp|xgroup|xkey|xlog|xprev|xrank)$"), 
      E = /[|/&^!+:\\\-*%$=~#;@><,?_'"\[\(\]\)\s{}]/;
      return {startState:function() {
        return {tokenize:tokenBase, context:null, indent:0, col:0};
      }, token:function(stream, state) {
        stream.sol() && (state.context && null == state.context.align && (state.context.align = !1), state.indent = stream.indentation());
        var style = state.tokenize(stream, state);
        "comment" != style && state.context && null == state.context.align && "pattern" != state.context.type && (state.context.align = !0);
        if ("(" == curPunc) {
          pushContext(state, ")", stream.column());
        } else if ("[" == curPunc) {
          pushContext(state, "]", stream.column());
        } else if ("{" == curPunc) {
          pushContext(state, "}", stream.column());
        } else if (/[\]\}\)]/.test(curPunc)) {
          for (; state.context && "pattern" == state.context.type;) {
            popContext(state);
          }
          state.context && curPunc == state.context.type && popContext(state);
        } else {
          "." == curPunc && state.context && "pattern" == state.context.type ? popContext(state) : /atom|string|variable/.test(style) && state.context && (/[\}\]]/.test(state.context.type) ? pushContext(state, "pattern", stream.column()) : "pattern" != state.context.type || state.context.align || (state.context.align = !0, state.context.col = stream.column()));
        }
        return style;
      }, indent:function(state, textAfter) {
        textAfter = textAfter && textAfter.charAt(0);
        state = state.context;
        if (/[\]\}]/.test(textAfter)) {
          for (; state && "pattern" == state.type;) {
            state = state.prev;
          }
        }
        textAfter = state && textAfter == state.type;
        return state ? "pattern" == state.type ? state.col : state.align ? state.col + (textAfter ? 0 : 1) : state.indent + (textAfter ? 0 : indentUnit) : 0;
      }};
    });
    CodeMirror.defineMIME("text/x-q", "q");
  });
};

//# sourceMappingURL=module$node_modules$codemirror$mode$q$q.js.map
