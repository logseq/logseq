shadow$provide.module$node_modules$codemirror$mode$pascal$pascal = function(global, require, module, exports) {
  (function(mod) {
    "object" == typeof exports && "object" == typeof module ? mod(require("module$node_modules$codemirror$lib$codemirror")) : "function" == typeof define && define.amd ? define(["../../lib/codemirror"], mod) : mod(CodeMirror);
  })(function(CodeMirror) {
    CodeMirror.defineMode("pascal", function() {
      function tokenBase(stream, state) {
        var ch = stream.next();
        if ("#" == ch && state.startOfLine) {
          return stream.skipToEnd(), "meta";
        }
        if ('"' == ch || "'" == ch) {
          return state.tokenize = tokenString(ch), state.tokenize(stream, state);
        }
        if ("(" == ch && stream.eat("*")) {
          return state.tokenize = tokenComment, tokenComment(stream, state);
        }
        if ("{" == ch) {
          return state.tokenize = tokenCommentBraces, tokenCommentBraces(stream, state);
        }
        if (/[\[\]\(\),;:\.]/.test(ch)) {
          return null;
        }
        if (/\d/.test(ch)) {
          return stream.eatWhile(/[\w\.]/), "number";
        }
        if ("/" == ch && stream.eat("/")) {
          return stream.skipToEnd(), "comment";
        }
        if (isOperatorChar.test(ch)) {
          return stream.eatWhile(isOperatorChar), "operator";
        }
        stream.eatWhile(/[\w\$_]/);
        stream = stream.current();
        return keywords.propertyIsEnumerable(stream) ? "keyword" : atoms.propertyIsEnumerable(stream) ? "atom" : "variable";
      }
      function tokenString(quote) {
        return function(stream, state) {
          for (var escaped = !1, next, end = !1; null != (next = stream.next());) {
            if (next == quote && !escaped) {
              end = !0;
              break;
            }
            escaped = !escaped && "\\" == next;
          }
          if (end || !escaped) {
            state.tokenize = null;
          }
          return "string";
        };
      }
      function tokenComment(stream, state) {
        for (var maybeEnd = !1, ch; ch = stream.next();) {
          if (")" == ch && maybeEnd) {
            state.tokenize = null;
            break;
          }
          maybeEnd = "*" == ch;
        }
        return "comment";
      }
      function tokenCommentBraces(stream, state) {
        for (var ch; ch = stream.next();) {
          if ("}" == ch) {
            state.tokenize = null;
            break;
          }
        }
        return "comment";
      }
      var keywords = function(str) {
        var obj = {};
        str = str.split(" ");
        for (var i = 0; i < str.length; ++i) {
          obj[str[i]] = !0;
        }
        return obj;
      }("absolute and array asm begin case const constructor destructor div do downto else end file for function goto if implementation in inherited inline interface label mod nil not object of operator or packed procedure program record reintroduce repeat self set shl shr string then to type unit until uses var while with xor as class dispinterface except exports finalization finally initialization inline is library on out packed property raise resourcestring threadvar try absolute abstract alias assembler bitpacked break cdecl continue cppdecl cvar default deprecated dynamic enumerator experimental export external far far16 forward generic helper implements index interrupt iocheck local message name near nodefault noreturn nostackframe oldfpccall otherwise overload override pascal platform private protected public published read register reintroduce result safecall saveregisters softfloat specialize static stdcall stored strict unaligned unimplemented varargs virtual write"), 
      atoms = {"null":!0}, isOperatorChar = /[+\-*&%=<>!?|\/]/;
      return {startState:function() {
        return {tokenize:null};
      }, token:function(stream, state) {
        return stream.eatSpace() ? null : (state.tokenize || tokenBase)(stream, state);
      }, electricChars:"{}"};
    });
    CodeMirror.defineMIME("text/x-pascal", "pascal");
  });
};

//# sourceMappingURL=module$node_modules$codemirror$mode$pascal$pascal.js.map
