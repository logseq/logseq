shadow$provide.module$node_modules$$js_joda$core$dist$js_joda = function(global, require, module, exports) {
  (function(global, factory) {
    "object" === typeof exports && "undefined" !== typeof module ? factory(exports) : "function" === typeof define && define.amd ? define(["exports"], factory) : (global = global || self, factory(global.JSJoda = {}));
  })(this, function(exports) {
    function createErrorType(name, init, superErrorClass) {
      function JsJodaException(message) {
        Error.captureStackTrace ? Error.captureStackTrace(this, this.constructor) : this.stack = Error().stack;
        this.message = message;
        init && init.apply(this, arguments);
        this.toString = function() {
          return this.name + ": " + this.message;
        };
      }
      void 0 === superErrorClass && (superErrorClass = Error);
      JsJodaException.prototype = Object.create(superErrorClass.prototype);
      JsJodaException.prototype.name = name;
      return JsJodaException.prototype.constructor = JsJodaException;
    }
    function _inheritsLoose(subClass, superClass) {
      subClass.prototype = Object.create(superClass.prototype);
      subClass.prototype.constructor = subClass;
      subClass.__proto__ = superClass;
    }
    function _assertThisInitialized(self) {
      if (void 0 === self) {
        throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
      }
      return self;
    }
    function assert(assertion, msg, error) {
      if (!assertion) {
        if (error) {
          throw new error(msg);
        }
        throw Error(msg);
      }
    }
    function requireNonNull(value, parameterName) {
      if (null == value) {
        throw new NullPointerException(parameterName + " must not be null");
      }
      return value;
    }
    function requireInstance(value, _class, parameterName) {
      if (!(value instanceof _class)) {
        throw new IllegalArgumentException(parameterName + " must be an instance of " + (_class.name ? _class.name : _class) + (value && value.constructor && value.constructor.name ? ", but is " + value.constructor.name : ""));
      }
      return value;
    }
    function abstractMethodFail(methodName) {
      throw new TypeError('abstract method "' + methodName + '" is not implemented');
    }
    function createTemporalQuery(name, queryFromFunction) {
      var ExtendedTemporalQuery = function(_TemporalQuery) {
        function ExtendedTemporalQuery() {
          return _TemporalQuery.apply(this, arguments) || this;
        }
        _inheritsLoose(ExtendedTemporalQuery, _TemporalQuery);
        return ExtendedTemporalQuery;
      }(TemporalQuery);
      ExtendedTemporalQuery.prototype.queryFrom = queryFromFunction;
      return new ExtendedTemporalQuery(name);
    }
    function convert(temporal, zone) {
      return new ToNativeJsConverter(temporal, zone);
    }
    function nativeJs(date, zone) {
      return new NativeJsTemporal(date, zone);
    }
    var DateTimeException = createErrorType("DateTimeException", function(message, cause) {
      void 0 === cause && (cause = null);
      message = message || this.name;
      null !== cause && cause instanceof Error && (message += "\n-------\nCaused by: " + cause.stack + "\n-------\n");
      this.message = message;
    }), DateTimeParseException = createErrorType("DateTimeParseException", function(message, text, index, cause) {
      void 0 === text && (text = "");
      void 0 === index && (index = 0);
      void 0 === cause && (cause = null);
      message = message || this.name;
      message += ": " + text + ", at index: " + index;
      null !== cause && cause instanceof Error && (message += "\n-------\nCaused by: " + cause.stack + "\n-------\n");
      this.message = message;
      this.parsedString = function() {
        return text;
      };
      this.errorIndex = function() {
        return index;
      };
    }), UnsupportedTemporalTypeException = createErrorType("UnsupportedTemporalTypeException", null, DateTimeException), ArithmeticException = createErrorType("ArithmeticException"), IllegalArgumentException = createErrorType("IllegalArgumentException"), IllegalStateException = createErrorType("IllegalStateException"), NullPointerException = createErrorType("NullPointerException"), assert$1 = Object.freeze({__proto__:null, assert, requireNonNull, requireInstance, abstractMethodFail}), MathUtil = 
    function() {
      function MathUtil() {
      }
      MathUtil.intDiv = function(x, y) {
        x = MathUtil.roundDown(x / y);
        return MathUtil.safeZero(x);
      };
      MathUtil.intMod = function(x, y) {
        x -= MathUtil.intDiv(x, y) * y;
        x = MathUtil.roundDown(x);
        return MathUtil.safeZero(x);
      };
      MathUtil.roundDown = function(r) {
        return 0 > r ? Math.ceil(r) : Math.floor(r);
      };
      MathUtil.floorDiv = function(x, y) {
        return MathUtil.safeZero(Math.floor(x / y));
      };
      MathUtil.floorMod = function(x, y) {
        x -= MathUtil.floorDiv(x, y) * y;
        return MathUtil.safeZero(x);
      };
      MathUtil.safeAdd = function(x, y) {
        MathUtil.verifyInt(x);
        MathUtil.verifyInt(y);
        if (0 === x) {
          return MathUtil.safeZero(y);
        }
        if (0 === y) {
          return MathUtil.safeZero(x);
        }
        var r = MathUtil.safeToInt(x + y);
        if (r === x || r === y) {
          throw new ArithmeticException("Invalid addition beyond MAX_SAFE_INTEGER!");
        }
        return r;
      };
      MathUtil.safeSubtract = function(x, y) {
        MathUtil.verifyInt(x);
        MathUtil.verifyInt(y);
        return 0 === x && 0 === y ? 0 : 0 === x ? MathUtil.safeZero(-1 * y) : 0 === y ? MathUtil.safeZero(x) : MathUtil.safeToInt(x - y);
      };
      MathUtil.safeMultiply = function(x, y) {
        MathUtil.verifyInt(x);
        MathUtil.verifyInt(y);
        if (1 === x) {
          return MathUtil.safeZero(y);
        }
        if (1 === y) {
          return MathUtil.safeZero(x);
        }
        if (0 === x || 0 === y) {
          return 0;
        }
        var r = MathUtil.safeToInt(x * y);
        if (r / y !== x || -9007199254740991 === x && -1 === y || -9007199254740991 === y && -1 === x) {
          throw new ArithmeticException("Multiplication overflows: " + x + " * " + y);
        }
        return r;
      };
      MathUtil.parseInt = function(_parseInt) {
        function parseInt(_x) {
          return _parseInt.apply(this, arguments);
        }
        parseInt.toString = function() {
          return _parseInt.toString();
        };
        return parseInt;
      }(function(value) {
        value = parseInt(value);
        return MathUtil.safeToInt(value);
      });
      MathUtil.safeToInt = function(value) {
        MathUtil.verifyInt(value);
        return MathUtil.safeZero(value);
      };
      MathUtil.verifyInt = function(value) {
        if (null == value) {
          throw new ArithmeticException("Invalid value: '" + value + "', using null or undefined as argument");
        }
        if (isNaN(value)) {
          throw new ArithmeticException("Invalid int value, using NaN as argument");
        }
        if (0 !== value % 1) {
          throw new ArithmeticException("Invalid value: '" + value + "' is a float");
        }
        if (9007199254740991 < value || -9007199254740991 > value) {
          throw new ArithmeticException("Calculation overflows an int: " + value);
        }
      };
      MathUtil.safeZero = function(value) {
        return 0 === value ? 0 : +value;
      };
      MathUtil.compareNumbers = function(a, b) {
        return a < b ? -1 : a > b ? 1 : 0;
      };
      MathUtil.smi = function(int) {
        return int >>> 1 & 1073741824 | int & 3221225471;
      };
      MathUtil.hash = function(number) {
        if (number !== number || Infinity === number) {
          return 0;
        }
        for (var result = number; 4294967295 < number;) {
          number /= 4294967295, result ^= number;
        }
        return MathUtil.smi(result);
      };
      MathUtil.hashCode = function() {
        for (var result = 17, _len = arguments.length, numbers = Array(_len), _key = 0; _key < _len; _key++) {
          numbers[_key] = arguments[_key];
        }
        for (_len = 0; _len < numbers.length; _len++) {
          result = (result << 5) - result + MathUtil.hash(numbers[_len]);
        }
        return MathUtil.hash(result);
      };
      return MathUtil;
    }();
    MathUtil.MAX_SAFE_INTEGER = 9007199254740991;
    MathUtil.MIN_SAFE_INTEGER = -9007199254740991;
    var Enum = function() {
      function Enum(name) {
        this._name = name;
      }
      var _proto = Enum.prototype;
      _proto.equals = function(other) {
        return this === other;
      };
      _proto.toString = function() {
        return this._name;
      };
      _proto.toJSON = function() {
        return this.toString();
      };
      return Enum;
    }(), TemporalAmount = function() {
      function TemporalAmount() {
      }
      var _proto = TemporalAmount.prototype;
      _proto.get = function(unit) {
        abstractMethodFail("get");
      };
      _proto.units = function() {
        abstractMethodFail("units");
      };
      _proto.addTo = function(temporal) {
        abstractMethodFail("addTo");
      };
      _proto.subtractFrom = function(temporal) {
        abstractMethodFail("subtractFrom");
      };
      return TemporalAmount;
    }(), TemporalUnit = function() {
      function TemporalUnit() {
      }
      var _proto = TemporalUnit.prototype;
      _proto.duration = function() {
        abstractMethodFail("duration");
      };
      _proto.isDurationEstimated = function() {
        abstractMethodFail("isDurationEstimated");
      };
      _proto.isDateBased = function() {
        abstractMethodFail("isDateBased");
      };
      _proto.isTimeBased = function() {
        abstractMethodFail("isTimeBased");
      };
      _proto.isSupportedBy = function(temporal) {
        abstractMethodFail("isSupportedBy");
      };
      _proto.addTo = function(dateTime, periodToAdd) {
        abstractMethodFail("addTo");
      };
      _proto.between = function(temporal1, temporal2) {
        abstractMethodFail("between");
      };
      return TemporalUnit;
    }(), Duration = function(_TemporalAmount) {
      function Duration(seconds, nanos) {
        var _this = _TemporalAmount.call(this) || this;
        _this._seconds = MathUtil.safeToInt(seconds);
        _this._nanos = MathUtil.safeToInt(nanos);
        return _this;
      }
      _inheritsLoose(Duration, _TemporalAmount);
      Duration.ofDays = function(days) {
        return Duration._create(MathUtil.safeMultiply(days, LocalTime.SECONDS_PER_DAY), 0);
      };
      Duration.ofHours = function(hours) {
        return Duration._create(MathUtil.safeMultiply(hours, LocalTime.SECONDS_PER_HOUR), 0);
      };
      Duration.ofMinutes = function(minutes) {
        return Duration._create(MathUtil.safeMultiply(minutes, LocalTime.SECONDS_PER_MINUTE), 0);
      };
      Duration.ofSeconds = function(seconds, nanoAdjustment) {
        void 0 === nanoAdjustment && (nanoAdjustment = 0);
        seconds = MathUtil.safeAdd(seconds, MathUtil.floorDiv(nanoAdjustment, LocalTime.NANOS_PER_SECOND));
        nanoAdjustment = MathUtil.floorMod(nanoAdjustment, LocalTime.NANOS_PER_SECOND);
        return Duration._create(seconds, nanoAdjustment);
      };
      Duration.ofMillis = function(millis) {
        var secs = MathUtil.intDiv(millis, 1000);
        millis = MathUtil.intMod(millis, 1000);
        0 > millis && (millis += 1000, secs--);
        return Duration._create(secs, 1000000 * millis);
      };
      Duration.ofNanos = function(nanos) {
        var secs = MathUtil.intDiv(nanos, LocalTime.NANOS_PER_SECOND);
        nanos = MathUtil.intMod(nanos, LocalTime.NANOS_PER_SECOND);
        0 > nanos && (nanos += LocalTime.NANOS_PER_SECOND, secs--);
        return this._create(secs, nanos);
      };
      Duration.of = function(amount, unit) {
        return Duration.ZERO.plus(amount, unit);
      };
      Duration.from = function(amount) {
        requireNonNull(amount, "amount");
        requireInstance(amount, TemporalAmount);
        var duration = Duration.ZERO;
        amount.units().forEach(function(unit) {
          duration = duration.plus(amount.get(unit), unit);
        });
        return duration;
      };
      Duration.between = function(startInclusive, endExclusive) {
        requireNonNull(startInclusive, "startInclusive");
        requireNonNull(endExclusive, "endExclusive");
        var secs = startInclusive.until(endExclusive, ChronoUnit.SECONDS), nanos = 0;
        if (startInclusive.isSupported(ChronoField.NANO_OF_SECOND) && endExclusive.isSupported(ChronoField.NANO_OF_SECOND)) {
          try {
            var startNos = startInclusive.getLong(ChronoField.NANO_OF_SECOND);
            nanos = endExclusive.getLong(ChronoField.NANO_OF_SECOND) - startNos;
            if (0 < secs && 0 > nanos) {
              nanos += LocalTime.NANOS_PER_SECOND;
            } else if (0 > secs && 0 < nanos) {
              nanos -= LocalTime.NANOS_PER_SECOND;
            } else if (0 === secs && 0 !== nanos) {
              var adjustedEnd = endExclusive.with(ChronoField.NANO_OF_SECOND, startNos);
              secs = startInclusive.until(adjustedEnd, ChronoUnit.SECONDS);
            }
          } catch (e) {
          }
        }
        return this.ofSeconds(secs, nanos);
      };
      Duration.parse = function(text) {
        requireNonNull(text, "text");
        var matches = RegExp("([-+]?)P(?:([-+]?[0-9]+)D)?(T(?:([-+]?[0-9]+)H)?(?:([-+]?[0-9]+)M)?(?:([-+]?[0-9]+)(?:[.,]([0-9]{0,9}))?S)?)?", "i").exec(text);
        if (null !== matches && "T" === matches[3] === !1) {
          var negate = "-" === matches[1], dayMatch = matches[2], hourMatch = matches[4], minuteMatch = matches[5], secondMatch = matches[6];
          matches = matches[7];
          if (null != dayMatch || null != hourMatch || null != minuteMatch || null != secondMatch) {
            dayMatch = Duration._parseNumber(text, dayMatch, LocalTime.SECONDS_PER_DAY, "days");
            hourMatch = Duration._parseNumber(text, hourMatch, LocalTime.SECONDS_PER_HOUR, "hours");
            minuteMatch = Duration._parseNumber(text, minuteMatch, LocalTime.SECONDS_PER_MINUTE, "minutes");
            var seconds = Duration._parseNumber(text, secondMatch, 1, "seconds");
            secondMatch = null != secondMatch && "-" === secondMatch.charAt(0);
            secondMatch = Duration._parseFraction(text, matches, secondMatch ? -1 : 1);
            try {
              return Duration._create(negate, dayMatch, hourMatch, minuteMatch, seconds, secondMatch);
            } catch (ex) {
              throw new DateTimeParseException("Text cannot be parsed to a Duration: overflow", text, 0, ex);
            }
          }
        }
        throw new DateTimeParseException("Text cannot be parsed to a Duration", text, 0);
      };
      Duration._parseNumber = function(text, parsed, multiplier, errorText) {
        if (null == parsed) {
          return 0;
        }
        try {
          return "+" === parsed[0] && (parsed = parsed.substring(1)), MathUtil.safeMultiply(parseFloat(parsed), multiplier);
        } catch (ex) {
          throw new DateTimeParseException("Text cannot be parsed to a Duration: " + errorText, text, 0, ex);
        }
      };
      Duration._parseFraction = function(text, parsed, negate) {
        if (null == parsed || 0 === parsed.length) {
          return 0;
        }
        parsed = (parsed + "000000000").substring(0, 9);
        return parseFloat(parsed) * negate;
      };
      Duration._create = function() {
        return 2 >= arguments.length ? Duration._createSecondsNanos(arguments[0], arguments[1]) : Duration._createNegateDaysHoursMinutesSecondsNanos(arguments[0], arguments[1], arguments[2], arguments[3], arguments[4], arguments[5]);
      };
      Duration._createNegateDaysHoursMinutesSecondsNanos = function(negate, daysAsSecs, hoursAsSecs, minsAsSecs, secs, nanos) {
        daysAsSecs = MathUtil.safeAdd(daysAsSecs, MathUtil.safeAdd(hoursAsSecs, MathUtil.safeAdd(minsAsSecs, secs)));
        return negate ? Duration.ofSeconds(daysAsSecs, nanos).negated() : Duration.ofSeconds(daysAsSecs, nanos);
      };
      Duration._createSecondsNanos = function(seconds, nanoAdjustment) {
        void 0 === seconds && (seconds = 0);
        void 0 === nanoAdjustment && (nanoAdjustment = 0);
        return 0 === seconds && 0 === nanoAdjustment ? Duration.ZERO : new Duration(seconds, nanoAdjustment);
      };
      var _proto = Duration.prototype;
      _proto.get = function(unit) {
        if (unit === ChronoUnit.SECONDS) {
          return this._seconds;
        }
        if (unit === ChronoUnit.NANOS) {
          return this._nanos;
        }
        throw new UnsupportedTemporalTypeException("Unsupported unit: " + unit);
      };
      _proto.units = function() {
        return [ChronoUnit.SECONDS, ChronoUnit.NANOS];
      };
      _proto.isZero = function() {
        return 0 === this._seconds && 0 === this._nanos;
      };
      _proto.isNegative = function() {
        return 0 > this._seconds;
      };
      _proto.seconds = function() {
        return this._seconds;
      };
      _proto.nano = function() {
        return this._nanos;
      };
      _proto.withSeconds = function(seconds) {
        return Duration._create(seconds, this._nanos);
      };
      _proto.withNanos = function(nanoOfSecond) {
        ChronoField.NANO_OF_SECOND.checkValidIntValue(nanoOfSecond);
        return Duration._create(this._seconds, nanoOfSecond);
      };
      _proto.plusDuration = function(duration) {
        requireNonNull(duration, "duration");
        return this.plus(duration.seconds(), duration.nano());
      };
      _proto.plus = function(durationOrNumber, unitOrNumber) {
        return 1 === arguments.length ? this.plusDuration(durationOrNumber) : 2 === arguments.length && unitOrNumber instanceof TemporalUnit ? this.plusAmountUnit(durationOrNumber, unitOrNumber) : this.plusSecondsNanos(durationOrNumber, unitOrNumber);
      };
      _proto.plusAmountUnit = function(amountToAdd, unit) {
        requireNonNull(amountToAdd, "amountToAdd");
        requireNonNull(unit, "unit");
        if (unit === ChronoUnit.DAYS) {
          return this.plusSecondsNanos(MathUtil.safeMultiply(amountToAdd, LocalTime.SECONDS_PER_DAY), 0);
        }
        if (unit.isDurationEstimated()) {
          throw new UnsupportedTemporalTypeException("Unit must not have an estimated duration");
        }
        if (0 === amountToAdd) {
          return this;
        }
        if (unit instanceof ChronoUnit) {
          switch(unit) {
            case ChronoUnit.NANOS:
              return this.plusNanos(amountToAdd);
            case ChronoUnit.MICROS:
              return this.plusSecondsNanos(1000 * MathUtil.intDiv(amountToAdd, 1E9), 1000 * MathUtil.intMod(amountToAdd, 1E9));
            case ChronoUnit.MILLIS:
              return this.plusMillis(amountToAdd);
            case ChronoUnit.SECONDS:
              return this.plusSeconds(amountToAdd);
          }
          return this.plusSecondsNanos(MathUtil.safeMultiply(unit.duration().seconds(), amountToAdd), 0);
        }
        amountToAdd = unit.duration().multipliedBy(amountToAdd);
        return this.plusSecondsNanos(amountToAdd.seconds(), amountToAdd.nano());
      };
      _proto.plusDays = function(daysToAdd) {
        return this.plusSecondsNanos(MathUtil.safeMultiply(daysToAdd, LocalTime.SECONDS_PER_DAY), 0);
      };
      _proto.plusHours = function(hoursToAdd) {
        return this.plusSecondsNanos(MathUtil.safeMultiply(hoursToAdd, LocalTime.SECONDS_PER_HOUR), 0);
      };
      _proto.plusMinutes = function(minutesToAdd) {
        return this.plusSecondsNanos(MathUtil.safeMultiply(minutesToAdd, LocalTime.SECONDS_PER_MINUTE), 0);
      };
      _proto.plusSeconds = function(secondsToAdd) {
        return this.plusSecondsNanos(secondsToAdd, 0);
      };
      _proto.plusMillis = function(millisToAdd) {
        return this.plusSecondsNanos(MathUtil.intDiv(millisToAdd, 1000), 1000000 * MathUtil.intMod(millisToAdd, 1000));
      };
      _proto.plusNanos = function(nanosToAdd) {
        return this.plusSecondsNanos(0, nanosToAdd);
      };
      _proto.plusSecondsNanos = function(secondsToAdd, nanosToAdd) {
        requireNonNull(secondsToAdd, "secondsToAdd");
        requireNonNull(nanosToAdd, "nanosToAdd");
        if (0 === secondsToAdd && 0 === nanosToAdd) {
          return this;
        }
        secondsToAdd = MathUtil.safeAdd(this._seconds, secondsToAdd);
        secondsToAdd = MathUtil.safeAdd(secondsToAdd, MathUtil.intDiv(nanosToAdd, LocalTime.NANOS_PER_SECOND));
        nanosToAdd = MathUtil.intMod(nanosToAdd, LocalTime.NANOS_PER_SECOND);
        nanosToAdd = MathUtil.safeAdd(this._nanos, nanosToAdd);
        return Duration.ofSeconds(secondsToAdd, nanosToAdd);
      };
      _proto.minus = function(durationOrNumber, unit) {
        return 1 === arguments.length ? this.minusDuration(durationOrNumber) : this.minusAmountUnit(durationOrNumber, unit);
      };
      _proto.minusDuration = function(duration) {
        requireNonNull(duration, "duration");
        var secsToSubtract = duration.seconds();
        duration = duration.nano();
        return -9007199254740991 === secsToSubtract ? this.plus(9007199254740991, -duration) : this.plus(-secsToSubtract, -duration);
      };
      _proto.minusAmountUnit = function(amountToSubtract, unit) {
        requireNonNull(amountToSubtract, "amountToSubtract");
        requireNonNull(unit, "unit");
        return -9007199254740991 === amountToSubtract ? this.plusAmountUnit(9007199254740991, unit) : this.plusAmountUnit(-amountToSubtract, unit);
      };
      _proto.minusDays = function(daysToSubtract) {
        return -9007199254740991 === daysToSubtract ? this.plusDays(9007199254740991) : this.plusDays(-daysToSubtract);
      };
      _proto.minusHours = function(hoursToSubtract) {
        return -9007199254740991 === hoursToSubtract ? this.plusHours(9007199254740991) : this.plusHours(-hoursToSubtract);
      };
      _proto.minusMinutes = function(minutesToSubtract) {
        return -9007199254740991 === minutesToSubtract ? this.plusMinutes(9007199254740991) : this.plusMinutes(-minutesToSubtract);
      };
      _proto.minusSeconds = function(secondsToSubtract) {
        return -9007199254740991 === secondsToSubtract ? this.plusSeconds(9007199254740991) : this.plusSeconds(-secondsToSubtract);
      };
      _proto.minusMillis = function(millisToSubtract) {
        return -9007199254740991 === millisToSubtract ? this.plusMillis(9007199254740991) : this.plusMillis(-millisToSubtract);
      };
      _proto.minusNanos = function(nanosToSubtract) {
        return -9007199254740991 === nanosToSubtract ? this.plusNanos(9007199254740991) : this.plusNanos(-nanosToSubtract);
      };
      _proto.multipliedBy = function(multiplicand) {
        if (0 === multiplicand) {
          return Duration.ZERO;
        }
        if (1 === multiplicand) {
          return this;
        }
        var secs = MathUtil.safeMultiply(this._seconds, multiplicand);
        multiplicand = MathUtil.safeMultiply(this._nanos, multiplicand);
        secs += MathUtil.intDiv(multiplicand, LocalTime.NANOS_PER_SECOND);
        multiplicand = MathUtil.intMod(multiplicand, LocalTime.NANOS_PER_SECOND);
        return Duration.ofSeconds(secs, multiplicand);
      };
      _proto.dividedBy = function(divisor) {
        if (0 === divisor) {
          throw new ArithmeticException("Cannot divide by zero");
        }
        if (1 === divisor) {
          return this;
        }
        var secs = MathUtil.intDiv(this._seconds, divisor), secsMod = MathUtil.roundDown((this._seconds / divisor - secs) * LocalTime.NANOS_PER_SECOND);
        divisor = MathUtil.intDiv(this._nanos, divisor);
        return Duration.ofSeconds(secs, secsMod + divisor);
      };
      _proto.negated = function() {
        return this.multipliedBy(-1);
      };
      _proto.abs = function() {
        return this.isNegative() ? this.negated() : this;
      };
      _proto.addTo = function(temporal) {
        requireNonNull(temporal, "temporal");
        0 !== this._seconds && (temporal = temporal.plus(this._seconds, ChronoUnit.SECONDS));
        0 !== this._nanos && (temporal = temporal.plus(this._nanos, ChronoUnit.NANOS));
        return temporal;
      };
      _proto.subtractFrom = function(temporal) {
        requireNonNull(temporal, "temporal");
        0 !== this._seconds && (temporal = temporal.minus(this._seconds, ChronoUnit.SECONDS));
        0 !== this._nanos && (temporal = temporal.minus(this._nanos, ChronoUnit.NANOS));
        return temporal;
      };
      _proto.toDays = function() {
        return MathUtil.intDiv(this._seconds, LocalTime.SECONDS_PER_DAY);
      };
      _proto.toHours = function() {
        return MathUtil.intDiv(this._seconds, LocalTime.SECONDS_PER_HOUR);
      };
      _proto.toMinutes = function() {
        return MathUtil.intDiv(this._seconds, LocalTime.SECONDS_PER_MINUTE);
      };
      _proto.toMillis = function() {
        var millis = Math.round(MathUtil.safeMultiply(this._seconds, 1000));
        return millis = MathUtil.safeAdd(millis, MathUtil.intDiv(this._nanos, 1000000));
      };
      _proto.toNanos = function() {
        var totalNanos = MathUtil.safeMultiply(this._seconds, LocalTime.NANOS_PER_SECOND);
        return totalNanos = MathUtil.safeAdd(totalNanos, this._nanos);
      };
      _proto.compareTo = function(otherDuration) {
        requireNonNull(otherDuration, "otherDuration");
        requireInstance(otherDuration, Duration, "otherDuration");
        var cmp = MathUtil.compareNumbers(this._seconds, otherDuration.seconds());
        return 0 !== cmp ? cmp : this._nanos - otherDuration.nano();
      };
      _proto.equals = function(otherDuration) {
        return this === otherDuration ? !0 : otherDuration instanceof Duration ? this.seconds() === otherDuration.seconds() && this.nano() === otherDuration.nano() : !1;
      };
      _proto.toString = function() {
        if (this === Duration.ZERO) {
          return "PT0S";
        }
        var hours = MathUtil.intDiv(this._seconds, LocalTime.SECONDS_PER_HOUR), minutes = MathUtil.intDiv(MathUtil.intMod(this._seconds, LocalTime.SECONDS_PER_HOUR), LocalTime.SECONDS_PER_MINUTE), secs = MathUtil.intMod(this._seconds, LocalTime.SECONDS_PER_MINUTE), rval = "PT";
        0 !== hours && (rval += hours + "H");
        0 !== minutes && (rval += minutes + "M");
        if (0 === secs && 0 === this._nanos && 2 < rval.length) {
          return rval;
        }
        rval = 0 > secs && 0 < this._nanos ? -1 === secs ? rval + "-0" : rval + (secs + 1) : rval + secs;
        if (0 < this._nanos) {
          for (rval += ".", hours = 0 > secs ? "" + (2 * LocalTime.NANOS_PER_SECOND - this._nanos) : "" + (LocalTime.NANOS_PER_SECOND + this._nanos), hours = hours.slice(1, hours.length), rval += hours; "0" === rval.charAt(rval.length - 1);) {
            rval = rval.slice(0, rval.length - 1);
          }
        }
        return rval + "S";
      };
      _proto.toJSON = function() {
        return this.toString();
      };
      return Duration;
    }(TemporalAmount), YearConstants = function() {
    }, ChronoUnit = function(_TemporalUnit) {
      function ChronoUnit(name, estimatedDuration) {
        var _this = _TemporalUnit.call(this) || this;
        _this._name = name;
        _this._duration = estimatedDuration;
        return _this;
      }
      _inheritsLoose(ChronoUnit, _TemporalUnit);
      var _proto = ChronoUnit.prototype;
      _proto.duration = function() {
        return this._duration;
      };
      _proto.isDurationEstimated = function() {
        return this.isDateBased() || this === ChronoUnit.FOREVER;
      };
      _proto.isDateBased = function() {
        return 0 <= this.compareTo(ChronoUnit.DAYS) && this !== ChronoUnit.FOREVER;
      };
      _proto.isTimeBased = function() {
        return 0 > this.compareTo(ChronoUnit.DAYS);
      };
      _proto.isSupportedBy = function(temporal) {
        if (this === ChronoUnit.FOREVER) {
          return !1;
        }
        try {
          return temporal.plus(1, this), !0;
        } catch (e) {
          try {
            return temporal.plus(-1, this), !0;
          } catch (e2) {
            return !1;
          }
        }
      };
      _proto.addTo = function(temporal, amount) {
        return temporal.plus(amount, this);
      };
      _proto.between = function(temporal1, temporal2) {
        return temporal1.until(temporal2, this);
      };
      _proto.toString = function() {
        return this._name;
      };
      _proto.compareTo = function(other) {
        return this.duration().compareTo(other.duration());
      };
      return ChronoUnit;
    }(TemporalUnit), TemporalField = function() {
      function TemporalField() {
      }
      var _proto = TemporalField.prototype;
      _proto.isDateBased = function() {
        abstractMethodFail("isDateBased");
      };
      _proto.isTimeBased = function() {
        abstractMethodFail("isTimeBased");
      };
      _proto.baseUnit = function() {
        abstractMethodFail("baseUnit");
      };
      _proto.rangeUnit = function() {
        abstractMethodFail("rangeUnit");
      };
      _proto.range = function() {
        abstractMethodFail("range");
      };
      _proto.rangeRefinedBy = function(temporal) {
        abstractMethodFail("rangeRefinedBy");
      };
      _proto.getFrom = function(temporal) {
        abstractMethodFail("getFrom");
      };
      _proto.adjustInto = function(temporal, newValue) {
        abstractMethodFail("adjustInto");
      };
      _proto.isSupportedBy = function(temporal) {
        abstractMethodFail("isSupportedBy");
      };
      _proto.displayName = function() {
        abstractMethodFail("displayName");
      };
      _proto.equals = function(other) {
        abstractMethodFail("equals");
      };
      _proto.name = function() {
        abstractMethodFail("name");
      };
      return TemporalField;
    }(), ValueRange = function() {
      function ValueRange(minSmallest, minLargest, maxSmallest, maxLargest) {
        assert(!(minSmallest > minLargest), "Smallest minimum value '" + minSmallest + "' must be less than largest minimum value '" + minLargest + "'", IllegalArgumentException);
        assert(!(maxSmallest > maxLargest), "Smallest maximum value '" + maxSmallest + "' must be less than largest maximum value '" + maxLargest + "'", IllegalArgumentException);
        assert(!(minLargest > maxLargest), "Minimum value '" + minLargest + "' must be less than maximum value '" + maxLargest + "'", IllegalArgumentException);
        this._minSmallest = minSmallest;
        this._minLargest = minLargest;
        this._maxLargest = maxLargest;
        this._maxSmallest = maxSmallest;
      }
      var _proto = ValueRange.prototype;
      _proto.isFixed = function() {
        return this._minSmallest === this._minLargest && this._maxSmallest === this._maxLargest;
      };
      _proto.minimum = function() {
        return this._minSmallest;
      };
      _proto.largestMinimum = function() {
        return this._minLargest;
      };
      _proto.maximum = function() {
        return this._maxLargest;
      };
      _proto.smallestMaximum = function() {
        return this._maxSmallest;
      };
      _proto.isValidValue = function(value) {
        return this.minimum() <= value && value <= this.maximum();
      };
      _proto.checkValidValue = function(value, field) {
        return this.isValidValue(value) ? value : (value = null != field ? "Invalid value for " + field + " (valid values " + this.toString() + "): " + value : "Invalid value (valid values " + this.toString() + "): " + value, assert(!1, value, DateTimeException));
      };
      _proto.checkValidIntValue = function(value, field) {
        if (!1 === this.isValidIntValue(value)) {
          throw new DateTimeException("Invalid int value for " + field + ": " + value);
        }
        return value;
      };
      _proto.isValidIntValue = function(value) {
        return this.isIntValue() && this.isValidValue(value);
      };
      _proto.isIntValue = function() {
        return this.minimum() >= MathUtil.MIN_SAFE_INTEGER && this.maximum() <= MathUtil.MAX_SAFE_INTEGER;
      };
      _proto.equals = function(other) {
        return other === this ? !0 : other instanceof ValueRange ? this._minSmallest === other._minSmallest && this._minLargest === other._minLargest && this._maxSmallest === other._maxSmallest && this._maxLargest === other._maxLargest : !1;
      };
      _proto.hashCode = function() {
        return MathUtil.hashCode(this._minSmallest, this._minLargest, this._maxSmallest, this._maxLargest);
      };
      _proto.toString = function() {
        var str = this.minimum() + (this.minimum() !== this.largestMinimum() ? "/" + this.largestMinimum() : "");
        return str = str + " - " + (this.smallestMaximum() + (this.smallestMaximum() !== this.maximum() ? "/" + this.maximum() : ""));
      };
      ValueRange.of = function() {
        return 2 === arguments.length ? new ValueRange(arguments[0], arguments[0], arguments[1], arguments[1]) : 3 === arguments.length ? new ValueRange(arguments[0], arguments[0], arguments[1], arguments[2]) : 4 === arguments.length ? new ValueRange(arguments[0], arguments[1], arguments[2], arguments[3]) : assert(!1, "Invalid number of arguments " + arguments.length, IllegalArgumentException);
      };
      return ValueRange;
    }(), ChronoField = function(_TemporalField) {
      function ChronoField(name, baseUnit, rangeUnit, range) {
        var _this = _TemporalField.call(this) || this;
        _this._name = name;
        _this._baseUnit = baseUnit;
        _this._rangeUnit = rangeUnit;
        _this._range = range;
        return _this;
      }
      _inheritsLoose(ChronoField, _TemporalField);
      ChronoField.byName = function(fieldName) {
        for (var prop in ChronoField) {
          if (ChronoField[prop] && ChronoField[prop] instanceof ChronoField && ChronoField[prop].name() === fieldName) {
            return ChronoField[prop];
          }
        }
      };
      var _proto = ChronoField.prototype;
      _proto.name = function() {
        return this._name;
      };
      _proto.baseUnit = function() {
        return this._baseUnit;
      };
      _proto.rangeUnit = function() {
        return this._rangeUnit;
      };
      _proto.range = function() {
        return this._range;
      };
      _proto.displayName = function() {
        return this.toString();
      };
      _proto.checkValidValue = function(value) {
        return this.range().checkValidValue(value, this);
      };
      _proto.checkValidIntValue = function(value) {
        return this.range().checkValidIntValue(value, this);
      };
      _proto.isDateBased = function() {
        return this === ChronoField.DAY_OF_WEEK || this === ChronoField.ALIGNED_DAY_OF_WEEK_IN_MONTH || this === ChronoField.ALIGNED_DAY_OF_WEEK_IN_YEAR || this === ChronoField.DAY_OF_MONTH || this === ChronoField.DAY_OF_YEAR || this === ChronoField.EPOCH_DAY || this === ChronoField.ALIGNED_WEEK_OF_MONTH || this === ChronoField.ALIGNED_WEEK_OF_YEAR || this === ChronoField.MONTH_OF_YEAR || this === ChronoField.YEAR_OF_ERA || this === ChronoField.YEAR || this === ChronoField.ERA;
      };
      _proto.isTimeBased = function() {
        return this === ChronoField.NANO_OF_SECOND || this === ChronoField.NANO_OF_DAY || this === ChronoField.MICRO_OF_SECOND || this === ChronoField.MICRO_OF_DAY || this === ChronoField.MILLI_OF_SECOND || this === ChronoField.MILLI_OF_DAY || this === ChronoField.SECOND_OF_MINUTE || this === ChronoField.SECOND_OF_DAY || this === ChronoField.MINUTE_OF_HOUR || this === ChronoField.MINUTE_OF_DAY || this === ChronoField.HOUR_OF_AMPM || this === ChronoField.CLOCK_HOUR_OF_AMPM || this === ChronoField.HOUR_OF_DAY || 
        this === ChronoField.CLOCK_HOUR_OF_DAY || this === ChronoField.AMPM_OF_DAY;
      };
      _proto.rangeRefinedBy = function(temporal) {
        return temporal.range(this);
      };
      _proto.getFrom = function(temporal) {
        return temporal.getLong(this);
      };
      _proto.toString = function() {
        return this.name();
      };
      _proto.equals = function(other) {
        return this === other;
      };
      _proto.adjustInto = function(temporal, newValue) {
        return temporal.with(this, newValue);
      };
      _proto.isSupportedBy = function(temporal) {
        return temporal.isSupported(this);
      };
      return ChronoField;
    }(TemporalField), TemporalQueries = function() {
      function TemporalQueries() {
      }
      TemporalQueries.zoneId = function() {
        return TemporalQueries.ZONE_ID;
      };
      TemporalQueries.chronology = function() {
        return TemporalQueries.CHRONO;
      };
      TemporalQueries.precision = function() {
        return TemporalQueries.PRECISION;
      };
      TemporalQueries.zone = function() {
        return TemporalQueries.ZONE;
      };
      TemporalQueries.offset = function() {
        return TemporalQueries.OFFSET;
      };
      TemporalQueries.localDate = function() {
        return TemporalQueries.LOCAL_DATE;
      };
      TemporalQueries.localTime = function() {
        return TemporalQueries.LOCAL_TIME;
      };
      return TemporalQueries;
    }(), TemporalAccessor = function() {
      function TemporalAccessor() {
      }
      var _proto = TemporalAccessor.prototype;
      _proto.query = function(_query) {
        return _query === TemporalQueries.zoneId() || _query === TemporalQueries.chronology() || _query === TemporalQueries.precision() ? null : _query.queryFrom(this);
      };
      _proto.get = function(field) {
        return this.range(field).checkValidIntValue(this.getLong(field), field);
      };
      _proto.range = function(field) {
        if (field instanceof ChronoField) {
          if (this.isSupported(field)) {
            return field.range();
          }
          throw new UnsupportedTemporalTypeException("Unsupported field: " + field);
        }
        return field.rangeRefinedBy(this);
      };
      return TemporalAccessor;
    }(), TemporalQuery = function(_Enum) {
      function TemporalQuery() {
        return _Enum.apply(this, arguments) || this;
      }
      _inheritsLoose(TemporalQuery, _Enum);
      TemporalQuery.prototype.queryFrom = function(temporal) {
        abstractMethodFail("queryFrom");
      };
      return TemporalQuery;
    }(Enum), DayOfWeek = function(_TemporalAccessor) {
      function DayOfWeek(ordinal, name) {
        var _this = _TemporalAccessor.call(this) || this;
        _this._ordinal = ordinal;
        _this._name = name;
        return _this;
      }
      _inheritsLoose(DayOfWeek, _TemporalAccessor);
      var _proto = DayOfWeek.prototype;
      _proto.ordinal = function() {
        return this._ordinal;
      };
      _proto.name = function() {
        return this._name;
      };
      DayOfWeek.values = function() {
        return ENUMS.slice();
      };
      DayOfWeek.valueOf = function(name) {
        var ordinal = 0;
        for (ordinal; ordinal < ENUMS.length && ENUMS[ordinal].name() !== name; ordinal++) {
        }
        return DayOfWeek.of(ordinal + 1);
      };
      DayOfWeek.of = function(dayOfWeek) {
        if (1 > dayOfWeek || 7 < dayOfWeek) {
          throw new DateTimeException("Invalid value for DayOfWeek: " + dayOfWeek);
        }
        return ENUMS[dayOfWeek - 1];
      };
      DayOfWeek.from = function(temporal) {
        assert(null != temporal, "temporal", NullPointerException);
        if (temporal instanceof DayOfWeek) {
          return temporal;
        }
        try {
          return DayOfWeek.of(temporal.get(ChronoField.DAY_OF_WEEK));
        } catch (ex) {
          if (ex instanceof DateTimeException) {
            throw new DateTimeException("Unable to obtain DayOfWeek from TemporalAccessor: " + temporal + ", type " + (null != temporal.constructor ? temporal.constructor.name : ""), ex);
          }
          throw ex;
        }
      };
      _proto.value = function() {
        return this._ordinal + 1;
      };
      _proto.displayName = function(style, locale) {
        throw new IllegalArgumentException("Pattern using (localized) text not implemented yet!");
      };
      _proto.isSupported = function(field) {
        return field instanceof ChronoField ? field === ChronoField.DAY_OF_WEEK : null != field && field.isSupportedBy(this);
      };
      _proto.range = function(field) {
        if (field === ChronoField.DAY_OF_WEEK) {
          return field.range();
        }
        if (field instanceof ChronoField) {
          throw new UnsupportedTemporalTypeException("Unsupported field: " + field);
        }
        return field.rangeRefinedBy(this);
      };
      _proto.get = function(field) {
        return field === ChronoField.DAY_OF_WEEK ? this.value() : this.range(field).checkValidIntValue(this.getLong(field), field);
      };
      _proto.getLong = function(field) {
        if (field === ChronoField.DAY_OF_WEEK) {
          return this.value();
        }
        if (field instanceof ChronoField) {
          throw new UnsupportedTemporalTypeException("Unsupported field: " + field);
        }
        return field.getFrom(this);
      };
      _proto.plus = function(days) {
        days = MathUtil.floorMod(days, 7);
        return ENUMS[MathUtil.floorMod(this._ordinal + (days + 7), 7)];
      };
      _proto.minus = function(days) {
        return this.plus(-1 * MathUtil.floorMod(days, 7));
      };
      _proto.query = function(_query) {
        if (_query === TemporalQueries.precision()) {
          return ChronoUnit.DAYS;
        }
        if (_query === TemporalQueries.localDate() || _query === TemporalQueries.localTime() || _query === TemporalQueries.chronology() || _query === TemporalQueries.zone() || _query === TemporalQueries.zoneId() || _query === TemporalQueries.offset()) {
          return null;
        }
        assert(null != _query, "query", NullPointerException);
        return _query.queryFrom(this);
      };
      _proto.adjustInto = function(temporal) {
        requireNonNull(temporal, "temporal");
        return temporal.with(ChronoField.DAY_OF_WEEK, this.value());
      };
      _proto.equals = function(other) {
        return this === other;
      };
      _proto.toString = function() {
        return this._name;
      };
      _proto.compareTo = function(other) {
        requireNonNull(other, "other");
        requireInstance(other, DayOfWeek, "other");
        return this._ordinal - other._ordinal;
      };
      _proto.toJSON = function() {
        return this.toString();
      };
      return DayOfWeek;
    }(TemporalAccessor), ENUMS, Month = function(_TemporalAccessor) {
      function Month(value, name) {
        var _this = _TemporalAccessor.call(this) || this;
        _this._value = MathUtil.safeToInt(value);
        _this._name = name;
        return _this;
      }
      _inheritsLoose(Month, _TemporalAccessor);
      var _proto = Month.prototype;
      _proto.value = function() {
        return this._value;
      };
      _proto.ordinal = function() {
        return this._value - 1;
      };
      _proto.name = function() {
        return this._name;
      };
      _proto.displayName = function(style, locale) {
        throw new IllegalArgumentException("Pattern using (localized) text not implemented yet!");
      };
      _proto.isSupported = function(field) {
        return null === field ? !1 : field instanceof ChronoField ? field === ChronoField.MONTH_OF_YEAR : null != field && field.isSupportedBy(this);
      };
      _proto.get = function(field) {
        return field === ChronoField.MONTH_OF_YEAR ? this.value() : this.range(field).checkValidIntValue(this.getLong(field), field);
      };
      _proto.getLong = function(field) {
        if (field === ChronoField.MONTH_OF_YEAR) {
          return this.value();
        }
        if (field instanceof ChronoField) {
          throw new UnsupportedTemporalTypeException("Unsupported field: " + field);
        }
        return field.getFrom(this);
      };
      _proto.plus = function(months) {
        months = MathUtil.intMod(months, 12) + 12;
        months = MathUtil.intMod(this.value() + months, 12);
        return Month.of(0 === months ? 12 : months);
      };
      _proto.minus = function(months) {
        return this.plus(-1 * MathUtil.intMod(months, 12));
      };
      _proto.length = function(leapYear) {
        switch(this) {
          case Month.FEBRUARY:
            return leapYear ? 29 : 28;
          case Month.APRIL:
          case Month.JUNE:
          case Month.SEPTEMBER:
          case Month.NOVEMBER:
            return 30;
          default:
            return 31;
        }
      };
      _proto.minLength = function() {
        switch(this) {
          case Month.FEBRUARY:
            return 28;
          case Month.APRIL:
          case Month.JUNE:
          case Month.SEPTEMBER:
          case Month.NOVEMBER:
            return 30;
          default:
            return 31;
        }
      };
      _proto.maxLength = function() {
        switch(this) {
          case Month.FEBRUARY:
            return 29;
          case Month.APRIL:
          case Month.JUNE:
          case Month.SEPTEMBER:
          case Month.NOVEMBER:
            return 30;
          default:
            return 31;
        }
      };
      _proto.firstDayOfYear = function(leapYear) {
        leapYear = leapYear ? 1 : 0;
        switch(this) {
          case Month.JANUARY:
            return 1;
          case Month.FEBRUARY:
            return 32;
          case Month.MARCH:
            return 60 + leapYear;
          case Month.APRIL:
            return 91 + leapYear;
          case Month.MAY:
            return 121 + leapYear;
          case Month.JUNE:
            return 152 + leapYear;
          case Month.JULY:
            return 182 + leapYear;
          case Month.AUGUST:
            return 213 + leapYear;
          case Month.SEPTEMBER:
            return 244 + leapYear;
          case Month.OCTOBER:
            return 274 + leapYear;
          case Month.NOVEMBER:
            return 305 + leapYear;
          default:
            return 335 + leapYear;
        }
      };
      _proto.firstMonthOfQuarter = function() {
        switch(this) {
          case Month.JANUARY:
          case Month.FEBRUARY:
          case Month.MARCH:
            return Month.JANUARY;
          case Month.APRIL:
          case Month.MAY:
          case Month.JUNE:
            return Month.APRIL;
          case Month.JULY:
          case Month.AUGUST:
          case Month.SEPTEMBER:
            return Month.JULY;
          default:
            return Month.OCTOBER;
        }
      };
      _proto.query = function(_query) {
        assert(null != _query, "query() parameter must not be null", DateTimeException);
        return _query === TemporalQueries.chronology() ? IsoChronology.INSTANCE : _query === TemporalQueries.precision() ? ChronoUnit.MONTHS : _TemporalAccessor.prototype.query.call(this, _query);
      };
      _proto.toString = function() {
        switch(this) {
          case Month.JANUARY:
            return "JANUARY";
          case Month.FEBRUARY:
            return "FEBRUARY";
          case Month.MARCH:
            return "MARCH";
          case Month.APRIL:
            return "APRIL";
          case Month.MAY:
            return "MAY";
          case Month.JUNE:
            return "JUNE";
          case Month.JULY:
            return "JULY";
          case Month.AUGUST:
            return "AUGUST";
          case Month.SEPTEMBER:
            return "SEPTEMBER";
          case Month.OCTOBER:
            return "OCTOBER";
          case Month.NOVEMBER:
            return "NOVEMBER";
          case Month.DECEMBER:
            return "DECEMBER";
          default:
            return "unknown Month, value: " + this.value();
        }
      };
      _proto.toJSON = function() {
        return this.toString();
      };
      _proto.adjustInto = function(temporal) {
        return temporal.with(ChronoField.MONTH_OF_YEAR, this.value());
      };
      _proto.compareTo = function(other) {
        requireNonNull(other, "other");
        requireInstance(other, Month, "other");
        return this._value - other._value;
      };
      _proto.equals = function(other) {
        return this === other;
      };
      Month.valueOf = function(name) {
        var ordinal = 0;
        for (ordinal; ordinal < MONTHS.length && MONTHS[ordinal].name() !== name; ordinal++) {
        }
        return Month.of(ordinal + 1);
      };
      Month.values = function() {
        return MONTHS.slice();
      };
      Month.of = function(month) {
        (1 > month || 12 < month) && assert(!1, "Invalid value for MonthOfYear: " + month, DateTimeException);
        return MONTHS[month - 1];
      };
      Month.from = function(temporal) {
        if (temporal instanceof Month) {
          return temporal;
        }
        try {
          return Month.of(temporal.get(ChronoField.MONTH_OF_YEAR));
        } catch (ex) {
          throw new DateTimeException("Unable to obtain Month from TemporalAccessor: " + temporal + " of type " + (temporal && null != temporal.constructor ? temporal.constructor.name : ""), ex);
        }
      };
      return Month;
    }(TemporalAccessor), PATTERN = /([-+]?)P(?:([-+]?[0-9]+)Y)?(?:([-+]?[0-9]+)M)?(?:([-+]?[0-9]+)W)?(?:([-+]?[0-9]+)D)?/, Period = function(_TemporalAmount) {
      function Period(years, months, days) {
        var _this = _TemporalAmount.call(this) || this;
        years = MathUtil.safeToInt(years);
        months = MathUtil.safeToInt(months);
        days = MathUtil.safeToInt(days);
        if (0 === years && 0 === months && 0 === days) {
          return Period.ZERO || (_this._years = years, _this._months = months, _this._days = days, Period.ZERO = _assertThisInitialized(_this)), Period.ZERO || _assertThisInitialized(_this);
        }
        _this._years = years;
        _this._months = months;
        _this._days = days;
        return _this;
      }
      _inheritsLoose(Period, _TemporalAmount);
      Period.ofYears = function(years) {
        return Period.create(years, 0, 0);
      };
      Period.ofMonths = function(months) {
        return Period.create(0, months, 0);
      };
      Period.ofWeeks = function(weeks) {
        return Period.create(0, 0, MathUtil.safeMultiply(weeks, 7));
      };
      Period.ofDays = function(days) {
        return Period.create(0, 0, days);
      };
      Period.of = function(years, months, days) {
        return Period.create(years, months, days);
      };
      Period.from = function(amount) {
        if (amount instanceof Period) {
          return amount;
        }
        requireNonNull(amount, "amount");
        for (var years = 0, months = 0, days = 0, units = amount.units(), i = 0; i < units.length; i++) {
          var unit = units[i], unitAmount = amount.get(unit);
          if (unit === ChronoUnit.YEARS) {
            years = MathUtil.safeToInt(unitAmount);
          } else if (unit === ChronoUnit.MONTHS) {
            months = MathUtil.safeToInt(unitAmount);
          } else if (unit === ChronoUnit.DAYS) {
            days = MathUtil.safeToInt(unitAmount);
          } else {
            throw new DateTimeException("Unit must be Years, Months or Days, but was " + unit);
          }
        }
        return Period.create(years, months, days);
      };
      Period.between = function(startDate, endDate) {
        requireNonNull(startDate, "startDate");
        requireNonNull(endDate, "endDate");
        requireInstance(startDate, LocalDate, "startDate");
        requireInstance(endDate, LocalDate, "endDate");
        return startDate.until(endDate);
      };
      Period.parse = function(text) {
        requireNonNull(text, "text");
        try {
          return Period._parse(text);
        } catch (ex) {
          if (ex instanceof ArithmeticException) {
            throw new DateTimeParseException("Text cannot be parsed to a Period", text, 0, ex);
          }
          throw ex;
        }
      };
      Period._parse = function(text) {
        var matches = PATTERN.exec(text);
        if (null != matches) {
          var negate = "-" === matches[1] ? -1 : 1, yearMatch = matches[2], monthMatch = matches[3], weekMatch = matches[4];
          matches = matches[5];
          if (null != yearMatch || null != monthMatch || null != weekMatch || null != matches) {
            return yearMatch = Period._parseNumber(text, yearMatch, negate), monthMatch = Period._parseNumber(text, monthMatch, negate), weekMatch = Period._parseNumber(text, weekMatch, negate), text = Period._parseNumber(text, matches, negate), text = MathUtil.safeAdd(text, MathUtil.safeMultiply(weekMatch, 7)), Period.create(yearMatch, monthMatch, text);
          }
        }
        throw new DateTimeParseException("Text cannot be parsed to a Period", text, 0);
      };
      Period._parseNumber = function(text, str, negate) {
        if (null == str) {
          return 0;
        }
        text = MathUtil.parseInt(str);
        return MathUtil.safeMultiply(text, negate);
      };
      Period.create = function(years, months, days) {
        return new Period(years, months, days);
      };
      var _proto = Period.prototype;
      _proto.units = function() {
        return [ChronoUnit.YEARS, ChronoUnit.MONTHS, ChronoUnit.DAYS];
      };
      _proto.chronology = function() {
        return IsoChronology.INSTANCE;
      };
      _proto.get = function(unit) {
        if (unit === ChronoUnit.YEARS) {
          return this._years;
        }
        if (unit === ChronoUnit.MONTHS) {
          return this._months;
        }
        if (unit === ChronoUnit.DAYS) {
          return this._days;
        }
        throw new UnsupportedTemporalTypeException("Unsupported unit: " + unit);
      };
      _proto.isZero = function() {
        return this === Period.ZERO;
      };
      _proto.isNegative = function() {
        return 0 > this._years || 0 > this._months || 0 > this._days;
      };
      _proto.years = function() {
        return this._years;
      };
      _proto.months = function() {
        return this._months;
      };
      _proto.days = function() {
        return this._days;
      };
      _proto.withYears = function(years) {
        return years === this._years ? this : Period.create(years, this._months, this._days);
      };
      _proto.withMonths = function(months) {
        return months === this._months ? this : Period.create(this._years, months, this._days);
      };
      _proto.withDays = function(days) {
        return days === this._days ? this : Period.create(this._years, this._months, days);
      };
      _proto.plus = function(amountToAdd) {
        amountToAdd = Period.from(amountToAdd);
        return Period.create(MathUtil.safeAdd(this._years, amountToAdd._years), MathUtil.safeAdd(this._months, amountToAdd._months), MathUtil.safeAdd(this._days, amountToAdd._days));
      };
      _proto.plusYears = function(yearsToAdd) {
        return 0 === yearsToAdd ? this : Period.create(MathUtil.safeToInt(MathUtil.safeAdd(this._years, yearsToAdd)), this._months, this._days);
      };
      _proto.plusMonths = function(monthsToAdd) {
        return 0 === monthsToAdd ? this : Period.create(this._years, MathUtil.safeToInt(MathUtil.safeAdd(this._months, monthsToAdd)), this._days);
      };
      _proto.plusDays = function(daysToAdd) {
        return 0 === daysToAdd ? this : Period.create(this._years, this._months, MathUtil.safeToInt(MathUtil.safeAdd(this._days, daysToAdd)));
      };
      _proto.minus = function(amountToSubtract) {
        amountToSubtract = Period.from(amountToSubtract);
        return Period.create(MathUtil.safeSubtract(this._years, amountToSubtract._years), MathUtil.safeSubtract(this._months, amountToSubtract._months), MathUtil.safeSubtract(this._days, amountToSubtract._days));
      };
      _proto.minusYears = function(yearsToSubtract) {
        return this.plusYears(-1 * yearsToSubtract);
      };
      _proto.minusMonths = function(monthsToSubtract) {
        return this.plusMonths(-1 * monthsToSubtract);
      };
      _proto.minusDays = function(daysToSubtract) {
        return this.plusDays(-1 * daysToSubtract);
      };
      _proto.multipliedBy = function(scalar) {
        return this === Period.ZERO || 1 === scalar ? this : Period.create(MathUtil.safeMultiply(this._years, scalar), MathUtil.safeMultiply(this._months, scalar), MathUtil.safeMultiply(this._days, scalar));
      };
      _proto.negated = function() {
        return this.multipliedBy(-1);
      };
      _proto.normalized = function() {
        var totalMonths = this.toTotalMonths(), splitYears = MathUtil.intDiv(totalMonths, 12);
        totalMonths = MathUtil.intMod(totalMonths, 12);
        return splitYears === this._years && totalMonths === this._months ? this : Period.create(MathUtil.safeToInt(splitYears), totalMonths, this._days);
      };
      _proto.toTotalMonths = function() {
        return 12 * this._years + this._months;
      };
      _proto.addTo = function(temporal) {
        requireNonNull(temporal, "temporal");
        0 !== this._years ? temporal = 0 !== this._months ? temporal.plus(this.toTotalMonths(), ChronoUnit.MONTHS) : temporal.plus(this._years, ChronoUnit.YEARS) : 0 !== this._months && (temporal = temporal.plus(this._months, ChronoUnit.MONTHS));
        0 !== this._days && (temporal = temporal.plus(this._days, ChronoUnit.DAYS));
        return temporal;
      };
      _proto.subtractFrom = function(temporal) {
        requireNonNull(temporal, "temporal");
        0 !== this._years ? temporal = 0 !== this._months ? temporal.minus(this.toTotalMonths(), ChronoUnit.MONTHS) : temporal.minus(this._years, ChronoUnit.YEARS) : 0 !== this._months && (temporal = temporal.minus(this._months, ChronoUnit.MONTHS));
        0 !== this._days && (temporal = temporal.minus(this._days, ChronoUnit.DAYS));
        return temporal;
      };
      _proto.equals = function(obj) {
        return this === obj ? !0 : obj instanceof Period ? this._years === obj._years && this._months === obj._months && this._days === obj._days : !1;
      };
      _proto.hashCode = function() {
        return MathUtil.hashCode(this._years, this._months, this._days);
      };
      _proto.toString = function() {
        if (this === Period.ZERO) {
          return "P0D";
        }
        var buf = "P";
        0 !== this._years && (buf += "" + this._years + "Y");
        0 !== this._months && (buf += "" + this._months + "M");
        0 !== this._days && (buf += "" + this._days + "D");
        return buf;
      };
      _proto.toJSON = function() {
        return this.toString();
      };
      return Period;
    }(TemporalAmount), ParsePosition = function() {
      function ParsePosition(index) {
        this._index = index;
        this._errorIndex = -1;
      }
      var _proto = ParsePosition.prototype;
      _proto.getIndex = function() {
        return this._index;
      };
      _proto.setIndex = function(index) {
        this._index = index;
      };
      _proto.getErrorIndex = function() {
        return this._errorIndex;
      };
      _proto.setErrorIndex = function(errorIndex) {
        this._errorIndex = errorIndex;
      };
      return ParsePosition;
    }(), EnumMap = function() {
      function EnumMap() {
        this._map = {};
      }
      var _proto = EnumMap.prototype;
      _proto.putAll = function(otherMap) {
        for (var key in otherMap._map) {
          this._map[key] = otherMap._map[key];
        }
        return this;
      };
      _proto.containsKey = function(key) {
        return this._map.hasOwnProperty(key.name()) && void 0 !== this.get(key);
      };
      _proto.get = function(key) {
        return this._map[key.name()];
      };
      _proto.put = function(key, val) {
        return this.set(key, val);
      };
      _proto.set = function(key, val) {
        this._map[key.name()] = val;
        return this;
      };
      _proto.retainAll = function(keyList) {
        for (var map = {}, i = 0; i < keyList.length; i++) {
          var key = keyList[i].name();
          map[key] = this._map[key];
        }
        this._map = map;
        return this;
      };
      _proto.remove = function(key) {
        key = key.name();
        var val = this._map[key];
        this._map[key] = void 0;
        return val;
      };
      _proto.keySet = function() {
        return this._map;
      };
      _proto.clear = function() {
        this._map = {};
      };
      return EnumMap;
    }(), ResolverStyle = function(_Enum) {
      function ResolverStyle() {
        return _Enum.apply(this, arguments) || this;
      }
      _inheritsLoose(ResolverStyle, _Enum);
      return ResolverStyle;
    }(Enum);
    ResolverStyle.STRICT = new ResolverStyle("STRICT");
    ResolverStyle.SMART = new ResolverStyle("SMART");
    ResolverStyle.LENIENT = new ResolverStyle("LENIENT");
    var TemporalAdjuster = function() {
      function TemporalAdjuster() {
      }
      TemporalAdjuster.prototype.adjustInto = function(temporal) {
        abstractMethodFail("adjustInto");
      };
      return TemporalAdjuster;
    }(), Temporal = function(_TemporalAccessor) {
      function Temporal() {
        return _TemporalAccessor.apply(this, arguments) || this;
      }
      _inheritsLoose(Temporal, _TemporalAccessor);
      var _proto = Temporal.prototype;
      _proto.isSupported = function(unit) {
        abstractMethodFail("isSupported");
      };
      _proto.minus = function(p1, p2) {
        return 2 > arguments.length ? this.minusAmount(p1) : this.minusAmountUnit(p1, p2);
      };
      _proto.minusAmount = function(amount) {
        abstractMethodFail("minusAmount");
      };
      _proto.minusAmountUnit = function(amountToSubtract, unit) {
        abstractMethodFail("minusAmountUnit");
      };
      _proto.plus = function(p1, p2) {
        return 2 > arguments.length ? this.plusAmount(p1) : this.plusAmountUnit(p1, p2);
      };
      _proto.plusAmount = function(amount) {
        abstractMethodFail("plusAmount");
      };
      _proto.plusAmountUnit = function(amountToAdd, unit) {
        abstractMethodFail("plusAmountUnit");
      };
      _proto.until = function(endTemporal, unit) {
        abstractMethodFail("until");
      };
      _proto.with = function(p1, p2) {
        return 2 > arguments.length ? this.withAdjuster(p1) : this.withFieldValue(p1, p2);
      };
      _proto.withAdjuster = function(adjuster) {
        abstractMethodFail("withAdjuster");
      };
      _proto.withFieldValue = function(field, newValue) {
        abstractMethodFail("withFieldValue");
      };
      return Temporal;
    }(TemporalAccessor), DefaultInterfaceTemporal = function(_Temporal) {
      function DefaultInterfaceTemporal() {
        return _Temporal.apply(this, arguments) || this;
      }
      _inheritsLoose(DefaultInterfaceTemporal, _Temporal);
      var _proto = DefaultInterfaceTemporal.prototype;
      _proto.withAdjuster = function(adjuster) {
        requireNonNull(adjuster, "adjuster");
        requireInstance(adjuster, TemporalAdjuster, "adjuster");
        return adjuster.adjustInto(this);
      };
      _proto.plusAmount = function(amount) {
        requireNonNull(amount, "amount");
        requireInstance(amount, TemporalAmount, "amount");
        return amount.addTo(this);
      };
      _proto.minusAmount = function(amount) {
        requireNonNull(amount, "amount");
        requireInstance(amount, TemporalAmount, "amount");
        return amount.subtractFrom(this);
      };
      _proto.minusAmountUnit = function(amountToSubtract, unit) {
        requireNonNull(amountToSubtract, "amountToSubtract");
        requireNonNull(unit, "unit");
        requireInstance(unit, TemporalUnit, "unit");
        return -9007199254740991 === amountToSubtract ? this.plusAmountUnit(9007199254740991, unit).plusAmountUnit(1, unit) : this.plusAmount(-amountToSubtract, unit);
      };
      return DefaultInterfaceTemporal;
    }(Temporal), ChronoLocalDate = function(_DefaultInterfaceTemp) {
      function ChronoLocalDate() {
        return _DefaultInterfaceTemp.apply(this, arguments) || this;
      }
      _inheritsLoose(ChronoLocalDate, _DefaultInterfaceTemp);
      var _proto = ChronoLocalDate.prototype;
      _proto.isSupported = function(fieldOrUnit) {
        return fieldOrUnit instanceof ChronoField || fieldOrUnit instanceof ChronoUnit ? fieldOrUnit.isDateBased() : null != fieldOrUnit && fieldOrUnit.isSupportedBy(this);
      };
      _proto.query = function(_query) {
        return _query === TemporalQueries.chronology() ? this.chronology() : _query === TemporalQueries.precision() ? ChronoUnit.DAYS : _query === TemporalQueries.localDate() ? LocalDate.ofEpochDay(this.toEpochDay()) : _query === TemporalQueries.localTime() || _query === TemporalQueries.zone() || _query === TemporalQueries.zoneId() || _query === TemporalQueries.offset() ? null : _DefaultInterfaceTemp.prototype.query.call(this, _query);
      };
      _proto.adjustInto = function(temporal) {
        return temporal.with(ChronoField.EPOCH_DAY, this.toEpochDay());
      };
      _proto.format = function(formatter) {
        requireNonNull(formatter, "formatter");
        requireInstance(formatter, DateTimeFormatter, "formatter");
        return formatter.format(this);
      };
      return ChronoLocalDate;
    }(DefaultInterfaceTemporal), StringUtil = function() {
      function StringUtil() {
      }
      StringUtil.startsWith = function(text, pattern) {
        return 0 === text.indexOf(pattern);
      };
      StringUtil.hashCode = function(text) {
        var len = text.length;
        if (0 === len) {
          return 0;
        }
        for (var hash = 0, i = 0; i < len; i++) {
          var chr = text.charCodeAt(i);
          hash = (hash << 5) - hash + chr;
          hash |= 0;
        }
        return MathUtil.smi(hash);
      };
      return StringUtil;
    }(), ZoneId = function() {
      function ZoneId() {
      }
      ZoneId.systemDefault = function() {
        throw new DateTimeException("not supported operation");
      };
      ZoneId.getAvailableZoneIds = function() {
        throw new DateTimeException("not supported operation");
      };
      ZoneId.of = function(zoneId) {
        throw new DateTimeException("not supported operation" + zoneId);
      };
      ZoneId.ofOffset = function(prefix, offset) {
        throw new DateTimeException("not supported operation" + prefix + offset);
      };
      ZoneId.from = function(temporal) {
        throw new DateTimeException("not supported operation" + temporal);
      };
      var _proto = ZoneId.prototype;
      _proto.id = function() {
        abstractMethodFail("ZoneId.id");
      };
      _proto.rules = function() {
        abstractMethodFail("ZoneId.rules");
      };
      _proto.normalized = function() {
        var rules = this.rules();
        return rules.isFixedOffset() ? rules.offset(Instant.EPOCH) : this;
      };
      _proto.equals = function(other) {
        return this === other ? !0 : other instanceof ZoneId ? this.id() === other.id() : !1;
      };
      _proto.hashCode = function() {
        return StringUtil.hashCode(this.id());
      };
      _proto.toString = function() {
        return this.id();
      };
      _proto.toJSON = function() {
        return this.toString();
      };
      return ZoneId;
    }(), ZoneRules = function() {
      function ZoneRules() {
      }
      ZoneRules.of = function(offset) {
        requireNonNull(offset, "offset");
        return new Fixed(offset);
      };
      var _proto = ZoneRules.prototype;
      _proto.isFixedOffset = function() {
        abstractMethodFail("ZoneRules.isFixedOffset");
      };
      _proto.offset = function(instantOrLocalDateTime) {
        return instantOrLocalDateTime instanceof Instant ? this.offsetOfInstant(instantOrLocalDateTime) : this.offsetOfLocalDateTime(instantOrLocalDateTime);
      };
      _proto.offsetOfInstant = function(instant) {
        abstractMethodFail("ZoneRules.offsetInstant");
      };
      _proto.offsetOfEpochMilli = function(epochMilli) {
        abstractMethodFail("ZoneRules.offsetOfEpochMilli");
      };
      _proto.offsetOfLocalDateTime = function(localDateTime) {
        abstractMethodFail("ZoneRules.offsetLocalDateTime");
      };
      _proto.validOffsets = function(localDateTime) {
        abstractMethodFail("ZoneRules.validOffsets");
      };
      _proto.transition = function(localDateTime) {
        abstractMethodFail("ZoneRules.transition");
      };
      _proto.standardOffset = function(instant) {
        abstractMethodFail("ZoneRules.standardOffset");
      };
      _proto.daylightSavings = function(instant) {
        abstractMethodFail("ZoneRules.daylightSavings");
      };
      _proto.isDaylightSavings = function(instant) {
        abstractMethodFail("ZoneRules.isDaylightSavings");
      };
      _proto.isValidOffset = function(localDateTime, offset) {
        abstractMethodFail("ZoneRules.isValidOffset");
      };
      _proto.nextTransition = function(instant) {
        abstractMethodFail("ZoneRules.nextTransition");
      };
      _proto.previousTransition = function(instant) {
        abstractMethodFail("ZoneRules.previousTransition");
      };
      _proto.transitions = function() {
        abstractMethodFail("ZoneRules.transitions");
      };
      _proto.transitionRules = function() {
        abstractMethodFail("ZoneRules.transitionRules");
      };
      _proto.toString = function() {
        abstractMethodFail("ZoneRules.toString");
      };
      _proto.toJSON = function() {
        return this.toString();
      };
      return ZoneRules;
    }(), Fixed = function(_ZoneRules) {
      function Fixed(offset) {
        var _this = _ZoneRules.call(this) || this;
        _this._offset = offset;
        return _this;
      }
      _inheritsLoose(Fixed, _ZoneRules);
      var _proto2 = Fixed.prototype;
      _proto2.isFixedOffset = function() {
        return !0;
      };
      _proto2.offsetOfInstant = function() {
        return this._offset;
      };
      _proto2.offsetOfEpochMilli = function() {
        return this._offset;
      };
      _proto2.offsetOfLocalDateTime = function() {
        return this._offset;
      };
      _proto2.validOffsets = function() {
        return [this._offset];
      };
      _proto2.transition = function() {
        return null;
      };
      _proto2.standardOffset = function() {
        return this._offset;
      };
      _proto2.daylightSavings = function() {
        return Duration.ZERO;
      };
      _proto2.isDaylightSavings = function() {
        return !1;
      };
      _proto2.isValidOffset = function(localDateTime, offset) {
        return this._offset.equals(offset);
      };
      _proto2.nextTransition = function() {
        return null;
      };
      _proto2.previousTransition = function() {
        return null;
      };
      _proto2.transitions = function() {
        return [];
      };
      _proto2.transitionRules = function() {
        return [];
      };
      _proto2.equals = function(other) {
        return this === other ? !0 : other instanceof Fixed ? this._offset.equals(other._offset) : !1;
      };
      _proto2.toString = function() {
        return "FixedRules:" + this._offset.toString();
      };
      return Fixed;
    }(ZoneRules), SECONDS_CACHE = {}, ID_CACHE = {}, ZoneOffset = function(_ZoneId) {
      function ZoneOffset(totalSeconds) {
        var _this = _ZoneId.call(this) || this;
        ZoneOffset._validateTotalSeconds(totalSeconds);
        _this._totalSeconds = MathUtil.safeToInt(totalSeconds);
        _this._rules = ZoneRules.of(_assertThisInitialized(_this));
        _this._id = ZoneOffset._buildId(totalSeconds);
        return _this;
      }
      _inheritsLoose(ZoneOffset, _ZoneId);
      var _proto = ZoneOffset.prototype;
      _proto.totalSeconds = function() {
        return this._totalSeconds;
      };
      _proto.id = function() {
        return this._id;
      };
      ZoneOffset._buildId = function(totalSeconds) {
        if (0 === totalSeconds) {
          return "Z";
        }
        var absTotalSeconds = Math.abs(totalSeconds), absHours = MathUtil.intDiv(absTotalSeconds, LocalTime.SECONDS_PER_HOUR), absMinutes = MathUtil.intMod(MathUtil.intDiv(absTotalSeconds, LocalTime.SECONDS_PER_MINUTE), LocalTime.MINUTES_PER_HOUR);
        totalSeconds = (0 > totalSeconds ? "-" : "+") + (10 > absHours ? "0" : "") + absHours + (10 > absMinutes ? ":0" : ":") + absMinutes;
        absTotalSeconds = MathUtil.intMod(absTotalSeconds, LocalTime.SECONDS_PER_MINUTE);
        0 !== absTotalSeconds && (totalSeconds += (10 > absTotalSeconds ? ":0" : ":") + absTotalSeconds);
        return totalSeconds;
      };
      ZoneOffset._validateTotalSeconds = function(totalSeconds) {
        if (Math.abs(totalSeconds) > ZoneOffset.MAX_SECONDS) {
          throw new DateTimeException("Zone offset not in valid range: -18:00 to +18:00");
        }
      };
      ZoneOffset._validate = function(hours, minutes, seconds) {
        if (-18 > hours || 18 < hours) {
          throw new DateTimeException("Zone offset hours not in valid range: value " + hours + " is not in the range -18 to 18");
        }
        if (0 < hours) {
          if (0 > minutes || 0 > seconds) {
            throw new DateTimeException("Zone offset minutes and seconds must be positive because hours is positive");
          }
        } else if (0 > hours) {
          if (0 < minutes || 0 < seconds) {
            throw new DateTimeException("Zone offset minutes and seconds must be negative because hours is negative");
          }
        } else if (0 < minutes && 0 > seconds || 0 > minutes && 0 < seconds) {
          throw new DateTimeException("Zone offset minutes and seconds must have the same sign");
        }
        if (59 < Math.abs(minutes)) {
          throw new DateTimeException("Zone offset minutes not in valid range: abs(value) " + Math.abs(minutes) + " is not in the range 0 to 59");
        }
        if (59 < Math.abs(seconds)) {
          throw new DateTimeException("Zone offset seconds not in valid range: abs(value) " + Math.abs(seconds) + " is not in the range 0 to 59");
        }
        if (18 === Math.abs(hours) && (0 < Math.abs(minutes) || 0 < Math.abs(seconds))) {
          throw new DateTimeException("Zone offset not in valid range: -18:00 to +18:00");
        }
      };
      ZoneOffset.of = function(offsetId) {
        requireNonNull(offsetId, "offsetId");
        var offset = ID_CACHE[offsetId];
        if (null != offset) {
          return offset;
        }
        var minutes;
        switch(offsetId.length) {
          case 2:
            offsetId = offsetId[0] + "0" + offsetId[1];
          case 3:
            offset = ZoneOffset._parseNumber(offsetId, 1, !1);
            var seconds = minutes = 0;
            break;
          case 5:
            offset = ZoneOffset._parseNumber(offsetId, 1, !1);
            minutes = ZoneOffset._parseNumber(offsetId, 3, !1);
            seconds = 0;
            break;
          case 6:
            offset = ZoneOffset._parseNumber(offsetId, 1, !1);
            minutes = ZoneOffset._parseNumber(offsetId, 4, !0);
            seconds = 0;
            break;
          case 7:
            offset = ZoneOffset._parseNumber(offsetId, 1, !1);
            minutes = ZoneOffset._parseNumber(offsetId, 3, !1);
            seconds = ZoneOffset._parseNumber(offsetId, 5, !1);
            break;
          case 9:
            offset = ZoneOffset._parseNumber(offsetId, 1, !1);
            minutes = ZoneOffset._parseNumber(offsetId, 4, !0);
            seconds = ZoneOffset._parseNumber(offsetId, 7, !0);
            break;
          default:
            throw new DateTimeException("Invalid ID for ZoneOffset, invalid format: " + offsetId);
        }
        var first = offsetId[0];
        if ("+" !== first && "-" !== first) {
          throw new DateTimeException("Invalid ID for ZoneOffset, plus/minus not found when expected: " + offsetId);
        }
        return "-" === first ? ZoneOffset.ofHoursMinutesSeconds(-offset, -minutes, -seconds) : ZoneOffset.ofHoursMinutesSeconds(offset, minutes, seconds);
      };
      ZoneOffset._parseNumber = function(offsetId, pos, precededByColon) {
        if (precededByColon && ":" !== offsetId[pos - 1]) {
          throw new DateTimeException("Invalid ID for ZoneOffset, colon not found when expected: " + offsetId);
        }
        precededByColon = offsetId[pos];
        pos = offsetId[pos + 1];
        if ("0" > precededByColon || "9" < precededByColon || "0" > pos || "9" < pos) {
          throw new DateTimeException("Invalid ID for ZoneOffset, non numeric characters found: " + offsetId);
        }
        return 10 * (precededByColon.charCodeAt(0) - 48) + (pos.charCodeAt(0) - 48);
      };
      ZoneOffset.ofHours = function(hours) {
        return ZoneOffset.ofHoursMinutesSeconds(hours, 0, 0);
      };
      ZoneOffset.ofHoursMinutes = function(hours, minutes) {
        return ZoneOffset.ofHoursMinutesSeconds(hours, minutes, 0);
      };
      ZoneOffset.ofHoursMinutesSeconds = function(hours, minutes, seconds) {
        ZoneOffset._validate(hours, minutes, seconds);
        return ZoneOffset.ofTotalSeconds(hours * LocalTime.SECONDS_PER_HOUR + minutes * LocalTime.SECONDS_PER_MINUTE + seconds);
      };
      ZoneOffset.ofTotalMinutes = function(totalMinutes) {
        return ZoneOffset.ofTotalSeconds(totalMinutes * LocalTime.SECONDS_PER_MINUTE);
      };
      ZoneOffset.ofTotalSeconds = function(totalSeconds) {
        if (0 === totalSeconds % (15 * LocalTime.SECONDS_PER_MINUTE)) {
          var result = SECONDS_CACHE[totalSeconds];
          null == result && (result = new ZoneOffset(totalSeconds), SECONDS_CACHE[totalSeconds] = result, ID_CACHE[result.id()] = result);
          return result;
        }
        return new ZoneOffset(totalSeconds);
      };
      _proto.rules = function() {
        return this._rules;
      };
      _proto.get = function(field) {
        return this.getLong(field);
      };
      _proto.getLong = function(field) {
        if (field === ChronoField.OFFSET_SECONDS) {
          return this._totalSeconds;
        }
        if (field instanceof ChronoField) {
          throw new DateTimeException("Unsupported field: " + field);
        }
        return field.getFrom(this);
      };
      _proto.query = function(_query) {
        requireNonNull(_query, "query");
        return _query === TemporalQueries.offset() || _query === TemporalQueries.zone() ? this : _query === TemporalQueries.localDate() || _query === TemporalQueries.localTime() || _query === TemporalQueries.precision() || _query === TemporalQueries.chronology() || _query === TemporalQueries.zoneId() ? null : _query.queryFrom(this);
      };
      _proto.adjustInto = function(temporal) {
        return temporal.with(ChronoField.OFFSET_SECONDS, this._totalSeconds);
      };
      _proto.compareTo = function(other) {
        requireNonNull(other, "other");
        return other._totalSeconds - this._totalSeconds;
      };
      _proto.equals = function(obj) {
        return this === obj ? !0 : obj instanceof ZoneOffset ? this._totalSeconds === obj._totalSeconds : !1;
      };
      _proto.hashCode = function() {
        return this._totalSeconds;
      };
      _proto.toString = function() {
        return this._id;
      };
      return ZoneOffset;
    }(ZoneId), DateTimeBuilder = function(_TemporalAccessor) {
      function DateTimeBuilder() {
        var _this = _TemporalAccessor.call(this) || this;
        _this.fieldValues = new EnumMap();
        _this.chrono = null;
        _this.zone = null;
        _this.date = null;
        _this.time = null;
        _this.leapSecond = !1;
        _this.excessDays = null;
        return _this;
      }
      _inheritsLoose(DateTimeBuilder, _TemporalAccessor);
      DateTimeBuilder.create = function(field, value) {
        var dtb = new DateTimeBuilder();
        dtb._addFieldValue(field, value);
        return dtb;
      };
      var _proto = DateTimeBuilder.prototype;
      _proto.getFieldValue0 = function(field) {
        return this.fieldValues.get(field);
      };
      _proto._addFieldValue = function(field, value) {
        requireNonNull(field, "field");
        var old = this.getFieldValue0(field);
        if (null != old && old !== value) {
          throw new DateTimeException("Conflict found: " + field + " " + old + " differs from " + field + " " + value + ": " + this);
        }
        return this._putFieldValue0(field, value);
      };
      _proto._putFieldValue0 = function(field, value) {
        this.fieldValues.put(field, value);
        return this;
      };
      _proto.resolve = function(resolverStyle, resolverFields) {
        null != resolverFields && this.fieldValues.retainAll(resolverFields);
        this._mergeDate(resolverStyle);
        this._mergeTime(resolverStyle);
        this._resolveTimeInferZeroes(resolverStyle);
        null != this.excessDays && !1 === this.excessDays.isZero() && null != this.date && null != this.time && (this.date = this.date.plus(this.excessDays), this.excessDays = Period.ZERO);
        this._resolveInstant();
        return this;
      };
      _proto._mergeDate = function(resolverStyle) {
        this._checkDate(IsoChronology.INSTANCE.resolveDate(this.fieldValues, resolverStyle));
      };
      _proto._checkDate = function(date) {
        if (null != date) {
          this._addObject(date);
          for (var fieldName in this.fieldValues.keySet()) {
            var field = ChronoField.byName(fieldName);
            if (field && void 0 !== this.fieldValues.get(field) && field.isDateBased()) {
              var val1 = void 0;
              try {
                val1 = date.getLong(field);
              } catch (ex) {
                if (ex instanceof DateTimeException) {
                  continue;
                } else {
                  throw ex;
                }
              }
              var val2 = this.fieldValues.get(field);
              if (val1 !== val2) {
                throw new DateTimeException("Conflict found: Field " + field + " " + val1 + " differs from " + field + " " + val2 + " derived from " + date);
              }
            }
          }
        }
      };
      _proto._mergeTime = function(resolverStyle) {
        if (this.fieldValues.containsKey(ChronoField.CLOCK_HOUR_OF_DAY)) {
          var ch = this.fieldValues.remove(ChronoField.CLOCK_HOUR_OF_DAY);
          resolverStyle === ResolverStyle.LENIENT || resolverStyle === ResolverStyle.SMART && 0 === ch || ChronoField.CLOCK_HOUR_OF_DAY.checkValidValue(ch);
          this._addFieldValue(ChronoField.HOUR_OF_DAY, 24 === ch ? 0 : ch);
        }
        this.fieldValues.containsKey(ChronoField.CLOCK_HOUR_OF_AMPM) && (ch = this.fieldValues.remove(ChronoField.CLOCK_HOUR_OF_AMPM), resolverStyle === ResolverStyle.LENIENT || resolverStyle === ResolverStyle.SMART && 0 === ch || ChronoField.CLOCK_HOUR_OF_AMPM.checkValidValue(ch), this._addFieldValue(ChronoField.HOUR_OF_AMPM, 12 === ch ? 0 : ch));
        resolverStyle !== ResolverStyle.LENIENT && (this.fieldValues.containsKey(ChronoField.AMPM_OF_DAY) && ChronoField.AMPM_OF_DAY.checkValidValue(this.fieldValues.get(ChronoField.AMPM_OF_DAY)), this.fieldValues.containsKey(ChronoField.HOUR_OF_AMPM) && ChronoField.HOUR_OF_AMPM.checkValidValue(this.fieldValues.get(ChronoField.HOUR_OF_AMPM)));
        if (this.fieldValues.containsKey(ChronoField.AMPM_OF_DAY) && this.fieldValues.containsKey(ChronoField.HOUR_OF_AMPM)) {
          ch = this.fieldValues.remove(ChronoField.AMPM_OF_DAY);
          var hap = this.fieldValues.remove(ChronoField.HOUR_OF_AMPM);
          this._addFieldValue(ChronoField.HOUR_OF_DAY, 12 * ch + hap);
        }
        this.fieldValues.containsKey(ChronoField.NANO_OF_DAY) && (ch = this.fieldValues.remove(ChronoField.NANO_OF_DAY), resolverStyle !== ResolverStyle.LENIENT && ChronoField.NANO_OF_DAY.checkValidValue(ch), this._addFieldValue(ChronoField.SECOND_OF_DAY, MathUtil.intDiv(ch, 1000000000)), this._addFieldValue(ChronoField.NANO_OF_SECOND, MathUtil.intMod(ch, 1000000000)));
        this.fieldValues.containsKey(ChronoField.MICRO_OF_DAY) && (ch = this.fieldValues.remove(ChronoField.MICRO_OF_DAY), resolverStyle !== ResolverStyle.LENIENT && ChronoField.MICRO_OF_DAY.checkValidValue(ch), this._addFieldValue(ChronoField.SECOND_OF_DAY, MathUtil.intDiv(ch, 1000000)), this._addFieldValue(ChronoField.MICRO_OF_SECOND, MathUtil.intMod(ch, 1000000)));
        this.fieldValues.containsKey(ChronoField.MILLI_OF_DAY) && (ch = this.fieldValues.remove(ChronoField.MILLI_OF_DAY), resolverStyle !== ResolverStyle.LENIENT && ChronoField.MILLI_OF_DAY.checkValidValue(ch), this._addFieldValue(ChronoField.SECOND_OF_DAY, MathUtil.intDiv(ch, 1000)), this._addFieldValue(ChronoField.MILLI_OF_SECOND, MathUtil.intMod(ch, 1000)));
        this.fieldValues.containsKey(ChronoField.SECOND_OF_DAY) && (ch = this.fieldValues.remove(ChronoField.SECOND_OF_DAY), resolverStyle !== ResolverStyle.LENIENT && ChronoField.SECOND_OF_DAY.checkValidValue(ch), this._addFieldValue(ChronoField.HOUR_OF_DAY, MathUtil.intDiv(ch, 3600)), this._addFieldValue(ChronoField.MINUTE_OF_HOUR, MathUtil.intMod(MathUtil.intDiv(ch, 60), 60)), this._addFieldValue(ChronoField.SECOND_OF_MINUTE, MathUtil.intMod(ch, 60)));
        this.fieldValues.containsKey(ChronoField.MINUTE_OF_DAY) && (ch = this.fieldValues.remove(ChronoField.MINUTE_OF_DAY), resolverStyle !== ResolverStyle.LENIENT && ChronoField.MINUTE_OF_DAY.checkValidValue(ch), this._addFieldValue(ChronoField.HOUR_OF_DAY, MathUtil.intDiv(ch, 60)), this._addFieldValue(ChronoField.MINUTE_OF_HOUR, MathUtil.intMod(ch, 60)));
        resolverStyle !== ResolverStyle.LENIENT && (this.fieldValues.containsKey(ChronoField.MILLI_OF_SECOND) && ChronoField.MILLI_OF_SECOND.checkValidValue(this.fieldValues.get(ChronoField.MILLI_OF_SECOND)), this.fieldValues.containsKey(ChronoField.MICRO_OF_SECOND) && ChronoField.MICRO_OF_SECOND.checkValidValue(this.fieldValues.get(ChronoField.MICRO_OF_SECOND)));
        this.fieldValues.containsKey(ChronoField.MILLI_OF_SECOND) && this.fieldValues.containsKey(ChronoField.MICRO_OF_SECOND) && (resolverStyle = this.fieldValues.remove(ChronoField.MILLI_OF_SECOND), ch = this.fieldValues.get(ChronoField.MICRO_OF_SECOND), this._putFieldValue0(ChronoField.MICRO_OF_SECOND, 1000 * resolverStyle + MathUtil.intMod(ch, 1000)));
        this.fieldValues.containsKey(ChronoField.MICRO_OF_SECOND) && this.fieldValues.containsKey(ChronoField.NANO_OF_SECOND) && (resolverStyle = this.fieldValues.get(ChronoField.NANO_OF_SECOND), this._putFieldValue0(ChronoField.MICRO_OF_SECOND, MathUtil.intDiv(resolverStyle, 1000)), this.fieldValues.remove(ChronoField.MICRO_OF_SECOND));
        this.fieldValues.containsKey(ChronoField.MILLI_OF_SECOND) && this.fieldValues.containsKey(ChronoField.NANO_OF_SECOND) && (resolverStyle = this.fieldValues.get(ChronoField.NANO_OF_SECOND), this._putFieldValue0(ChronoField.MILLI_OF_SECOND, MathUtil.intDiv(resolverStyle, 1000000)), this.fieldValues.remove(ChronoField.MILLI_OF_SECOND));
        this.fieldValues.containsKey(ChronoField.MICRO_OF_SECOND) ? (resolverStyle = this.fieldValues.remove(ChronoField.MICRO_OF_SECOND), this._putFieldValue0(ChronoField.NANO_OF_SECOND, 1000 * resolverStyle)) : this.fieldValues.containsKey(ChronoField.MILLI_OF_SECOND) && (resolverStyle = this.fieldValues.remove(ChronoField.MILLI_OF_SECOND), this._putFieldValue0(ChronoField.NANO_OF_SECOND, 1000000 * resolverStyle));
      };
      _proto._resolveTimeInferZeroes = function(resolverStyle) {
        var hod = this.fieldValues.get(ChronoField.HOUR_OF_DAY), moh = this.fieldValues.get(ChronoField.MINUTE_OF_HOUR), som = this.fieldValues.get(ChronoField.SECOND_OF_MINUTE), nos = this.fieldValues.get(ChronoField.NANO_OF_SECOND);
        null == hod || null == moh && (null != som || null != nos) || null != moh && null == som && null != nos || (resolverStyle !== ResolverStyle.LENIENT ? null != hod && (resolverStyle !== ResolverStyle.SMART || 24 !== hod || null != moh && 0 !== moh || null != som && 0 !== som || null != nos && 0 !== nos || (hod = 0, this.excessDays = Period.ofDays(1)), resolverStyle = ChronoField.HOUR_OF_DAY.checkValidIntValue(hod), null != moh ? (moh = ChronoField.MINUTE_OF_HOUR.checkValidIntValue(moh), null != 
        som ? (som = ChronoField.SECOND_OF_MINUTE.checkValidIntValue(som), null != nos ? (nos = ChronoField.NANO_OF_SECOND.checkValidIntValue(nos), this._addObject(LocalTime.of(resolverStyle, moh, som, nos))) : this._addObject(LocalTime.of(resolverStyle, moh, som))) : null == nos && this._addObject(LocalTime.of(resolverStyle, moh))) : null == som && null == nos && this._addObject(LocalTime.of(resolverStyle, 0))) : null != hod && (resolverStyle = hod, null != moh ? null != som ? (null == nos && (nos = 
        0), resolverStyle = MathUtil.safeMultiply(resolverStyle, 3600000000000), resolverStyle = MathUtil.safeAdd(resolverStyle, MathUtil.safeMultiply(moh, 60000000000)), resolverStyle = MathUtil.safeAdd(resolverStyle, MathUtil.safeMultiply(som, 1000000000)), resolverStyle = MathUtil.safeAdd(resolverStyle, nos), nos = MathUtil.floorDiv(resolverStyle, 86400000000000), moh = MathUtil.floorMod(resolverStyle, 86400000000000), this._addObject(LocalTime.ofNanoOfDay(moh)), this.excessDays = Period.ofDays(nos)) : 
        (nos = MathUtil.safeMultiply(resolverStyle, 3600), nos = MathUtil.safeAdd(nos, MathUtil.safeMultiply(moh, 60)), moh = MathUtil.floorDiv(nos, 86400), nos = MathUtil.floorMod(nos, 86400), this._addObject(LocalTime.ofSecondOfDay(nos)), this.excessDays = Period.ofDays(moh)) : (nos = MathUtil.safeToInt(MathUtil.floorDiv(resolverStyle, 24)), resolverStyle = MathUtil.floorMod(resolverStyle, 24), this._addObject(LocalTime.of(resolverStyle, 0)), this.excessDays = Period.ofDays(nos))), this.fieldValues.remove(ChronoField.HOUR_OF_DAY), 
        this.fieldValues.remove(ChronoField.MINUTE_OF_HOUR), this.fieldValues.remove(ChronoField.SECOND_OF_MINUTE), this.fieldValues.remove(ChronoField.NANO_OF_SECOND));
      };
      _proto._addObject = function(dateOrTime) {
        dateOrTime instanceof ChronoLocalDate ? this.date = dateOrTime : dateOrTime instanceof LocalTime && (this.time = dateOrTime);
      };
      _proto._resolveInstant = function() {
        if (null != this.date && null != this.time) {
          var offsetSecs = this.fieldValues.get(ChronoField.OFFSET_SECONDS);
          null != offsetSecs ? (offsetSecs = ZoneOffset.ofTotalSeconds(offsetSecs), offsetSecs = this.date.atTime(this.time).atZone(offsetSecs).getLong(ChronoField.INSTANT_SECONDS), this.fieldValues.put(ChronoField.INSTANT_SECONDS, offsetSecs)) : null != this.zone && (offsetSecs = this.date.atTime(this.time).atZone(this.zone).getLong(ChronoField.INSTANT_SECONDS), this.fieldValues.put(ChronoField.INSTANT_SECONDS, offsetSecs));
        }
      };
      _proto.build = function(type) {
        return type.queryFrom(this);
      };
      _proto.isSupported = function(field) {
        return null == field ? !1 : this.fieldValues.containsKey(field) && void 0 !== this.fieldValues.get(field) || null != this.date && this.date.isSupported(field) || null != this.time && this.time.isSupported(field);
      };
      _proto.getLong = function(field) {
        requireNonNull(field, "field");
        var value = this.getFieldValue0(field);
        if (null == value) {
          if (null != this.date && this.date.isSupported(field)) {
            return this.date.getLong(field);
          }
          if (null != this.time && this.time.isSupported(field)) {
            return this.time.getLong(field);
          }
          throw new DateTimeException("Field not found: " + field);
        }
        return value;
      };
      _proto.query = function(_query) {
        return _query === TemporalQueries.zoneId() ? this.zone : _query === TemporalQueries.chronology() ? this.chrono : _query === TemporalQueries.localDate() ? null != this.date ? LocalDate.from(this.date) : null : _query === TemporalQueries.localTime() ? this.time : _query !== TemporalQueries.zone() && _query !== TemporalQueries.offset() && _query === TemporalQueries.precision() ? null : _query.queryFrom(this);
      };
      return DateTimeBuilder;
    }(TemporalAccessor), DateTimeParseContext = function() {
      function DateTimeParseContext() {
        if (1 === arguments.length) {
          if (arguments[0] instanceof DateTimeParseContext) {
            this._constructorSelf.apply(this, arguments);
            return;
          }
          this._constructorFormatter.apply(this, arguments);
        } else {
          this._constructorParam.apply(this, arguments);
        }
        this._strict = this._caseSensitive = !0;
        this._parsed = [new Parsed(this)];
      }
      var _proto = DateTimeParseContext.prototype;
      _proto._constructorParam = function(locale, symbols, chronology) {
        this._locale = locale;
        this._symbols = symbols;
        this._overrideChronology = chronology;
      };
      _proto._constructorFormatter = function(formatter) {
        this._locale = formatter.locale();
        this._symbols = formatter.decimalStyle();
        this._overrideChronology = formatter.chronology();
      };
      _proto._constructorSelf = function(other) {
        this._locale = other._locale;
        this._symbols = other._symbols;
        this._overrideChronology = other._overrideChronology;
        this._overrideZone = other._overrideZone;
        this._caseSensitive = other._caseSensitive;
        this._strict = other._strict;
        this._parsed = [new Parsed(this)];
      };
      _proto.copy = function() {
        return new DateTimeParseContext(this);
      };
      _proto.symbols = function() {
        return this._symbols;
      };
      _proto.isStrict = function() {
        return this._strict;
      };
      _proto.setStrict = function(strict) {
        this._strict = strict;
      };
      _proto.locale = function() {
        return this._locale;
      };
      _proto.setLocale = function(locale) {
        this._locale = locale;
      };
      _proto.startOptional = function() {
        this._parsed.push(this.currentParsed().copy());
      };
      _proto.endOptional = function(successful) {
        successful ? this._parsed.splice(this._parsed.length - 2, 1) : this._parsed.splice(this._parsed.length - 1, 1);
      };
      _proto.isCaseSensitive = function() {
        return this._caseSensitive;
      };
      _proto.setCaseSensitive = function(caseSensitive) {
        this._caseSensitive = caseSensitive;
      };
      _proto.subSequenceEquals = function(cs1, offset1, cs2, offset2, length) {
        if (offset1 + length > cs1.length || offset2 + length > cs2.length) {
          return !1;
        }
        this.isCaseSensitive() || (cs1 = cs1.toLowerCase(), cs2 = cs2.toLowerCase());
        for (var i = 0; i < length; i++) {
          if (cs1[offset1 + i] !== cs2[offset2 + i]) {
            return !1;
          }
        }
        return !0;
      };
      _proto.charEquals = function(ch1, ch2) {
        return this.isCaseSensitive() ? ch1 === ch2 : this.charEqualsIgnoreCase(ch1, ch2);
      };
      _proto.charEqualsIgnoreCase = function(c1, c2) {
        return c1 === c2 || c1.toLowerCase() === c2.toLowerCase();
      };
      _proto.setParsedField = function(field, value, errorPos, successPos) {
        var currentParsedFieldValues = this.currentParsed().fieldValues, old = currentParsedFieldValues.get(field);
        currentParsedFieldValues.set(field, value);
        return null != old && old !== value ? ~errorPos : successPos;
      };
      _proto.setParsedZone = function(zone) {
        requireNonNull(zone, "zone");
        this.currentParsed().zone = zone;
      };
      _proto.getParsed = function(field) {
        return this.currentParsed().fieldValues.get(field);
      };
      _proto.toParsed = function() {
        return this.currentParsed();
      };
      _proto.currentParsed = function() {
        return this._parsed[this._parsed.length - 1];
      };
      _proto.setParsedLeapSecond = function() {
        this.currentParsed().leapSecond = !0;
      };
      _proto.getEffectiveChronology = function() {
        var chrono = this.currentParsed().chrono;
        null == chrono && (chrono = this._overrideChronology, null == chrono && (chrono = IsoChronology.INSTANCE));
        return chrono;
      };
      return DateTimeParseContext;
    }(), Parsed = function(_Temporal) {
      function Parsed(dateTimeParseContext) {
        var _this = _Temporal.call(this) || this;
        _this.chrono = null;
        _this.zone = null;
        _this.fieldValues = new EnumMap();
        _this.leapSecond = !1;
        _this.dateTimeParseContext = dateTimeParseContext;
        return _this;
      }
      _inheritsLoose(Parsed, _Temporal);
      var _proto2 = Parsed.prototype;
      _proto2.copy = function() {
        var cloned = new Parsed();
        cloned.chrono = this.chrono;
        cloned.zone = this.zone;
        cloned.fieldValues.putAll(this.fieldValues);
        cloned.leapSecond = this.leapSecond;
        cloned.dateTimeParseContext = this.dateTimeParseContext;
        return cloned;
      };
      _proto2.toString = function() {
        return this.fieldValues + ", " + this.chrono + ", " + this.zone;
      };
      _proto2.isSupported = function(field) {
        return this.fieldValues.containsKey(field);
      };
      _proto2.get = function(field) {
        field = this.fieldValues.get(field);
        assert(null != field);
        return field;
      };
      _proto2.query = function(_query) {
        return _query === TemporalQueries.chronology() ? this.chrono : _query === TemporalQueries.zoneId() || _query === TemporalQueries.zone() ? this.zone : _Temporal.prototype.query.call(this, _query);
      };
      _proto2.toBuilder = function() {
        var builder = new DateTimeBuilder();
        builder.fieldValues.putAll(this.fieldValues);
        builder.chrono = this.dateTimeParseContext.getEffectiveChronology();
        builder.zone = null != this.zone ? this.zone : this.overrideZone;
        builder.leapSecond = this.leapSecond;
        builder.excessDays = this.excessDays;
        return builder;
      };
      return Parsed;
    }(Temporal), DateTimePrintContext = function() {
      function DateTimePrintContext(temporal, localeOrFormatter, symbols) {
        2 === arguments.length && arguments[1] instanceof DateTimeFormatter ? (this._temporal = DateTimePrintContext.adjust(temporal, localeOrFormatter), this._locale = localeOrFormatter.locale(), this._symbols = localeOrFormatter.decimalStyle()) : (this._temporal = temporal, this._locale = localeOrFormatter, this._symbols = symbols);
        this._optional = 0;
      }
      DateTimePrintContext.adjust = function(temporal, formatter) {
        return temporal;
      };
      var _proto = DateTimePrintContext.prototype;
      _proto.symbols = function() {
        return this._symbols;
      };
      _proto.startOptional = function() {
        this._optional++;
      };
      _proto.endOptional = function() {
        this._optional--;
      };
      _proto.getValueQuery = function(query) {
        query = this._temporal.query(query);
        if (null == query && 0 === this._optional) {
          throw new DateTimeException("Unable to extract value: " + this._temporal);
        }
        return query;
      };
      _proto.getValue = function(field) {
        try {
          return this._temporal.getLong(field);
        } catch (ex) {
          if (ex instanceof DateTimeException && 0 < this._optional) {
            return null;
          }
          throw ex;
        }
      };
      _proto.temporal = function() {
        return this._temporal;
      };
      _proto.locale = function() {
        return this._locale;
      };
      _proto.setDateTime = function(temporal) {
        this._temporal = temporal;
      };
      _proto.setLocale = function(locale) {
        this._locale = locale;
      };
      return DateTimePrintContext;
    }(), IsoFields = {}, QUARTER_DAYS = [0, 90, 181, 273, 0, 91, 182, 274], Field = function(_TemporalField) {
      function Field() {
        return _TemporalField.apply(this, arguments) || this;
      }
      _inheritsLoose(Field, _TemporalField);
      var _proto = Field.prototype;
      _proto.isDateBased = function() {
        return !0;
      };
      _proto.isTimeBased = function() {
        return !1;
      };
      _proto._isIso = function() {
        return !0;
      };
      Field._getWeekRangeByLocalDate = function(date) {
        date = Field._getWeekBasedYear(date);
        return ValueRange.of(1, Field._getWeekRangeByYear(date));
      };
      Field._getWeekRangeByYear = function(wby) {
        wby = LocalDate.of(wby, 1, 1);
        return wby.dayOfWeek() === DayOfWeek.THURSDAY || wby.dayOfWeek() === DayOfWeek.WEDNESDAY && wby.isLeapYear() ? 53 : 52;
      };
      Field._getWeek = function(date) {
        var dow0 = date.dayOfWeek().ordinal(), doy0 = date.dayOfYear() - 1;
        dow0 = doy0 + (3 - dow0);
        var alignedWeek = MathUtil.intDiv(dow0, 7);
        dow0 = dow0 - 7 * alignedWeek - 3;
        -3 > dow0 && (dow0 += 7);
        if (doy0 < dow0) {
          return Field._getWeekRangeByLocalDate(date.withDayOfYear(180).minusYears(1)).maximum();
        }
        doy0 = MathUtil.intDiv(doy0 - dow0, 7) + 1;
        53 === doy0 && !1 === (-3 === dow0 || -2 === dow0 && date.isLeapYear()) && (doy0 = 1);
        return doy0;
      };
      Field._getWeekBasedYear = function(date) {
        var year = date.year(), doy = date.dayOfYear();
        if (3 >= doy) {
          date = date.dayOfWeek().ordinal(), -2 > doy - date && year--;
        } else if (363 <= doy) {
          var _dow = date.dayOfWeek().ordinal();
          doy = doy - 363 - (date.isLeapYear() ? 1 : 0);
          0 <= doy - _dow && year++;
        }
        return year;
      };
      _proto.displayName = function() {
        return this.toString();
      };
      _proto.resolve = function() {
        return null;
      };
      _proto.name = function() {
        return this.toString();
      };
      return Field;
    }(TemporalField), DAY_OF_QUARTER_FIELD = function(_Field) {
      function DAY_OF_QUARTER_FIELD() {
        return _Field.apply(this, arguments) || this;
      }
      _inheritsLoose(DAY_OF_QUARTER_FIELD, _Field);
      var _proto2 = DAY_OF_QUARTER_FIELD.prototype;
      _proto2.toString = function() {
        return "DayOfQuarter";
      };
      _proto2.baseUnit = function() {
        return ChronoUnit.DAYS;
      };
      _proto2.rangeUnit = function() {
        return QUARTER_YEARS;
      };
      _proto2.range = function() {
        return ValueRange.of(1, 90, 92);
      };
      _proto2.isSupportedBy = function(temporal) {
        return temporal.isSupported(ChronoField.DAY_OF_YEAR) && temporal.isSupported(ChronoField.MONTH_OF_YEAR) && temporal.isSupported(ChronoField.YEAR) && this._isIso(temporal);
      };
      _proto2.rangeRefinedBy = function(temporal) {
        if (!1 === temporal.isSupported(this)) {
          throw new UnsupportedTemporalTypeException("Unsupported field: DayOfQuarter");
        }
        var qoy = temporal.getLong(QUARTER_OF_YEAR);
        return 1 === qoy ? (temporal = temporal.getLong(ChronoField.YEAR), IsoChronology.isLeapYear(temporal) ? ValueRange.of(1, 91) : ValueRange.of(1, 90)) : 2 === qoy ? ValueRange.of(1, 91) : 3 === qoy || 4 === qoy ? ValueRange.of(1, 92) : this.range();
      };
      _proto2.getFrom = function(temporal) {
        if (!1 === temporal.isSupported(this)) {
          throw new UnsupportedTemporalTypeException("Unsupported field: DayOfQuarter");
        }
        var doy = temporal.get(ChronoField.DAY_OF_YEAR), moy = temporal.get(ChronoField.MONTH_OF_YEAR);
        temporal = temporal.getLong(ChronoField.YEAR);
        return doy - QUARTER_DAYS[MathUtil.intDiv(moy - 1, 3) + (IsoChronology.isLeapYear(temporal) ? 4 : 0)];
      };
      _proto2.adjustInto = function(temporal, newValue) {
        var curValue = this.getFrom(temporal);
        this.range().checkValidValue(newValue, this);
        return temporal.with(ChronoField.DAY_OF_YEAR, temporal.getLong(ChronoField.DAY_OF_YEAR) + (newValue - curValue));
      };
      _proto2.resolve = function(fieldValues, partialTemporal, resolverStyle) {
        partialTemporal = fieldValues.get(ChronoField.YEAR);
        var qoyLong = fieldValues.get(QUARTER_OF_YEAR);
        if (null == partialTemporal || null == qoyLong) {
          return null;
        }
        var y = ChronoField.YEAR.checkValidIntValue(partialTemporal);
        partialTemporal = fieldValues.get(DAY_OF_QUARTER);
        resolverStyle === ResolverStyle.LENIENT ? (resolverStyle = LocalDate.of(y, 1, 1), resolverStyle = resolverStyle.plusMonths(MathUtil.safeMultiply(MathUtil.safeSubtract(qoyLong, 1), 3)), resolverStyle = resolverStyle.plusDays(MathUtil.safeSubtract(partialTemporal, 1))) : (qoyLong = QUARTER_OF_YEAR.range().checkValidIntValue(qoyLong, QUARTER_OF_YEAR), resolverStyle === ResolverStyle.STRICT ? (resolverStyle = 92, 1 === qoyLong ? resolverStyle = IsoChronology.isLeapYear(y) ? 91 : 90 : 2 === qoyLong && 
        (resolverStyle = 91), ValueRange.of(1, resolverStyle).checkValidValue(partialTemporal, this)) : this.range().checkValidValue(partialTemporal, this), resolverStyle = LocalDate.of(y, 3 * (qoyLong - 1) + 1, 1).plusDays(partialTemporal - 1));
        fieldValues.remove(this);
        fieldValues.remove(ChronoField.YEAR);
        fieldValues.remove(QUARTER_OF_YEAR);
        return resolverStyle;
      };
      return DAY_OF_QUARTER_FIELD;
    }(Field), QUARTER_OF_YEAR_FIELD = function(_Field2) {
      function QUARTER_OF_YEAR_FIELD() {
        return _Field2.apply(this, arguments) || this;
      }
      _inheritsLoose(QUARTER_OF_YEAR_FIELD, _Field2);
      var _proto3 = QUARTER_OF_YEAR_FIELD.prototype;
      _proto3.toString = function() {
        return "QuarterOfYear";
      };
      _proto3.baseUnit = function() {
        return QUARTER_YEARS;
      };
      _proto3.rangeUnit = function() {
        return ChronoUnit.YEARS;
      };
      _proto3.range = function() {
        return ValueRange.of(1, 4);
      };
      _proto3.isSupportedBy = function(temporal) {
        return temporal.isSupported(ChronoField.MONTH_OF_YEAR) && this._isIso(temporal);
      };
      _proto3.rangeRefinedBy = function(temporal) {
        return this.range();
      };
      _proto3.getFrom = function(temporal) {
        if (!1 === temporal.isSupported(this)) {
          throw new UnsupportedTemporalTypeException("Unsupported field: QuarterOfYear");
        }
        temporal = temporal.getLong(ChronoField.MONTH_OF_YEAR);
        return MathUtil.intDiv(temporal + 2, 3);
      };
      _proto3.adjustInto = function(temporal, newValue) {
        var curValue = this.getFrom(temporal);
        this.range().checkValidValue(newValue, this);
        return temporal.with(ChronoField.MONTH_OF_YEAR, temporal.getLong(ChronoField.MONTH_OF_YEAR) + 3 * (newValue - curValue));
      };
      return QUARTER_OF_YEAR_FIELD;
    }(Field), WEEK_OF_WEEK_BASED_YEAR_FIELD = function(_Field3) {
      function WEEK_OF_WEEK_BASED_YEAR_FIELD() {
        return _Field3.apply(this, arguments) || this;
      }
      _inheritsLoose(WEEK_OF_WEEK_BASED_YEAR_FIELD, _Field3);
      var _proto4 = WEEK_OF_WEEK_BASED_YEAR_FIELD.prototype;
      _proto4.toString = function() {
        return "WeekOfWeekBasedYear";
      };
      _proto4.baseUnit = function() {
        return ChronoUnit.WEEKS;
      };
      _proto4.rangeUnit = function() {
        return WEEK_BASED_YEARS;
      };
      _proto4.range = function() {
        return ValueRange.of(1, 52, 53);
      };
      _proto4.isSupportedBy = function(temporal) {
        return temporal.isSupported(ChronoField.EPOCH_DAY) && this._isIso(temporal);
      };
      _proto4.rangeRefinedBy = function(temporal) {
        if (!1 === temporal.isSupported(this)) {
          throw new UnsupportedTemporalTypeException("Unsupported field: WeekOfWeekBasedYear");
        }
        return Field._getWeekRangeByLocalDate(LocalDate.from(temporal));
      };
      _proto4.getFrom = function(temporal) {
        if (!1 === temporal.isSupported(this)) {
          throw new UnsupportedTemporalTypeException("Unsupported field: WeekOfWeekBasedYear");
        }
        return Field._getWeek(LocalDate.from(temporal));
      };
      _proto4.adjustInto = function(temporal, newValue) {
        this.range().checkValidValue(newValue, this);
        return temporal.plus(MathUtil.safeSubtract(newValue, this.getFrom(temporal)), ChronoUnit.WEEKS);
      };
      _proto4.resolve = function(fieldValues, partialTemporal, resolverStyle) {
        partialTemporal = fieldValues.get(WEEK_BASED_YEAR);
        var dowLong = fieldValues.get(ChronoField.DAY_OF_WEEK);
        if (null == partialTemporal || null == dowLong) {
          return null;
        }
        partialTemporal = WEEK_BASED_YEAR.range().checkValidIntValue(partialTemporal, WEEK_BASED_YEAR);
        var wowby = fieldValues.get(WEEK_OF_WEEK_BASED_YEAR);
        resolverStyle === ResolverStyle.LENIENT ? (resolverStyle = dowLong, dowLong = 0, 7 < resolverStyle ? (dowLong = MathUtil.intDiv(resolverStyle - 1, 7), resolverStyle = MathUtil.intMod(resolverStyle - 1, 7) + 1) : 1 > resolverStyle && (dowLong = MathUtil.intDiv(resolverStyle, 7) - 1, resolverStyle = MathUtil.intMod(resolverStyle, 7) + 7), partialTemporal = LocalDate.of(partialTemporal, 1, 4).plusWeeks(wowby - 1).plusWeeks(dowLong).with(ChronoField.DAY_OF_WEEK, resolverStyle)) : (dowLong = ChronoField.DAY_OF_WEEK.checkValidIntValue(dowLong), 
        resolverStyle === ResolverStyle.STRICT ? (resolverStyle = LocalDate.of(partialTemporal, 1, 4), Field._getWeekRangeByLocalDate(resolverStyle).checkValidValue(wowby, this)) : this.range().checkValidValue(wowby, this), partialTemporal = LocalDate.of(partialTemporal, 1, 4).plusWeeks(wowby - 1).with(ChronoField.DAY_OF_WEEK, dowLong));
        fieldValues.remove(this);
        fieldValues.remove(WEEK_BASED_YEAR);
        fieldValues.remove(ChronoField.DAY_OF_WEEK);
        return partialTemporal;
      };
      _proto4.displayName = function() {
        return "Week";
      };
      return WEEK_OF_WEEK_BASED_YEAR_FIELD;
    }(Field), WEEK_BASED_YEAR_FIELD = function(_Field4) {
      function WEEK_BASED_YEAR_FIELD() {
        return _Field4.apply(this, arguments) || this;
      }
      _inheritsLoose(WEEK_BASED_YEAR_FIELD, _Field4);
      var _proto5 = WEEK_BASED_YEAR_FIELD.prototype;
      _proto5.toString = function() {
        return "WeekBasedYear";
      };
      _proto5.baseUnit = function() {
        return WEEK_BASED_YEARS;
      };
      _proto5.rangeUnit = function() {
        return ChronoUnit.FOREVER;
      };
      _proto5.range = function() {
        return ChronoField.YEAR.range();
      };
      _proto5.isSupportedBy = function(temporal) {
        return temporal.isSupported(ChronoField.EPOCH_DAY) && this._isIso(temporal);
      };
      _proto5.rangeRefinedBy = function(temporal) {
        return ChronoField.YEAR.range();
      };
      _proto5.getFrom = function(temporal) {
        if (!1 === temporal.isSupported(this)) {
          throw new UnsupportedTemporalTypeException("Unsupported field: WeekBasedYear");
        }
        return Field._getWeekBasedYear(LocalDate.from(temporal));
      };
      _proto5.adjustInto = function(temporal, newValue) {
        if (!1 === this.isSupportedBy(temporal)) {
          throw new UnsupportedTemporalTypeException("Unsupported field: WeekBasedYear");
        }
        var newWby = this.range().checkValidIntValue(newValue, WEEK_BASED_YEAR), date = LocalDate.from(temporal);
        newValue = date.get(ChronoField.DAY_OF_WEEK);
        date = Field._getWeek(date);
        53 === date && 52 === Field._getWeekRangeByYear(newWby) && (date = 52);
        newWby = LocalDate.of(newWby, 1, 4);
        newValue = newValue - newWby.get(ChronoField.DAY_OF_WEEK) + 7 * (date - 1);
        newWby = newWby.plusDays(newValue);
        return temporal.with(newWby);
      };
      return WEEK_BASED_YEAR_FIELD;
    }(Field), Unit = function(_TemporalUnit) {
      function Unit(name, estimatedDuration) {
        var _this = _TemporalUnit.call(this) || this;
        _this._name = name;
        _this._duration = estimatedDuration;
        return _this;
      }
      _inheritsLoose(Unit, _TemporalUnit);
      var _proto6 = Unit.prototype;
      _proto6.duration = function() {
        return this._duration;
      };
      _proto6.isDurationEstimated = function() {
        return !0;
      };
      _proto6.isDateBased = function() {
        return !0;
      };
      _proto6.isTimeBased = function() {
        return !1;
      };
      _proto6.isSupportedBy = function(temporal) {
        return temporal.isSupported(ChronoField.EPOCH_DAY);
      };
      _proto6.addTo = function(temporal, periodToAdd) {
        switch(this) {
          case WEEK_BASED_YEARS:
            return periodToAdd = MathUtil.safeAdd(temporal.get(WEEK_BASED_YEAR), periodToAdd), temporal.with(WEEK_BASED_YEAR, periodToAdd);
          case QUARTER_YEARS:
            return temporal.plus(MathUtil.intDiv(periodToAdd, 256), ChronoUnit.YEARS).plus(3 * MathUtil.intMod(periodToAdd, 256), ChronoUnit.MONTHS);
          default:
            throw new IllegalStateException("Unreachable");
        }
      };
      _proto6.between = function(temporal1, temporal2) {
        switch(this) {
          case WEEK_BASED_YEARS:
            return MathUtil.safeSubtract(temporal2.getLong(WEEK_BASED_YEAR), temporal1.getLong(WEEK_BASED_YEAR));
          case QUARTER_YEARS:
            return MathUtil.intDiv(temporal1.until(temporal2, ChronoUnit.MONTHS), 3);
          default:
            throw new IllegalStateException("Unreachable");
        }
      };
      _proto6.toString = function() {
        return name;
      };
      return Unit;
    }(TemporalUnit), DAY_OF_QUARTER = null, QUARTER_OF_YEAR = null, WEEK_OF_WEEK_BASED_YEAR = null, WEEK_BASED_YEAR = null, WEEK_BASED_YEARS = null, QUARTER_YEARS = null, DecimalStyle = function() {
      function DecimalStyle(zeroChar, positiveSignChar, negativeSignChar, decimalPointChar) {
        this._zeroDigit = zeroChar;
        this._zeroDigitCharCode = zeroChar.charCodeAt(0);
        this._positiveSign = positiveSignChar;
        this._negativeSign = negativeSignChar;
        this._decimalSeparator = decimalPointChar;
      }
      var _proto = DecimalStyle.prototype;
      _proto.positiveSign = function() {
        return this._positiveSign;
      };
      _proto.withPositiveSign = function(positiveSign) {
        return positiveSign === this._positiveSign ? this : new DecimalStyle(this._zeroDigit, positiveSign, this._negativeSign, this._decimalSeparator);
      };
      _proto.negativeSign = function() {
        return this._negativeSign;
      };
      _proto.withNegativeSign = function(negativeSign) {
        return negativeSign === this._negativeSign ? this : new DecimalStyle(this._zeroDigit, this._positiveSign, negativeSign, this._decimalSeparator);
      };
      _proto.zeroDigit = function() {
        return this._zeroDigit;
      };
      _proto.withZeroDigit = function(zeroDigit) {
        return zeroDigit === this._zeroDigit ? this : new DecimalStyle(zeroDigit, this._positiveSign, this._negativeSign, this._decimalSeparator);
      };
      _proto.decimalSeparator = function() {
        return this._decimalSeparator;
      };
      _proto.withDecimalSeparator = function(decimalSeparator) {
        return decimalSeparator === this._decimalSeparator ? this : new DecimalStyle(this._zeroDigit, this._positiveSign, this._negativeSign, decimalSeparator);
      };
      _proto.convertToDigit = function(char) {
        char = char.charCodeAt(0) - this._zeroDigitCharCode;
        return 0 <= char && 9 >= char ? char : -1;
      };
      _proto.convertNumberToI18N = function(numericText) {
        if ("0" === this._zeroDigit) {
          return numericText;
        }
        for (var diff = this._zeroDigitCharCode - 48, convertedText = "", i = 0; i < numericText.length; i++) {
          convertedText += String.fromCharCode(numericText.charCodeAt(i) + diff);
        }
        return convertedText;
      };
      _proto.equals = function(other) {
        return this === other ? !0 : other instanceof DecimalStyle ? this._zeroDigit === other._zeroDigit && this._positiveSign === other._positiveSign && this._negativeSign === other._negativeSign && this._decimalSeparator === other._decimalSeparator : !1;
      };
      _proto.hashCode = function() {
        return this._zeroDigit + this._positiveSign + this._negativeSign + this._decimalSeparator;
      };
      _proto.toString = function() {
        return "DecimalStyle[" + this._zeroDigit + this._positiveSign + this._negativeSign + this._decimalSeparator + "]";
      };
      DecimalStyle.of = function() {
        throw Error("not yet supported");
      };
      DecimalStyle.availableLocales = function() {
        throw Error("not yet supported");
      };
      return DecimalStyle;
    }();
    DecimalStyle.STANDARD = new DecimalStyle("0", "+", "-", ".");
    var SignStyle = function(_Enum) {
      function SignStyle() {
        return _Enum.apply(this, arguments) || this;
      }
      _inheritsLoose(SignStyle, _Enum);
      SignStyle.prototype.parse = function(positive, strict, fixedWidth) {
        switch(this) {
          case SignStyle.NORMAL:
            return !positive || !strict;
          case SignStyle.ALWAYS:
          case SignStyle.EXCEEDS_PAD:
            return !0;
          default:
            return !strict && !fixedWidth;
        }
      };
      return SignStyle;
    }(Enum);
    SignStyle.NORMAL = new SignStyle("NORMAL");
    SignStyle.NEVER = new SignStyle("NEVER");
    SignStyle.ALWAYS = new SignStyle("ALWAYS");
    SignStyle.EXCEEDS_PAD = new SignStyle("EXCEEDS_PAD");
    SignStyle.NOT_NEGATIVE = new SignStyle("NOT_NEGATIVE");
    var TextStyle = function(_Enum) {
      function TextStyle() {
        return _Enum.apply(this, arguments) || this;
      }
      _inheritsLoose(TextStyle, _Enum);
      var _proto = TextStyle.prototype;
      _proto.isStandalone = function() {
        switch(this) {
          case TextStyle.FULL_STANDALONE:
          case TextStyle.SHORT_STANDALONE:
          case TextStyle.NARROW_STANDALONE:
            return !0;
          default:
            return !1;
        }
      };
      _proto.asStandalone = function() {
        switch(this) {
          case TextStyle.FULL:
            return TextStyle.FULL_STANDALONE;
          case TextStyle.SHORT:
            return TextStyle.SHORT_STANDALONE;
          case TextStyle.NARROW:
            return TextStyle.NARROW_STANDALONE;
          default:
            return this;
        }
      };
      _proto.asNormal = function() {
        switch(this) {
          case TextStyle.FULL_STANDALONE:
            return TextStyle.FULL;
          case TextStyle.SHORT_STANDALONE:
            return TextStyle.SHORT;
          case TextStyle.NARROW_STANDALONE:
            return TextStyle.NARROW;
          default:
            return this;
        }
      };
      return TextStyle;
    }(Enum);
    TextStyle.FULL = new TextStyle("FULL");
    TextStyle.FULL_STANDALONE = new TextStyle("FULL_STANDALONE");
    TextStyle.SHORT = new TextStyle("SHORT");
    TextStyle.SHORT_STANDALONE = new TextStyle("SHORT_STANDALONE");
    TextStyle.NARROW = new TextStyle("NARROW");
    TextStyle.NARROW_STANDALONE = new TextStyle("NARROW_STANDALONE");
    var CharLiteralPrinterParser = function() {
      function CharLiteralPrinterParser(literal) {
        if (1 < literal.length) {
          throw new IllegalArgumentException('invalid literal, too long: "' + literal + '"');
        }
        this._literal = literal;
      }
      var _proto = CharLiteralPrinterParser.prototype;
      _proto.print = function(context, buf) {
        buf.append(this._literal);
        return !0;
      };
      _proto.parse = function(context, text, position) {
        if (position === text.length) {
          return ~position;
        }
        text = text.charAt(position);
        return !1 === context.charEquals(this._literal, text) ? ~position : position + this._literal.length;
      };
      _proto.toString = function() {
        return "'" === this._literal ? "''" : "'" + this._literal + "'";
      };
      return CharLiteralPrinterParser;
    }(), CompositePrinterParser = function() {
      function CompositePrinterParser(printerParsers, optional) {
        this._printerParsers = printerParsers;
        this._optional = optional;
      }
      var _proto = CompositePrinterParser.prototype;
      _proto.withOptional = function(optional) {
        return optional === this._optional ? this : new CompositePrinterParser(this._printerParsers, optional);
      };
      _proto.print = function(context, buf) {
        var length = buf.length();
        this._optional && context.startOptional();
        try {
          for (var i = 0; i < this._printerParsers.length; i++) {
            if (!1 === this._printerParsers[i].print(context, buf)) {
              buf.setLength(length);
              break;
            }
          }
        } finally {
          this._optional && context.endOptional();
        }
        return !0;
      };
      _proto.parse = function(context, text, position) {
        if (this._optional) {
          context.startOptional();
          for (var pos = position, i = 0; i < this._printerParsers.length; i++) {
            if (pos = this._printerParsers[i].parse(context, text, pos), 0 > pos) {
              return context.endOptional(!1), position;
            }
          }
          context.endOptional(!0);
          return pos;
        }
        for (pos = 0; pos < this._printerParsers.length && !(position = this._printerParsers[pos].parse(context, text, position), 0 > position); pos++) {
        }
        return position;
      };
      _proto.toString = function() {
        var buf = "";
        if (null != this._printerParsers) {
          buf += this._optional ? "[" : "(";
          for (var i = 0; i < this._printerParsers.length; i++) {
            buf += this._printerParsers[i].toString();
          }
          buf += this._optional ? "]" : ")";
        }
        return buf;
      };
      return CompositePrinterParser;
    }(), FractionPrinterParser = function() {
      function FractionPrinterParser(field, minWidth, maxWidth, decimalPoint) {
        requireNonNull(field, "field");
        if (!1 === field.range().isFixed()) {
          throw new IllegalArgumentException("Field must have a fixed set of values: " + field);
        }
        if (0 > minWidth || 9 < minWidth) {
          throw new IllegalArgumentException("Minimum width must be from 0 to 9 inclusive but was " + minWidth);
        }
        if (1 > maxWidth || 9 < maxWidth) {
          throw new IllegalArgumentException("Maximum width must be from 1 to 9 inclusive but was " + maxWidth);
        }
        if (maxWidth < minWidth) {
          throw new IllegalArgumentException("Maximum width must exceed or equal the minimum width but " + maxWidth + " \x3c " + minWidth);
        }
        this.field = field;
        this.minWidth = minWidth;
        this.maxWidth = maxWidth;
        this.decimalPoint = decimalPoint;
      }
      var _proto = FractionPrinterParser.prototype;
      _proto.print = function(context, buf) {
        var value = context.getValue(this.field);
        if (null === value) {
          return !1;
        }
        context = context.symbols();
        if (0 === value) {
          if (0 < this.minWidth) {
            for (this.decimalPoint && buf.append(context.decimalSeparator()), value = 0; value < this.minWidth; value++) {
              buf.append(context.zeroDigit());
            }
          }
        } else {
          value = this.convertToFraction(value, context.zeroDigit());
          value = value.substr(0, Math.min(Math.max(value.length, this.minWidth), this.maxWidth));
          if (0 < 1 * value) {
            for (; value.length > this.minWidth && "0" === value[value.length - 1];) {
              value = value.substr(0, value.length - 1);
            }
          }
          value = context.convertNumberToI18N(value);
          this.decimalPoint && buf.append(context.decimalSeparator());
          buf.append(value);
        }
        return !0;
      };
      _proto.parse = function(context, text, position) {
        var effectiveMin = context.isStrict() ? this.minWidth : 0, effectiveMax = context.isStrict() ? this.maxWidth : 9, length = text.length;
        if (position === length) {
          return 0 < effectiveMin ? ~position : position;
        }
        if (this.decimalPoint) {
          if (text[position] !== context.symbols().decimalSeparator()) {
            return 0 < effectiveMin ? ~position : position;
          }
          position++;
        }
        effectiveMin = position + effectiveMin;
        if (effectiveMin > length) {
          return ~position;
        }
        length = Math.min(position + effectiveMax, length);
        var total = 0;
        for (effectiveMax = position; effectiveMax < length;) {
          var ch = text.charAt(effectiveMax++);
          ch = context.symbols().convertToDigit(ch);
          if (0 > ch) {
            if (effectiveMax < effectiveMin) {
              return ~position;
            }
            effectiveMax--;
            break;
          }
          total = 10 * total + ch;
        }
        text = this.convertFromFraction(total, Math.pow(10, effectiveMax - position));
        return context.setParsedField(this.field, text, position, effectiveMax);
      };
      _proto.convertToFraction = function(value, zeroDigit) {
        var range = this.field.range();
        range.checkValidValue(value, this.field);
        var _min = range.minimum();
        range = range.maximum() - _min + 1;
        for (value = "" + MathUtil.intDiv(1000000000 * (value - _min), range); 9 > value.length;) {
          value = zeroDigit + value;
        }
        return value;
      };
      _proto.convertFromFraction = function(total, scale) {
        var range = this.field.range(), _min = range.minimum();
        range = range.maximum() - _min + 1;
        return MathUtil.intDiv(total * range, scale);
      };
      _proto.toString = function() {
        return "Fraction(" + this.field + "," + this.minWidth + "," + this.maxWidth + (this.decimalPoint ? ",DecimalPoint" : "") + ")";
      };
      return FractionPrinterParser;
    }(), EXCEED_POINTS = [0, 10, 100, 1000, 10000, 100000, 1000000, 10000000, 100000000, 1000000000], NumberPrinterParser = function() {
      function NumberPrinterParser(field, minWidth, maxWidth, signStyle, subsequentWidth) {
        void 0 === subsequentWidth && (subsequentWidth = 0);
        this._field = field;
        this._minWidth = minWidth;
        this._maxWidth = maxWidth;
        this._signStyle = signStyle;
        this._subsequentWidth = subsequentWidth;
      }
      var _proto = NumberPrinterParser.prototype;
      _proto.field = function() {
        return this._field;
      };
      _proto.minWidth = function() {
        return this._minWidth;
      };
      _proto.maxWidth = function() {
        return this._maxWidth;
      };
      _proto.signStyle = function() {
        return this._signStyle;
      };
      _proto.withFixedWidth = function() {
        return -1 === this._subsequentWidth ? this : new NumberPrinterParser(this._field, this._minWidth, this._maxWidth, this._signStyle, -1);
      };
      _proto.withSubsequentWidth = function(subsequentWidth) {
        return new NumberPrinterParser(this._field, this._minWidth, this._maxWidth, this._signStyle, this._subsequentWidth + subsequentWidth);
      };
      _proto._isFixedWidth = function() {
        return -1 === this._subsequentWidth || 0 < this._subsequentWidth && this._minWidth === this._maxWidth && this._signStyle === SignStyle.NOT_NEGATIVE;
      };
      _proto.print = function(context, buf) {
        var contextValue = context.getValue(this._field);
        if (null == contextValue) {
          return !1;
        }
        contextValue = this._getValue(context, contextValue);
        context = context.symbols();
        var str = "" + Math.abs(contextValue);
        if (str.length > this._maxWidth) {
          throw new DateTimeException("Field " + this._field + " cannot be printed as the value " + contextValue + " exceeds the maximum print width of " + this._maxWidth);
        }
        str = context.convertNumberToI18N(str);
        if (0 <= contextValue) {
          switch(this._signStyle) {
            case SignStyle.EXCEEDS_PAD:
              15 > this._minWidth && contextValue >= EXCEED_POINTS[this._minWidth] && buf.append(context.positiveSign());
              break;
            case SignStyle.ALWAYS:
              buf.append(context.positiveSign());
          }
        } else {
          switch(this._signStyle) {
            case SignStyle.NORMAL:
            case SignStyle.EXCEEDS_PAD:
            case SignStyle.ALWAYS:
              buf.append(context.negativeSign());
              break;
            case SignStyle.NOT_NEGATIVE:
              throw new DateTimeException("Field " + this._field + " cannot be printed as the value " + contextValue + " cannot be negative according to the SignStyle");
          }
        }
        for (contextValue = 0; contextValue < this._minWidth - str.length; contextValue++) {
          buf.append(context.zeroDigit());
        }
        buf.append(str);
        return !0;
      };
      _proto.parse = function(context, text, position) {
        var length = text.length;
        if (position === length) {
          return ~position;
        }
        assert(0 <= position && position < length);
        var sign = text.charAt(position), negative = !1, positive = !1;
        if (sign === context.symbols().positiveSign()) {
          if (!1 === this._signStyle.parse(!0, context.isStrict(), this._minWidth === this._maxWidth)) {
            return ~position;
          }
          positive = !0;
          position++;
        } else if (sign === context.symbols().negativeSign()) {
          if (!1 === this._signStyle.parse(!1, context.isStrict(), this._minWidth === this._maxWidth)) {
            return ~position;
          }
          negative = !0;
          position++;
        } else {
          if (this._signStyle === SignStyle.ALWAYS && context.isStrict()) {
            return ~position;
          }
        }
        var effMinWidth = context.isStrict() || this._isFixedWidth() ? this._minWidth : 1, minEndPos = position + effMinWidth;
        if (minEndPos > length) {
          return ~position;
        }
        var effMaxWidth = (context.isStrict() || this._isFixedWidth() ? this._maxWidth : 9) + Math.max(this._subsequentWidth, 0);
        sign = 0;
        for (var pos = position, pass = 0; 2 > pass; pass++) {
          for (effMaxWidth = Math.min(pos + effMaxWidth, length); pos < effMaxWidth;) {
            var ch = text.charAt(pos++);
            ch = context.symbols().convertToDigit(ch);
            if (0 > ch) {
              pos--;
              if (pos < minEndPos) {
                return ~position;
              }
              break;
            }
            if (15 < pos - position) {
              throw new ArithmeticException("number text exceeds length");
            }
            sign = 10 * sign + ch;
          }
          if (0 < this._subsequentWidth && 0 === pass) {
            effMaxWidth = Math.max(effMinWidth, pos - position - this._subsequentWidth), pos = position, sign = 0;
          } else {
            break;
          }
        }
        if (negative) {
          if (0 === sign && context.isStrict()) {
            return ~(position - 1);
          }
          0 !== sign && (sign = -sign);
        } else if (this._signStyle === SignStyle.EXCEEDS_PAD && context.isStrict()) {
          if (text = pos - position, positive) {
            if (text <= this._minWidth) {
              return ~(position - 1);
            }
          } else {
            if (text > this._minWidth) {
              return ~position;
            }
          }
        }
        return this._setValue(context, sign, position, pos);
      };
      _proto._getValue = function(context, value) {
        return value;
      };
      _proto._setValue = function(context, value, errorPos, successPos) {
        return context.setParsedField(this._field, value, errorPos, successPos);
      };
      _proto.toString = function() {
        return 1 === this._minWidth && 15 === this._maxWidth && this._signStyle === SignStyle.NORMAL ? "Value(" + this._field + ")" : this._minWidth === this._maxWidth && this._signStyle === SignStyle.NOT_NEGATIVE ? "Value(" + this._field + "," + this._minWidth + ")" : "Value(" + this._field + "," + this._minWidth + "," + this._maxWidth + "," + this._signStyle + ")";
      };
      return NumberPrinterParser;
    }(), ReducedPrinterParser = function(_NumberPrinterParser) {
      function ReducedPrinterParser(field, width, maxWidth, baseValue, baseDate) {
        var _this = _NumberPrinterParser.call(this, field, width, maxWidth, SignStyle.NOT_NEGATIVE) || this;
        if (1 > width || 10 < width) {
          throw new IllegalArgumentException("The width must be from 1 to 10 inclusive but was " + width);
        }
        if (1 > maxWidth || 10 < maxWidth) {
          throw new IllegalArgumentException("The maxWidth must be from 1 to 10 inclusive but was " + maxWidth);
        }
        if (maxWidth < width) {
          throw new IllegalArgumentException("The maxWidth must be greater than the width");
        }
        if (null === baseDate) {
          if (!1 === field.range().isValidValue(baseValue)) {
            throw new IllegalArgumentException("The base value must be within the range of the field");
          }
          if (baseValue + EXCEED_POINTS[width] > MathUtil.MAX_SAFE_INTEGER) {
            throw new DateTimeException("Unable to add printer-parser as the range exceeds the capacity of an int");
          }
        }
        _this._baseValue = baseValue;
        _this._baseDate = baseDate;
        return _this;
      }
      _inheritsLoose(ReducedPrinterParser, _NumberPrinterParser);
      var _proto2 = ReducedPrinterParser.prototype;
      _proto2._getValue = function(context, value) {
        var absValue = Math.abs(value), baseValue = this._baseValue;
        null !== this._baseDate && (context.temporal(), baseValue = IsoChronology.INSTANCE.date(this._baseDate).get(this._field));
        return value >= baseValue && value < baseValue + EXCEED_POINTS[this._minWidth] ? absValue % EXCEED_POINTS[this._minWidth] : absValue % EXCEED_POINTS[this._maxWidth];
      };
      _proto2._setValue = function(context, value, errorPos, successPos) {
        var baseValue = this._baseValue;
        null != this._baseDate && (baseValue = context.getEffectiveChronology().date(this._baseDate).get(this._field));
        if (successPos - errorPos === this._minWidth && 0 <= value) {
          var range = EXCEED_POINTS[this._minWidth], basePart = baseValue - baseValue % range;
          value = 0 < baseValue ? basePart + value : basePart - value;
          value < baseValue && (value += range);
        }
        return context.setParsedField(this._field, value, errorPos, successPos);
      };
      _proto2.withFixedWidth = function() {
        return -1 === this._subsequentWidth ? this : new ReducedPrinterParser(this._field, this._minWidth, this._maxWidth, this._baseValue, this._baseDate, -1);
      };
      _proto2.withSubsequentWidth = function(subsequentWidth) {
        return new ReducedPrinterParser(this._field, this._minWidth, this._maxWidth, this._baseValue, this._baseDate, this._subsequentWidth + subsequentWidth);
      };
      _proto2.isFixedWidth = function(context) {
        return !1 === context.isStrict() ? !1 : _NumberPrinterParser.prototype.isFixedWidth.call(this, context);
      };
      _proto2.toString = function() {
        return "ReducedValue(" + this._field + "," + this._minWidth + "," + this._maxWidth + "," + (null != this._baseDate ? this._baseDate : this._baseValue) + ")";
      };
      return ReducedPrinterParser;
    }(NumberPrinterParser), PATTERNS = "+HH +HHmm +HH:mm +HHMM +HH:MM +HHMMss +HH:MM:ss +HHMMSS +HH:MM:SS".split(" "), OffsetIdPrinterParser = function() {
      function OffsetIdPrinterParser(noOffsetText, pattern) {
        requireNonNull(noOffsetText, "noOffsetText");
        requireNonNull(pattern, "pattern");
        this.noOffsetText = noOffsetText;
        this.type = this._checkPattern(pattern);
      }
      var _proto = OffsetIdPrinterParser.prototype;
      _proto._checkPattern = function(pattern) {
        for (var i = 0; i < PATTERNS.length; i++) {
          if (PATTERNS[i] === pattern) {
            return i;
          }
        }
        throw new IllegalArgumentException("Invalid zone offset pattern: " + pattern);
      };
      _proto.print = function(context, buf) {
        context = context.getValue(ChronoField.OFFSET_SECONDS);
        if (null == context) {
          return !1;
        }
        context = MathUtil.safeToInt(context);
        if (0 === context) {
          buf.append(this.noOffsetText);
        } else {
          var absHours = Math.abs(MathUtil.intMod(MathUtil.intDiv(context, 3600), 100)), absMinutes = Math.abs(MathUtil.intMod(MathUtil.intDiv(context, 60), 60)), absSeconds = Math.abs(MathUtil.intMod(context, 60)), bufPos = buf.length(), output = absHours;
          buf.append(0 > context ? "-" : "+").appendChar(MathUtil.intDiv(absHours, 10) + "0").appendChar(MathUtil.intMod(absHours, 10) + "0");
          if (3 <= this.type || 1 <= this.type && 0 < absMinutes) {
            if (buf.append(0 === this.type % 2 ? ":" : "").appendChar(MathUtil.intDiv(absMinutes, 10) + "0").appendChar(absMinutes % 10 + "0"), output += absMinutes, 7 <= this.type || 5 <= this.type && 0 < absSeconds) {
              buf.append(0 === this.type % 2 ? ":" : "").appendChar(MathUtil.intDiv(absSeconds, 10) + "0").appendChar(absSeconds % 10 + "0"), output += absSeconds;
            }
          }
          0 === output && (buf.setLength(bufPos), buf.append(this.noOffsetText));
        }
        return !0;
      };
      _proto.parse = function(context, text, position) {
        var length = text.length, noOffsetLen = this.noOffsetText.length;
        if (0 === noOffsetLen) {
          if (position === length) {
            return context.setParsedField(ChronoField.OFFSET_SECONDS, 0, position, position);
          }
        } else {
          if (position === length) {
            return ~position;
          }
          if (context.subSequenceEquals(text, position, this.noOffsetText, 0, noOffsetLen)) {
            return context.setParsedField(ChronoField.OFFSET_SECONDS, 0, position, position + noOffsetLen);
          }
        }
        length = text[position];
        if ("+" === length || "-" === length) {
          var negative = "-" === length ? -1 : 1;
          length = [0, 0, 0, 0];
          length[0] = position + 1;
          if (!1 === (this._parseNumber(length, 1, text, !0) || this._parseNumber(length, 2, text, 3 <= this.type) || this._parseNumber(length, 3, text, !1))) {
            return text = MathUtil.safeZero(negative * (3600 * length[1] + 60 * length[2] + length[3])), context.setParsedField(ChronoField.OFFSET_SECONDS, text, position, length[0]);
          }
        }
        return 0 === noOffsetLen ? context.setParsedField(ChronoField.OFFSET_SECONDS, 0, position, position + noOffsetLen) : ~position;
      };
      _proto._parseNumber = function(array, arrayIndex, parseText, required) {
        if ((this.type + 3) / 2 < arrayIndex) {
          return !1;
        }
        var pos = array[0];
        if (0 === this.type % 2 && 1 < arrayIndex) {
          if (pos + 1 > parseText.length || ":" !== parseText[pos]) {
            return required;
          }
          pos++;
        }
        if (pos + 2 > parseText.length) {
          return required;
        }
        var ch1 = parseText[pos++];
        parseText = parseText[pos++];
        if ("0" > ch1 || "9" < ch1 || "0" > parseText || "9" < parseText) {
          return required;
        }
        ch1 = 10 * (ch1.charCodeAt(0) - 48) + (parseText.charCodeAt(0) - 48);
        if (0 > ch1 || 59 < ch1) {
          return required;
        }
        array[arrayIndex] = ch1;
        array[0] = pos;
        return !1;
      };
      _proto.toString = function() {
        var converted = this.noOffsetText.replace("'", "''");
        return "Offset(" + PATTERNS[this.type] + ",'" + converted + "')";
      };
      return OffsetIdPrinterParser;
    }();
    OffsetIdPrinterParser.INSTANCE_ID = new OffsetIdPrinterParser("Z", "+HH:MM:ss");
    OffsetIdPrinterParser.PATTERNS = PATTERNS;
    var PadPrinterParserDecorator = function() {
      function PadPrinterParserDecorator(printerParser, padWidth, padChar) {
        this._printerParser = printerParser;
        this._padWidth = padWidth;
        this._padChar = padChar;
      }
      var _proto = PadPrinterParserDecorator.prototype;
      _proto.print = function(context, buf) {
        var preLen = buf.length();
        if (!1 === this._printerParser.print(context, buf)) {
          return !1;
        }
        context = buf.length() - preLen;
        if (context > this._padWidth) {
          throw new DateTimeException("Cannot print as output of " + context + " characters exceeds pad width of " + this._padWidth);
        }
        for (var i = 0; i < this._padWidth - context; i++) {
          buf.insert(preLen, this._padChar);
        }
        return !0;
      };
      _proto.parse = function(context, text, position) {
        var strict = context.isStrict(), caseSensitive = context.isCaseSensitive();
        assert(!(position > text.length));
        assert(0 <= position);
        if (position === text.length) {
          return ~position;
        }
        var endPos = position + this._padWidth;
        if (endPos > text.length) {
          if (strict) {
            return ~position;
          }
          endPos = text.length;
        }
        for (var pos = position; pos < endPos && (caseSensitive ? text[pos] === this._padChar : context.charEquals(text[pos], this._padChar));) {
          pos++;
        }
        text = text.substring(0, endPos);
        context = this._printerParser.parse(context, text, pos);
        return context !== endPos && strict ? ~(position + pos) : context;
      };
      _proto.toString = function() {
        return "Pad(" + this._printerParser + "," + this._padWidth + (" " === this._padChar ? ")" : ",'" + this._padChar + "')");
      };
      return PadPrinterParserDecorator;
    }(), SettingsParser = function(_Enum) {
      function SettingsParser() {
        return _Enum.apply(this, arguments) || this;
      }
      _inheritsLoose(SettingsParser, _Enum);
      var _proto = SettingsParser.prototype;
      _proto.print = function() {
        return !0;
      };
      _proto.parse = function(context, text, position) {
        switch(this) {
          case SettingsParser.SENSITIVE:
            context.setCaseSensitive(!0);
            break;
          case SettingsParser.INSENSITIVE:
            context.setCaseSensitive(!1);
            break;
          case SettingsParser.STRICT:
            context.setStrict(!0);
            break;
          case SettingsParser.LENIENT:
            context.setStrict(!1);
        }
        return position;
      };
      _proto.toString = function() {
        switch(this) {
          case SettingsParser.SENSITIVE:
            return "ParseCaseSensitive(true)";
          case SettingsParser.INSENSITIVE:
            return "ParseCaseSensitive(false)";
          case SettingsParser.STRICT:
            return "ParseStrict(true)";
          case SettingsParser.LENIENT:
            return "ParseStrict(false)";
        }
      };
      return SettingsParser;
    }(Enum);
    SettingsParser.SENSITIVE = new SettingsParser("SENSITIVE");
    SettingsParser.INSENSITIVE = new SettingsParser("INSENSITIVE");
    SettingsParser.STRICT = new SettingsParser("STRICT");
    SettingsParser.LENIENT = new SettingsParser("LENIENT");
    var StringLiteralPrinterParser = function() {
      function StringLiteralPrinterParser(literal) {
        this._literal = literal;
      }
      var _proto = StringLiteralPrinterParser.prototype;
      _proto.print = function(context, buf) {
        buf.append(this._literal);
        return !0;
      };
      _proto.parse = function(context, text, position) {
        assert(!(position > text.length || 0 > position));
        return !1 === context.subSequenceEquals(text, position, this._literal, 0, this._literal.length) ? ~position : position + this._literal.length;
      };
      _proto.toString = function() {
        return "'" + this._literal.replace("'", "''") + "'";
      };
      return StringLiteralPrinterParser;
    }(), ZoneRulesProvider = function() {
      function ZoneRulesProvider() {
      }
      ZoneRulesProvider.getRules = function(zoneId) {
        throw new DateTimeException("unsupported ZoneId:" + zoneId);
      };
      ZoneRulesProvider.getAvailableZoneIds = function() {
        return [];
      };
      return ZoneRulesProvider;
    }(), ZoneRegion = function(_ZoneId) {
      function ZoneRegion(id, rules) {
        var _this = _ZoneId.call(this) || this;
        _this._id = id;
        _this._rules = rules;
        return _this;
      }
      _inheritsLoose(ZoneRegion, _ZoneId);
      ZoneRegion.ofId = function(zoneId) {
        var rules = ZoneRulesProvider.getRules(zoneId);
        return new ZoneRegion(zoneId, rules);
      };
      var _proto = ZoneRegion.prototype;
      _proto.id = function() {
        return this._id;
      };
      _proto.rules = function() {
        return this._rules;
      };
      return ZoneRegion;
    }(ZoneId), ZoneIdPrinterParser = function() {
      function ZoneIdPrinterParser(query, description) {
        this.query = query;
        this.description = description;
      }
      var _proto = ZoneIdPrinterParser.prototype;
      _proto.print = function(context, buf) {
        context = context.getValueQuery(this.query);
        if (null == context) {
          return !1;
        }
        buf.append(context.id());
        return !0;
      };
      _proto.parse = function(context, text, position) {
        var length = text.length;
        if (position > length || position === length) {
          return ~position;
        }
        var nextChar = text.charAt(position);
        if ("+" === nextChar || "-" === nextChar) {
          length = context.copy();
          text = OffsetIdPrinterParser.INSTANCE_ID.parse(length, text, position);
          if (0 > text) {
            return text;
          }
          position = length.getParsed(ChronoField.OFFSET_SECONDS);
          position = ZoneOffset.ofTotalSeconds(position);
          context.setParsedZone(position);
          return text;
        }
        if (length >= position + 2) {
          var nextNextChar = text.charAt(position + 1);
          if (context.charEquals(nextChar, "U") && context.charEquals(nextNextChar, "T")) {
            return length >= position + 3 && context.charEquals(text.charAt(position + 2), "C") ? this._parsePrefixedOffset(context, text, position, position + 3) : this._parsePrefixedOffset(context, text, position, position + 2);
          }
          if (context.charEquals(nextChar, "G") && length >= position + 3 && context.charEquals(nextNextChar, "M") && context.charEquals(text.charAt(position + 2), "T")) {
            return this._parsePrefixedOffset(context, text, position, position + 3);
          }
        }
        if ("SYSTEM" === text.substr(position, 6)) {
          return context.setParsedZone(ZoneId.systemDefault()), position + 6;
        }
        if (context.charEquals(nextChar, "Z")) {
          return context.setParsedZone(ZoneOffset.UTC), position + 1;
        }
        nextChar = ZoneRulesProvider.getAvailableZoneIds();
        zoneIdTree.size !== nextChar.length && (zoneIdTree = ZoneIdTree.createTreeMap(nextChar));
        length -= position;
        nextChar = zoneIdTree.treeMap;
        nextNextChar = null;
        for (var parseLength = 0; null != nextChar;) {
          var parsedSubZoneId = text.substr(position, Math.min(nextChar.length, length));
          nextChar = nextChar.get(parsedSubZoneId);
          null != nextChar && nextChar.isLeaf && (nextNextChar = parsedSubZoneId, parseLength = nextChar.length);
        }
        return null != nextNextChar ? (context.setParsedZone(ZoneRegion.ofId(nextNextChar)), position + parseLength) : ~position;
      };
      _proto._parsePrefixedOffset = function(context, text, prefixPos, position) {
        prefixPos = text.substring(prefixPos, position).toUpperCase();
        var newContext = context.copy();
        if (position < text.length && context.charEquals(text.charAt(position), "Z")) {
          return context.setParsedZone(ZoneId.ofOffset(prefixPos, ZoneOffset.UTC)), position;
        }
        text = OffsetIdPrinterParser.INSTANCE_ID.parse(newContext, text, position);
        if (0 > text) {
          return context.setParsedZone(ZoneId.ofOffset(prefixPos, ZoneOffset.UTC)), position;
        }
        position = newContext.getParsed(ChronoField.OFFSET_SECONDS);
        position = ZoneOffset.ofTotalSeconds(position);
        context.setParsedZone(ZoneId.ofOffset(prefixPos, position));
        return text;
      };
      _proto.toString = function() {
        return this.description;
      };
      return ZoneIdPrinterParser;
    }(), ZoneIdTree = function() {
      function ZoneIdTree(size, treeMap) {
        this.size = size;
        this.treeMap = treeMap;
      }
      ZoneIdTree.createTreeMap = function(availableZoneIds) {
        availableZoneIds = availableZoneIds.sort(function(a, b) {
          return a.length - b.length;
        });
        for (var treeMap = new ZoneIdTreeMap(availableZoneIds[0].length, !1), i = 0; i < availableZoneIds.length; i++) {
          treeMap.add(availableZoneIds[i]);
        }
        return new ZoneIdTree(availableZoneIds.length, treeMap);
      };
      return ZoneIdTree;
    }(), ZoneIdTreeMap = function() {
      function ZoneIdTreeMap(length, isLeaf) {
        void 0 === length && (length = 0);
        void 0 === isLeaf && (isLeaf = !1);
        this.length = length;
        this.isLeaf = isLeaf;
        this._treeMap = {};
      }
      var _proto2 = ZoneIdTreeMap.prototype;
      _proto2.add = function(zoneId) {
        var idLength = zoneId.length;
        if (idLength === this.length) {
          this._treeMap[zoneId] = new ZoneIdTreeMap(idLength, !0);
        } else if (idLength > this.length) {
          var subZoneId = zoneId.substr(0, this.length), subTreeMap = this._treeMap[subZoneId];
          null == subTreeMap && (subTreeMap = new ZoneIdTreeMap(idLength, !1), this._treeMap[subZoneId] = subTreeMap);
          subTreeMap.add(zoneId);
        }
      };
      _proto2.get = function(zoneId) {
        return this._treeMap[zoneId];
      };
      return ZoneIdTreeMap;
    }(), zoneIdTree = new ZoneIdTree([]), DateTimeFormatterBuilder = function() {
      function DateTimeFormatterBuilder() {
        this._active = this;
        this._parent = null;
        this._printerParsers = [];
        this._optional = !1;
        this._padNextWidth = 0;
        this._padNextChar = null;
        this._valueParserIndex = -1;
      }
      DateTimeFormatterBuilder._of = function(parent, optional) {
        requireNonNull(parent, "parent");
        requireNonNull(optional, "optional");
        var dtFormatterBuilder = new DateTimeFormatterBuilder();
        dtFormatterBuilder._parent = parent;
        dtFormatterBuilder._optional = optional;
        return dtFormatterBuilder;
      };
      var _proto = DateTimeFormatterBuilder.prototype;
      _proto.parseCaseSensitive = function() {
        this._appendInternalPrinterParser(SettingsParser.SENSITIVE);
        return this;
      };
      _proto.parseCaseInsensitive = function() {
        this._appendInternalPrinterParser(SettingsParser.INSENSITIVE);
        return this;
      };
      _proto.parseStrict = function() {
        this._appendInternalPrinterParser(SettingsParser.STRICT);
        return this;
      };
      _proto.parseLenient = function() {
        this._appendInternalPrinterParser(SettingsParser.LENIENT);
        return this;
      };
      _proto.appendValue = function() {
        return 1 === arguments.length ? this._appendValue1.apply(this, arguments) : 2 === arguments.length ? this._appendValue2.apply(this, arguments) : this._appendValue4.apply(this, arguments);
      };
      _proto._appendValue1 = function(field) {
        requireNonNull(field);
        this._appendValuePrinterParser(new NumberPrinterParser(field, 1, 15, SignStyle.NORMAL));
        return this;
      };
      _proto._appendValue2 = function(field, width) {
        requireNonNull(field);
        if (1 > width || 15 < width) {
          throw new IllegalArgumentException("The width must be from 1 to 15 inclusive but was " + width);
        }
        field = new NumberPrinterParser(field, width, width, SignStyle.NOT_NEGATIVE);
        this._appendValuePrinterParser(field);
        return this;
      };
      _proto._appendValue4 = function(field, minWidth, maxWidth, signStyle) {
        requireNonNull(field);
        requireNonNull(signStyle);
        if (minWidth === maxWidth && signStyle === SignStyle.NOT_NEGATIVE) {
          return this._appendValue2(field, maxWidth);
        }
        if (1 > minWidth || 15 < minWidth) {
          throw new IllegalArgumentException("The minimum width must be from 1 to 15 inclusive but was " + minWidth);
        }
        if (1 > maxWidth || 15 < maxWidth) {
          throw new IllegalArgumentException("The minimum width must be from 1 to 15 inclusive but was " + maxWidth);
        }
        if (maxWidth < minWidth) {
          throw new IllegalArgumentException("The maximum width must exceed or equal the minimum width but " + maxWidth + " \x3c " + minWidth);
        }
        field = new NumberPrinterParser(field, minWidth, maxWidth, signStyle);
        this._appendValuePrinterParser(field);
        return this;
      };
      _proto.appendValueReduced = function() {
        return 4 === arguments.length && arguments[3] instanceof ChronoLocalDate ? this._appendValueReducedFieldWidthMaxWidthBaseDate.apply(this, arguments) : this._appendValueReducedFieldWidthMaxWidthBaseValue.apply(this, arguments);
      };
      _proto._appendValueReducedFieldWidthMaxWidthBaseValue = function(field, width, maxWidth, baseValue) {
        requireNonNull(field, "field");
        field = new ReducedPrinterParser(field, width, maxWidth, baseValue, null);
        this._appendValuePrinterParser(field);
        return this;
      };
      _proto._appendValueReducedFieldWidthMaxWidthBaseDate = function(field, width, maxWidth, baseDate) {
        requireNonNull(field, "field");
        requireNonNull(baseDate, "baseDate");
        requireInstance(baseDate, ChronoLocalDate, "baseDate");
        field = new ReducedPrinterParser(field, width, maxWidth, 0, baseDate);
        this._appendValuePrinterParser(field);
        return this;
      };
      _proto._appendValuePrinterParser = function(pp) {
        assert(null != pp);
        if (0 <= this._active._valueParserIndex && this._active._printerParsers[this._active._valueParserIndex] instanceof NumberPrinterParser) {
          var activeValueParser = this._active._valueParserIndex, basePP = this._active._printerParsers[activeValueParser];
          pp.minWidth() === pp.maxWidth() && pp.signStyle() === SignStyle.NOT_NEGATIVE ? (basePP = basePP.withSubsequentWidth(pp.maxWidth()), this._appendInternal(pp.withFixedWidth()), this._active._valueParserIndex = activeValueParser) : (basePP = basePP.withFixedWidth(), this._active._valueParserIndex = this._appendInternal(pp));
          this._active._printerParsers[activeValueParser] = basePP;
        } else {
          this._active._valueParserIndex = this._appendInternal(pp);
        }
        return this;
      };
      _proto.appendFraction = function(field, minWidth, maxWidth, decimalPoint) {
        this._appendInternal(new FractionPrinterParser(field, minWidth, maxWidth, decimalPoint));
        return this;
      };
      _proto.appendInstant = function(fractionalDigits) {
        void 0 === fractionalDigits && (fractionalDigits = -2);
        if (-2 > fractionalDigits || 9 < fractionalDigits) {
          throw new IllegalArgumentException("Invalid fractional digits: " + fractionalDigits);
        }
        this._appendInternal(new InstantPrinterParser(fractionalDigits));
        return this;
      };
      _proto.appendOffsetId = function() {
        this._appendInternal(OffsetIdPrinterParser.INSTANCE_ID);
        return this;
      };
      _proto.appendOffset = function(pattern, noOffsetText) {
        this._appendInternalPrinterParser(new OffsetIdPrinterParser(noOffsetText, pattern));
        return this;
      };
      _proto.appendZoneId = function() {
        this._appendInternal(new ZoneIdPrinterParser(TemporalQueries.zoneId(), "ZoneId()"));
        return this;
      };
      _proto.appendPattern = function(pattern) {
        requireNonNull(pattern, "pattern");
        this._parsePattern(pattern);
        return this;
      };
      _proto.appendZoneText = function() {
        throw new IllegalArgumentException("Pattern using (localized) text not implemented, use js-joda-locale plugin!");
      };
      _proto.appendText = function() {
        throw new IllegalArgumentException("Pattern using (localized) text not implemented, use js-joda-locale plugin!");
      };
      _proto.appendLocalizedOffset = function() {
        throw new IllegalArgumentException("Pattern using (localized) text not implemented, use js-joda-locale plugin!");
      };
      _proto.appendWeekField = function() {
        throw new IllegalArgumentException("Pattern using (localized) text not implemented, use js-joda-locale plugin!");
      };
      _proto._parsePattern = function(pattern) {
        for (var FIELD_MAP = {G:ChronoField.ERA, y:ChronoField.YEAR_OF_ERA, u:ChronoField.YEAR, Q:IsoFields.QUARTER_OF_YEAR, q:IsoFields.QUARTER_OF_YEAR, M:ChronoField.MONTH_OF_YEAR, L:ChronoField.MONTH_OF_YEAR, D:ChronoField.DAY_OF_YEAR, d:ChronoField.DAY_OF_MONTH, F:ChronoField.ALIGNED_DAY_OF_WEEK_IN_MONTH, E:ChronoField.DAY_OF_WEEK, c:ChronoField.DAY_OF_WEEK, e:ChronoField.DAY_OF_WEEK, a:ChronoField.AMPM_OF_DAY, H:ChronoField.HOUR_OF_DAY, k:ChronoField.CLOCK_HOUR_OF_DAY, K:ChronoField.HOUR_OF_AMPM, 
        h:ChronoField.CLOCK_HOUR_OF_AMPM, m:ChronoField.MINUTE_OF_HOUR, s:ChronoField.SECOND_OF_MINUTE, S:ChronoField.NANO_OF_SECOND, A:ChronoField.MILLI_OF_DAY, n:ChronoField.NANO_OF_SECOND, N:ChronoField.NANO_OF_DAY}, pos = 0; pos < pattern.length; pos++) {
          var cur = pattern.charAt(pos);
          if ("A" <= cur && "Z" >= cur || "a" <= cur && "z" >= cur) {
            for (var start = pos++; pos < pattern.length && pattern.charAt(pos) === cur; pos++) {
            }
            start = pos - start;
            if ("p" === cur) {
              var pad = 0;
              if (pos < pattern.length && (cur = pattern.charAt(pos), "A" <= cur && "Z" >= cur || "a" <= cur && "z" >= cur)) {
                pad = start;
                for (start = pos++; pos < pattern.length && pattern.charAt(pos) === cur; pos++) {
                }
                start = pos - start;
              }
              if (0 === pad) {
                throw new IllegalArgumentException("Pad letter 'p' must be followed by valid pad pattern: " + pattern);
              }
              this.padNext(pad);
            }
            pad = FIELD_MAP[cur];
            if (null != pad) {
              this._parseField(cur, start, pad);
            } else if ("z" === cur) {
              if (4 < start) {
                throw new IllegalArgumentException("Too many pattern letters: " + cur);
              }
              4 === start ? this.appendZoneText(TextStyle.FULL) : this.appendZoneText(TextStyle.SHORT);
            } else if ("V" === cur) {
              if (2 !== start) {
                throw new IllegalArgumentException("Pattern letter count must be 2: " + cur);
              }
              this.appendZoneId();
            } else if ("Z" === cur) {
              if (4 > start) {
                this.appendOffset("+HHMM", "+0000");
              } else if (4 === start) {
                this.appendLocalizedOffset(TextStyle.FULL);
              } else if (5 === start) {
                this.appendOffset("+HH:MM:ss", "Z");
              } else {
                throw new IllegalArgumentException("Too many pattern letters: " + cur);
              }
            } else if ("O" === cur) {
              if (1 === start) {
                this.appendLocalizedOffset(TextStyle.SHORT);
              } else if (4 === start) {
                this.appendLocalizedOffset(TextStyle.FULL);
              } else {
                throw new IllegalArgumentException("Pattern letter count must be 1 or 4: " + cur);
              }
            } else if ("X" === cur) {
              if (5 < start) {
                throw new IllegalArgumentException("Too many pattern letters: " + cur);
              }
              this.appendOffset(OffsetIdPrinterParser.PATTERNS[start + (1 === start ? 0 : 1)], "Z");
            } else if ("x" === cur) {
              if (5 < start) {
                throw new IllegalArgumentException("Too many pattern letters: " + cur);
              }
              this.appendOffset(OffsetIdPrinterParser.PATTERNS[start + (1 === start ? 0 : 1)], 1 === start ? "+00" : 0 === start % 2 ? "+0000" : "+00:00");
            } else if ("W" === cur) {
              if (1 < start) {
                throw new IllegalArgumentException("Too many pattern letters: " + cur);
              }
              this.appendWeekField("W", start);
            } else if ("w" === cur) {
              if (2 < start) {
                throw new IllegalArgumentException("Too many pattern letters: " + cur);
              }
              this.appendWeekField("w", start);
            } else if ("Y" === cur) {
              this.appendWeekField("Y", start);
            } else {
              throw new IllegalArgumentException("Unknown pattern letter: " + cur);
            }
            pos--;
          } else if ("'" === cur) {
            for (cur = pos++; pos < pattern.length; pos++) {
              if ("'" === pattern.charAt(pos)) {
                if (pos + 1 < pattern.length && "'" === pattern.charAt(pos + 1)) {
                  pos++;
                } else {
                  break;
                }
              }
            }
            if (pos >= pattern.length) {
              throw new IllegalArgumentException("Pattern ends with an incomplete string literal: " + pattern);
            }
            cur = pattern.substring(cur + 1, pos);
            0 === cur.length ? this.appendLiteral("'") : this.appendLiteral(cur.replace("''", "'"));
          } else if ("[" === cur) {
            this.optionalStart();
          } else if ("]" === cur) {
            if (null === this._active._parent) {
              throw new IllegalArgumentException("Pattern invalid as it contains ] without previous [");
            }
            this.optionalEnd();
          } else {
            if ("{" === cur || "}" === cur || "#" === cur) {
              throw new IllegalArgumentException("Pattern includes reserved character: '" + cur + "'");
            }
            this.appendLiteral(cur);
          }
        }
      };
      _proto._parseField = function(cur, count, field) {
        switch(cur) {
          case "u":
          case "y":
            2 === count ? this.appendValueReduced(field, 2, 2, ReducedPrinterParser.BASE_DATE) : 4 > count ? this.appendValue(field, count, 15, SignStyle.NORMAL) : this.appendValue(field, count, 15, SignStyle.EXCEEDS_PAD);
            break;
          case "M":
          case "Q":
            switch(count) {
              case 1:
                this.appendValue(field);
                break;
              case 2:
                this.appendValue(field, 2);
                break;
              case 3:
                this.appendText(field, TextStyle.SHORT);
                break;
              case 4:
                this.appendText(field, TextStyle.FULL);
                break;
              case 5:
                this.appendText(field, TextStyle.NARROW);
                break;
              default:
                throw new IllegalArgumentException("Too many pattern letters: " + cur);
            }break;
          case "L":
          case "q":
            switch(count) {
              case 1:
                this.appendValue(field);
                break;
              case 2:
                this.appendValue(field, 2);
                break;
              case 3:
                this.appendText(field, TextStyle.SHORT_STANDALONE);
                break;
              case 4:
                this.appendText(field, TextStyle.FULL_STANDALONE);
                break;
              case 5:
                this.appendText(field, TextStyle.NARROW_STANDALONE);
                break;
              default:
                throw new IllegalArgumentException("Too many pattern letters: " + cur);
            }break;
          case "e":
            switch(count) {
              case 1:
              case 2:
                this.appendWeekField("e", count);
                break;
              case 3:
                this.appendText(field, TextStyle.SHORT);
                break;
              case 4:
                this.appendText(field, TextStyle.FULL);
                break;
              case 5:
                this.appendText(field, TextStyle.NARROW);
                break;
              default:
                throw new IllegalArgumentException("Too many pattern letters: " + cur);
            }break;
          case "c":
            switch(count) {
              case 1:
                this.appendWeekField("c", count);
                break;
              case 2:
                throw new IllegalArgumentException("Invalid number of pattern letters: " + cur);
              case 3:
                this.appendText(field, TextStyle.SHORT_STANDALONE);
                break;
              case 4:
                this.appendText(field, TextStyle.FULL_STANDALONE);
                break;
              case 5:
                this.appendText(field, TextStyle.NARROW_STANDALONE);
                break;
              default:
                throw new IllegalArgumentException("Too many pattern letters: " + cur);
            }break;
          case "a":
            if (1 === count) {
              this.appendText(field, TextStyle.SHORT);
            } else {
              throw new IllegalArgumentException("Too many pattern letters: " + cur);
            }
            break;
          case "E":
          case "G":
            switch(count) {
              case 1:
              case 2:
              case 3:
                this.appendText(field, TextStyle.SHORT);
                break;
              case 4:
                this.appendText(field, TextStyle.FULL);
                break;
              case 5:
                this.appendText(field, TextStyle.NARROW);
                break;
              default:
                throw new IllegalArgumentException("Too many pattern letters: " + cur);
            }break;
          case "S":
            this.appendFraction(ChronoField.NANO_OF_SECOND, count, count, !1);
            break;
          case "F":
            if (1 === count) {
              this.appendValue(field);
            } else {
              throw new IllegalArgumentException("Too many pattern letters: " + cur);
            }
            break;
          case "d":
          case "h":
          case "H":
          case "k":
          case "K":
          case "m":
          case "s":
            if (1 === count) {
              this.appendValue(field);
            } else if (2 === count) {
              this.appendValue(field, count);
            } else {
              throw new IllegalArgumentException("Too many pattern letters: " + cur);
            }
            break;
          case "D":
            if (1 === count) {
              this.appendValue(field);
            } else if (3 >= count) {
              this.appendValue(field, count);
            } else {
              throw new IllegalArgumentException("Too many pattern letters: " + cur);
            }
            break;
          default:
            1 === count ? this.appendValue(field) : this.appendValue(field, count);
        }
      };
      _proto.padNext = function() {
        return 1 === arguments.length ? this._padNext1.apply(this, arguments) : this._padNext2.apply(this, arguments);
      };
      _proto._padNext1 = function(padWidth) {
        return this._padNext2(padWidth, " ");
      };
      _proto._padNext2 = function(padWidth, padChar) {
        if (1 > padWidth) {
          throw new IllegalArgumentException("The pad width must be at least one but was " + padWidth);
        }
        this._active._padNextWidth = padWidth;
        this._active._padNextChar = padChar;
        this._active._valueParserIndex = -1;
        return this;
      };
      _proto.optionalStart = function() {
        this._active._valueParserIndex = -1;
        this._active = DateTimeFormatterBuilder._of(this._active, !0);
        return this;
      };
      _proto.optionalEnd = function() {
        if (null == this._active._parent) {
          throw new IllegalStateException("Cannot call optionalEnd() as there was no previous call to optionalStart()");
        }
        if (0 < this._active._printerParsers.length) {
          var cpp = new CompositePrinterParser(this._active._printerParsers, this._active._optional);
          this._active = this._active._parent;
          this._appendInternal(cpp);
        } else {
          this._active = this._active._parent;
        }
        return this;
      };
      _proto._appendInternal = function(pp) {
        assert(null != pp);
        0 < this._active._padNextWidth && (null != pp && (pp = new PadPrinterParserDecorator(pp, this._active._padNextWidth, this._active._padNextChar)), this._active._padNextWidth = 0, this._active._padNextChar = 0);
        this._active._printerParsers.push(pp);
        this._active._valueParserIndex = -1;
        return this._active._printerParsers.length - 1;
      };
      _proto.appendLiteral = function(literal) {
        assert(null != literal);
        0 < literal.length && (1 === literal.length ? this._appendInternalPrinterParser(new CharLiteralPrinterParser(literal.charAt(0))) : this._appendInternalPrinterParser(new StringLiteralPrinterParser(literal)));
        return this;
      };
      _proto._appendInternalPrinterParser = function(pp) {
        assert(null != pp);
        0 < this._active._padNextWidth && (null != pp && (pp = new PadPrinterParserDecorator(pp, this._active._padNextWidth, this._active._padNextChar)), this._active._padNextWidth = 0, this._active._padNextChar = 0);
        this._active._printerParsers.push(pp);
        this._active._valueParserIndex = -1;
        return this._active._printerParsers.length - 1;
      };
      _proto.append = function(formatter) {
        requireNonNull(formatter, "formatter");
        this._appendInternal(formatter._toPrinterParser(!1));
        return this;
      };
      _proto.toFormatter = function(resolverStyle) {
        void 0 === resolverStyle && (resolverStyle = ResolverStyle.SMART);
        for (; null != this._active._parent;) {
          this.optionalEnd();
        }
        var pp = new CompositePrinterParser(this._printerParsers, !1);
        return new DateTimeFormatter(pp, null, DecimalStyle.STANDARD, resolverStyle, null, null, null);
      };
      return DateTimeFormatterBuilder;
    }(), InstantPrinterParser = function() {
      function InstantPrinterParser(fractionalDigits) {
        this.fractionalDigits = fractionalDigits;
      }
      var _proto2 = InstantPrinterParser.prototype;
      _proto2.print = function(context, buf) {
        var inSecs = context.getValue(ChronoField.INSTANT_SECONDS), inNanos = 0;
        context.temporal().isSupported(ChronoField.NANO_OF_SECOND) && (inNanos = context.temporal().getLong(ChronoField.NANO_OF_SECOND));
        if (null == inSecs) {
          return !1;
        }
        context = ChronoField.NANO_OF_SECOND.checkValidIntValue(inNanos);
        if (-62167219200 <= inSecs) {
          inNanos = inSecs - 31556952E4 + 62167219200, inSecs = MathUtil.floorDiv(inNanos, 31556952E4) + 1, inNanos = MathUtil.floorMod(inNanos, 31556952E4), inNanos = LocalDateTime.ofEpochSecond(inNanos - 62167219200, 0, ZoneOffset.UTC), 0 < inSecs && buf.append("+").append(inSecs), buf.append(inNanos), 0 === inNanos.second() && buf.append(":00");
        } else {
          inNanos = inSecs + 62167219200;
          inSecs = MathUtil.intDiv(inNanos, 31556952E4);
          inNanos = MathUtil.intMod(inNanos, 31556952E4);
          var _ldt = LocalDateTime.ofEpochSecond(inNanos - 62167219200, 0, ZoneOffset.UTC), pos = buf.length();
          buf.append(_ldt);
          0 === _ldt.second() && buf.append(":00");
          0 > inSecs && (-10000 === _ldt.year() ? buf.replace(pos, pos + 2, "" + (inSecs - 1)) : 0 === inNanos ? buf.insert(pos, inSecs) : buf.insert(pos + 1, Math.abs(inSecs)));
        }
        if (-2 === this.fractionalDigits) {
          0 !== context && (buf.append("."), 0 === MathUtil.intMod(context, 1000000) ? buf.append(("" + (MathUtil.intDiv(context, 1000000) + 1000)).substring(1)) : 0 === MathUtil.intMod(context, 1000) ? buf.append(("" + (MathUtil.intDiv(context, 1000) + 1000000)).substring(1)) : buf.append(("" + (context + 1000000000)).substring(1)));
        } else if (0 < this.fractionalDigits || -1 === this.fractionalDigits && 0 < context) {
          for (buf.append("."), inSecs = 100000000, inNanos = 0; -1 === this.fractionalDigits && 0 < context || inNanos < this.fractionalDigits; inNanos++) {
            _ldt = MathUtil.intDiv(context, inSecs), buf.append(_ldt), context -= _ldt * inSecs, inSecs = MathUtil.intDiv(inSecs, 10);
          }
        }
        buf.append("Z");
        return !0;
      };
      _proto2.parse = function(context, text, position) {
        var newContext = context.copy(), minDigits = 0 > this.fractionalDigits ? 0 : this.fractionalDigits, maxDigits = 0 > this.fractionalDigits ? 9 : this.fractionalDigits;
        text = (new DateTimeFormatterBuilder()).append(DateTimeFormatter.ISO_LOCAL_DATE).appendLiteral("T").appendValue(ChronoField.HOUR_OF_DAY, 2).appendLiteral(":").appendValue(ChronoField.MINUTE_OF_HOUR, 2).appendLiteral(":").appendValue(ChronoField.SECOND_OF_MINUTE, 2).appendFraction(ChronoField.NANO_OF_SECOND, minDigits, maxDigits, !0).appendLiteral("Z").toFormatter()._toPrinterParser(!1).parse(newContext, text, position);
        if (0 > text) {
          return text;
        }
        minDigits = newContext.getParsed(ChronoField.YEAR);
        maxDigits = newContext.getParsed(ChronoField.MONTH_OF_YEAR);
        var day = newContext.getParsed(ChronoField.DAY_OF_MONTH), hour = newContext.getParsed(ChronoField.HOUR_OF_DAY), min = newContext.getParsed(ChronoField.MINUTE_OF_HOUR), secVal = newContext.getParsed(ChronoField.SECOND_OF_MINUTE), nanoVal = newContext.getParsed(ChronoField.NANO_OF_SECOND);
        newContext = null != secVal ? secVal : 0;
        secVal = null != nanoVal ? nanoVal : 0;
        nanoVal = MathUtil.intMod(minDigits, 10000);
        var days = 0;
        24 === hour && 0 === min && 0 === newContext && 0 === secVal ? (hour = 0, days = 1) : 23 === hour && 59 === min && 60 === newContext && (context.setParsedLeapSecond(), newContext = 59);
        try {
          var instantSecs = LocalDateTime.of(nanoVal, maxDigits, day, hour, min, newContext, 0).plusDays(days).toEpochSecond(ZoneOffset.UTC);
          instantSecs += MathUtil.safeMultiply(MathUtil.intDiv(minDigits, 10000), 31556952E4);
        } catch (ex) {
          return ~position;
        }
        instantSecs = context.setParsedField(ChronoField.INSTANT_SECONDS, instantSecs, position, text);
        return context.setParsedField(ChronoField.NANO_OF_SECOND, secVal, position, instantSecs);
      };
      _proto2.toString = function() {
        return "Instant()";
      };
      return InstantPrinterParser;
    }(), StringBuilder = function() {
      function StringBuilder() {
        this._str = "";
      }
      var _proto = StringBuilder.prototype;
      _proto.append = function(str) {
        this._str += str;
        return this;
      };
      _proto.appendChar = function(str) {
        this._str += str[0];
        return this;
      };
      _proto.insert = function(offset, str) {
        this._str = this._str.slice(0, offset) + str + this._str.slice(offset);
        return this;
      };
      _proto.replace = function(start, end, str) {
        this._str = this._str.slice(0, start) + str + this._str.slice(end);
        return this;
      };
      _proto.length = function() {
        return this._str.length;
      };
      _proto.setLength = function(length) {
        this._str = this._str.slice(0, length);
        return this;
      };
      _proto.toString = function() {
        return this._str;
      };
      return StringBuilder;
    }(), DateTimeFormatter = function() {
      function DateTimeFormatter(printerParser, locale, decimalStyle, resolverStyle, resolverFields, chrono, zone) {
        void 0 === chrono && (chrono = IsoChronology.INSTANCE);
        assert(null != printerParser);
        assert(null != decimalStyle);
        assert(null != resolverStyle);
        this._printerParser = printerParser;
        this._locale = locale;
        this._decimalStyle = decimalStyle;
        this._resolverStyle = resolverStyle;
        this._resolverFields = resolverFields;
        this._chrono = chrono;
        this._zone = zone;
      }
      DateTimeFormatter.parsedExcessDays = function() {
        return DateTimeFormatter.PARSED_EXCESS_DAYS;
      };
      DateTimeFormatter.parsedLeapSecond = function() {
        return DateTimeFormatter.PARSED_LEAP_SECOND;
      };
      DateTimeFormatter.ofPattern = function(pattern) {
        return (new DateTimeFormatterBuilder()).appendPattern(pattern).toFormatter();
      };
      var _proto = DateTimeFormatter.prototype;
      _proto.locale = function() {
        return this._locale;
      };
      _proto.decimalStyle = function() {
        return this._decimalStyle;
      };
      _proto.chronology = function() {
        return this._chrono;
      };
      _proto.withChronology = function(chrono) {
        return null != this._chrono && this._chrono.equals(chrono) ? this : new DateTimeFormatter(this._printerParser, this._locale, this._decimalStyle, this._resolverStyle, this._resolverFields, chrono, this._zone);
      };
      _proto.withLocale = function() {
        return this;
      };
      _proto.withResolverStyle = function(resolverStyle) {
        requireNonNull(resolverStyle, "resolverStyle");
        return resolverStyle.equals(this._resolverStyle) ? this : new DateTimeFormatter(this._printerParser, this._locale, this._decimalStyle, resolverStyle, this._resolverFields, this._chrono, this._zone);
      };
      _proto.format = function(temporal) {
        var buf = new StringBuilder(32);
        this._formatTo(temporal, buf);
        return buf.toString();
      };
      _proto._formatTo = function(temporal, appendable) {
        requireNonNull(temporal, "temporal");
        requireNonNull(appendable, "appendable");
        temporal = new DateTimePrintContext(temporal, this);
        this._printerParser.print(temporal, appendable);
      };
      _proto.parse = function(text, type) {
        return 1 === arguments.length ? this.parse1(text) : this.parse2(text, type);
      };
      _proto.parse1 = function(text) {
        requireNonNull(text, "text");
        try {
          return this._parseToBuilder(text, null).resolve(this._resolverStyle, this._resolverFields);
        } catch (ex) {
          if (ex instanceof DateTimeParseException) {
            throw ex;
          }
          throw this._createError(text, ex);
        }
      };
      _proto.parse2 = function(text, type) {
        requireNonNull(text, "text");
        requireNonNull(type, "type");
        try {
          return this._parseToBuilder(text, null).resolve(this._resolverStyle, this._resolverFields).build(type);
        } catch (ex) {
          if (ex instanceof DateTimeParseException) {
            throw ex;
          }
          throw this._createError(text, ex);
        }
      };
      _proto._createError = function(text, ex) {
        var abbr = 64 < text.length ? text.substring(0, 64) + "..." : text;
        return new DateTimeParseException("Text '" + abbr + "' could not be parsed: " + ex.message, text, 0, ex);
      };
      _proto._parseToBuilder = function(text, position) {
        var pos = null != position ? position : new ParsePosition(0), result = this._parseUnresolved0(text, pos);
        if (null == result || 0 <= pos.getErrorIndex() || null == position && pos.getIndex() < text.length) {
          position = 64 < text.length ? text.substr(0, 64).toString() + "..." : text;
          if (0 <= pos.getErrorIndex()) {
            throw new DateTimeParseException("Text '" + position + "' could not be parsed at index " + pos.getErrorIndex(), text, pos.getErrorIndex());
          }
          throw new DateTimeParseException("Text '" + position + "' could not be parsed, unparsed text found at index " + pos.getIndex(), text, pos.getIndex());
        }
        return result.toBuilder();
      };
      _proto.parseUnresolved = function(text, position) {
        return this._parseUnresolved0(text, position);
      };
      _proto._parseUnresolved0 = function(text, position) {
        assert(null != text, "text", NullPointerException);
        assert(null != position, "position", NullPointerException);
        var context = new DateTimeParseContext(this), pos = position.getIndex();
        pos = this._printerParser.parse(context, text, pos);
        if (0 > pos) {
          return position.setErrorIndex(~pos), null;
        }
        position.setIndex(pos);
        return context.toParsed();
      };
      _proto._toPrinterParser = function(optional) {
        return this._printerParser.withOptional(optional);
      };
      _proto.toString = function() {
        var pattern = this._printerParser.toString();
        return 0 === pattern.indexOf("[") ? pattern : pattern.substring(1, pattern.length - 1);
      };
      return DateTimeFormatter;
    }(), MonthDay = function(_TemporalAccessor) {
      function MonthDay(month, dayOfMonth) {
        var _this = _TemporalAccessor.call(this) || this;
        _this._month = MathUtil.safeToInt(month);
        _this._day = MathUtil.safeToInt(dayOfMonth);
        return _this;
      }
      _inheritsLoose(MonthDay, _TemporalAccessor);
      MonthDay.now = function(zoneIdOrClock) {
        return 0 === arguments.length ? MonthDay.now0() : 1 === arguments.length && zoneIdOrClock instanceof ZoneId ? MonthDay.nowZoneId(zoneIdOrClock) : MonthDay.nowClock(zoneIdOrClock);
      };
      MonthDay.now0 = function() {
        return this.nowClock(Clock.systemDefaultZone());
      };
      MonthDay.nowZoneId = function(zone) {
        requireNonNull(zone, "zone");
        return this.nowClock(Clock.system(zone));
      };
      MonthDay.nowClock = function(clock) {
        requireNonNull(clock, "clock");
        clock = LocalDate.now(clock);
        return MonthDay.of(clock.month(), clock.dayOfMonth());
      };
      MonthDay.of = function(monthOrNumber, number) {
        return 2 === arguments.length && monthOrNumber instanceof Month ? MonthDay.ofMonthNumber(monthOrNumber, number) : MonthDay.ofNumberNumber(monthOrNumber, number);
      };
      MonthDay.ofMonthNumber = function(month, dayOfMonth) {
        requireNonNull(month, "month");
        ChronoField.DAY_OF_MONTH.checkValidValue(dayOfMonth);
        if (dayOfMonth > month.maxLength()) {
          throw new DateTimeException("Illegal value for DayOfMonth field, value " + dayOfMonth + " is not valid for month " + month.toString());
        }
        return new MonthDay(month.value(), dayOfMonth);
      };
      MonthDay.ofNumberNumber = function(month, dayOfMonth) {
        requireNonNull(month, "month");
        requireNonNull(dayOfMonth, "dayOfMonth");
        return MonthDay.of(Month.of(month), dayOfMonth);
      };
      MonthDay.from = function(temporal) {
        requireNonNull(temporal, "temporal");
        requireInstance(temporal, TemporalAccessor, "temporal");
        if (temporal instanceof MonthDay) {
          return temporal;
        }
        try {
          return MonthDay.of(temporal.get(ChronoField.MONTH_OF_YEAR), temporal.get(ChronoField.DAY_OF_MONTH));
        } catch (ex) {
          throw new DateTimeException("Unable to obtain MonthDay from TemporalAccessor: " + temporal + ", type " + (temporal && null != temporal.constructor ? temporal.constructor.name : ""));
        }
      };
      MonthDay.parse = function(text, formatter) {
        return 1 === arguments.length ? MonthDay.parseString(text) : MonthDay.parseStringFormatter(text, formatter);
      };
      MonthDay.parseString = function(text) {
        return MonthDay.parseStringFormatter(text, PARSER);
      };
      MonthDay.parseStringFormatter = function(text, formatter) {
        requireNonNull(text, "text");
        requireNonNull(formatter, "formatter");
        requireInstance(formatter, DateTimeFormatter, "formatter");
        return formatter.parse(text, MonthDay.FROM);
      };
      var _proto = MonthDay.prototype;
      _proto.monthValue = function() {
        return this._month;
      };
      _proto.month = function() {
        return Month.of(this._month);
      };
      _proto.dayOfMonth = function() {
        return this._day;
      };
      _proto.isSupported = function(field) {
        return field instanceof ChronoField ? field === ChronoField.MONTH_OF_YEAR || field === ChronoField.DAY_OF_MONTH : null != field && field.isSupportedBy(this);
      };
      _proto.range = function(field) {
        return field === ChronoField.MONTH_OF_YEAR ? field.range() : field === ChronoField.DAY_OF_MONTH ? ValueRange.of(1, this.month().minLength(), this.month().maxLength()) : _TemporalAccessor.prototype.range.call(this, field);
      };
      _proto.get = function(field) {
        return this.range(field).checkValidIntValue(this.getLong(field), field);
      };
      _proto.getLong = function(field) {
        requireNonNull(field, "field");
        if (field instanceof ChronoField) {
          switch(field) {
            case ChronoField.DAY_OF_MONTH:
              return this._day;
            case ChronoField.MONTH_OF_YEAR:
              return this._month;
          }
          throw new UnsupportedTemporalTypeException("Unsupported field: " + field);
        }
        return field.getFrom(this);
      };
      _proto.isValidYear = function(year) {
        return !1 === (29 === this._day && 2 === this._month && !1 === Year.isLeap(year));
      };
      _proto.withMonth = function(month) {
        return this.with(Month.of(month));
      };
      _proto.with = function(month) {
        requireNonNull(month, "month");
        if (month.value() === this._month) {
          return this;
        }
        var day = Math.min(this._day, month.maxLength());
        return new MonthDay(month.value(), day);
      };
      _proto.withDayOfMonth = function(dayOfMonth) {
        return dayOfMonth === this._day ? this : MonthDay.of(this._month, dayOfMonth);
      };
      _proto.query = function(_query) {
        requireNonNull(_query, "query");
        requireInstance(_query, TemporalQuery, "query");
        return _query === TemporalQueries.chronology() ? IsoChronology.INSTANCE : _TemporalAccessor.prototype.query.call(this, _query);
      };
      _proto.adjustInto = function(temporal) {
        requireNonNull(temporal, "temporal");
        temporal = temporal.with(ChronoField.MONTH_OF_YEAR, this._month);
        return temporal.with(ChronoField.DAY_OF_MONTH, Math.min(temporal.range(ChronoField.DAY_OF_MONTH).maximum(), this._day));
      };
      _proto.atYear = function(year) {
        return LocalDate.of(year, this._month, this.isValidYear(year) ? this._day : 28);
      };
      _proto.compareTo = function(other) {
        requireNonNull(other, "other");
        requireInstance(other, MonthDay, "other");
        var cmp = this._month - other.monthValue();
        0 === cmp && (cmp = this._day - other.dayOfMonth());
        return cmp;
      };
      _proto.isAfter = function(other) {
        requireNonNull(other, "other");
        requireInstance(other, MonthDay, "other");
        return 0 < this.compareTo(other);
      };
      _proto.isBefore = function(other) {
        requireNonNull(other, "other");
        requireInstance(other, MonthDay, "other");
        return 0 > this.compareTo(other);
      };
      _proto.equals = function(obj) {
        return this === obj ? !0 : obj instanceof MonthDay ? this.monthValue() === obj.monthValue() && this.dayOfMonth() === obj.dayOfMonth() : !1;
      };
      _proto.toString = function() {
        return "--" + (10 > this._month ? "0" : "") + this._month + (10 > this._day ? "-0" : "-") + this._day;
      };
      _proto.toJSON = function() {
        return this.toString();
      };
      _proto.format = function(formatter) {
        requireNonNull(formatter, "formatter");
        requireInstance(formatter, DateTimeFormatter, "formatter");
        return formatter.format(this);
      };
      return MonthDay;
    }(TemporalAccessor), PARSER, YearMonth = function(_Temporal) {
      function YearMonth(year, month) {
        var _this = _Temporal.call(this) || this;
        _this._year = MathUtil.safeToInt(year);
        _this._month = MathUtil.safeToInt(month);
        return _this;
      }
      _inheritsLoose(YearMonth, _Temporal);
      YearMonth.now = function(zoneIdOrClock) {
        return 0 === arguments.length ? YearMonth.now0() : 1 === arguments.length && zoneIdOrClock instanceof ZoneId ? YearMonth.nowZoneId(zoneIdOrClock) : YearMonth.nowClock(zoneIdOrClock);
      };
      YearMonth.now0 = function() {
        return YearMonth.nowClock(Clock.systemDefaultZone());
      };
      YearMonth.nowZoneId = function(zone) {
        return YearMonth.nowClock(Clock.system(zone));
      };
      YearMonth.nowClock = function(clock) {
        clock = LocalDate.now(clock);
        return YearMonth.of(clock.year(), clock.month());
      };
      YearMonth.of = function(year, monthOrNumber) {
        return 2 === arguments.length && monthOrNumber instanceof Month ? YearMonth.ofNumberMonth(year, monthOrNumber) : YearMonth.ofNumberNumber(year, monthOrNumber);
      };
      YearMonth.ofNumberMonth = function(year, month) {
        requireNonNull(month, "month");
        requireInstance(month, Month, "month");
        return YearMonth.ofNumberNumber(year, month.value());
      };
      YearMonth.ofNumberNumber = function(year, month) {
        requireNonNull(year, "year");
        requireNonNull(month, "month");
        ChronoField.YEAR.checkValidValue(year);
        ChronoField.MONTH_OF_YEAR.checkValidValue(month);
        return new YearMonth(year, month);
      };
      YearMonth.from = function(temporal) {
        requireNonNull(temporal, "temporal");
        if (temporal instanceof YearMonth) {
          return temporal;
        }
        try {
          return YearMonth.of(temporal.get(ChronoField.YEAR), temporal.get(ChronoField.MONTH_OF_YEAR));
        } catch (ex) {
          throw new DateTimeException("Unable to obtain YearMonth from TemporalAccessor: " + temporal + ", type " + (temporal && null != temporal.constructor ? temporal.constructor.name : ""));
        }
      };
      YearMonth.parse = function(text, formatter) {
        return 1 === arguments.length ? YearMonth.parseString(text) : YearMonth.parseStringFormatter(text, formatter);
      };
      YearMonth.parseString = function(text) {
        return YearMonth.parseStringFormatter(text, PARSER$1);
      };
      YearMonth.parseStringFormatter = function(text, formatter) {
        requireNonNull(formatter, "formatter");
        return formatter.parse(text, YearMonth.FROM);
      };
      var _proto = YearMonth.prototype;
      _proto.isSupported = function(fieldOrUnit) {
        return 1 === arguments.length && fieldOrUnit instanceof TemporalField ? this.isSupportedField(fieldOrUnit) : this.isSupportedUnit(fieldOrUnit);
      };
      _proto.isSupportedField = function(field) {
        return field instanceof ChronoField ? field === ChronoField.YEAR || field === ChronoField.MONTH_OF_YEAR || field === ChronoField.PROLEPTIC_MONTH || field === ChronoField.YEAR_OF_ERA || field === ChronoField.ERA : null != field && field.isSupportedBy(this);
      };
      _proto.isSupportedUnit = function(unit) {
        return unit instanceof ChronoUnit ? unit === ChronoUnit.MONTHS || unit === ChronoUnit.YEARS || unit === ChronoUnit.DECADES || unit === ChronoUnit.CENTURIES || unit === ChronoUnit.MILLENNIA || unit === ChronoUnit.ERAS : null != unit && unit.isSupportedBy(this);
      };
      _proto.range = function(field) {
        return field === ChronoField.YEAR_OF_ERA ? 0 >= this.year() ? ValueRange.of(1, Year.MAX_VALUE + 1) : ValueRange.of(1, Year.MAX_VALUE) : _Temporal.prototype.range.call(this, field);
      };
      _proto.get = function(field) {
        requireNonNull(field, "field");
        requireInstance(field, TemporalField, "field");
        return this.range(field).checkValidIntValue(this.getLong(field), field);
      };
      _proto.getLong = function(field) {
        requireNonNull(field, "field");
        requireInstance(field, TemporalField, "field");
        if (field instanceof ChronoField) {
          switch(field) {
            case ChronoField.MONTH_OF_YEAR:
              return this._month;
            case ChronoField.PROLEPTIC_MONTH:
              return this._getProlepticMonth();
            case ChronoField.YEAR_OF_ERA:
              return 1 > this._year ? 1 - this._year : this._year;
            case ChronoField.YEAR:
              return this._year;
            case ChronoField.ERA:
              return 1 > this._year ? 0 : 1;
          }
          throw new UnsupportedTemporalTypeException("Unsupported field: " + field);
        }
        return field.getFrom(this);
      };
      _proto._getProlepticMonth = function() {
        return MathUtil.safeAdd(MathUtil.safeMultiply(this._year, 12), this._month - 1);
      };
      _proto.year = function() {
        return this._year;
      };
      _proto.monthValue = function() {
        return this._month;
      };
      _proto.month = function() {
        return Month.of(this._month);
      };
      _proto.isLeapYear = function() {
        return IsoChronology.isLeapYear(this._year);
      };
      _proto.isValidDay = function(dayOfMonth) {
        return 1 <= dayOfMonth && dayOfMonth <= this.lengthOfMonth();
      };
      _proto.lengthOfMonth = function() {
        return this.month().length(this.isLeapYear());
      };
      _proto.lengthOfYear = function() {
        return this.isLeapYear() ? 366 : 365;
      };
      _proto.with = function(adjusterOrField, value) {
        return 1 === arguments.length ? this.withAdjuster(adjusterOrField) : this.withFieldValue(adjusterOrField, value);
      };
      _proto.withAdjuster = function(adjuster) {
        requireNonNull(adjuster, "adjuster");
        return adjuster.adjustInto(this);
      };
      _proto.withFieldValue = function(field, newValue) {
        requireNonNull(field, "field");
        requireInstance(field, TemporalField, "field");
        if (field instanceof ChronoField) {
          field.checkValidValue(newValue);
          switch(field) {
            case ChronoField.MONTH_OF_YEAR:
              return this.withMonth(newValue);
            case ChronoField.PROLEPTIC_MONTH:
              return this.plusMonths(newValue - this.getLong(ChronoField.PROLEPTIC_MONTH));
            case ChronoField.YEAR_OF_ERA:
              return this.withYear(1 > this._year ? 1 - newValue : newValue);
            case ChronoField.YEAR:
              return this.withYear(newValue);
            case ChronoField.ERA:
              return this.getLong(ChronoField.ERA) === newValue ? this : this.withYear(1 - this._year);
          }
          throw new UnsupportedTemporalTypeException("Unsupported field: " + field);
        }
        return field.adjustInto(this, newValue);
      };
      _proto.withYear = function(year) {
        ChronoField.YEAR.checkValidValue(year);
        return new YearMonth(year, this._month);
      };
      _proto.withMonth = function(month) {
        ChronoField.MONTH_OF_YEAR.checkValidValue(month);
        return new YearMonth(this._year, month);
      };
      _proto.plusAmount = function(amount) {
        requireNonNull(amount, "amount");
        requireInstance(amount, TemporalAmount, "amount");
        return amount.addTo(this);
      };
      _proto.plusAmountUnit = function(amountToAdd, unit) {
        requireNonNull(unit, "unit");
        requireInstance(unit, TemporalUnit, "unit");
        if (unit instanceof ChronoUnit) {
          switch(unit) {
            case ChronoUnit.MONTHS:
              return this.plusMonths(amountToAdd);
            case ChronoUnit.YEARS:
              return this.plusYears(amountToAdd);
            case ChronoUnit.DECADES:
              return this.plusYears(MathUtil.safeMultiply(amountToAdd, 10));
            case ChronoUnit.CENTURIES:
              return this.plusYears(MathUtil.safeMultiply(amountToAdd, 100));
            case ChronoUnit.MILLENNIA:
              return this.plusYears(MathUtil.safeMultiply(amountToAdd, 1000));
            case ChronoUnit.ERAS:
              return this.with(ChronoField.ERA, MathUtil.safeAdd(this.getLong(ChronoField.ERA), amountToAdd));
          }
          throw new UnsupportedTemporalTypeException("Unsupported unit: " + unit);
        }
        return unit.addTo(this, amountToAdd);
      };
      _proto.plusYears = function(yearsToAdd) {
        if (0 === yearsToAdd) {
          return this;
        }
        yearsToAdd = ChronoField.YEAR.checkValidIntValue(this._year + yearsToAdd);
        return this.withYear(yearsToAdd);
      };
      _proto.plusMonths = function(monthsToAdd) {
        if (0 === monthsToAdd) {
          return this;
        }
        var calcMonths = 12 * this._year + (this._month - 1) + monthsToAdd;
        monthsToAdd = ChronoField.YEAR.checkValidIntValue(MathUtil.floorDiv(calcMonths, 12));
        calcMonths = MathUtil.floorMod(calcMonths, 12) + 1;
        return new YearMonth(monthsToAdd, calcMonths);
      };
      _proto.minusAmount = function(amount) {
        requireNonNull(amount, "amount");
        return amount.subtractFrom(this);
      };
      _proto.minusAmountUnit = function(amountToSubtract, unit) {
        return amountToSubtract === MathUtil.MIN_SAFE_INTEGER ? this.plusAmountUnit(MathUtil.MAX_SAFE_INTEGER, unit).plusAmountUnit(1, unit) : this.plusAmountUnit(-amountToSubtract, unit);
      };
      _proto.minusYears = function(yearsToSubtract) {
        return yearsToSubtract === MathUtil.MIN_SAFE_INTEGER ? this.plusYears(MathUtil.MIN_SAFE_INTEGER).plusYears(1) : this.plusYears(-yearsToSubtract);
      };
      _proto.minusMonths = function(monthsToSubtract) {
        return monthsToSubtract === MathUtil.MIN_SAFE_INTEGER ? this.plusMonths(Math.MAX_SAFE_INTEGER).plusMonths(1) : this.plusMonths(-monthsToSubtract);
      };
      _proto.query = function(_query) {
        requireNonNull(_query, "query");
        requireInstance(_query, TemporalQuery, "query");
        return _query === TemporalQueries.chronology() ? IsoChronology.INSTANCE : _query === TemporalQueries.precision() ? ChronoUnit.MONTHS : _query === TemporalQueries.localDate() || _query === TemporalQueries.localTime() || _query === TemporalQueries.zone() || _query === TemporalQueries.zoneId() || _query === TemporalQueries.offset() ? null : _Temporal.prototype.query.call(this, _query);
      };
      _proto.adjustInto = function(temporal) {
        requireNonNull(temporal, "temporal");
        requireInstance(temporal, Temporal, "temporal");
        return temporal.with(ChronoField.PROLEPTIC_MONTH, this._getProlepticMonth());
      };
      _proto.until = function(endExclusive, unit) {
        requireNonNull(endExclusive, "endExclusive");
        requireNonNull(unit, "unit");
        requireInstance(endExclusive, Temporal, "endExclusive");
        requireInstance(unit, TemporalUnit, "unit");
        endExclusive = YearMonth.from(endExclusive);
        if (unit instanceof ChronoUnit) {
          var monthsUntil = endExclusive._getProlepticMonth() - this._getProlepticMonth();
          switch(unit) {
            case ChronoUnit.MONTHS:
              return monthsUntil;
            case ChronoUnit.YEARS:
              return monthsUntil / 12;
            case ChronoUnit.DECADES:
              return monthsUntil / 120;
            case ChronoUnit.CENTURIES:
              return monthsUntil / 1200;
            case ChronoUnit.MILLENNIA:
              return monthsUntil / 12000;
            case ChronoUnit.ERAS:
              return endExclusive.getLong(ChronoField.ERA) - this.getLong(ChronoField.ERA);
          }
          throw new UnsupportedTemporalTypeException("Unsupported unit: " + unit);
        }
        return unit.between(this, endExclusive);
      };
      _proto.atDay = function(dayOfMonth) {
        return LocalDate.of(this._year, this._month, dayOfMonth);
      };
      _proto.atEndOfMonth = function() {
        return LocalDate.of(this._year, this._month, this.lengthOfMonth());
      };
      _proto.compareTo = function(other) {
        requireNonNull(other, "other");
        requireInstance(other, YearMonth, "other");
        var cmp = this._year - other.year();
        0 === cmp && (cmp = this._month - other.monthValue());
        return cmp;
      };
      _proto.isAfter = function(other) {
        return 0 < this.compareTo(other);
      };
      _proto.isBefore = function(other) {
        return 0 > this.compareTo(other);
      };
      _proto.equals = function(obj) {
        return this === obj ? !0 : obj instanceof YearMonth ? this.year() === obj.year() && this.monthValue() === obj.monthValue() : !1;
      };
      _proto.toString = function() {
        return PARSER$1.format(this);
      };
      _proto.toJSON = function() {
        return this.toString();
      };
      _proto.format = function(formatter) {
        requireNonNull(formatter, "formatter");
        return formatter.format(this);
      };
      return YearMonth;
    }(Temporal), PARSER$1, Year = function(_Temporal) {
      function Year(value) {
        var _this = _Temporal.call(this) || this;
        _this._year = MathUtil.safeToInt(value);
        return _this;
      }
      _inheritsLoose(Year, _Temporal);
      var _proto = Year.prototype;
      _proto.value = function() {
        return this._year;
      };
      Year.now = function(zoneIdOrClock) {
        void 0 === zoneIdOrClock && (zoneIdOrClock = void 0);
        return void 0 === zoneIdOrClock ? Year.now0() : zoneIdOrClock instanceof ZoneId ? Year.nowZoneId(zoneIdOrClock) : Year.nowClock(zoneIdOrClock);
      };
      Year.now0 = function() {
        return Year.nowClock(Clock.systemDefaultZone());
      };
      Year.nowZoneId = function(zone) {
        requireNonNull(zone, "zone");
        requireInstance(zone, ZoneId, "zone");
        return Year.nowClock(Clock.system(zone));
      };
      Year.nowClock = function(clock) {
        requireNonNull(clock, "clock");
        requireInstance(clock, Clock, "clock");
        clock = LocalDate.now(clock);
        return Year.of(clock.year());
      };
      Year.of = function(isoYear) {
        requireNonNull(isoYear, "isoYear");
        ChronoField.YEAR.checkValidValue(isoYear);
        return new Year(isoYear);
      };
      Year.from = function(temporal) {
        requireNonNull(temporal, "temporal");
        requireInstance(temporal, TemporalAccessor, "temporal");
        if (temporal instanceof Year) {
          return temporal;
        }
        try {
          return Year.of(temporal.get(ChronoField.YEAR));
        } catch (ex) {
          throw new DateTimeException("Unable to obtain Year from TemporalAccessor: " + temporal + ", type " + (temporal && null != temporal.constructor ? temporal.constructor.name : ""));
        }
      };
      Year.parse = function(text, formatter) {
        return 1 >= arguments.length ? Year.parseText(text) : Year.parseTextFormatter(text, formatter);
      };
      Year.parseText = function(text) {
        requireNonNull(text, "text");
        return Year.parse(text, PARSER$2);
      };
      Year.parseTextFormatter = function(text, formatter) {
        void 0 === formatter && (formatter = PARSER$2);
        requireNonNull(text, "text");
        requireNonNull(formatter, "formatter");
        requireInstance(formatter, DateTimeFormatter, "formatter");
        return formatter.parse(text, Year.FROM);
      };
      Year.isLeap = function(year) {
        return 0 === MathUtil.intMod(year, 4) && (0 !== MathUtil.intMod(year, 100) || 0 === MathUtil.intMod(year, 400));
      };
      _proto.isSupported = function(fieldOrUnit) {
        return 1 === arguments.length && fieldOrUnit instanceof TemporalField ? this.isSupportedField(fieldOrUnit) : this.isSupportedUnit(fieldOrUnit);
      };
      _proto.isSupportedField = function(field) {
        return field instanceof ChronoField ? field === ChronoField.YEAR || field === ChronoField.YEAR_OF_ERA || field === ChronoField.ERA : null != field && field.isSupportedBy(this);
      };
      _proto.isSupportedUnit = function(unit) {
        return unit instanceof ChronoUnit ? unit === ChronoUnit.YEARS || unit === ChronoUnit.DECADES || unit === ChronoUnit.CENTURIES || unit === ChronoUnit.MILLENNIA || unit === ChronoUnit.ERAS : null != unit && unit.isSupportedBy(this);
      };
      _proto.range = function(field) {
        if (this.isSupported(field)) {
          return field.range();
        }
        if (field instanceof ChronoField) {
          throw new UnsupportedTemporalTypeException("Unsupported field: " + field);
        }
        return _Temporal.prototype.range.call(this, field);
      };
      _proto.get = function(field) {
        return this.range(field).checkValidIntValue(this.getLong(field), field);
      };
      _proto.getLong = function(field) {
        requireNonNull(field, "field");
        if (field instanceof ChronoField) {
          switch(field) {
            case ChronoField.YEAR_OF_ERA:
              return 1 > this._year ? 1 - this._year : this._year;
            case ChronoField.YEAR:
              return this._year;
            case ChronoField.ERA:
              return 1 > this._year ? 0 : 1;
          }
          throw new UnsupportedTemporalTypeException("Unsupported field: " + field);
        }
        return field.getFrom(this);
      };
      _proto.isLeap = function() {
        return Year.isLeap(this._year);
      };
      _proto.withAdjuster = function(adjuster) {
        requireNonNull(adjuster, "adjuster");
        return adjuster.adjustInto(this);
      };
      _proto.withFieldValue = function(field, newValue) {
        requireNonNull(field, "field");
        requireInstance(field, TemporalField, "field");
        if (field instanceof ChronoField) {
          field.checkValidValue(newValue);
          switch(field) {
            case ChronoField.YEAR_OF_ERA:
              return Year.of(1 > this._year ? 1 - newValue : newValue);
            case ChronoField.YEAR:
              return Year.of(newValue);
            case ChronoField.ERA:
              return this.getLong(ChronoField.ERA) === newValue ? this : Year.of(1 - this._year);
          }
          throw new UnsupportedTemporalTypeException("Unsupported field: " + field);
        }
        return field.adjustInto(this, newValue);
      };
      _proto.plusAmount = function(amount) {
        requireNonNull(amount, "amount");
        requireInstance(amount, TemporalAmount, "amount");
        return amount.addTo(this);
      };
      _proto.plusAmountUnit = function(amountToAdd, unit) {
        requireNonNull(amountToAdd, "amountToAdd");
        requireNonNull(unit, "unit");
        requireInstance(unit, TemporalUnit, "unit");
        if (unit instanceof ChronoUnit) {
          switch(unit) {
            case ChronoUnit.YEARS:
              return this.plusYears(amountToAdd);
            case ChronoUnit.DECADES:
              return this.plusYears(MathUtil.safeMultiply(amountToAdd, 10));
            case ChronoUnit.CENTURIES:
              return this.plusYears(MathUtil.safeMultiply(amountToAdd, 100));
            case ChronoUnit.MILLENNIA:
              return this.plusYears(MathUtil.safeMultiply(amountToAdd, 1000));
            case ChronoUnit.ERAS:
              return this.with(ChronoField.ERA, MathUtil.safeAdd(this.getLong(ChronoField.ERA), amountToAdd));
          }
          throw new UnsupportedTemporalTypeException("Unsupported unit: " + unit);
        }
        return unit.addTo(this, amountToAdd);
      };
      _proto.plusYears = function(yearsToAdd) {
        return 0 === yearsToAdd ? this : Year.of(ChronoField.YEAR.checkValidIntValue(MathUtil.safeAdd(this._year, yearsToAdd)));
      };
      _proto.minusAmount = function(amount) {
        requireNonNull(amount, "amount");
        requireInstance(amount, TemporalAmount, "amount");
        return amount.subtractFrom(this);
      };
      _proto.minusAmountUnit = function(amountToSubtract, unit) {
        requireNonNull(amountToSubtract, "amountToSubtract");
        requireNonNull(unit, "unit");
        requireInstance(unit, TemporalUnit, "unit");
        return amountToSubtract === MathUtil.MIN_SAFE_INTEGER ? this.plus(MathUtil.MAX_SAFE_INTEGER, unit).plus(1, unit) : this.plus(-amountToSubtract, unit);
      };
      _proto.minusYears = function(yearsToSubtract) {
        return yearsToSubtract === MathUtil.MIN_SAFE_INTEGER ? this.plusYears(MathUtil.MAX_SAFE_INTEGER).plusYears(1) : this.plusYears(-yearsToSubtract);
      };
      _proto.adjustInto = function(temporal) {
        requireNonNull(temporal, "temporal");
        return temporal.with(ChronoField.YEAR, this._year);
      };
      _proto.isValidMonthDay = function(monthDay) {
        return null != monthDay && monthDay.isValidYear(this._year);
      };
      _proto.length = function() {
        return this.isLeap() ? 366 : 365;
      };
      _proto.atDay = function(dayOfYear) {
        return LocalDate.ofYearDay(this._year, dayOfYear);
      };
      _proto.atMonth = function(monthOrNumber) {
        return 1 === arguments.length && monthOrNumber instanceof Month ? this.atMonthMonth(monthOrNumber) : this.atMonthNumber(monthOrNumber);
      };
      _proto.atMonthMonth = function(month) {
        requireNonNull(month, "month");
        requireInstance(month, Month, "month");
        return YearMonth.of(this._year, month);
      };
      _proto.atMonthNumber = function(month) {
        requireNonNull(month, "month");
        return YearMonth.of(this._year, month);
      };
      _proto.atMonthDay = function(monthDay) {
        requireNonNull(monthDay, "monthDay");
        requireInstance(monthDay, MonthDay, "monthDay");
        return monthDay.atYear(this._year);
      };
      _proto.query = function(_query) {
        requireNonNull(_query, "query()");
        requireInstance(_query, TemporalQuery, "query()");
        return _query === TemporalQueries.chronology() ? IsoChronology.INSTANCE : _query === TemporalQueries.precision() ? ChronoUnit.YEARS : _query === TemporalQueries.localDate() || _query === TemporalQueries.localTime() || _query === TemporalQueries.zone() || _query === TemporalQueries.zoneId() || _query === TemporalQueries.offset() ? null : _Temporal.prototype.query.call(this, _query);
      };
      _proto.compareTo = function(other) {
        requireNonNull(other, "other");
        requireInstance(other, Year, "other");
        return this._year - other._year;
      };
      _proto.isAfter = function(other) {
        requireNonNull(other, "other");
        requireInstance(other, Year, "other");
        return this._year > other._year;
      };
      _proto.isBefore = function(other) {
        requireNonNull(other, "other");
        requireInstance(other, Year, "other");
        return this._year < other._year;
      };
      _proto.format = function(formatter) {
        requireNonNull(formatter, "formatter");
        requireInstance(formatter, DateTimeFormatter, "formatter");
        return formatter.format(this);
      };
      _proto.equals = function(other) {
        return this === other ? !0 : other instanceof Year ? this.value() === other.value() : !1;
      };
      _proto.toString = function() {
        return "" + this._year;
      };
      _proto.toJSON = function() {
        return this.toString();
      };
      _proto.until = function(endExclusive, unit) {
        endExclusive = Year.from(endExclusive);
        if (unit instanceof ChronoUnit) {
          var yearsUntil = endExclusive.value() - this.value();
          switch(unit) {
            case ChronoUnit.YEARS:
              return yearsUntil;
            case ChronoUnit.DECADES:
              return MathUtil.intDiv(yearsUntil, 10);
            case ChronoUnit.CENTURIES:
              return MathUtil.intDiv(yearsUntil, 100);
            case ChronoUnit.MILLENNIA:
              return MathUtil.intDiv(yearsUntil, 1000);
            case ChronoUnit.ERAS:
              return endExclusive.getLong(ChronoField.ERA) - this.getLong(ChronoField.ERA);
          }
          throw new UnsupportedTemporalTypeException("Unsupported unit: " + unit);
        }
        return unit.between(this, endExclusive);
      };
      return Year;
    }(Temporal), PARSER$2, TemporalAdjusters = function() {
      function TemporalAdjusters() {
      }
      TemporalAdjusters.firstDayOfMonth = function() {
        return Impl.FIRST_DAY_OF_MONTH;
      };
      TemporalAdjusters.lastDayOfMonth = function() {
        return Impl.LAST_DAY_OF_MONTH;
      };
      TemporalAdjusters.firstDayOfNextMonth = function() {
        return Impl.FIRST_DAY_OF_NEXT_MONTH;
      };
      TemporalAdjusters.firstDayOfYear = function() {
        return Impl.FIRST_DAY_OF_YEAR;
      };
      TemporalAdjusters.lastDayOfYear = function() {
        return Impl.LAST_DAY_OF_YEAR;
      };
      TemporalAdjusters.firstDayOfNextYear = function() {
        return Impl.FIRST_DAY_OF_NEXT_YEAR;
      };
      TemporalAdjusters.firstInMonth = function(dayOfWeek) {
        requireNonNull(dayOfWeek, "dayOfWeek");
        return new DayOfWeekInMonth(1, dayOfWeek);
      };
      TemporalAdjusters.lastInMonth = function(dayOfWeek) {
        requireNonNull(dayOfWeek, "dayOfWeek");
        return new DayOfWeekInMonth(-1, dayOfWeek);
      };
      TemporalAdjusters.dayOfWeekInMonth = function(ordinal, dayOfWeek) {
        requireNonNull(dayOfWeek, "dayOfWeek");
        return new DayOfWeekInMonth(ordinal, dayOfWeek);
      };
      TemporalAdjusters.next = function(dayOfWeek) {
        return new RelativeDayOfWeek(2, dayOfWeek);
      };
      TemporalAdjusters.nextOrSame = function(dayOfWeek) {
        return new RelativeDayOfWeek(0, dayOfWeek);
      };
      TemporalAdjusters.previous = function(dayOfWeek) {
        return new RelativeDayOfWeek(3, dayOfWeek);
      };
      TemporalAdjusters.previousOrSame = function(dayOfWeek) {
        return new RelativeDayOfWeek(1, dayOfWeek);
      };
      return TemporalAdjusters;
    }(), Impl = function(_TemporalAdjuster) {
      function Impl(ordinal) {
        var _this = _TemporalAdjuster.call(this) || this;
        _this._ordinal = ordinal;
        return _this;
      }
      _inheritsLoose(Impl, _TemporalAdjuster);
      Impl.prototype.adjustInto = function(temporal) {
        switch(this._ordinal) {
          case 0:
            return temporal.with(ChronoField.DAY_OF_MONTH, 1);
          case 1:
            return temporal.with(ChronoField.DAY_OF_MONTH, temporal.range(ChronoField.DAY_OF_MONTH).maximum());
          case 2:
            return temporal.with(ChronoField.DAY_OF_MONTH, 1).plus(1, ChronoUnit.MONTHS);
          case 3:
            return temporal.with(ChronoField.DAY_OF_YEAR, 1);
          case 4:
            return temporal.with(ChronoField.DAY_OF_YEAR, temporal.range(ChronoField.DAY_OF_YEAR).maximum());
          case 5:
            return temporal.with(ChronoField.DAY_OF_YEAR, 1).plus(1, ChronoUnit.YEARS);
        }
        throw new IllegalStateException("Unreachable");
      };
      return Impl;
    }(TemporalAdjuster);
    Impl.FIRST_DAY_OF_MONTH = new Impl(0);
    Impl.LAST_DAY_OF_MONTH = new Impl(1);
    Impl.FIRST_DAY_OF_NEXT_MONTH = new Impl(2);
    Impl.FIRST_DAY_OF_YEAR = new Impl(3);
    Impl.LAST_DAY_OF_YEAR = new Impl(4);
    Impl.FIRST_DAY_OF_NEXT_YEAR = new Impl(5);
    var DayOfWeekInMonth = function(_TemporalAdjuster2) {
      function DayOfWeekInMonth(ordinal, dow) {
        var _this2 = _TemporalAdjuster2.call(this) || this;
        _this2._ordinal = ordinal;
        _this2._dowValue = dow.value();
        return _this2;
      }
      _inheritsLoose(DayOfWeekInMonth, _TemporalAdjuster2);
      DayOfWeekInMonth.prototype.adjustInto = function(temporal) {
        if (0 <= this._ordinal) {
          temporal = temporal.with(ChronoField.DAY_OF_MONTH, 1);
          var curDow = temporal.get(ChronoField.DAY_OF_WEEK);
          curDow = MathUtil.intMod(this._dowValue - curDow + 7, 7);
          curDow += 7 * (this._ordinal - 1);
          return temporal.plus(curDow, ChronoUnit.DAYS);
        }
        temporal = temporal.with(ChronoField.DAY_OF_MONTH, temporal.range(ChronoField.DAY_OF_MONTH).maximum());
        curDow = temporal.get(ChronoField.DAY_OF_WEEK);
        curDow = this._dowValue - curDow;
        curDow = (0 === curDow ? 0 : 0 < curDow ? curDow - 7 : curDow) - 7 * (-this._ordinal - 1);
        return temporal.plus(curDow, ChronoUnit.DAYS);
      };
      return DayOfWeekInMonth;
    }(TemporalAdjuster), RelativeDayOfWeek = function(_TemporalAdjuster3) {
      function RelativeDayOfWeek(relative, dayOfWeek) {
        var _this3 = _TemporalAdjuster3.call(this) || this;
        requireNonNull(dayOfWeek, "dayOfWeek");
        _this3._relative = relative;
        _this3._dowValue = dayOfWeek.value();
        return _this3;
      }
      _inheritsLoose(RelativeDayOfWeek, _TemporalAdjuster3);
      RelativeDayOfWeek.prototype.adjustInto = function(temporal) {
        var calDow = temporal.get(ChronoField.DAY_OF_WEEK);
        if (2 > this._relative && calDow === this._dowValue) {
          return temporal;
        }
        if (0 === (this._relative & 1)) {
          return calDow -= this._dowValue, temporal.plus(0 <= calDow ? 7 - calDow : -calDow, ChronoUnit.DAYS);
        }
        calDow = this._dowValue - calDow;
        return temporal.minus(0 <= calDow ? 7 - calDow : -calDow, ChronoUnit.DAYS);
      };
      return RelativeDayOfWeek;
    }(TemporalAdjuster), IsoChronology = function(_Enum) {
      function IsoChronology() {
        return _Enum.apply(this, arguments) || this;
      }
      _inheritsLoose(IsoChronology, _Enum);
      IsoChronology.isLeapYear = function(prolepticYear) {
        return 0 === (prolepticYear & 3) && (0 !== prolepticYear % 100 || 0 === prolepticYear % 400);
      };
      var _proto = IsoChronology.prototype;
      _proto._updateResolveMap = function(fieldValues, field, value) {
        requireNonNull(fieldValues, "fieldValues");
        requireNonNull(field, "field");
        var current = fieldValues.get(field);
        if (null != current && current !== value) {
          throw new DateTimeException("Invalid state, field: " + field + " " + current + " conflicts with " + field + " " + value);
        }
        fieldValues.put(field, value);
      };
      _proto.resolveDate = function(fieldValues, resolverStyle) {
        if (fieldValues.containsKey(ChronoField.EPOCH_DAY)) {
          return LocalDate.ofEpochDay(fieldValues.remove(ChronoField.EPOCH_DAY));
        }
        var prolepticMonth = fieldValues.remove(ChronoField.PROLEPTIC_MONTH);
        null != prolepticMonth && (resolverStyle !== ResolverStyle.LENIENT && ChronoField.PROLEPTIC_MONTH.checkValidValue(prolepticMonth), this._updateResolveMap(fieldValues, ChronoField.MONTH_OF_YEAR, MathUtil.floorMod(prolepticMonth, 12) + 1), this._updateResolveMap(fieldValues, ChronoField.YEAR, MathUtil.floorDiv(prolepticMonth, 12)));
        prolepticMonth = fieldValues.remove(ChronoField.YEAR_OF_ERA);
        if (null != prolepticMonth) {
          resolverStyle !== ResolverStyle.LENIENT && ChronoField.YEAR_OF_ERA.checkValidValue(prolepticMonth);
          var era = fieldValues.remove(ChronoField.ERA);
          if (null == era) {
            era = fieldValues.get(ChronoField.YEAR), resolverStyle === ResolverStyle.STRICT ? null != era ? this._updateResolveMap(fieldValues, ChronoField.YEAR, 0 < era ? prolepticMonth : MathUtil.safeSubtract(1, prolepticMonth)) : fieldValues.put(ChronoField.YEAR_OF_ERA, prolepticMonth) : this._updateResolveMap(fieldValues, ChronoField.YEAR, null == era || 0 < era ? prolepticMonth : MathUtil.safeSubtract(1, prolepticMonth));
          } else if (1 === era) {
            this._updateResolveMap(fieldValues, ChronoField.YEAR, prolepticMonth);
          } else if (0 === era) {
            this._updateResolveMap(fieldValues, ChronoField.YEAR, MathUtil.safeSubtract(1, prolepticMonth));
          } else {
            throw new DateTimeException("Invalid value for era: " + era);
          }
        } else {
          fieldValues.containsKey(ChronoField.ERA) && ChronoField.ERA.checkValidValue(fieldValues.get(ChronoField.ERA));
        }
        if (fieldValues.containsKey(ChronoField.YEAR)) {
          if (fieldValues.containsKey(ChronoField.MONTH_OF_YEAR) && fieldValues.containsKey(ChronoField.DAY_OF_MONTH)) {
            prolepticMonth = ChronoField.YEAR.checkValidIntValue(fieldValues.remove(ChronoField.YEAR));
            era = fieldValues.remove(ChronoField.MONTH_OF_YEAR);
            fieldValues = fieldValues.remove(ChronoField.DAY_OF_MONTH);
            if (resolverStyle === ResolverStyle.LENIENT) {
              return resolverStyle = era - 1, --fieldValues, LocalDate.of(prolepticMonth, 1, 1).plusMonths(resolverStyle).plusDays(fieldValues);
            }
            resolverStyle === ResolverStyle.SMART && (ChronoField.DAY_OF_MONTH.checkValidValue(fieldValues), 4 === era || 6 === era || 9 === era || 11 === era ? fieldValues = Math.min(fieldValues, 30) : 2 === era && (fieldValues = Math.min(fieldValues, Month.FEBRUARY.length(Year.isLeap(prolepticMonth)))));
            return LocalDate.of(prolepticMonth, era, fieldValues);
          }
          if (fieldValues.containsKey(ChronoField.DAY_OF_YEAR)) {
            prolepticMonth = ChronoField.YEAR.checkValidIntValue(fieldValues.remove(ChronoField.YEAR));
            if (resolverStyle === ResolverStyle.LENIENT) {
              return resolverStyle = MathUtil.safeSubtract(fieldValues.remove(ChronoField.DAY_OF_YEAR), 1), LocalDate.ofYearDay(prolepticMonth, 1).plusDays(resolverStyle);
            }
            resolverStyle = ChronoField.DAY_OF_YEAR.checkValidIntValue(fieldValues.remove(ChronoField.DAY_OF_YEAR));
            return LocalDate.ofYearDay(prolepticMonth, resolverStyle);
          }
          if (fieldValues.containsKey(ChronoField.ALIGNED_WEEK_OF_YEAR)) {
            if (fieldValues.containsKey(ChronoField.ALIGNED_DAY_OF_WEEK_IN_YEAR)) {
              prolepticMonth = ChronoField.YEAR.checkValidIntValue(fieldValues.remove(ChronoField.YEAR));
              if (resolverStyle === ResolverStyle.LENIENT) {
                return resolverStyle = MathUtil.safeSubtract(fieldValues.remove(ChronoField.ALIGNED_WEEK_OF_YEAR), 1), fieldValues = MathUtil.safeSubtract(fieldValues.remove(ChronoField.ALIGNED_DAY_OF_WEEK_IN_YEAR), 1), LocalDate.of(prolepticMonth, 1, 1).plusWeeks(resolverStyle).plusDays(fieldValues);
              }
              era = ChronoField.ALIGNED_WEEK_OF_YEAR.checkValidIntValue(fieldValues.remove(ChronoField.ALIGNED_WEEK_OF_YEAR));
              fieldValues = ChronoField.ALIGNED_DAY_OF_WEEK_IN_YEAR.checkValidIntValue(fieldValues.remove(ChronoField.ALIGNED_DAY_OF_WEEK_IN_YEAR));
              fieldValues = LocalDate.of(prolepticMonth, 1, 1).plusDays(7 * (era - 1) + (fieldValues - 1));
              if (resolverStyle === ResolverStyle.STRICT && fieldValues.get(ChronoField.YEAR) !== prolepticMonth) {
                throw new DateTimeException("Strict mode rejected date parsed to a different year");
              }
              return fieldValues;
            }
            if (fieldValues.containsKey(ChronoField.DAY_OF_WEEK)) {
              prolepticMonth = ChronoField.YEAR.checkValidIntValue(fieldValues.remove(ChronoField.YEAR));
              if (resolverStyle === ResolverStyle.LENIENT) {
                return resolverStyle = MathUtil.safeSubtract(fieldValues.remove(ChronoField.ALIGNED_WEEK_OF_YEAR), 1), fieldValues = MathUtil.safeSubtract(fieldValues.remove(ChronoField.DAY_OF_WEEK), 1), LocalDate.of(prolepticMonth, 1, 1).plusWeeks(resolverStyle).plusDays(fieldValues);
              }
              era = ChronoField.ALIGNED_WEEK_OF_YEAR.checkValidIntValue(fieldValues.remove(ChronoField.ALIGNED_WEEK_OF_YEAR));
              fieldValues = ChronoField.DAY_OF_WEEK.checkValidIntValue(fieldValues.remove(ChronoField.DAY_OF_WEEK));
              fieldValues = LocalDate.of(prolepticMonth, 1, 1).plusWeeks(era - 1).with(TemporalAdjusters.nextOrSame(DayOfWeek.of(fieldValues)));
              if (resolverStyle === ResolverStyle.STRICT && fieldValues.get(ChronoField.YEAR) !== prolepticMonth) {
                throw new DateTimeException("Strict mode rejected date parsed to a different month");
              }
              return fieldValues;
            }
          }
        }
        return null;
      };
      _proto.date = function(temporal) {
        return LocalDate.from(temporal);
      };
      return IsoChronology;
    }(Enum), OffsetTime = function(_DefaultInterfaceTemp) {
      function OffsetTime(time, offset) {
        var _this = _DefaultInterfaceTemp.call(this) || this;
        requireNonNull(time, "time");
        requireInstance(time, LocalTime, "time");
        requireNonNull(offset, "offset");
        requireInstance(offset, ZoneOffset, "offset");
        _this._time = time;
        _this._offset = offset;
        return _this;
      }
      _inheritsLoose(OffsetTime, _DefaultInterfaceTemp);
      OffsetTime.from = function(temporal) {
        requireNonNull(temporal, "temporal");
        if (temporal instanceof OffsetTime) {
          return temporal;
        }
        if (temporal instanceof OffsetDateTime) {
          return temporal.toOffsetTime();
        }
        try {
          var time = LocalTime.from(temporal), offset = ZoneOffset.from(temporal);
          return new OffsetTime(time, offset);
        } catch (ex) {
          throw new DateTimeException("Unable to obtain OffsetTime TemporalAccessor: " + temporal + ", type " + (null != temporal.constructor ? temporal.constructor.name : ""));
        }
      };
      OffsetTime.now = function(clockOrZone) {
        return 0 === arguments.length ? OffsetTime._now(Clock.systemDefaultZone()) : clockOrZone instanceof Clock ? OffsetTime._now(clockOrZone) : OffsetTime._now(Clock.system(clockOrZone));
      };
      OffsetTime._now = function(clock) {
        requireNonNull(clock, "clock");
        var now = clock.instant();
        return OffsetTime.ofInstant(now, clock.zone().rules().offset(now));
      };
      OffsetTime.of = function() {
        return 2 >= arguments.length ? OffsetTime.ofTimeAndOffset.apply(this, arguments) : OffsetTime.ofNumbers.apply(this, arguments);
      };
      OffsetTime.ofNumbers = function(hour, minute, second, nanoOfSecond, offset) {
        hour = LocalTime.of(hour, minute, second, nanoOfSecond);
        return new OffsetTime(hour, offset);
      };
      OffsetTime.ofTimeAndOffset = function(time, offset) {
        return new OffsetTime(time, offset);
      };
      OffsetTime.ofInstant = function(instant, zone) {
        requireNonNull(instant, "instant");
        requireInstance(instant, Instant, "instant");
        requireNonNull(zone, "zone");
        requireInstance(zone, ZoneId, "zone");
        zone = zone.rules().offset(instant);
        var secsOfDay = instant.epochSecond() % LocalTime.SECONDS_PER_DAY;
        secsOfDay = (secsOfDay + zone.totalSeconds()) % LocalTime.SECONDS_PER_DAY;
        0 > secsOfDay && (secsOfDay += LocalTime.SECONDS_PER_DAY);
        instant = LocalTime.ofSecondOfDay(secsOfDay, instant.nano());
        return new OffsetTime(instant, zone);
      };
      OffsetTime.parse = function(text, formatter) {
        void 0 === formatter && (formatter = DateTimeFormatter.ISO_OFFSET_TIME);
        requireNonNull(formatter, "formatter");
        return formatter.parse(text, OffsetTime.FROM);
      };
      var _proto = OffsetTime.prototype;
      _proto.adjustInto = function(temporal) {
        return temporal.with(ChronoField.NANO_OF_DAY, this._time.toNanoOfDay()).with(ChronoField.OFFSET_SECONDS, this.offset().totalSeconds());
      };
      _proto.atDate = function(date) {
        return OffsetDateTime.of(date, this._time, this._offset);
      };
      _proto.format = function(formatter) {
        requireNonNull(formatter, "formatter");
        return formatter.format(this, OffsetTime.FROM);
      };
      _proto.get = function(field) {
        return _DefaultInterfaceTemp.prototype.get.call(this, field);
      };
      _proto.getLong = function(field) {
        return field instanceof ChronoField ? field === ChronoField.OFFSET_SECONDS ? this._offset.totalSeconds() : this._time.getLong(field) : field.getFrom(this);
      };
      _proto.hour = function() {
        return this._time.hour();
      };
      _proto.minute = function() {
        return this._time.minute();
      };
      _proto.second = function() {
        return this._time.second();
      };
      _proto.nano = function() {
        return this._time.nano();
      };
      _proto.offset = function() {
        return this._offset;
      };
      _proto.isAfter = function(other) {
        requireNonNull(other, "other");
        return this._toEpochNano() > other._toEpochNano();
      };
      _proto.isBefore = function(other) {
        requireNonNull(other, "other");
        return this._toEpochNano() < other._toEpochNano();
      };
      _proto.isEqual = function(other) {
        requireNonNull(other, "other");
        return this._toEpochNano() === other._toEpochNano();
      };
      _proto.isSupported = function(fieldOrUnit) {
        return fieldOrUnit instanceof ChronoField ? fieldOrUnit.isTimeBased() || fieldOrUnit === ChronoField.OFFSET_SECONDS : fieldOrUnit instanceof ChronoUnit ? fieldOrUnit.isTimeBased() : null != fieldOrUnit && fieldOrUnit.isSupportedBy(this);
      };
      _proto.minusHours = function(hours) {
        return this._withLocalTimeOffset(this._time.minusHours(hours), this._offset);
      };
      _proto.minusMinutes = function(minutes) {
        return this._withLocalTimeOffset(this._time.minusMinutes(minutes), this._offset);
      };
      _proto.minusSeconds = function(seconds) {
        return this._withLocalTimeOffset(this._time.minusSeconds(seconds), this._offset);
      };
      _proto.minusNanos = function(nanos) {
        return this._withLocalTimeOffset(this._time.minusNanos(nanos), this._offset);
      };
      _proto.minusAmount = function(amount) {
        requireNonNull(amount);
        return amount.subtractFrom(this);
      };
      _proto.minusAmountUnit = function(amountToSubtract, unit) {
        return this.plus(-1 * amountToSubtract, unit);
      };
      _proto.plusAmount = function(amount) {
        requireNonNull(amount);
        return amount.addTo(this);
      };
      _proto.plusAmountUnit = function(amountToAdd, unit) {
        return unit instanceof ChronoUnit ? this._withLocalTimeOffset(this._time.plus(amountToAdd, unit), this._offset) : unit.addTo(this, amountToAdd);
      };
      _proto.plusHours = function(hours) {
        return this._withLocalTimeOffset(this._time.plusHours(hours), this._offset);
      };
      _proto.plusMinutes = function(minutes) {
        return this._withLocalTimeOffset(this._time.plusMinutes(minutes), this._offset);
      };
      _proto.plusSeconds = function(seconds) {
        return this._withLocalTimeOffset(this._time.plusSeconds(seconds), this._offset);
      };
      _proto.plusNanos = function(nanos) {
        return this._withLocalTimeOffset(this._time.plusNanos(nanos), this._offset);
      };
      _proto.query = function(_query) {
        requireNonNull(_query, "query");
        return _query === TemporalQueries.precision() ? ChronoUnit.NANOS : _query === TemporalQueries.offset() || _query === TemporalQueries.zone() ? this.offset() : _query === TemporalQueries.localTime() ? this._time : _query === TemporalQueries.chronology() || _query === TemporalQueries.localDate() || _query === TemporalQueries.zoneId() ? null : _DefaultInterfaceTemp.prototype.query.call(this, _query);
      };
      _proto.range = function(field) {
        return field instanceof ChronoField ? field === ChronoField.OFFSET_SECONDS ? field.range() : this._time.range(field) : field.rangeRefinedBy(this);
      };
      _proto.toLocalTime = function() {
        return this._time;
      };
      _proto.truncatedTo = function(unit) {
        return this._withLocalTimeOffset(this._time.truncatedTo(unit), this._offset);
      };
      _proto.until = function(endExclusive, unit) {
        requireNonNull(endExclusive, "endExclusive");
        requireNonNull(unit, "unit");
        endExclusive = OffsetTime.from(endExclusive);
        if (unit instanceof ChronoUnit) {
          endExclusive = endExclusive._toEpochNano() - this._toEpochNano();
          switch(unit) {
            case ChronoUnit.NANOS:
              return endExclusive;
            case ChronoUnit.MICROS:
              return Math.floor(endExclusive / 1000);
            case ChronoUnit.MILLIS:
              return Math.floor(endExclusive / 1000000);
            case ChronoUnit.SECONDS:
              return Math.floor(endExclusive / LocalTime.NANOS_PER_SECOND);
            case ChronoUnit.MINUTES:
              return Math.floor(endExclusive / LocalTime.NANOS_PER_MINUTE);
            case ChronoUnit.HOURS:
              return Math.floor(endExclusive / LocalTime.NANOS_PER_HOUR);
            case ChronoUnit.HALF_DAYS:
              return Math.floor(endExclusive / (12 * LocalTime.NANOS_PER_HOUR));
          }
          throw new UnsupportedTemporalTypeException("Unsupported unit: " + unit);
        }
        return unit.between(this, endExclusive);
      };
      _proto.withHour = function(hour) {
        return this._withLocalTimeOffset(this._time.withHour(hour), this._offset);
      };
      _proto.withMinute = function(minute) {
        return this._withLocalTimeOffset(this._time.withMinute(minute), this._offset);
      };
      _proto.withSecond = function(second) {
        return this._withLocalTimeOffset(this._time.withSecond(second), this._offset);
      };
      _proto.withNano = function(nano) {
        return this._withLocalTimeOffset(this._time.withNano(nano), this._offset);
      };
      _proto.withOffsetSameInstant = function(offset) {
        requireNonNull(offset, "offset");
        if (offset.equals(this._offset)) {
          return this;
        }
        var difference = offset.totalSeconds() - this._offset.totalSeconds();
        difference = this._time.plusSeconds(difference);
        return new OffsetTime(difference, offset);
      };
      _proto.withOffsetSameLocal = function(offset) {
        return null != offset && offset.equals(this._offset) ? this : new OffsetTime(this._time, offset);
      };
      _proto._toEpochNano = function() {
        var nod = this._time.toNanoOfDay(), offsetNanos = this._offset.totalSeconds() * LocalTime.NANOS_PER_SECOND;
        return nod - offsetNanos;
      };
      _proto.withAdjuster = function(adjuster) {
        requireNonNull(adjuster, "adjuster");
        return adjuster instanceof LocalTime ? this._withLocalTimeOffset(adjuster, this._offset) : adjuster instanceof ZoneOffset ? this._withLocalTimeOffset(this._time, adjuster) : adjuster instanceof OffsetTime ? adjuster : adjuster.adjustInto(this);
      };
      _proto.withFieldValue = function(field, newValue) {
        requireNonNull(field, "field");
        return field instanceof ChronoField ? field === ChronoField.OFFSET_SECONDS ? this._withLocalTimeOffset(this._time, ZoneOffset.ofTotalSeconds(field.checkValidIntValue(newValue))) : this._withLocalTimeOffset(this._time.with(field, newValue), this._offset) : field.adjustInto(this, newValue);
      };
      _proto._withLocalTimeOffset = function(time, offset) {
        return this._time === time && this._offset.equals(offset) ? this : new OffsetTime(time, offset);
      };
      _proto.compareTo = function(other) {
        requireNonNull(other, "other");
        requireInstance(other, OffsetTime, "other");
        if (this._offset.equals(other._offset)) {
          return this._time.compareTo(other._time);
        }
        var compare = MathUtil.compareNumbers(this._toEpochNano(), other._toEpochNano());
        return 0 === compare ? this._time.compareTo(other._time) : compare;
      };
      _proto.equals = function(other) {
        return this === other ? !0 : other instanceof OffsetTime ? this._time.equals(other._time) && this._offset.equals(other._offset) : !1;
      };
      _proto.hashCode = function() {
        return this._time.hashCode() ^ this._offset.hashCode();
      };
      _proto.toString = function() {
        return this._time.toString() + this._offset.toString();
      };
      _proto.toJSON = function() {
        return this.toString();
      };
      return OffsetTime;
    }(DefaultInterfaceTemporal), ChronoZonedDateTime = function(_DefaultInterfaceTemp) {
      function ChronoZonedDateTime() {
        return _DefaultInterfaceTemp.apply(this, arguments) || this;
      }
      _inheritsLoose(ChronoZonedDateTime, _DefaultInterfaceTemp);
      var _proto = ChronoZonedDateTime.prototype;
      _proto.query = function(_query) {
        return _query === TemporalQueries.zoneId() || _query === TemporalQueries.zone() ? this.zone() : _query === TemporalQueries.chronology() ? this.toLocalDate().chronology() : _query === TemporalQueries.precision() ? ChronoUnit.NANOS : _query === TemporalQueries.offset() ? this.offset() : _query === TemporalQueries.localDate() ? LocalDate.ofEpochDay(this.toLocalDate().toEpochDay()) : _query === TemporalQueries.localTime() ? this.toLocalTime() : _DefaultInterfaceTemp.prototype.query.call(this, 
        _query);
      };
      _proto.format = function(formatter) {
        requireNonNull(formatter, "formatter");
        return formatter.format(this);
      };
      _proto.toInstant = function() {
        return Instant.ofEpochSecond(this.toEpochSecond(), this.toLocalTime().nano());
      };
      _proto.toEpochSecond = function() {
        var secs = 86400 * this.toLocalDate().toEpochDay() + this.toLocalTime().toSecondOfDay();
        return secs -= this.offset().totalSeconds();
      };
      _proto.compareTo = function(other) {
        requireNonNull(other, "other");
        var cmp = MathUtil.compareNumbers(this.toEpochSecond(), other.toEpochSecond());
        0 === cmp && (cmp = this.toLocalTime().nano() - other.toLocalTime().nano(), 0 === cmp && (cmp = this.toLocalDateTime().compareTo(other.toLocalDateTime()), 0 === cmp && (cmp = this.zone().id(), other = other.zone().id(), cmp = cmp < other ? -1 : cmp > other ? 1 : 0)));
        return cmp;
      };
      _proto.isAfter = function(other) {
        requireNonNull(other, "other");
        var thisEpochSec = this.toEpochSecond(), otherEpochSec = other.toEpochSecond();
        return thisEpochSec > otherEpochSec || thisEpochSec === otherEpochSec && this.toLocalTime().nano() > other.toLocalTime().nano();
      };
      _proto.isBefore = function(other) {
        requireNonNull(other, "other");
        var thisEpochSec = this.toEpochSecond(), otherEpochSec = other.toEpochSecond();
        return thisEpochSec < otherEpochSec || thisEpochSec === otherEpochSec && this.toLocalTime().nano() < other.toLocalTime().nano();
      };
      _proto.isEqual = function(other) {
        requireNonNull(other, "other");
        return this.toEpochSecond() === other.toEpochSecond() && this.toLocalTime().nano() === other.toLocalTime().nano();
      };
      _proto.equals = function(other) {
        return this === other ? !0 : other instanceof ChronoZonedDateTime ? 0 === this.compareTo(other) : !1;
      };
      return ChronoZonedDateTime;
    }(DefaultInterfaceTemporal), ZonedDateTime = function(_ChronoZonedDateTime) {
      function ZonedDateTime(dateTime, offset, zone) {
        requireNonNull(dateTime, "dateTime");
        requireNonNull(offset, "offset");
        requireNonNull(zone, "zone");
        var _this = _ChronoZonedDateTime.call(this) || this;
        _this._dateTime = dateTime;
        _this._offset = offset;
        _this._zone = zone;
        return _this;
      }
      _inheritsLoose(ZonedDateTime, _ChronoZonedDateTime);
      ZonedDateTime.now = function(clockOrZone) {
        clockOrZone = clockOrZone instanceof ZoneId ? Clock.system(clockOrZone) : null == clockOrZone ? Clock.systemDefaultZone() : clockOrZone;
        return ZonedDateTime.ofInstant(clockOrZone.instant(), clockOrZone.zone());
      };
      ZonedDateTime.of = function() {
        return 2 >= arguments.length ? ZonedDateTime.of2.apply(this, arguments) : 3 === arguments.length && arguments[0] instanceof LocalDate ? ZonedDateTime.of3.apply(this, arguments) : ZonedDateTime.of8.apply(this, arguments);
      };
      ZonedDateTime.of3 = function(date, time, zone) {
        return ZonedDateTime.of2(LocalDateTime.of(date, time), zone);
      };
      ZonedDateTime.of2 = function(localDateTime, zone) {
        return ZonedDateTime.ofLocal(localDateTime, zone, null);
      };
      ZonedDateTime.of8 = function(year, month, dayOfMonth, hour, minute, second, nanoOfSecond, zone) {
        year = LocalDateTime.of(year, month, dayOfMonth, hour, minute, second, nanoOfSecond);
        return ZonedDateTime.ofLocal(year, zone, null);
      };
      ZonedDateTime.ofLocal = function(localDateTime, zone, preferredOffset) {
        requireNonNull(localDateTime, "localDateTime");
        requireNonNull(zone, "zone");
        if (zone instanceof ZoneOffset) {
          return new ZonedDateTime(localDateTime, zone, zone);
        }
        var offset = null;
        offset = zone.rules();
        var validOffsets = offset.validOffsets(localDateTime);
        1 === validOffsets.length ? offset = validOffsets[0] : 0 === validOffsets.length ? (offset = offset.transition(localDateTime), localDateTime = localDateTime.plusSeconds(offset.duration().seconds()), offset = offset.offsetAfter()) : offset = null != preferredOffset && validOffsets.some(function(validOffset) {
          return validOffset.equals(preferredOffset);
        }) ? preferredOffset : requireNonNull(validOffsets[0], "offset");
        return new ZonedDateTime(localDateTime, offset, zone);
      };
      ZonedDateTime.ofInstant = function() {
        return 2 === arguments.length ? ZonedDateTime.ofInstant2.apply(this, arguments) : ZonedDateTime.ofInstant3.apply(this, arguments);
      };
      ZonedDateTime.ofInstant2 = function(instant, zone) {
        requireNonNull(instant, "instant");
        requireNonNull(zone, "zone");
        return ZonedDateTime._create(instant.epochSecond(), instant.nano(), zone);
      };
      ZonedDateTime.ofInstant3 = function(localDateTime, offset, zone) {
        requireNonNull(localDateTime, "localDateTime");
        requireNonNull(offset, "offset");
        requireNonNull(zone, "zone");
        return ZonedDateTime._create(localDateTime.toEpochSecond(offset), localDateTime.nano(), zone);
      };
      ZonedDateTime._create = function(epochSecond, nanoOfSecond, zone) {
        var rules = zone.rules(), instant = Instant.ofEpochSecond(epochSecond, nanoOfSecond);
        rules = rules.offset(instant);
        epochSecond = LocalDateTime.ofEpochSecond(epochSecond, nanoOfSecond, rules);
        return new ZonedDateTime(epochSecond, rules, zone);
      };
      ZonedDateTime.ofStrict = function(localDateTime, offset, zone) {
        requireNonNull(localDateTime, "localDateTime");
        requireNonNull(offset, "offset");
        requireNonNull(zone, "zone");
        var rules = zone.rules();
        if (!1 === rules.isValidOffset(localDateTime, offset)) {
          rules = rules.transition(localDateTime);
          if (null != rules && rules.isGap()) {
            throw new DateTimeException("LocalDateTime " + localDateTime + " does not exist in zone " + zone + " due to a gap in the local time-line, typically caused by daylight savings");
          }
          throw new DateTimeException('ZoneOffset "' + offset + '" is not valid for LocalDateTime "' + localDateTime + '" in zone "' + zone + '"');
        }
        return new ZonedDateTime(localDateTime, offset, zone);
      };
      ZonedDateTime.ofLenient = function(localDateTime, offset, zone) {
        requireNonNull(localDateTime, "localDateTime");
        requireNonNull(offset, "offset");
        requireNonNull(zone, "zone");
        if (zone instanceof ZoneOffset && !1 === offset.equals(zone)) {
          throw new IllegalArgumentException("ZoneId must match ZoneOffset");
        }
        return new ZonedDateTime(localDateTime, offset, zone);
      };
      ZonedDateTime.from = function(temporal) {
        requireNonNull(temporal, "temporal");
        if (temporal instanceof ZonedDateTime) {
          return temporal;
        }
        var zone = ZoneId.from(temporal);
        if (temporal.isSupported(ChronoField.INSTANT_SECONDS)) {
          var zdt = ZonedDateTime._from(temporal, zone);
          if (null != zdt) {
            return zdt;
          }
        }
        temporal = LocalDateTime.from(temporal);
        return ZonedDateTime.of2(temporal, zone);
      };
      ZonedDateTime._from = function(temporal, zone) {
        try {
          return ZonedDateTime.__from(temporal, zone);
        } catch (ex) {
          if (!(ex instanceof DateTimeException)) {
            throw ex;
          }
        }
      };
      ZonedDateTime.__from = function(temporal, zone) {
        var epochSecond = temporal.getLong(ChronoField.INSTANT_SECONDS);
        temporal = temporal.get(ChronoField.NANO_OF_SECOND);
        return ZonedDateTime._create(epochSecond, temporal, zone);
      };
      ZonedDateTime.parse = function(text, formatter) {
        void 0 === formatter && (formatter = DateTimeFormatter.ISO_ZONED_DATE_TIME);
        requireNonNull(formatter, "formatter");
        return formatter.parse(text, ZonedDateTime.FROM);
      };
      var _proto = ZonedDateTime.prototype;
      _proto._resolveLocal = function(newDateTime) {
        requireNonNull(newDateTime, "newDateTime");
        return ZonedDateTime.ofLocal(newDateTime, this._zone, this._offset);
      };
      _proto._resolveInstant = function(newDateTime) {
        return ZonedDateTime.ofInstant3(newDateTime, this._offset, this._zone);
      };
      _proto._resolveOffset = function(offset) {
        return !1 === offset.equals(this._offset) && this._zone.rules().isValidOffset(this._dateTime, offset) ? new ZonedDateTime(this._dateTime, offset, this._zone) : this;
      };
      _proto.isSupported = function(fieldOrUnit) {
        return fieldOrUnit instanceof ChronoField ? !0 : fieldOrUnit instanceof ChronoUnit ? fieldOrUnit.isDateBased() || fieldOrUnit.isTimeBased() : null != fieldOrUnit && fieldOrUnit.isSupportedBy(this);
      };
      _proto.range = function(field) {
        return field instanceof ChronoField ? field === ChronoField.INSTANT_SECONDS || field === ChronoField.OFFSET_SECONDS ? field.range() : this._dateTime.range(field) : field.rangeRefinedBy(this);
      };
      _proto.get = function(field) {
        return this.getLong(field);
      };
      _proto.getLong = function(field) {
        if (field instanceof ChronoField) {
          switch(field) {
            case ChronoField.INSTANT_SECONDS:
              return this.toEpochSecond();
            case ChronoField.OFFSET_SECONDS:
              return this._offset.totalSeconds();
          }
          return this._dateTime.getLong(field);
        }
        requireNonNull(field, "field");
        return field.getFrom(this);
      };
      _proto.offset = function() {
        return this._offset;
      };
      _proto.withEarlierOffsetAtOverlap = function() {
        var trans = this._zone.rules().transition(this._dateTime);
        return null != trans && trans.isOverlap() && (trans = trans.offsetBefore(), !1 === trans.equals(this._offset)) ? new ZonedDateTime(this._dateTime, trans, this._zone) : this;
      };
      _proto.withLaterOffsetAtOverlap = function() {
        var trans = this._zone.rules().transition(this.toLocalDateTime());
        return null != trans && (trans = trans.offsetAfter(), !1 === trans.equals(this._offset)) ? new ZonedDateTime(this._dateTime, trans, this._zone) : this;
      };
      _proto.zone = function() {
        return this._zone;
      };
      _proto.withZoneSameLocal = function(zone) {
        requireNonNull(zone, "zone");
        return this._zone.equals(zone) ? this : ZonedDateTime.ofLocal(this._dateTime, zone, this._offset);
      };
      _proto.withZoneSameInstant = function(zone) {
        requireNonNull(zone, "zone");
        return this._zone.equals(zone) ? this : ZonedDateTime._create(this._dateTime.toEpochSecond(this._offset), this._dateTime.nano(), zone);
      };
      _proto.withFixedOffsetZone = function() {
        return this._zone.equals(this._offset) ? this : new ZonedDateTime(this._dateTime, this._offset, this._offset);
      };
      _proto.year = function() {
        return this._dateTime.year();
      };
      _proto.monthValue = function() {
        return this._dateTime.monthValue();
      };
      _proto.month = function() {
        return this._dateTime.month();
      };
      _proto.dayOfMonth = function() {
        return this._dateTime.dayOfMonth();
      };
      _proto.dayOfYear = function() {
        return this._dateTime.dayOfYear();
      };
      _proto.dayOfWeek = function() {
        return this._dateTime.dayOfWeek();
      };
      _proto.hour = function() {
        return this._dateTime.hour();
      };
      _proto.minute = function() {
        return this._dateTime.minute();
      };
      _proto.second = function() {
        return this._dateTime.second();
      };
      _proto.nano = function() {
        return this._dateTime.nano();
      };
      _proto.withAdjuster = function(adjuster) {
        if (adjuster instanceof LocalDate) {
          return this._resolveLocal(LocalDateTime.of(adjuster, this._dateTime.toLocalTime()));
        }
        if (adjuster instanceof LocalTime) {
          return this._resolveLocal(LocalDateTime.of(this._dateTime.toLocalDate(), adjuster));
        }
        if (adjuster instanceof LocalDateTime) {
          return this._resolveLocal(adjuster);
        }
        if (adjuster instanceof Instant) {
          return ZonedDateTime._create(adjuster.epochSecond(), adjuster.nano(), this._zone);
        }
        if (adjuster instanceof ZoneOffset) {
          return this._resolveOffset(adjuster);
        }
        requireNonNull(adjuster, "adjuster");
        return adjuster.adjustInto(this);
      };
      _proto.withFieldValue = function(field, newValue) {
        if (field instanceof ChronoField) {
          switch(field) {
            case ChronoField.INSTANT_SECONDS:
              return ZonedDateTime._create(newValue, this.nano(), this._zone);
            case ChronoField.OFFSET_SECONDS:
              return field = ZoneOffset.ofTotalSeconds(field.checkValidIntValue(newValue)), this._resolveOffset(field);
          }
          return this._resolveLocal(this._dateTime.with(field, newValue));
        }
        return field.adjustInto(this, newValue);
      };
      _proto.withYear = function(year) {
        return this._resolveLocal(this._dateTime.withYear(year));
      };
      _proto.withMonth = function(month) {
        return this._resolveLocal(this._dateTime.withMonth(month));
      };
      _proto.withDayOfMonth = function(dayOfMonth) {
        return this._resolveLocal(this._dateTime.withDayOfMonth(dayOfMonth));
      };
      _proto.withDayOfYear = function(dayOfYear) {
        return this._resolveLocal(this._dateTime.withDayOfYear(dayOfYear));
      };
      _proto.withHour = function(hour) {
        return this._resolveLocal(this._dateTime.withHour(hour));
      };
      _proto.withMinute = function(minute) {
        return this._resolveLocal(this._dateTime.withMinute(minute));
      };
      _proto.withSecond = function(second) {
        return this._resolveLocal(this._dateTime.withSecond(second));
      };
      _proto.withNano = function(nanoOfSecond) {
        return this._resolveLocal(this._dateTime.withNano(nanoOfSecond));
      };
      _proto.truncatedTo = function(unit) {
        return this._resolveLocal(this._dateTime.truncatedTo(unit));
      };
      _proto.plusAmount = function(amount) {
        requireNonNull(amount);
        return amount.addTo(this);
      };
      _proto.plusAmountUnit = function(amountToAdd, unit) {
        if (unit instanceof ChronoUnit) {
          return unit.isDateBased() ? this._resolveLocal(this._dateTime.plus(amountToAdd, unit)) : this._resolveInstant(this._dateTime.plus(amountToAdd, unit));
        }
        requireNonNull(unit, "unit");
        return unit.addTo(this, amountToAdd);
      };
      _proto.plusYears = function(years) {
        return this._resolveLocal(this._dateTime.plusYears(years));
      };
      _proto.plusMonths = function(months) {
        return this._resolveLocal(this._dateTime.plusMonths(months));
      };
      _proto.plusWeeks = function(weeks) {
        return this._resolveLocal(this._dateTime.plusWeeks(weeks));
      };
      _proto.plusDays = function(days) {
        return this._resolveLocal(this._dateTime.plusDays(days));
      };
      _proto.plusHours = function(hours) {
        return this._resolveInstant(this._dateTime.plusHours(hours));
      };
      _proto.plusMinutes = function(minutes) {
        return this._resolveInstant(this._dateTime.plusMinutes(minutes));
      };
      _proto.plusSeconds = function(seconds) {
        return this._resolveInstant(this._dateTime.plusSeconds(seconds));
      };
      _proto.plusNanos = function(nanos) {
        return this._resolveInstant(this._dateTime.plusNanos(nanos));
      };
      _proto.minusAmount = function(amount) {
        requireNonNull(amount, "amount");
        return amount.subtractFrom(this);
      };
      _proto.minusAmountUnit = function(amountToSubtract, unit) {
        return this.plusAmountUnit(-1 * amountToSubtract, unit);
      };
      _proto.minusYears = function(years) {
        return this.plusYears(-1 * years);
      };
      _proto.minusMonths = function(months) {
        return this.plusMonths(-1 * months);
      };
      _proto.minusWeeks = function(weeks) {
        return this.plusWeeks(-1 * weeks);
      };
      _proto.minusDays = function(days) {
        return this.plusDays(-1 * days);
      };
      _proto.minusHours = function(hours) {
        return this.plusHours(-1 * hours);
      };
      _proto.minusMinutes = function(minutes) {
        return this.plusMinutes(-1 * minutes);
      };
      _proto.minusSeconds = function(seconds) {
        return this.plusSeconds(-1 * seconds);
      };
      _proto.minusNanos = function(nanos) {
        return this.plusNanos(-1 * nanos);
      };
      _proto.query = function(_query) {
        if (_query === TemporalQueries.localDate()) {
          return this.toLocalDate();
        }
        requireNonNull(_query, "query");
        return _ChronoZonedDateTime.prototype.query.call(this, _query);
      };
      _proto.until = function(endExclusive, unit) {
        endExclusive = ZonedDateTime.from(endExclusive);
        if (unit instanceof ChronoUnit) {
          endExclusive = endExclusive.withZoneSameInstant(this._zone);
          if (unit.isDateBased()) {
            return this._dateTime.until(endExclusive._dateTime, unit);
          }
          var difference = this._offset.totalSeconds() - endExclusive._offset.totalSeconds();
          endExclusive = endExclusive._dateTime.plusSeconds(difference);
          return this._dateTime.until(endExclusive, unit);
        }
        return unit.between(this, endExclusive);
      };
      _proto.toLocalDateTime = function() {
        return this._dateTime;
      };
      _proto.toLocalDate = function() {
        return this._dateTime.toLocalDate();
      };
      _proto.toLocalTime = function() {
        return this._dateTime.toLocalTime();
      };
      _proto.toOffsetDateTime = function() {
        return OffsetDateTime.of(this._dateTime, this._offset);
      };
      _proto.equals = function(other) {
        return this === other ? !0 : other instanceof ZonedDateTime ? this._dateTime.equals(other._dateTime) && this._offset.equals(other._offset) && this._zone.equals(other._zone) : !1;
      };
      _proto.hashCode = function() {
        return MathUtil.hashCode(this._dateTime.hashCode(), this._offset.hashCode(), this._zone.hashCode());
      };
      _proto.toString = function() {
        var str = this._dateTime.toString() + this._offset.toString();
        this._offset !== this._zone && (str += "[" + this._zone.toString() + "]");
        return str;
      };
      _proto.toJSON = function() {
        return this.toString();
      };
      _proto.format = function(formatter) {
        return _ChronoZonedDateTime.prototype.format.call(this, formatter);
      };
      return ZonedDateTime;
    }(ChronoZonedDateTime), OffsetDateTime = function(_DefaultInterfaceTemp) {
      function OffsetDateTime(dateTime, offset) {
        var _this = _DefaultInterfaceTemp.call(this) || this;
        requireNonNull(dateTime, "dateTime");
        requireInstance(dateTime, LocalDateTime, "dateTime");
        requireNonNull(offset, "offset");
        requireInstance(offset, ZoneOffset, "offset");
        _this._dateTime = dateTime;
        _this._offset = offset;
        return _this;
      }
      _inheritsLoose(OffsetDateTime, _DefaultInterfaceTemp);
      OffsetDateTime.from = function(temporal) {
        requireNonNull(temporal, "temporal");
        if (temporal instanceof OffsetDateTime) {
          return temporal;
        }
        try {
          var offset = ZoneOffset.from(temporal);
          try {
            var ldt = LocalDateTime.from(temporal);
            return OffsetDateTime.of(ldt, offset);
          } catch (_) {
            var instant = Instant.from(temporal);
            return OffsetDateTime.ofInstant(instant, offset);
          }
        } catch (ex) {
          throw new DateTimeException("Unable to obtain OffsetDateTime TemporalAccessor: " + temporal + ", type " + (null != temporal.constructor ? temporal.constructor.name : ""));
        }
      };
      OffsetDateTime.now = function(clockOrZone) {
        if (0 === arguments.length) {
          return OffsetDateTime.now(Clock.systemDefaultZone());
        }
        requireNonNull(clockOrZone, "clockOrZone");
        if (clockOrZone instanceof ZoneId) {
          return OffsetDateTime.now(Clock.system(clockOrZone));
        }
        if (clockOrZone instanceof Clock) {
          var now = clockOrZone.instant();
          return OffsetDateTime.ofInstant(now, clockOrZone.zone().rules().offset(now));
        }
        throw new IllegalArgumentException("clockOrZone must be an instance of ZoneId or Clock");
      };
      OffsetDateTime.of = function() {
        return 2 >= arguments.length ? OffsetDateTime.ofDateTime.apply(this, arguments) : 3 === arguments.length ? OffsetDateTime.ofDateAndTime.apply(this, arguments) : OffsetDateTime.ofNumbers.apply(this, arguments);
      };
      OffsetDateTime.ofDateTime = function(dateTime, offset) {
        return new OffsetDateTime(dateTime, offset);
      };
      OffsetDateTime.ofDateAndTime = function(date, time, offset) {
        date = LocalDateTime.of(date, time);
        return new OffsetDateTime(date, offset);
      };
      OffsetDateTime.ofNumbers = function(year, month, dayOfMonth, hour, minute, second, nanoOfSecond, offset) {
        void 0 === hour && (hour = 0);
        void 0 === minute && (minute = 0);
        void 0 === second && (second = 0);
        void 0 === nanoOfSecond && (nanoOfSecond = 0);
        year = LocalDateTime.of(year, month, dayOfMonth, hour, minute, second, nanoOfSecond);
        return new OffsetDateTime(year, offset);
      };
      OffsetDateTime.ofInstant = function(instant, zone) {
        requireNonNull(instant, "instant");
        requireNonNull(zone, "zone");
        zone = zone.rules().offset(instant);
        instant = LocalDateTime.ofEpochSecond(instant.epochSecond(), instant.nano(), zone);
        return new OffsetDateTime(instant, zone);
      };
      OffsetDateTime.parse = function(text, formatter) {
        void 0 === formatter && (formatter = DateTimeFormatter.ISO_OFFSET_DATE_TIME);
        requireNonNull(formatter, "formatter");
        return formatter.parse(text, OffsetDateTime.FROM);
      };
      var _proto = OffsetDateTime.prototype;
      _proto.adjustInto = function(temporal) {
        return temporal.with(ChronoField.EPOCH_DAY, this.toLocalDate().toEpochDay()).with(ChronoField.NANO_OF_DAY, this.toLocalTime().toNanoOfDay()).with(ChronoField.OFFSET_SECONDS, this.offset().totalSeconds());
      };
      _proto.until = function(endExclusive, unit) {
        endExclusive = OffsetDateTime.from(endExclusive);
        return unit instanceof ChronoUnit ? (endExclusive = endExclusive.withOffsetSameInstant(this._offset), this._dateTime.until(endExclusive._dateTime, unit)) : unit.between(this, endExclusive);
      };
      _proto.atZoneSameInstant = function(zone) {
        return ZonedDateTime.ofInstant(this._dateTime, this._offset, zone);
      };
      _proto.atZoneSimilarLocal = function(zone) {
        return ZonedDateTime.ofLocal(this._dateTime, zone, this._offset);
      };
      _proto.query = function(_query) {
        requireNonNull(_query, "query");
        return _query === TemporalQueries.chronology() ? IsoChronology.INSTANCE : _query === TemporalQueries.precision() ? ChronoUnit.NANOS : _query === TemporalQueries.offset() || _query === TemporalQueries.zone() ? this.offset() : _query === TemporalQueries.localDate() ? this.toLocalDate() : _query === TemporalQueries.localTime() ? this.toLocalTime() : _query === TemporalQueries.zoneId() ? null : _DefaultInterfaceTemp.prototype.query.call(this, _query);
      };
      _proto.get = function(field) {
        if (field instanceof ChronoField) {
          switch(field) {
            case ChronoField.INSTANT_SECONDS:
              throw new DateTimeException("Field too large for an int: " + field);
            case ChronoField.OFFSET_SECONDS:
              return this.offset().totalSeconds();
          }
          return this._dateTime.get(field);
        }
        return _DefaultInterfaceTemp.prototype.get.call(this, field);
      };
      _proto.getLong = function(field) {
        if (field instanceof ChronoField) {
          switch(field) {
            case ChronoField.INSTANT_SECONDS:
              return this.toEpochSecond();
            case ChronoField.OFFSET_SECONDS:
              return this.offset().totalSeconds();
          }
          return this._dateTime.getLong(field);
        }
        return field.getFrom(this);
      };
      _proto.offset = function() {
        return this._offset;
      };
      _proto.year = function() {
        return this._dateTime.year();
      };
      _proto.monthValue = function() {
        return this._dateTime.monthValue();
      };
      _proto.month = function() {
        return this._dateTime.month();
      };
      _proto.dayOfMonth = function() {
        return this._dateTime.dayOfMonth();
      };
      _proto.dayOfYear = function() {
        return this._dateTime.dayOfYear();
      };
      _proto.dayOfWeek = function() {
        return this._dateTime.dayOfWeek();
      };
      _proto.hour = function() {
        return this._dateTime.hour();
      };
      _proto.minute = function() {
        return this._dateTime.minute();
      };
      _proto.second = function() {
        return this._dateTime.second();
      };
      _proto.nano = function() {
        return this._dateTime.nano();
      };
      _proto.toLocalDateTime = function() {
        return this._dateTime;
      };
      _proto.toLocalDate = function() {
        return this._dateTime.toLocalDate();
      };
      _proto.toLocalTime = function() {
        return this._dateTime.toLocalTime();
      };
      _proto.toOffsetTime = function() {
        return OffsetTime.of(this._dateTime.toLocalTime(), this._offset);
      };
      _proto.toZonedDateTime = function() {
        return ZonedDateTime.of(this._dateTime, this._offset);
      };
      _proto.toInstant = function() {
        return this._dateTime.toInstant(this._offset);
      };
      _proto.toEpochSecond = function() {
        return this._dateTime.toEpochSecond(this._offset);
      };
      _proto.isSupported = function(fieldOrUnit) {
        return fieldOrUnit instanceof ChronoField || fieldOrUnit instanceof ChronoUnit ? fieldOrUnit.isDateBased() || fieldOrUnit.isTimeBased() : null != fieldOrUnit && fieldOrUnit.isSupportedBy(this);
      };
      _proto.range = function(field) {
        return field instanceof ChronoField ? field === ChronoField.INSTANT_SECONDS || field === ChronoField.OFFSET_SECONDS ? field.range() : this._dateTime.range(field) : field.rangeRefinedBy(this);
      };
      _proto.withAdjuster = function(adjuster) {
        requireNonNull(adjuster);
        return adjuster instanceof LocalDate || adjuster instanceof LocalTime || adjuster instanceof LocalDateTime ? this._withDateTimeOffset(this._dateTime.with(adjuster), this._offset) : adjuster instanceof Instant ? OffsetDateTime.ofInstant(adjuster, this._offset) : adjuster instanceof ZoneOffset ? this._withDateTimeOffset(this._dateTime, adjuster) : adjuster instanceof OffsetDateTime ? adjuster : adjuster.adjustInto(this);
      };
      _proto.withFieldValue = function(field, newValue) {
        requireNonNull(field);
        if (field instanceof ChronoField) {
          switch(field) {
            case ChronoField.INSTANT_SECONDS:
              return OffsetDateTime.ofInstant(Instant.ofEpochSecond(newValue, this.nano()), this._offset);
            case ChronoField.OFFSET_SECONDS:
              return this._withDateTimeOffset(this._dateTime, ZoneOffset.ofTotalSeconds(field.checkValidIntValue(newValue)));
          }
          return this._withDateTimeOffset(this._dateTime.with(field, newValue), this._offset);
        }
        return field.adjustInto(this, newValue);
      };
      _proto._withDateTimeOffset = function(dateTime, offset) {
        return this._dateTime === dateTime && this._offset.equals(offset) ? this : new OffsetDateTime(dateTime, offset);
      };
      _proto.withYear = function(year) {
        return this._withDateTimeOffset(this._dateTime.withYear(year), this._offset);
      };
      _proto.withMonth = function(month) {
        return this._withDateTimeOffset(this._dateTime.withMonth(month), this._offset);
      };
      _proto.withDayOfMonth = function(dayOfMonth) {
        return this._withDateTimeOffset(this._dateTime.withDayOfMonth(dayOfMonth), this._offset);
      };
      _proto.withDayOfYear = function(dayOfYear) {
        return this._withDateTimeOffset(this._dateTime.withDayOfYear(dayOfYear), this._offset);
      };
      _proto.withHour = function(hour) {
        return this._withDateTimeOffset(this._dateTime.withHour(hour), this._offset);
      };
      _proto.withMinute = function(minute) {
        return this._withDateTimeOffset(this._dateTime.withMinute(minute), this._offset);
      };
      _proto.withSecond = function(second) {
        return this._withDateTimeOffset(this._dateTime.withSecond(second), this._offset);
      };
      _proto.withNano = function(nanoOfSecond) {
        return this._withDateTimeOffset(this._dateTime.withNano(nanoOfSecond), this._offset);
      };
      _proto.withOffsetSameLocal = function(offset) {
        requireNonNull(offset, "offset");
        return this._withDateTimeOffset(this._dateTime, offset);
      };
      _proto.withOffsetSameInstant = function(offset) {
        requireNonNull(offset, "offset");
        if (offset.equals(this._offset)) {
          return this;
        }
        var difference = offset.totalSeconds() - this._offset.totalSeconds();
        difference = this._dateTime.plusSeconds(difference);
        return new OffsetDateTime(difference, offset);
      };
      _proto.truncatedTo = function(unit) {
        return this._withDateTimeOffset(this._dateTime.truncatedTo(unit), this._offset);
      };
      _proto.plusAmount = function(amount) {
        requireNonNull(amount, "amount");
        return amount.addTo(this);
      };
      _proto.plusAmountUnit = function(amountToAdd, unit) {
        return unit instanceof ChronoUnit ? this._withDateTimeOffset(this._dateTime.plus(amountToAdd, unit), this._offset) : unit.addTo(this, amountToAdd);
      };
      _proto.plusYears = function(years) {
        return this._withDateTimeOffset(this._dateTime.plusYears(years), this._offset);
      };
      _proto.plusMonths = function(months) {
        return this._withDateTimeOffset(this._dateTime.plusMonths(months), this._offset);
      };
      _proto.plusWeeks = function(weeks) {
        return this._withDateTimeOffset(this._dateTime.plusWeeks(weeks), this._offset);
      };
      _proto.plusDays = function(days) {
        return this._withDateTimeOffset(this._dateTime.plusDays(days), this._offset);
      };
      _proto.plusHours = function(hours) {
        return this._withDateTimeOffset(this._dateTime.plusHours(hours), this._offset);
      };
      _proto.plusMinutes = function(minutes) {
        return this._withDateTimeOffset(this._dateTime.plusMinutes(minutes), this._offset);
      };
      _proto.plusSeconds = function(seconds) {
        return this._withDateTimeOffset(this._dateTime.plusSeconds(seconds), this._offset);
      };
      _proto.plusNanos = function(nanos) {
        return this._withDateTimeOffset(this._dateTime.plusNanos(nanos), this._offset);
      };
      _proto.minusAmount = function(amount) {
        requireNonNull(amount);
        return amount.subtractFrom(this);
      };
      _proto.minusAmountUnit = function(amountToSubtract, unit) {
        return this.plus(-1 * amountToSubtract, unit);
      };
      _proto.minusYears = function(years) {
        return this._withDateTimeOffset(this._dateTime.minusYears(years), this._offset);
      };
      _proto.minusMonths = function(months) {
        return this._withDateTimeOffset(this._dateTime.minusMonths(months), this._offset);
      };
      _proto.minusWeeks = function(weeks) {
        return this._withDateTimeOffset(this._dateTime.minusWeeks(weeks), this._offset);
      };
      _proto.minusDays = function(days) {
        return this._withDateTimeOffset(this._dateTime.minusDays(days), this._offset);
      };
      _proto.minusHours = function(hours) {
        return this._withDateTimeOffset(this._dateTime.minusHours(hours), this._offset);
      };
      _proto.minusMinutes = function(minutes) {
        return this._withDateTimeOffset(this._dateTime.minusMinutes(minutes), this._offset);
      };
      _proto.minusSeconds = function(seconds) {
        return this._withDateTimeOffset(this._dateTime.minusSeconds(seconds), this._offset);
      };
      _proto.minusNanos = function(nanos) {
        return this._withDateTimeOffset(this._dateTime.minusNanos(nanos), this._offset);
      };
      _proto.compareTo = function(other) {
        requireNonNull(other, "other");
        requireInstance(other, OffsetDateTime, "other");
        if (this.offset().equals(other.offset())) {
          return this.toLocalDateTime().compareTo(other.toLocalDateTime());
        }
        var cmp = MathUtil.compareNumbers(this.toEpochSecond(), other.toEpochSecond());
        0 === cmp && (cmp = this.toLocalTime().nano() - other.toLocalTime().nano(), 0 === cmp && (cmp = this.toLocalDateTime().compareTo(other.toLocalDateTime())));
        return cmp;
      };
      _proto.isAfter = function(other) {
        requireNonNull(other, "other");
        var thisEpochSec = this.toEpochSecond(), otherEpochSec = other.toEpochSecond();
        return thisEpochSec > otherEpochSec || thisEpochSec === otherEpochSec && this.toLocalTime().nano() > other.toLocalTime().nano();
      };
      _proto.isBefore = function(other) {
        requireNonNull(other, "other");
        var thisEpochSec = this.toEpochSecond(), otherEpochSec = other.toEpochSecond();
        return thisEpochSec < otherEpochSec || thisEpochSec === otherEpochSec && this.toLocalTime().nano() < other.toLocalTime().nano();
      };
      _proto.isEqual = function(other) {
        requireNonNull(other, "other");
        return this.toEpochSecond() === other.toEpochSecond() && this.toLocalTime().nano() === other.toLocalTime().nano();
      };
      _proto.equals = function(other) {
        return this === other ? !0 : other instanceof OffsetDateTime ? this._dateTime.equals(other._dateTime) && this._offset.equals(other._offset) : !1;
      };
      _proto.hashCode = function() {
        return this._dateTime.hashCode() ^ this._offset.hashCode();
      };
      _proto.toString = function() {
        return this._dateTime.toString() + this._offset.toString();
      };
      _proto.toJSON = function() {
        return this.toString();
      };
      _proto.format = function(formatter) {
        requireNonNull(formatter, "formatter");
        return formatter.format(this);
      };
      return OffsetDateTime;
    }(DefaultInterfaceTemporal), LocalDate = function(_ChronoLocalDate) {
      function LocalDate(year, month, dayOfMonth) {
        var _this = _ChronoLocalDate.call(this) || this;
        requireNonNull(year, "year");
        requireNonNull(month, "month");
        requireNonNull(dayOfMonth, "dayOfMonth");
        month instanceof Month && (month = month.value());
        _this._year = MathUtil.safeToInt(year);
        _this._month = MathUtil.safeToInt(month);
        _this._day = MathUtil.safeToInt(dayOfMonth);
        LocalDate._validate(_this._year, _this._month, _this._day);
        return _this;
      }
      _inheritsLoose(LocalDate, _ChronoLocalDate);
      LocalDate.now = function(clockOrZone) {
        clockOrZone = null == clockOrZone ? Clock.systemDefaultZone() : clockOrZone instanceof ZoneId ? Clock.system(clockOrZone) : clockOrZone;
        return LocalDate.ofInstant(clockOrZone.instant(), clockOrZone.zone());
      };
      LocalDate.ofInstant = function(instant, zone) {
        void 0 === zone && (zone = ZoneId.systemDefault());
        requireNonNull(instant, "instant");
        zone = zone.rules().offset(instant);
        instant = instant.epochSecond() + zone.totalSeconds();
        instant = MathUtil.floorDiv(instant, LocalTime.SECONDS_PER_DAY);
        return LocalDate.ofEpochDay(instant);
      };
      LocalDate.of = function(year, month, dayOfMonth) {
        return new LocalDate(year, month, dayOfMonth);
      };
      LocalDate.ofYearDay = function(year, dayOfYear) {
        ChronoField.YEAR.checkValidValue(year);
        var leap = IsoChronology.isLeapYear(year);
        366 === dayOfYear && !1 === leap && assert(!1, "Invalid date 'DayOfYear 366' as '" + year + "' is not a leap year", DateTimeException);
        var moy = Month.of(Math.floor((dayOfYear - 1) / 31 + 1)), monthEnd = moy.firstDayOfYear(leap) + moy.length(leap) - 1;
        dayOfYear > monthEnd && (moy = moy.plus(1));
        dayOfYear = dayOfYear - moy.firstDayOfYear(leap) + 1;
        return new LocalDate(year, moy.value(), dayOfYear);
      };
      LocalDate.ofEpochDay = function(epochDay) {
        void 0 === epochDay && (epochDay = 0);
        var zeroDay = epochDay + 719528 - 60;
        var adjust = 0;
        0 > zeroDay && (epochDay = MathUtil.intDiv(zeroDay + 1, 146097) - 1, adjust = 400 * epochDay, zeroDay += 146097 * -epochDay);
        epochDay = MathUtil.intDiv(400 * zeroDay + 591, 146097);
        var doyEst = zeroDay - (365 * epochDay + MathUtil.intDiv(epochDay, 4) - MathUtil.intDiv(epochDay, 100) + MathUtil.intDiv(epochDay, 400));
        0 > doyEst && (epochDay--, doyEst = zeroDay - (365 * epochDay + MathUtil.intDiv(epochDay, 4) - MathUtil.intDiv(epochDay, 100) + MathUtil.intDiv(epochDay, 400)));
        epochDay += adjust;
        adjust = MathUtil.intDiv(5 * doyEst + 2, 153);
        zeroDay = (adjust + 2) % 12 + 1;
        doyEst = doyEst - MathUtil.intDiv(306 * adjust + 5, 10) + 1;
        epochDay += MathUtil.intDiv(adjust, 10);
        return new LocalDate(epochDay, zeroDay, doyEst);
      };
      LocalDate.from = function(temporal) {
        requireNonNull(temporal, "temporal");
        var date = temporal.query(TemporalQueries.localDate());
        if (null == date) {
          throw new DateTimeException("Unable to obtain LocalDate from TemporalAccessor: " + temporal + ", type " + (null != temporal.constructor ? temporal.constructor.name : ""));
        }
        return date;
      };
      LocalDate.parse = function(text, formatter) {
        void 0 === formatter && (formatter = DateTimeFormatter.ISO_LOCAL_DATE);
        assert(null != formatter, "formatter", NullPointerException);
        return formatter.parse(text, LocalDate.FROM);
      };
      LocalDate._resolvePreviousValid = function(year, month, day) {
        switch(month) {
          case 2:
            day = Math.min(day, IsoChronology.isLeapYear(year) ? 29 : 28);
            break;
          case 4:
          case 6:
          case 9:
          case 11:
            day = Math.min(day, 30);
        }
        return LocalDate.of(year, month, day);
      };
      LocalDate._validate = function(year, month, dayOfMonth) {
        ChronoField.YEAR.checkValidValue(year);
        ChronoField.MONTH_OF_YEAR.checkValidValue(month);
        ChronoField.DAY_OF_MONTH.checkValidValue(dayOfMonth);
        if (28 < dayOfMonth) {
          var dom = 31;
          switch(month) {
            case 2:
              dom = IsoChronology.isLeapYear(year) ? 29 : 28;
              break;
            case 4:
            case 6:
            case 9:
            case 11:
              dom = 30;
          }
          dayOfMonth > dom && (29 === dayOfMonth ? assert(!1, "Invalid date 'February 29' as '" + year + "' is not a leap year", DateTimeException) : assert(!1, "Invalid date '" + year + "' '" + month + "' '" + dayOfMonth + "'", DateTimeException));
        }
      };
      var _proto = LocalDate.prototype;
      _proto.isSupported = function(field) {
        return _ChronoLocalDate.prototype.isSupported.call(this, field);
      };
      _proto.range = function(field) {
        if (field instanceof ChronoField) {
          if (field.isDateBased()) {
            switch(field) {
              case ChronoField.DAY_OF_MONTH:
                return ValueRange.of(1, this.lengthOfMonth());
              case ChronoField.DAY_OF_YEAR:
                return ValueRange.of(1, this.lengthOfYear());
              case ChronoField.ALIGNED_WEEK_OF_MONTH:
                return ValueRange.of(1, this.month() === Month.FEBRUARY && !1 === this.isLeapYear() ? 4 : 5);
              case ChronoField.YEAR_OF_ERA:
                return 0 >= this._year ? ValueRange.of(1, Year.MAX_VALUE + 1) : ValueRange.of(1, Year.MAX_VALUE);
            }
            return field.range();
          }
          throw new UnsupportedTemporalTypeException("Unsupported field: " + field);
        }
        return field.rangeRefinedBy(this);
      };
      _proto.get = function(field) {
        return this.getLong(field);
      };
      _proto.getLong = function(field) {
        assert(null != field, "", NullPointerException);
        return field instanceof ChronoField ? this._get0(field) : field.getFrom(this);
      };
      _proto._get0 = function(field) {
        switch(field) {
          case ChronoField.DAY_OF_WEEK:
            return this.dayOfWeek().value();
          case ChronoField.ALIGNED_DAY_OF_WEEK_IN_MONTH:
            return MathUtil.intMod(this._day - 1, 7) + 1;
          case ChronoField.ALIGNED_DAY_OF_WEEK_IN_YEAR:
            return MathUtil.intMod(this.dayOfYear() - 1, 7) + 1;
          case ChronoField.DAY_OF_MONTH:
            return this._day;
          case ChronoField.DAY_OF_YEAR:
            return this.dayOfYear();
          case ChronoField.EPOCH_DAY:
            return this.toEpochDay();
          case ChronoField.ALIGNED_WEEK_OF_MONTH:
            return MathUtil.intDiv(this._day - 1, 7) + 1;
          case ChronoField.ALIGNED_WEEK_OF_YEAR:
            return MathUtil.intDiv(this.dayOfYear() - 1, 7) + 1;
          case ChronoField.MONTH_OF_YEAR:
            return this._month;
          case ChronoField.PROLEPTIC_MONTH:
            return this._prolepticMonth();
          case ChronoField.YEAR_OF_ERA:
            return 1 <= this._year ? this._year : 1 - this._year;
          case ChronoField.YEAR:
            return this._year;
          case ChronoField.ERA:
            return 1 <= this._year ? 1 : 0;
        }
        throw new UnsupportedTemporalTypeException("Unsupported field: " + field);
      };
      _proto._prolepticMonth = function() {
        return 12 * this._year + (this._month - 1);
      };
      _proto.chronology = function() {
        return IsoChronology.INSTANCE;
      };
      _proto.year = function() {
        return this._year;
      };
      _proto.monthValue = function() {
        return this._month;
      };
      _proto.month = function() {
        return Month.of(this._month);
      };
      _proto.dayOfMonth = function() {
        return this._day;
      };
      _proto.dayOfYear = function() {
        return this.month().firstDayOfYear(this.isLeapYear()) + this._day - 1;
      };
      _proto.dayOfWeek = function() {
        var dow0 = MathUtil.floorMod(this.toEpochDay() + 3, 7);
        return DayOfWeek.of(dow0 + 1);
      };
      _proto.isLeapYear = function() {
        return IsoChronology.isLeapYear(this._year);
      };
      _proto.lengthOfMonth = function() {
        switch(this._month) {
          case 2:
            return this.isLeapYear() ? 29 : 28;
          case 4:
          case 6:
          case 9:
          case 11:
            return 30;
          default:
            return 31;
        }
      };
      _proto.lengthOfYear = function() {
        return this.isLeapYear() ? 366 : 365;
      };
      _proto.withAdjuster = function(adjuster) {
        requireNonNull(adjuster, "adjuster");
        if (adjuster instanceof LocalDate) {
          return adjuster;
        }
        assert("function" === typeof adjuster.adjustInto, "adjuster", IllegalArgumentException);
        return adjuster.adjustInto(this);
      };
      _proto.withFieldValue = function(field, newValue) {
        assert(null != field, "field", NullPointerException);
        if (field instanceof ChronoField) {
          field.checkValidValue(newValue);
          switch(field) {
            case ChronoField.DAY_OF_WEEK:
              return this.plusDays(newValue - this.dayOfWeek().value());
            case ChronoField.ALIGNED_DAY_OF_WEEK_IN_MONTH:
              return this.plusDays(newValue - this.getLong(ChronoField.ALIGNED_DAY_OF_WEEK_IN_MONTH));
            case ChronoField.ALIGNED_DAY_OF_WEEK_IN_YEAR:
              return this.plusDays(newValue - this.getLong(ChronoField.ALIGNED_DAY_OF_WEEK_IN_YEAR));
            case ChronoField.DAY_OF_MONTH:
              return this.withDayOfMonth(newValue);
            case ChronoField.DAY_OF_YEAR:
              return this.withDayOfYear(newValue);
            case ChronoField.EPOCH_DAY:
              return LocalDate.ofEpochDay(newValue);
            case ChronoField.ALIGNED_WEEK_OF_MONTH:
              return this.plusWeeks(newValue - this.getLong(ChronoField.ALIGNED_WEEK_OF_MONTH));
            case ChronoField.ALIGNED_WEEK_OF_YEAR:
              return this.plusWeeks(newValue - this.getLong(ChronoField.ALIGNED_WEEK_OF_YEAR));
            case ChronoField.MONTH_OF_YEAR:
              return this.withMonth(newValue);
            case ChronoField.PROLEPTIC_MONTH:
              return this.plusMonths(newValue - this.getLong(ChronoField.PROLEPTIC_MONTH));
            case ChronoField.YEAR_OF_ERA:
              return this.withYear(1 <= this._year ? newValue : 1 - newValue);
            case ChronoField.YEAR:
              return this.withYear(newValue);
            case ChronoField.ERA:
              return this.getLong(ChronoField.ERA) === newValue ? this : this.withYear(1 - this._year);
          }
          throw new UnsupportedTemporalTypeException("Unsupported field: " + field);
        }
        return field.adjustInto(this, newValue);
      };
      _proto.withYear = function(year) {
        if (this._year === year) {
          return this;
        }
        ChronoField.YEAR.checkValidValue(year);
        return LocalDate._resolvePreviousValid(year, this._month, this._day);
      };
      _proto.withMonth = function(month) {
        month = month instanceof Month ? month.value() : month;
        if (this._month === month) {
          return this;
        }
        ChronoField.MONTH_OF_YEAR.checkValidValue(month);
        return LocalDate._resolvePreviousValid(this._year, month, this._day);
      };
      _proto.withDayOfMonth = function(dayOfMonth) {
        return this._day === dayOfMonth ? this : LocalDate.of(this._year, this._month, dayOfMonth);
      };
      _proto.withDayOfYear = function(dayOfYear) {
        return this.dayOfYear() === dayOfYear ? this : LocalDate.ofYearDay(this._year, dayOfYear);
      };
      _proto.plusAmount = function(amount) {
        requireNonNull(amount, "amount");
        return amount.addTo(this);
      };
      _proto.plusAmountUnit = function(amountToAdd, unit) {
        requireNonNull(amountToAdd, "amountToAdd");
        requireNonNull(unit, "unit");
        if (unit instanceof ChronoUnit) {
          switch(unit) {
            case ChronoUnit.DAYS:
              return this.plusDays(amountToAdd);
            case ChronoUnit.WEEKS:
              return this.plusWeeks(amountToAdd);
            case ChronoUnit.MONTHS:
              return this.plusMonths(amountToAdd);
            case ChronoUnit.YEARS:
              return this.plusYears(amountToAdd);
            case ChronoUnit.DECADES:
              return this.plusYears(MathUtil.safeMultiply(amountToAdd, 10));
            case ChronoUnit.CENTURIES:
              return this.plusYears(MathUtil.safeMultiply(amountToAdd, 100));
            case ChronoUnit.MILLENNIA:
              return this.plusYears(MathUtil.safeMultiply(amountToAdd, 1000));
            case ChronoUnit.ERAS:
              return this.with(ChronoField.ERA, MathUtil.safeAdd(this.getLong(ChronoField.ERA), amountToAdd));
          }
          throw new UnsupportedTemporalTypeException("Unsupported unit: " + unit);
        }
        return unit.addTo(this, amountToAdd);
      };
      _proto.plusYears = function(yearsToAdd) {
        if (0 === yearsToAdd) {
          return this;
        }
        yearsToAdd = ChronoField.YEAR.checkValidIntValue(this._year + yearsToAdd);
        return LocalDate._resolvePreviousValid(yearsToAdd, this._month, this._day);
      };
      _proto.plusMonths = function(monthsToAdd) {
        if (0 === monthsToAdd) {
          return this;
        }
        var calcMonths = 12 * this._year + (this._month - 1) + monthsToAdd;
        monthsToAdd = ChronoField.YEAR.checkValidIntValue(MathUtil.floorDiv(calcMonths, 12));
        calcMonths = MathUtil.floorMod(calcMonths, 12) + 1;
        return LocalDate._resolvePreviousValid(monthsToAdd, calcMonths, this._day);
      };
      _proto.plusWeeks = function(weeksToAdd) {
        return this.plusDays(MathUtil.safeMultiply(weeksToAdd, 7));
      };
      _proto.plusDays = function(daysToAdd) {
        if (0 === daysToAdd) {
          return this;
        }
        daysToAdd = MathUtil.safeAdd(this.toEpochDay(), daysToAdd);
        return LocalDate.ofEpochDay(daysToAdd);
      };
      _proto.minusAmount = function(amount) {
        requireNonNull(amount, "amount");
        return amount.subtractFrom(this);
      };
      _proto.minusAmountUnit = function(amountToSubtract, unit) {
        requireNonNull(amountToSubtract, "amountToSubtract");
        requireNonNull(unit, "unit");
        return this.plusAmountUnit(-1 * amountToSubtract, unit);
      };
      _proto.minusYears = function(yearsToSubtract) {
        return this.plusYears(-1 * yearsToSubtract);
      };
      _proto.minusMonths = function(monthsToSubtract) {
        return this.plusMonths(-1 * monthsToSubtract);
      };
      _proto.minusWeeks = function(weeksToSubtract) {
        return this.plusWeeks(-1 * weeksToSubtract);
      };
      _proto.minusDays = function(daysToSubtract) {
        return this.plusDays(-1 * daysToSubtract);
      };
      _proto.query = function(_query) {
        requireNonNull(_query, "query");
        return _query === TemporalQueries.localDate() ? this : _ChronoLocalDate.prototype.query.call(this, _query);
      };
      _proto.adjustInto = function(temporal) {
        return _ChronoLocalDate.prototype.adjustInto.call(this, temporal);
      };
      _proto.until = function(p1, p2) {
        return 2 > arguments.length ? this.until1(p1) : this.until2(p1, p2);
      };
      _proto.until2 = function(endExclusive, unit) {
        endExclusive = LocalDate.from(endExclusive);
        if (unit instanceof ChronoUnit) {
          switch(unit) {
            case ChronoUnit.DAYS:
              return this.daysUntil(endExclusive);
            case ChronoUnit.WEEKS:
              return MathUtil.intDiv(this.daysUntil(endExclusive), 7);
            case ChronoUnit.MONTHS:
              return this._monthsUntil(endExclusive);
            case ChronoUnit.YEARS:
              return MathUtil.intDiv(this._monthsUntil(endExclusive), 12);
            case ChronoUnit.DECADES:
              return MathUtil.intDiv(this._monthsUntil(endExclusive), 120);
            case ChronoUnit.CENTURIES:
              return MathUtil.intDiv(this._monthsUntil(endExclusive), 1200);
            case ChronoUnit.MILLENNIA:
              return MathUtil.intDiv(this._monthsUntil(endExclusive), 12000);
            case ChronoUnit.ERAS:
              return endExclusive.getLong(ChronoField.ERA) - this.getLong(ChronoField.ERA);
          }
          throw new UnsupportedTemporalTypeException("Unsupported unit: " + unit);
        }
        return unit.between(this, endExclusive);
      };
      _proto.daysUntil = function(end) {
        return end.toEpochDay() - this.toEpochDay();
      };
      _proto._monthsUntil = function(end) {
        var packed1 = 32 * this._prolepticMonth() + this.dayOfMonth();
        end = 32 * end._prolepticMonth() + end.dayOfMonth();
        return MathUtil.intDiv(end - packed1, 32);
      };
      _proto.until1 = function(endDate) {
        var end = LocalDate.from(endDate);
        endDate = end._prolepticMonth() - this._prolepticMonth();
        var days = end._day - this._day;
        0 < endDate && 0 > days ? (endDate--, days = this.plusMonths(endDate), days = end.toEpochDay() - days.toEpochDay()) : 0 > endDate && 0 < days && (endDate++, days -= end.lengthOfMonth());
        end = MathUtil.intDiv(endDate, 12);
        endDate = MathUtil.intMod(endDate, 12);
        return Period.of(end, endDate, days);
      };
      _proto.atTime = function() {
        return 1 === arguments.length ? this.atTime1.apply(this, arguments) : this.atTime4.apply(this, arguments);
      };
      _proto.atTime1 = function(time) {
        requireNonNull(time, "time");
        if (time instanceof LocalTime) {
          return LocalDateTime.of(this, time);
        }
        if (time instanceof OffsetTime) {
          return this._atTimeOffsetTime(time);
        }
        throw new IllegalArgumentException("time must be an instance of LocalTime or OffsetTime" + (time && time.constructor && time.constructor.name ? ", but is " + time.constructor.name : ""));
      };
      _proto.atTime4 = function(hour, minute, second, nanoOfSecond) {
        void 0 === second && (second = 0);
        void 0 === nanoOfSecond && (nanoOfSecond = 0);
        return this.atTime1(LocalTime.of(hour, minute, second, nanoOfSecond));
      };
      _proto._atTimeOffsetTime = function(time) {
        return OffsetDateTime.of(LocalDateTime.of(this, time.toLocalTime()), time.offset());
      };
      _proto.atStartOfDay = function(zone) {
        return null != zone ? this.atStartOfDayWithZone(zone) : LocalDateTime.of(this, LocalTime.MIDNIGHT);
      };
      _proto.atStartOfDayWithZone = function(zone) {
        requireNonNull(zone, "zone");
        var ldt = this.atTime(LocalTime.MIDNIGHT);
        if (!1 === zone instanceof ZoneOffset) {
          var trans = zone.rules().transition(ldt);
          null != trans && trans.isGap() && (ldt = trans.dateTimeAfter());
        }
        return ZonedDateTime.of(ldt, zone);
      };
      _proto.toEpochDay = function() {
        var y = this._year, m = this._month;
        var total = 365 * y;
        total = 0 <= y ? total + (MathUtil.intDiv(y + 3, 4) - MathUtil.intDiv(y + 99, 100) + MathUtil.intDiv(y + 399, 400)) : total - (MathUtil.intDiv(y, -4) - MathUtil.intDiv(y, -100) + MathUtil.intDiv(y, -400));
        total += MathUtil.intDiv(367 * m - 362, 12);
        total += this.dayOfMonth() - 1;
        2 < m && (total--, IsoChronology.isLeapYear(y) || total--);
        return total - 719528;
      };
      _proto.compareTo = function(other) {
        requireNonNull(other, "other");
        requireInstance(other, LocalDate, "other");
        return this._compareTo0(other);
      };
      _proto._compareTo0 = function(otherDate) {
        var cmp = this._year - otherDate._year;
        0 === cmp && (cmp = this._month - otherDate._month, 0 === cmp && (cmp = this._day - otherDate._day));
        return cmp;
      };
      _proto.isAfter = function(other) {
        return 0 < this.compareTo(other);
      };
      _proto.isBefore = function(other) {
        return 0 > this.compareTo(other);
      };
      _proto.isEqual = function(other) {
        return 0 === this.compareTo(other);
      };
      _proto.equals = function(other) {
        return this === other ? !0 : other instanceof LocalDate ? 0 === this._compareTo0(other) : !1;
      };
      _proto.hashCode = function() {
        var yearValue = this._year;
        return MathUtil.hash(yearValue & 4294965248 ^ (yearValue << 11) + (this._month << 6) + this._day);
      };
      _proto.toString = function() {
        var yearValue = this._year, monthValue = this._month, dayValue = this._day;
        return (1000 > Math.abs(yearValue) ? 0 > yearValue ? "-" + ("" + (yearValue - 10000)).slice(-4) : ("" + (yearValue + 10000)).slice(-4) : 9999 < yearValue ? "+" + yearValue : "" + yearValue) + (10 > monthValue ? "-0" + monthValue : "-" + monthValue) + (10 > dayValue ? "-0" + dayValue : "-" + dayValue);
      };
      _proto.toJSON = function() {
        return this.toString();
      };
      _proto.format = function(formatter) {
        requireNonNull(formatter, "formatter");
        requireInstance(formatter, DateTimeFormatter, "formatter");
        return _ChronoLocalDate.prototype.format.call(this, formatter);
      };
      return LocalDate;
    }(ChronoLocalDate), ChronoLocalDateTime = function(_DefaultInterfaceTemp) {
      function ChronoLocalDateTime() {
        return _DefaultInterfaceTemp.apply(this, arguments) || this;
      }
      _inheritsLoose(ChronoLocalDateTime, _DefaultInterfaceTemp);
      var _proto = ChronoLocalDateTime.prototype;
      _proto.chronology = function() {
        return this.toLocalDate().chronology();
      };
      _proto.query = function(_query) {
        return _query === TemporalQueries.chronology() ? this.chronology() : _query === TemporalQueries.precision() ? ChronoUnit.NANOS : _query === TemporalQueries.localDate() ? LocalDate.ofEpochDay(this.toLocalDate().toEpochDay()) : _query === TemporalQueries.localTime() ? this.toLocalTime() : _query === TemporalQueries.zone() || _query === TemporalQueries.zoneId() || _query === TemporalQueries.offset() ? null : _DefaultInterfaceTemp.prototype.query.call(this, _query);
      };
      _proto.adjustInto = function(temporal) {
        return temporal.with(ChronoField.EPOCH_DAY, this.toLocalDate().toEpochDay()).with(ChronoField.NANO_OF_DAY, this.toLocalTime().toNanoOfDay());
      };
      _proto.toInstant = function(offset) {
        requireInstance(offset, ZoneOffset, "zoneId");
        return Instant.ofEpochSecond(this.toEpochSecond(offset), this.toLocalTime().nano());
      };
      _proto.toEpochSecond = function(offset) {
        requireNonNull(offset, "offset");
        var secs = 86400 * this.toLocalDate().toEpochDay() + this.toLocalTime().toSecondOfDay();
        secs -= offset.totalSeconds();
        return MathUtil.safeToInt(secs);
      };
      return ChronoLocalDateTime;
    }(DefaultInterfaceTemporal), LocalDateTime = function(_ChronoLocalDateTime) {
      function LocalDateTime(date, time) {
        var _this = _ChronoLocalDateTime.call(this) || this;
        requireInstance(date, LocalDate, "date");
        requireInstance(time, LocalTime, "time");
        _this._date = date;
        _this._time = time;
        return _this;
      }
      _inheritsLoose(LocalDateTime, _ChronoLocalDateTime);
      LocalDateTime.now = function(clockOrZone) {
        return null == clockOrZone ? LocalDateTime._now(Clock.systemDefaultZone()) : clockOrZone instanceof Clock ? LocalDateTime._now(clockOrZone) : LocalDateTime._now(Clock.system(clockOrZone));
      };
      LocalDateTime._now = function(clock) {
        requireNonNull(clock, "clock");
        return LocalDateTime.ofInstant(clock.instant(), clock.zone());
      };
      LocalDateTime._ofEpochMillis = function(epochMilli, offset) {
        var localSecond = MathUtil.floorDiv(epochMilli, 1000) + offset.totalSeconds();
        offset = MathUtil.floorDiv(localSecond, LocalTime.SECONDS_PER_DAY);
        localSecond = MathUtil.floorMod(localSecond, LocalTime.SECONDS_PER_DAY);
        epochMilli = 1000000 * MathUtil.floorMod(epochMilli, 1000);
        offset = LocalDate.ofEpochDay(offset);
        epochMilli = LocalTime.ofSecondOfDay(localSecond, epochMilli);
        return new LocalDateTime(offset, epochMilli);
      };
      LocalDateTime.of = function() {
        return 2 >= arguments.length ? LocalDateTime.ofDateAndTime.apply(this, arguments) : LocalDateTime.ofNumbers.apply(this, arguments);
      };
      LocalDateTime.ofNumbers = function(year, month, dayOfMonth, hour, minute, second, nanoOfSecond) {
        void 0 === hour && (hour = 0);
        void 0 === minute && (minute = 0);
        void 0 === second && (second = 0);
        void 0 === nanoOfSecond && (nanoOfSecond = 0);
        year = LocalDate.of(year, month, dayOfMonth);
        hour = LocalTime.of(hour, minute, second, nanoOfSecond);
        return new LocalDateTime(year, hour);
      };
      LocalDateTime.ofDateAndTime = function(date, time) {
        requireNonNull(date, "date");
        requireNonNull(time, "time");
        return new LocalDateTime(date, time);
      };
      LocalDateTime.ofInstant = function(instant, zone) {
        void 0 === zone && (zone = ZoneId.systemDefault());
        requireNonNull(instant, "instant");
        requireInstance(instant, Instant, "instant");
        requireNonNull(zone, "zone");
        zone = zone.rules().offset(instant);
        return LocalDateTime.ofEpochSecond(instant.epochSecond(), instant.nano(), zone);
      };
      LocalDateTime.ofEpochSecond = function(epochSecond, nanoOfSecond, offset) {
        void 0 === epochSecond && (epochSecond = 0);
        void 0 === nanoOfSecond && (nanoOfSecond = 0);
        2 === arguments.length && nanoOfSecond instanceof ZoneOffset && (offset = nanoOfSecond, nanoOfSecond = 0);
        requireNonNull(offset, "offset");
        var localSecond = epochSecond + offset.totalSeconds(), localEpochDay = MathUtil.floorDiv(localSecond, LocalTime.SECONDS_PER_DAY);
        localSecond = MathUtil.floorMod(localSecond, LocalTime.SECONDS_PER_DAY);
        localEpochDay = LocalDate.ofEpochDay(localEpochDay);
        localSecond = LocalTime.ofSecondOfDay(localSecond, nanoOfSecond);
        return new LocalDateTime(localEpochDay, localSecond);
      };
      LocalDateTime.from = function(temporal) {
        requireNonNull(temporal, "temporal");
        if (temporal instanceof LocalDateTime) {
          return temporal;
        }
        if (temporal instanceof ZonedDateTime) {
          return temporal.toLocalDateTime();
        }
        try {
          var date = LocalDate.from(temporal), time = LocalTime.from(temporal);
          return new LocalDateTime(date, time);
        } catch (ex) {
          throw new DateTimeException("Unable to obtain LocalDateTime TemporalAccessor: " + temporal + ", type " + (null != temporal.constructor ? temporal.constructor.name : ""));
        }
      };
      LocalDateTime.parse = function(text, formatter) {
        void 0 === formatter && (formatter = DateTimeFormatter.ISO_LOCAL_DATE_TIME);
        requireNonNull(formatter, "formatter");
        return formatter.parse(text, LocalDateTime.FROM);
      };
      var _proto = LocalDateTime.prototype;
      _proto._withDateTime = function(newDate, newTime) {
        return this._date.equals(newDate) && this._time.equals(newTime) ? this : new LocalDateTime(newDate, newTime);
      };
      _proto.isSupported = function(fieldOrUnit) {
        return fieldOrUnit instanceof ChronoField || fieldOrUnit instanceof ChronoUnit ? fieldOrUnit.isDateBased() || fieldOrUnit.isTimeBased() : null != fieldOrUnit && fieldOrUnit.isSupportedBy(this);
      };
      _proto.range = function(field) {
        return field instanceof ChronoField ? field.isTimeBased() ? this._time.range(field) : this._date.range(field) : field.rangeRefinedBy(this);
      };
      _proto.get = function(field) {
        return field instanceof ChronoField ? field.isTimeBased() ? this._time.get(field) : this._date.get(field) : _ChronoLocalDateTime.prototype.get.call(this, field);
      };
      _proto.getLong = function(field) {
        requireNonNull(field, "field");
        return field instanceof ChronoField ? field.isTimeBased() ? this._time.getLong(field) : this._date.getLong(field) : field.getFrom(this);
      };
      _proto.year = function() {
        return this._date.year();
      };
      _proto.monthValue = function() {
        return this._date.monthValue();
      };
      _proto.month = function() {
        return this._date.month();
      };
      _proto.dayOfMonth = function() {
        return this._date.dayOfMonth();
      };
      _proto.dayOfYear = function() {
        return this._date.dayOfYear();
      };
      _proto.dayOfWeek = function() {
        return this._date.dayOfWeek();
      };
      _proto.hour = function() {
        return this._time.hour();
      };
      _proto.minute = function() {
        return this._time.minute();
      };
      _proto.second = function() {
        return this._time.second();
      };
      _proto.nano = function() {
        return this._time.nano();
      };
      _proto.withAdjuster = function(adjuster) {
        requireNonNull(adjuster, "adjuster");
        if (adjuster instanceof LocalDate) {
          return this._withDateTime(adjuster, this._time);
        }
        if (adjuster instanceof LocalTime) {
          return this._withDateTime(this._date, adjuster);
        }
        if (adjuster instanceof LocalDateTime) {
          return adjuster;
        }
        assert("function" === typeof adjuster.adjustInto, "adjuster", IllegalArgumentException);
        return adjuster.adjustInto(this);
      };
      _proto.withFieldValue = function(field, newValue) {
        requireNonNull(field, "field");
        return field instanceof ChronoField ? field.isTimeBased() ? this._withDateTime(this._date, this._time.with(field, newValue)) : this._withDateTime(this._date.with(field, newValue), this._time) : field.adjustInto(this, newValue);
      };
      _proto.withYear = function(year) {
        return this._withDateTime(this._date.withYear(year), this._time);
      };
      _proto.withMonth = function(month) {
        return this._withDateTime(this._date.withMonth(month), this._time);
      };
      _proto.withDayOfMonth = function(dayOfMonth) {
        return this._withDateTime(this._date.withDayOfMonth(dayOfMonth), this._time);
      };
      _proto.withDayOfYear = function(dayOfYear) {
        return this._withDateTime(this._date.withDayOfYear(dayOfYear), this._time);
      };
      _proto.withHour = function(hour) {
        hour = this._time.withHour(hour);
        return this._withDateTime(this._date, hour);
      };
      _proto.withMinute = function(minute) {
        minute = this._time.withMinute(minute);
        return this._withDateTime(this._date, minute);
      };
      _proto.withSecond = function(second) {
        second = this._time.withSecond(second);
        return this._withDateTime(this._date, second);
      };
      _proto.withNano = function(nanoOfSecond) {
        nanoOfSecond = this._time.withNano(nanoOfSecond);
        return this._withDateTime(this._date, nanoOfSecond);
      };
      _proto.truncatedTo = function(unit) {
        return this._withDateTime(this._date, this._time.truncatedTo(unit));
      };
      _proto.plusAmount = function(amount) {
        requireNonNull(amount, "amount");
        return amount.addTo(this);
      };
      _proto.plusAmountUnit = function(amountToAdd, unit) {
        requireNonNull(unit, "unit");
        if (unit instanceof ChronoUnit) {
          switch(unit) {
            case ChronoUnit.NANOS:
              return this.plusNanos(amountToAdd);
            case ChronoUnit.MICROS:
              return this.plusDays(MathUtil.intDiv(amountToAdd, LocalTime.MICROS_PER_DAY)).plusNanos(1000 * MathUtil.intMod(amountToAdd, LocalTime.MICROS_PER_DAY));
            case ChronoUnit.MILLIS:
              return this.plusDays(MathUtil.intDiv(amountToAdd, LocalTime.MILLIS_PER_DAY)).plusNanos(1000000 * MathUtil.intMod(amountToAdd, LocalTime.MILLIS_PER_DAY));
            case ChronoUnit.SECONDS:
              return this.plusSeconds(amountToAdd);
            case ChronoUnit.MINUTES:
              return this.plusMinutes(amountToAdd);
            case ChronoUnit.HOURS:
              return this.plusHours(amountToAdd);
            case ChronoUnit.HALF_DAYS:
              return this.plusDays(MathUtil.intDiv(amountToAdd, 256)).plusHours(12 * MathUtil.intMod(amountToAdd, 256));
          }
          return this._withDateTime(this._date.plus(amountToAdd, unit), this._time);
        }
        return unit.addTo(this, amountToAdd);
      };
      _proto.plusYears = function(years) {
        years = this._date.plusYears(years);
        return this._withDateTime(years, this._time);
      };
      _proto.plusMonths = function(months) {
        months = this._date.plusMonths(months);
        return this._withDateTime(months, this._time);
      };
      _proto.plusWeeks = function(weeks) {
        weeks = this._date.plusWeeks(weeks);
        return this._withDateTime(weeks, this._time);
      };
      _proto.plusDays = function(days) {
        days = this._date.plusDays(days);
        return this._withDateTime(days, this._time);
      };
      _proto.plusHours = function(hours) {
        return this._plusWithOverflow(this._date, hours, 0, 0, 0, 1);
      };
      _proto.plusMinutes = function(minutes) {
        return this._plusWithOverflow(this._date, 0, minutes, 0, 0, 1);
      };
      _proto.plusSeconds = function(seconds) {
        return this._plusWithOverflow(this._date, 0, 0, seconds, 0, 1);
      };
      _proto.plusNanos = function(nanos) {
        return this._plusWithOverflow(this._date, 0, 0, 0, nanos, 1);
      };
      _proto.minusAmount = function(amount) {
        requireNonNull(amount, "amount");
        return amount.subtractFrom(this);
      };
      _proto.minusAmountUnit = function(amountToSubtract, unit) {
        requireNonNull(unit, "unit");
        return this.plusAmountUnit(-1 * amountToSubtract, unit);
      };
      _proto.minusYears = function(years) {
        return this.plusYears(-1 * years);
      };
      _proto.minusMonths = function(months) {
        return this.plusMonths(-1 * months);
      };
      _proto.minusWeeks = function(weeks) {
        return this.plusWeeks(-1 * weeks);
      };
      _proto.minusDays = function(days) {
        return this.plusDays(-1 * days);
      };
      _proto.minusHours = function(hours) {
        return this._plusWithOverflow(this._date, hours, 0, 0, 0, -1);
      };
      _proto.minusMinutes = function(minutes) {
        return this._plusWithOverflow(this._date, 0, minutes, 0, 0, -1);
      };
      _proto.minusSeconds = function(seconds) {
        return this._plusWithOverflow(this._date, 0, 0, seconds, 0, -1);
      };
      _proto.minusNanos = function(nanos) {
        return this._plusWithOverflow(this._date, 0, 0, 0, nanos, -1);
      };
      _proto._plusWithOverflow = function(newDate, hours, minutes, seconds, nanos, sign) {
        if (0 === hours && 0 === minutes && 0 === seconds && 0 === nanos) {
          return this._withDateTime(newDate, this._time);
        }
        var totDays = MathUtil.intDiv(nanos, LocalTime.NANOS_PER_DAY) + MathUtil.intDiv(seconds, LocalTime.SECONDS_PER_DAY) + MathUtil.intDiv(minutes, LocalTime.MINUTES_PER_DAY) + MathUtil.intDiv(hours, LocalTime.HOURS_PER_DAY);
        totDays *= sign;
        minutes = MathUtil.intMod(nanos, LocalTime.NANOS_PER_DAY) + MathUtil.intMod(seconds, LocalTime.SECONDS_PER_DAY) * LocalTime.NANOS_PER_SECOND + MathUtil.intMod(minutes, LocalTime.MINUTES_PER_DAY) * LocalTime.NANOS_PER_MINUTE + MathUtil.intMod(hours, LocalTime.HOURS_PER_DAY) * LocalTime.NANOS_PER_HOUR;
        hours = this._time.toNanoOfDay();
        minutes = minutes * sign + hours;
        totDays += MathUtil.floorDiv(minutes, LocalTime.NANOS_PER_DAY);
        sign = MathUtil.floorMod(minutes, LocalTime.NANOS_PER_DAY);
        sign = sign === hours ? this._time : LocalTime.ofNanoOfDay(sign);
        return this._withDateTime(newDate.plusDays(totDays), sign);
      };
      _proto.query = function(_query) {
        requireNonNull(_query, "query");
        return _query === TemporalQueries.localDate() ? this.toLocalDate() : _ChronoLocalDateTime.prototype.query.call(this, _query);
      };
      _proto.adjustInto = function(temporal) {
        return _ChronoLocalDateTime.prototype.adjustInto.call(this, temporal);
      };
      _proto.until = function(endExclusive, unit) {
        requireNonNull(endExclusive, "endExclusive");
        requireNonNull(unit, "unit");
        endExclusive = LocalDateTime.from(endExclusive);
        if (unit instanceof ChronoUnit) {
          if (unit.isTimeBased()) {
            var daysUntil = this._date.daysUntil(endExclusive._date);
            endExclusive = endExclusive._time.toNanoOfDay() - this._time.toNanoOfDay();
            0 < daysUntil && 0 > endExclusive ? (daysUntil--, endExclusive += LocalTime.NANOS_PER_DAY) : 0 > daysUntil && 0 < endExclusive && (daysUntil++, endExclusive -= LocalTime.NANOS_PER_DAY);
            switch(unit) {
              case ChronoUnit.NANOS:
                return daysUntil = MathUtil.safeMultiply(daysUntil, LocalTime.NANOS_PER_DAY), MathUtil.safeAdd(daysUntil, endExclusive);
              case ChronoUnit.MICROS:
                return daysUntil = MathUtil.safeMultiply(daysUntil, LocalTime.MICROS_PER_DAY), MathUtil.safeAdd(daysUntil, MathUtil.intDiv(endExclusive, 1000));
              case ChronoUnit.MILLIS:
                return daysUntil = MathUtil.safeMultiply(daysUntil, LocalTime.MILLIS_PER_DAY), MathUtil.safeAdd(daysUntil, MathUtil.intDiv(endExclusive, 1000000));
              case ChronoUnit.SECONDS:
                return daysUntil = MathUtil.safeMultiply(daysUntil, LocalTime.SECONDS_PER_DAY), MathUtil.safeAdd(daysUntil, MathUtil.intDiv(endExclusive, LocalTime.NANOS_PER_SECOND));
              case ChronoUnit.MINUTES:
                return daysUntil = MathUtil.safeMultiply(daysUntil, LocalTime.MINUTES_PER_DAY), MathUtil.safeAdd(daysUntil, MathUtil.intDiv(endExclusive, LocalTime.NANOS_PER_MINUTE));
              case ChronoUnit.HOURS:
                return daysUntil = MathUtil.safeMultiply(daysUntil, LocalTime.HOURS_PER_DAY), MathUtil.safeAdd(daysUntil, MathUtil.intDiv(endExclusive, LocalTime.NANOS_PER_HOUR));
              case ChronoUnit.HALF_DAYS:
                return daysUntil = MathUtil.safeMultiply(daysUntil, 2), MathUtil.safeAdd(daysUntil, MathUtil.intDiv(endExclusive, 12 * LocalTime.NANOS_PER_HOUR));
            }
            throw new UnsupportedTemporalTypeException("Unsupported unit: " + unit);
          }
          daysUntil = endExclusive._date;
          endExclusive = endExclusive._time;
          daysUntil.isAfter(this._date) && endExclusive.isBefore(this._time) ? daysUntil = daysUntil.minusDays(1) : daysUntil.isBefore(this._date) && endExclusive.isAfter(this._time) && (daysUntil = daysUntil.plusDays(1));
          return this._date.until(daysUntil, unit);
        }
        return unit.between(this, endExclusive);
      };
      _proto.atOffset = function(offset) {
        return OffsetDateTime.of(this, offset);
      };
      _proto.atZone = function(zone) {
        return ZonedDateTime.of(this, zone);
      };
      _proto.toLocalDate = function() {
        return this._date;
      };
      _proto.toLocalTime = function() {
        return this._time;
      };
      _proto.compareTo = function(other) {
        requireNonNull(other, "other");
        requireInstance(other, LocalDateTime, "other");
        return this._compareTo0(other);
      };
      _proto._compareTo0 = function(other) {
        var cmp = this._date.compareTo(other.toLocalDate());
        0 === cmp && (cmp = this._time.compareTo(other.toLocalTime()));
        return cmp;
      };
      _proto.isAfter = function(other) {
        return 0 < this.compareTo(other);
      };
      _proto.isBefore = function(other) {
        return 0 > this.compareTo(other);
      };
      _proto.isEqual = function(other) {
        return 0 === this.compareTo(other);
      };
      _proto.equals = function(other) {
        return this === other ? !0 : other instanceof LocalDateTime ? this._date.equals(other._date) && this._time.equals(other._time) : !1;
      };
      _proto.hashCode = function() {
        return this._date.hashCode() ^ this._time.hashCode();
      };
      _proto.toString = function() {
        return this._date.toString() + "T" + this._time.toString();
      };
      _proto.toJSON = function() {
        return this.toString();
      };
      _proto.format = function(formatter) {
        requireNonNull(formatter, "formatter");
        return formatter.format(this);
      };
      return LocalDateTime;
    }(ChronoLocalDateTime), LocalTime = function(_Temporal) {
      function LocalTime(hour, minute, second, nanoOfSecond) {
        void 0 === hour && (hour = 0);
        void 0 === minute && (minute = 0);
        void 0 === second && (second = 0);
        void 0 === nanoOfSecond && (nanoOfSecond = 0);
        var _this = _Temporal.call(this) || this;
        hour = MathUtil.safeToInt(hour);
        minute = MathUtil.safeToInt(minute);
        second = MathUtil.safeToInt(second);
        nanoOfSecond = MathUtil.safeToInt(nanoOfSecond);
        LocalTime._validate(hour, minute, second, nanoOfSecond);
        if (0 === minute && 0 === second && 0 === nanoOfSecond) {
          return LocalTime.HOURS[hour] || (_this._hour = hour, _this._minute = minute, _this._second = second, _this._nano = nanoOfSecond, LocalTime.HOURS[hour] = _assertThisInitialized(_this)), LocalTime.HOURS[hour] || _assertThisInitialized(_this);
        }
        _this._hour = hour;
        _this._minute = minute;
        _this._second = second;
        _this._nano = nanoOfSecond;
        return _this;
      }
      _inheritsLoose(LocalTime, _Temporal);
      LocalTime.now = function(clockOrZone) {
        return null == clockOrZone ? LocalTime._now(Clock.systemDefaultZone()) : clockOrZone instanceof Clock ? LocalTime._now(clockOrZone) : LocalTime._now(Clock.system(clockOrZone));
      };
      LocalTime._now = function(clock) {
        void 0 === clock && (clock = Clock.systemDefaultZone());
        requireNonNull(clock, "clock");
        return LocalTime.ofInstant(clock.instant(), clock.zone());
      };
      LocalTime.ofInstant = function(instant, zone) {
        void 0 === zone && (zone = ZoneId.systemDefault());
        zone = zone.rules().offset(instant);
        var secsOfDay = MathUtil.intMod(instant.epochSecond(), LocalTime.SECONDS_PER_DAY);
        secsOfDay = MathUtil.intMod(secsOfDay + zone.totalSeconds(), LocalTime.SECONDS_PER_DAY);
        0 > secsOfDay && (secsOfDay += LocalTime.SECONDS_PER_DAY);
        return LocalTime.ofSecondOfDay(secsOfDay, instant.nano());
      };
      LocalTime.of = function(hour, minute, second, nanoOfSecond) {
        return new LocalTime(hour, minute, second, nanoOfSecond);
      };
      LocalTime.ofSecondOfDay = function(secondOfDay, nanoOfSecond) {
        void 0 === secondOfDay && (secondOfDay = 0);
        void 0 === nanoOfSecond && (nanoOfSecond = 0);
        ChronoField.SECOND_OF_DAY.checkValidValue(secondOfDay);
        ChronoField.NANO_OF_SECOND.checkValidValue(nanoOfSecond);
        var hours = MathUtil.intDiv(secondOfDay, LocalTime.SECONDS_PER_HOUR);
        secondOfDay -= hours * LocalTime.SECONDS_PER_HOUR;
        var minutes = MathUtil.intDiv(secondOfDay, LocalTime.SECONDS_PER_MINUTE);
        secondOfDay -= minutes * LocalTime.SECONDS_PER_MINUTE;
        return new LocalTime(hours, minutes, secondOfDay, nanoOfSecond);
      };
      LocalTime.ofNanoOfDay = function(nanoOfDay) {
        void 0 === nanoOfDay && (nanoOfDay = 0);
        ChronoField.NANO_OF_DAY.checkValidValue(nanoOfDay);
        var hours = MathUtil.intDiv(nanoOfDay, LocalTime.NANOS_PER_HOUR);
        nanoOfDay -= hours * LocalTime.NANOS_PER_HOUR;
        var minutes = MathUtil.intDiv(nanoOfDay, LocalTime.NANOS_PER_MINUTE);
        nanoOfDay -= minutes * LocalTime.NANOS_PER_MINUTE;
        var seconds = MathUtil.intDiv(nanoOfDay, LocalTime.NANOS_PER_SECOND);
        nanoOfDay -= seconds * LocalTime.NANOS_PER_SECOND;
        return new LocalTime(hours, minutes, seconds, nanoOfDay);
      };
      LocalTime.from = function(temporal) {
        requireNonNull(temporal, "temporal");
        var time = temporal.query(TemporalQueries.localTime());
        if (null == time) {
          throw new DateTimeException("Unable to obtain LocalTime TemporalAccessor: " + temporal + ", type " + (null != temporal.constructor ? temporal.constructor.name : ""));
        }
        return time;
      };
      LocalTime.parse = function(text, formatter) {
        void 0 === formatter && (formatter = DateTimeFormatter.ISO_LOCAL_TIME);
        requireNonNull(formatter, "formatter");
        return formatter.parse(text, LocalTime.FROM);
      };
      LocalTime._validate = function(hour, minute, second, nanoOfSecond) {
        ChronoField.HOUR_OF_DAY.checkValidValue(hour);
        ChronoField.MINUTE_OF_HOUR.checkValidValue(minute);
        ChronoField.SECOND_OF_MINUTE.checkValidValue(second);
        ChronoField.NANO_OF_SECOND.checkValidValue(nanoOfSecond);
      };
      var _proto = LocalTime.prototype;
      _proto.isSupported = function(fieldOrUnit) {
        return fieldOrUnit instanceof ChronoField || fieldOrUnit instanceof ChronoUnit ? fieldOrUnit.isTimeBased() : null != fieldOrUnit && fieldOrUnit.isSupportedBy(this);
      };
      _proto.range = function(field) {
        requireNonNull(field);
        return _Temporal.prototype.range.call(this, field);
      };
      _proto.get = function(field) {
        return this.getLong(field);
      };
      _proto.getLong = function(field) {
        requireNonNull(field, "field");
        return field instanceof ChronoField ? this._get0(field) : field.getFrom(this);
      };
      _proto._get0 = function(field) {
        switch(field) {
          case ChronoField.NANO_OF_SECOND:
            return this._nano;
          case ChronoField.NANO_OF_DAY:
            return this.toNanoOfDay();
          case ChronoField.MICRO_OF_SECOND:
            return MathUtil.intDiv(this._nano, 1000);
          case ChronoField.MICRO_OF_DAY:
            return MathUtil.intDiv(this.toNanoOfDay(), 1000);
          case ChronoField.MILLI_OF_SECOND:
            return MathUtil.intDiv(this._nano, 1000000);
          case ChronoField.MILLI_OF_DAY:
            return MathUtil.intDiv(this.toNanoOfDay(), 1000000);
          case ChronoField.SECOND_OF_MINUTE:
            return this._second;
          case ChronoField.SECOND_OF_DAY:
            return this.toSecondOfDay();
          case ChronoField.MINUTE_OF_HOUR:
            return this._minute;
          case ChronoField.MINUTE_OF_DAY:
            return 60 * this._hour + this._minute;
          case ChronoField.HOUR_OF_AMPM:
            return MathUtil.intMod(this._hour, 12);
          case ChronoField.CLOCK_HOUR_OF_AMPM:
            return field = MathUtil.intMod(this._hour, 12), 0 === field % 12 ? 12 : field;
          case ChronoField.HOUR_OF_DAY:
            return this._hour;
          case ChronoField.CLOCK_HOUR_OF_DAY:
            return 0 === this._hour ? 24 : this._hour;
          case ChronoField.AMPM_OF_DAY:
            return MathUtil.intDiv(this._hour, 12);
        }
        throw new UnsupportedTemporalTypeException("Unsupported field: " + field);
      };
      _proto.hour = function() {
        return this._hour;
      };
      _proto.minute = function() {
        return this._minute;
      };
      _proto.second = function() {
        return this._second;
      };
      _proto.nano = function() {
        return this._nano;
      };
      _proto.withAdjuster = function(adjuster) {
        requireNonNull(adjuster, "adjuster");
        if (adjuster instanceof LocalTime) {
          return adjuster;
        }
        assert("function" === typeof adjuster.adjustInto, "adjuster", IllegalArgumentException);
        return adjuster.adjustInto(this);
      };
      _proto.withFieldValue = function(field, newValue) {
        requireNonNull(field, "field");
        requireInstance(field, TemporalField, "field");
        if (field instanceof ChronoField) {
          field.checkValidValue(newValue);
          switch(field) {
            case ChronoField.NANO_OF_SECOND:
              return this.withNano(newValue);
            case ChronoField.NANO_OF_DAY:
              return LocalTime.ofNanoOfDay(newValue);
            case ChronoField.MICRO_OF_SECOND:
              return this.withNano(1000 * newValue);
            case ChronoField.MICRO_OF_DAY:
              return LocalTime.ofNanoOfDay(1000 * newValue);
            case ChronoField.MILLI_OF_SECOND:
              return this.withNano(1000000 * newValue);
            case ChronoField.MILLI_OF_DAY:
              return LocalTime.ofNanoOfDay(1000000 * newValue);
            case ChronoField.SECOND_OF_MINUTE:
              return this.withSecond(newValue);
            case ChronoField.SECOND_OF_DAY:
              return this.plusSeconds(newValue - this.toSecondOfDay());
            case ChronoField.MINUTE_OF_HOUR:
              return this.withMinute(newValue);
            case ChronoField.MINUTE_OF_DAY:
              return this.plusMinutes(newValue - (60 * this._hour + this._minute));
            case ChronoField.HOUR_OF_AMPM:
              return this.plusHours(newValue - MathUtil.intMod(this._hour, 12));
            case ChronoField.CLOCK_HOUR_OF_AMPM:
              return this.plusHours((12 === newValue ? 0 : newValue) - MathUtil.intMod(this._hour, 12));
            case ChronoField.HOUR_OF_DAY:
              return this.withHour(newValue);
            case ChronoField.CLOCK_HOUR_OF_DAY:
              return this.withHour(24 === newValue ? 0 : newValue);
            case ChronoField.AMPM_OF_DAY:
              return this.plusHours(12 * (newValue - MathUtil.intDiv(this._hour, 12)));
          }
          throw new UnsupportedTemporalTypeException("Unsupported field: " + field);
        }
        return field.adjustInto(this, newValue);
      };
      _proto.withHour = function(hour) {
        void 0 === hour && (hour = 0);
        return this._hour === hour ? this : new LocalTime(hour, this._minute, this._second, this._nano);
      };
      _proto.withMinute = function(minute) {
        void 0 === minute && (minute = 0);
        return this._minute === minute ? this : new LocalTime(this._hour, minute, this._second, this._nano);
      };
      _proto.withSecond = function(second) {
        void 0 === second && (second = 0);
        return this._second === second ? this : new LocalTime(this._hour, this._minute, second, this._nano);
      };
      _proto.withNano = function(nanoOfSecond) {
        void 0 === nanoOfSecond && (nanoOfSecond = 0);
        return this._nano === nanoOfSecond ? this : new LocalTime(this._hour, this._minute, this._second, nanoOfSecond);
      };
      _proto.truncatedTo = function(unit) {
        requireNonNull(unit, "unit");
        if (unit === ChronoUnit.NANOS) {
          return this;
        }
        unit = unit.duration();
        if (unit.seconds() > LocalTime.SECONDS_PER_DAY) {
          throw new DateTimeException("Unit is too large to be used for truncation");
        }
        unit = unit.toNanos();
        if (0 !== MathUtil.intMod(LocalTime.NANOS_PER_DAY, unit)) {
          throw new DateTimeException("Unit must divide into a standard day without remainder");
        }
        var nod = this.toNanoOfDay();
        return LocalTime.ofNanoOfDay(MathUtil.intDiv(nod, unit) * unit);
      };
      _proto.plusAmount = function(amount) {
        requireNonNull(amount, "amount");
        return amount.addTo(this);
      };
      _proto.plusAmountUnit = function(amountToAdd, unit) {
        requireNonNull(unit, "unit");
        if (unit instanceof ChronoUnit) {
          switch(unit) {
            case ChronoUnit.NANOS:
              return this.plusNanos(amountToAdd);
            case ChronoUnit.MICROS:
              return this.plusNanos(1000 * MathUtil.intMod(amountToAdd, LocalTime.MICROS_PER_DAY));
            case ChronoUnit.MILLIS:
              return this.plusNanos(1000000 * MathUtil.intMod(amountToAdd, LocalTime.MILLIS_PER_DAY));
            case ChronoUnit.SECONDS:
              return this.plusSeconds(amountToAdd);
            case ChronoUnit.MINUTES:
              return this.plusMinutes(amountToAdd);
            case ChronoUnit.HOURS:
              return this.plusHours(amountToAdd);
            case ChronoUnit.HALF_DAYS:
              return this.plusHours(12 * MathUtil.intMod(amountToAdd, 2));
          }
          throw new UnsupportedTemporalTypeException("Unsupported unit: " + unit);
        }
        return unit.addTo(this, amountToAdd);
      };
      _proto.plusHours = function(hoursToAdd) {
        if (0 === hoursToAdd) {
          return this;
        }
        hoursToAdd = MathUtil.intMod(MathUtil.intMod(hoursToAdd, LocalTime.HOURS_PER_DAY) + this._hour + LocalTime.HOURS_PER_DAY, LocalTime.HOURS_PER_DAY);
        return new LocalTime(hoursToAdd, this._minute, this._second, this._nano);
      };
      _proto.plusMinutes = function(minutesToAdd) {
        if (0 === minutesToAdd) {
          return this;
        }
        var mofd = this._hour * LocalTime.MINUTES_PER_HOUR + this._minute;
        minutesToAdd = MathUtil.intMod(MathUtil.intMod(minutesToAdd, LocalTime.MINUTES_PER_DAY) + mofd + LocalTime.MINUTES_PER_DAY, LocalTime.MINUTES_PER_DAY);
        if (mofd === minutesToAdd) {
          return this;
        }
        mofd = MathUtil.intDiv(minutesToAdd, LocalTime.MINUTES_PER_HOUR);
        minutesToAdd = MathUtil.intMod(minutesToAdd, LocalTime.MINUTES_PER_HOUR);
        return new LocalTime(mofd, minutesToAdd, this._second, this._nano);
      };
      _proto.plusSeconds = function(secondsToAdd) {
        if (0 === secondsToAdd) {
          return this;
        }
        var sofd = this._hour * LocalTime.SECONDS_PER_HOUR + this._minute * LocalTime.SECONDS_PER_MINUTE + this._second;
        secondsToAdd = MathUtil.intMod(MathUtil.intMod(secondsToAdd, LocalTime.SECONDS_PER_DAY) + sofd + LocalTime.SECONDS_PER_DAY, LocalTime.SECONDS_PER_DAY);
        if (sofd === secondsToAdd) {
          return this;
        }
        sofd = MathUtil.intDiv(secondsToAdd, LocalTime.SECONDS_PER_HOUR);
        var newMinute = MathUtil.intMod(MathUtil.intDiv(secondsToAdd, LocalTime.SECONDS_PER_MINUTE), LocalTime.MINUTES_PER_HOUR);
        secondsToAdd = MathUtil.intMod(secondsToAdd, LocalTime.SECONDS_PER_MINUTE);
        return new LocalTime(sofd, newMinute, secondsToAdd, this._nano);
      };
      _proto.plusNanos = function(nanosToAdd) {
        if (0 === nanosToAdd) {
          return this;
        }
        var nofd = this.toNanoOfDay();
        nanosToAdd = MathUtil.intMod(MathUtil.intMod(nanosToAdd, LocalTime.NANOS_PER_DAY) + nofd + LocalTime.NANOS_PER_DAY, LocalTime.NANOS_PER_DAY);
        if (nofd === nanosToAdd) {
          return this;
        }
        nofd = MathUtil.intDiv(nanosToAdd, LocalTime.NANOS_PER_HOUR);
        var newMinute = MathUtil.intMod(MathUtil.intDiv(nanosToAdd, LocalTime.NANOS_PER_MINUTE), LocalTime.MINUTES_PER_HOUR), newSecond = MathUtil.intMod(MathUtil.intDiv(nanosToAdd, LocalTime.NANOS_PER_SECOND), LocalTime.SECONDS_PER_MINUTE);
        nanosToAdd = MathUtil.intMod(nanosToAdd, LocalTime.NANOS_PER_SECOND);
        return new LocalTime(nofd, newMinute, newSecond, nanosToAdd);
      };
      _proto.minusAmount = function(amount) {
        requireNonNull(amount, "amount");
        return amount.subtractFrom(this);
      };
      _proto.minusAmountUnit = function(amountToSubtract, unit) {
        requireNonNull(unit, "unit");
        return this.plusAmountUnit(-1 * amountToSubtract, unit);
      };
      _proto.minusHours = function(hoursToSubtract) {
        return this.plusHours(-1 * MathUtil.intMod(hoursToSubtract, LocalTime.HOURS_PER_DAY));
      };
      _proto.minusMinutes = function(minutesToSubtract) {
        return this.plusMinutes(-1 * MathUtil.intMod(minutesToSubtract, LocalTime.MINUTES_PER_DAY));
      };
      _proto.minusSeconds = function(secondsToSubtract) {
        return this.plusSeconds(-1 * MathUtil.intMod(secondsToSubtract, LocalTime.SECONDS_PER_DAY));
      };
      _proto.minusNanos = function(nanosToSubtract) {
        return this.plusNanos(-1 * MathUtil.intMod(nanosToSubtract, LocalTime.NANOS_PER_DAY));
      };
      _proto.query = function(_query) {
        requireNonNull(_query, "query");
        return _query === TemporalQueries.precision() ? ChronoUnit.NANOS : _query === TemporalQueries.localTime() ? this : _query === TemporalQueries.chronology() || _query === TemporalQueries.zoneId() || _query === TemporalQueries.zone() || _query === TemporalQueries.offset() || _query === TemporalQueries.localDate() ? null : _query.queryFrom(this);
      };
      _proto.adjustInto = function(temporal) {
        return temporal.with(LocalTime.NANO_OF_DAY, this.toNanoOfDay());
      };
      _proto.until = function(endExclusive, unit) {
        requireNonNull(endExclusive, "endExclusive");
        requireNonNull(unit, "unit");
        endExclusive = LocalTime.from(endExclusive);
        if (unit instanceof ChronoUnit) {
          endExclusive = endExclusive.toNanoOfDay() - this.toNanoOfDay();
          switch(unit) {
            case ChronoUnit.NANOS:
              return endExclusive;
            case ChronoUnit.MICROS:
              return MathUtil.intDiv(endExclusive, 1000);
            case ChronoUnit.MILLIS:
              return MathUtil.intDiv(endExclusive, 1000000);
            case ChronoUnit.SECONDS:
              return MathUtil.intDiv(endExclusive, LocalTime.NANOS_PER_SECOND);
            case ChronoUnit.MINUTES:
              return MathUtil.intDiv(endExclusive, LocalTime.NANOS_PER_MINUTE);
            case ChronoUnit.HOURS:
              return MathUtil.intDiv(endExclusive, LocalTime.NANOS_PER_HOUR);
            case ChronoUnit.HALF_DAYS:
              return MathUtil.intDiv(endExclusive, 12 * LocalTime.NANOS_PER_HOUR);
          }
          throw new UnsupportedTemporalTypeException("Unsupported unit: " + unit);
        }
        return unit.between(this, endExclusive);
      };
      _proto.atDate = function(date) {
        return LocalDateTime.of(date, this);
      };
      _proto.toSecondOfDay = function() {
        var total = this._hour * LocalTime.SECONDS_PER_HOUR;
        total += this._minute * LocalTime.SECONDS_PER_MINUTE;
        return total += this._second;
      };
      _proto.toNanoOfDay = function() {
        var total = this._hour * LocalTime.NANOS_PER_HOUR;
        total += this._minute * LocalTime.NANOS_PER_MINUTE;
        total += this._second * LocalTime.NANOS_PER_SECOND;
        return total += this._nano;
      };
      _proto.compareTo = function(other) {
        requireNonNull(other, "other");
        requireInstance(other, LocalTime, "other");
        var cmp = MathUtil.compareNumbers(this._hour, other._hour);
        0 === cmp && (cmp = MathUtil.compareNumbers(this._minute, other._minute), 0 === cmp && (cmp = MathUtil.compareNumbers(this._second, other._second), 0 === cmp && (cmp = MathUtil.compareNumbers(this._nano, other._nano))));
        return cmp;
      };
      _proto.isAfter = function(other) {
        return 0 < this.compareTo(other);
      };
      _proto.isBefore = function(other) {
        return 0 > this.compareTo(other);
      };
      _proto.equals = function(other) {
        return this === other ? !0 : other instanceof LocalTime ? this._hour === other._hour && this._minute === other._minute && this._second === other._second && this._nano === other._nano : !1;
      };
      _proto.hashCode = function() {
        var nod = this.toNanoOfDay();
        return MathUtil.hash(nod);
      };
      _proto.toString = function() {
        var buf = this._hour;
        var minuteValue = this._minute, secondValue = this._second, nanoValue = this._nano;
        buf = (10 > buf ? "0" : "") + buf + (10 > minuteValue ? ":0" : ":") + minuteValue;
        if (0 < secondValue || 0 < nanoValue) {
          buf += 10 > secondValue ? ":0" : ":", buf += secondValue, 0 < nanoValue && (buf += ".", buf = 0 === MathUtil.intMod(nanoValue, 1000000) ? buf + ("" + (MathUtil.intDiv(nanoValue, 1000000) + 1000)).substring(1) : 0 === MathUtil.intMod(nanoValue, 1000) ? buf + ("" + (MathUtil.intDiv(nanoValue, 1000) + 1000000)).substring(1) : buf + ("" + (nanoValue + 1000000000)).substring(1));
        }
        return buf;
      };
      _proto.toJSON = function() {
        return this.toString();
      };
      _proto.format = function(formatter) {
        requireNonNull(formatter, "formatter");
        return formatter.format(this);
      };
      return LocalTime;
    }(Temporal);
    LocalTime.HOURS_PER_DAY = 24;
    LocalTime.MINUTES_PER_HOUR = 60;
    LocalTime.MINUTES_PER_DAY = LocalTime.MINUTES_PER_HOUR * LocalTime.HOURS_PER_DAY;
    LocalTime.SECONDS_PER_MINUTE = 60;
    LocalTime.SECONDS_PER_HOUR = LocalTime.SECONDS_PER_MINUTE * LocalTime.MINUTES_PER_HOUR;
    LocalTime.SECONDS_PER_DAY = LocalTime.SECONDS_PER_HOUR * LocalTime.HOURS_PER_DAY;
    LocalTime.MILLIS_PER_DAY = 1000 * LocalTime.SECONDS_PER_DAY;
    LocalTime.MICROS_PER_DAY = 1000000 * LocalTime.SECONDS_PER_DAY;
    LocalTime.NANOS_PER_SECOND = 1000000000;
    LocalTime.NANOS_PER_MINUTE = LocalTime.NANOS_PER_SECOND * LocalTime.SECONDS_PER_MINUTE;
    LocalTime.NANOS_PER_HOUR = LocalTime.NANOS_PER_MINUTE * LocalTime.MINUTES_PER_HOUR;
    LocalTime.NANOS_PER_DAY = LocalTime.NANOS_PER_HOUR * LocalTime.HOURS_PER_DAY;
    var Instant = function(_Temporal) {
      function Instant(seconds, nanoOfSecond) {
        var _this = _Temporal.call(this) || this;
        Instant._validate(seconds, nanoOfSecond);
        _this._seconds = MathUtil.safeToInt(seconds);
        _this._nanos = MathUtil.safeToInt(nanoOfSecond);
        return _this;
      }
      _inheritsLoose(Instant, _Temporal);
      Instant.now = function(clock) {
        void 0 === clock && (clock = Clock.systemUTC());
        return clock.instant();
      };
      Instant.ofEpochSecond = function(epochSecond, nanoAdjustment) {
        void 0 === nanoAdjustment && (nanoAdjustment = 0);
        epochSecond += MathUtil.floorDiv(nanoAdjustment, LocalTime.NANOS_PER_SECOND);
        nanoAdjustment = MathUtil.floorMod(nanoAdjustment, LocalTime.NANOS_PER_SECOND);
        return Instant._create(epochSecond, nanoAdjustment);
      };
      Instant.ofEpochMilli = function(epochMilli) {
        var secs = MathUtil.floorDiv(epochMilli, 1000);
        epochMilli = MathUtil.floorMod(epochMilli, 1000);
        return Instant._create(secs, 1000000 * epochMilli);
      };
      Instant.from = function(temporal) {
        try {
          var instantSecs = temporal.getLong(ChronoField.INSTANT_SECONDS), nanoOfSecond = temporal.get(ChronoField.NANO_OF_SECOND);
          return Instant.ofEpochSecond(instantSecs, nanoOfSecond);
        } catch (ex) {
          throw new DateTimeException("Unable to obtain Instant from TemporalAccessor: " + temporal + ", type " + typeof temporal, ex);
        }
      };
      Instant.parse = function(text) {
        return DateTimeFormatter.ISO_INSTANT.parse(text, Instant.FROM);
      };
      Instant._create = function(seconds, nanoOfSecond) {
        return 0 === seconds && 0 === nanoOfSecond ? Instant.EPOCH : new Instant(seconds, nanoOfSecond);
      };
      Instant._validate = function(seconds, nanoOfSecond) {
        if (seconds < Instant.MIN_SECONDS || seconds > Instant.MAX_SECONDS) {
          throw new DateTimeException("Instant exceeds minimum or maximum instant");
        }
        if (0 > nanoOfSecond || nanoOfSecond > LocalTime.NANOS_PER_SECOND) {
          throw new DateTimeException("Instant exceeds minimum or maximum instant");
        }
      };
      var _proto = Instant.prototype;
      _proto.isSupported = function(fieldOrUnit) {
        return fieldOrUnit instanceof ChronoField ? fieldOrUnit === ChronoField.INSTANT_SECONDS || fieldOrUnit === ChronoField.NANO_OF_SECOND || fieldOrUnit === ChronoField.MICRO_OF_SECOND || fieldOrUnit === ChronoField.MILLI_OF_SECOND : fieldOrUnit instanceof ChronoUnit ? fieldOrUnit.isTimeBased() || fieldOrUnit === ChronoUnit.DAYS : null != fieldOrUnit && fieldOrUnit.isSupportedBy(this);
      };
      _proto.range = function(field) {
        return _Temporal.prototype.range.call(this, field);
      };
      _proto.get = function(field) {
        return this.getLong(field);
      };
      _proto.getLong = function(field) {
        if (field instanceof ChronoField) {
          switch(field) {
            case ChronoField.NANO_OF_SECOND:
              return this._nanos;
            case ChronoField.MICRO_OF_SECOND:
              return MathUtil.intDiv(this._nanos, 1000);
            case ChronoField.MILLI_OF_SECOND:
              return MathUtil.intDiv(this._nanos, 1000000);
            case ChronoField.INSTANT_SECONDS:
              return this._seconds;
          }
          throw new UnsupportedTemporalTypeException("Unsupported field: " + field);
        }
        return field.getFrom(this);
      };
      _proto.epochSecond = function() {
        return this._seconds;
      };
      _proto.nano = function() {
        return this._nanos;
      };
      _proto.withAdjuster = function(adjuster) {
        requireNonNull(adjuster, "adjuster");
        return adjuster.adjustInto(this);
      };
      _proto.withFieldValue = function(field, newValue) {
        requireNonNull(field, "field");
        if (field instanceof ChronoField) {
          field.checkValidValue(newValue);
          switch(field) {
            case ChronoField.MILLI_OF_SECOND:
              return field = 1000000 * newValue, field !== this._nanos ? Instant._create(this._seconds, field) : this;
            case ChronoField.MICRO_OF_SECOND:
              return field = 1000 * newValue, field !== this._nanos ? Instant._create(this._seconds, field) : this;
            case ChronoField.NANO_OF_SECOND:
              return newValue !== this._nanos ? Instant._create(this._seconds, newValue) : this;
            case ChronoField.INSTANT_SECONDS:
              return newValue !== this._seconds ? Instant._create(newValue, this._nanos) : this;
          }
          throw new UnsupportedTemporalTypeException("Unsupported field: " + field);
        }
        return field.adjustInto(this, newValue);
      };
      _proto.truncatedTo = function(unit) {
        requireNonNull(unit, "unit");
        if (unit === ChronoUnit.NANOS) {
          return this;
        }
        unit = unit.duration();
        if (unit.seconds() > LocalTime.SECONDS_PER_DAY) {
          throw new DateTimeException("Unit is too large to be used for truncation");
        }
        var dur = unit.toNanos();
        if (0 !== MathUtil.intMod(LocalTime.NANOS_PER_DAY, dur)) {
          throw new DateTimeException("Unit must divide into a standard day without remainder");
        }
        unit = MathUtil.intMod(this._seconds, LocalTime.SECONDS_PER_DAY) * LocalTime.NANOS_PER_SECOND + this._nanos;
        dur *= MathUtil.intDiv(unit, dur);
        return this.plusNanos(dur - unit);
      };
      _proto.plusAmount = function(amount) {
        requireNonNull(amount, "amount");
        return amount.addTo(this);
      };
      _proto.plusAmountUnit = function(amountToAdd, unit) {
        requireNonNull(amountToAdd, "amountToAdd");
        requireNonNull(unit, "unit");
        requireInstance(unit, TemporalUnit);
        if (unit instanceof ChronoUnit) {
          switch(unit) {
            case ChronoUnit.NANOS:
              return this.plusNanos(amountToAdd);
            case ChronoUnit.MICROS:
              return this._plus(MathUtil.intDiv(amountToAdd, 1000000), 1000 * MathUtil.intMod(amountToAdd, 1000000));
            case ChronoUnit.MILLIS:
              return this.plusMillis(amountToAdd);
            case ChronoUnit.SECONDS:
              return this.plusSeconds(amountToAdd);
            case ChronoUnit.MINUTES:
              return this.plusSeconds(MathUtil.safeMultiply(amountToAdd, LocalTime.SECONDS_PER_MINUTE));
            case ChronoUnit.HOURS:
              return this.plusSeconds(MathUtil.safeMultiply(amountToAdd, LocalTime.SECONDS_PER_HOUR));
            case ChronoUnit.HALF_DAYS:
              return this.plusSeconds(MathUtil.safeMultiply(amountToAdd, LocalTime.SECONDS_PER_DAY / 2));
            case ChronoUnit.DAYS:
              return this.plusSeconds(MathUtil.safeMultiply(amountToAdd, LocalTime.SECONDS_PER_DAY));
          }
          throw new UnsupportedTemporalTypeException("Unsupported unit: " + unit);
        }
        return unit.addTo(this, amountToAdd);
      };
      _proto.plusSeconds = function(secondsToAdd) {
        return this._plus(secondsToAdd, 0);
      };
      _proto.plusMillis = function(millisToAdd) {
        return this._plus(MathUtil.intDiv(millisToAdd, 1000), 1000000 * MathUtil.intMod(millisToAdd, 1000));
      };
      _proto.plusNanos = function(nanosToAdd) {
        return this._plus(0, nanosToAdd);
      };
      _proto._plus = function(secondsToAdd, nanosToAdd) {
        if (0 === secondsToAdd && 0 === nanosToAdd) {
          return this;
        }
        secondsToAdd = this._seconds + secondsToAdd;
        secondsToAdd += MathUtil.intDiv(nanosToAdd, LocalTime.NANOS_PER_SECOND);
        return Instant.ofEpochSecond(secondsToAdd, this._nanos + nanosToAdd % LocalTime.NANOS_PER_SECOND);
      };
      _proto.minusAmount = function(amount) {
        requireNonNull(amount, "amount");
        return amount.subtractFrom(this);
      };
      _proto.minusAmountUnit = function(amountToSubtract, unit) {
        return this.plusAmountUnit(-1 * amountToSubtract, unit);
      };
      _proto.minusSeconds = function(secondsToSubtract) {
        return this.plusSeconds(-1 * secondsToSubtract);
      };
      _proto.minusMillis = function(millisToSubtract) {
        return this.plusMillis(-1 * millisToSubtract);
      };
      _proto.minusNanos = function(nanosToSubtract) {
        return this.plusNanos(-1 * nanosToSubtract);
      };
      _proto.query = function(_query) {
        requireNonNull(_query, "query");
        return _query === TemporalQueries.precision() ? ChronoUnit.NANOS : _query === TemporalQueries.localDate() || _query === TemporalQueries.localTime() || _query === TemporalQueries.chronology() || _query === TemporalQueries.zoneId() || _query === TemporalQueries.zone() || _query === TemporalQueries.offset() ? null : _query.queryFrom(this);
      };
      _proto.adjustInto = function(temporal) {
        requireNonNull(temporal, "temporal");
        return temporal.with(ChronoField.INSTANT_SECONDS, this._seconds).with(ChronoField.NANO_OF_SECOND, this._nanos);
      };
      _proto.until = function(endExclusive, unit) {
        requireNonNull(endExclusive, "endExclusive");
        requireNonNull(unit, "unit");
        endExclusive = Instant.from(endExclusive);
        if (unit instanceof ChronoUnit) {
          switch(unit) {
            case ChronoUnit.NANOS:
              return this._nanosUntil(endExclusive);
            case ChronoUnit.MICROS:
              return MathUtil.intDiv(this._nanosUntil(endExclusive), 1000);
            case ChronoUnit.MILLIS:
              return MathUtil.safeSubtract(endExclusive.toEpochMilli(), this.toEpochMilli());
            case ChronoUnit.SECONDS:
              return this._secondsUntil(endExclusive);
            case ChronoUnit.MINUTES:
              return MathUtil.intDiv(this._secondsUntil(endExclusive), LocalTime.SECONDS_PER_MINUTE);
            case ChronoUnit.HOURS:
              return MathUtil.intDiv(this._secondsUntil(endExclusive), LocalTime.SECONDS_PER_HOUR);
            case ChronoUnit.HALF_DAYS:
              return MathUtil.intDiv(this._secondsUntil(endExclusive), 12 * LocalTime.SECONDS_PER_HOUR);
            case ChronoUnit.DAYS:
              return MathUtil.intDiv(this._secondsUntil(endExclusive), LocalTime.SECONDS_PER_DAY);
          }
          throw new UnsupportedTemporalTypeException("Unsupported unit: " + unit);
        }
        return unit.between(this, endExclusive);
      };
      _proto._nanosUntil = function(end) {
        var secsDiff = MathUtil.safeSubtract(end.epochSecond(), this.epochSecond());
        secsDiff = MathUtil.safeMultiply(secsDiff, LocalTime.NANOS_PER_SECOND);
        return MathUtil.safeAdd(secsDiff, end.nano() - this.nano());
      };
      _proto._secondsUntil = function(end) {
        var secsDiff = MathUtil.safeSubtract(end.epochSecond(), this.epochSecond());
        end = end.nano() - this.nano();
        0 < secsDiff && 0 > end ? secsDiff-- : 0 > secsDiff && 0 < end && secsDiff++;
        return secsDiff;
      };
      _proto.atOffset = function(offset) {
        return OffsetDateTime.ofInstant(this, offset);
      };
      _proto.atZone = function(zone) {
        return ZonedDateTime.ofInstant(this, zone);
      };
      _proto.toEpochMilli = function() {
        return MathUtil.safeMultiply(this._seconds, 1000) + MathUtil.intDiv(this._nanos, 1000000);
      };
      _proto.compareTo = function(otherInstant) {
        requireNonNull(otherInstant, "otherInstant");
        requireInstance(otherInstant, Instant, "otherInstant");
        var cmp = MathUtil.compareNumbers(this._seconds, otherInstant._seconds);
        return 0 !== cmp ? cmp : this._nanos - otherInstant._nanos;
      };
      _proto.isAfter = function(otherInstant) {
        return 0 < this.compareTo(otherInstant);
      };
      _proto.isBefore = function(otherInstant) {
        return 0 > this.compareTo(otherInstant);
      };
      _proto.equals = function(other) {
        return this === other ? !0 : other instanceof Instant ? this.epochSecond() === other.epochSecond() && this.nano() === other.nano() : !1;
      };
      _proto.hashCode = function() {
        return MathUtil.hashCode(this._seconds, this._nanos);
      };
      _proto.toString = function() {
        return DateTimeFormatter.ISO_INSTANT.format(this);
      };
      _proto.toJSON = function() {
        return this.toString();
      };
      return Instant;
    }(Temporal), Clock = function() {
      function Clock() {
      }
      Clock.systemUTC = function() {
        return new SystemClock(ZoneOffset.UTC);
      };
      Clock.systemDefaultZone = function() {
        return new SystemClock(ZoneId.systemDefault());
      };
      Clock.system = function(zone) {
        return new SystemClock(zone);
      };
      Clock.fixed = function(fixedInstant, zoneId) {
        return new FixedClock(fixedInstant, zoneId);
      };
      Clock.offset = function(baseClock, duration) {
        return new OffsetClock(baseClock, duration);
      };
      var _proto = Clock.prototype;
      _proto.millis = function() {
        abstractMethodFail("Clock.millis");
      };
      _proto.instant = function() {
        abstractMethodFail("Clock.instant");
      };
      _proto.zone = function() {
        abstractMethodFail("Clock.zone");
      };
      _proto.withZone = function() {
        abstractMethodFail("Clock.withZone");
      };
      return Clock;
    }(), SystemClock = function(_Clock) {
      function SystemClock(zone) {
        requireNonNull(zone, "zone");
        var _this = _Clock.call(this) || this;
        _this._zone = zone;
        return _this;
      }
      _inheritsLoose(SystemClock, _Clock);
      var _proto2 = SystemClock.prototype;
      _proto2.zone = function() {
        return this._zone;
      };
      _proto2.millis = function() {
        return (new Date()).getTime();
      };
      _proto2.instant = function() {
        return Instant.ofEpochMilli(this.millis());
      };
      _proto2.equals = function(obj) {
        return obj instanceof SystemClock ? this._zone.equals(obj._zone) : !1;
      };
      _proto2.withZone = function(zone) {
        return zone.equals(this._zone) ? this : new SystemClock(zone);
      };
      _proto2.toString = function() {
        return "SystemClock[" + this._zone.toString() + "]";
      };
      return SystemClock;
    }(Clock), FixedClock = function(_Clock2) {
      function FixedClock(fixedInstant, zoneId) {
        var _this2 = _Clock2.call(this) || this;
        _this2._instant = fixedInstant;
        _this2._zoneId = zoneId;
        return _this2;
      }
      _inheritsLoose(FixedClock, _Clock2);
      var _proto3 = FixedClock.prototype;
      _proto3.instant = function() {
        return this._instant;
      };
      _proto3.millis = function() {
        return this._instant.toEpochMilli();
      };
      _proto3.zone = function() {
        return this._zoneId;
      };
      _proto3.toString = function() {
        return "FixedClock[]";
      };
      _proto3.equals = function(obj) {
        return obj instanceof FixedClock ? this._instant.equals(obj._instant) && this._zoneId.equals(obj._zoneId) : !1;
      };
      _proto3.withZone = function(zone) {
        return zone.equals(this._zoneId) ? this : new FixedClock(this._instant, zone);
      };
      return FixedClock;
    }(Clock), OffsetClock = function(_Clock3) {
      function OffsetClock(baseClock, offset) {
        var _this3 = _Clock3.call(this) || this;
        _this3._baseClock = baseClock;
        _this3._offset = offset;
        return _this3;
      }
      _inheritsLoose(OffsetClock, _Clock3);
      var _proto4 = OffsetClock.prototype;
      _proto4.zone = function() {
        return this._baseClock.zone();
      };
      _proto4.withZone = function(zone) {
        return zone.equals(this._baseClock.zone()) ? this : new OffsetClock(this._baseClock.withZone(zone), this._offset);
      };
      _proto4.millis = function() {
        return this._baseClock.millis() + this._offset.toMillis();
      };
      _proto4.instant = function() {
        return this._baseClock.instant().plus(this._offset);
      };
      _proto4.equals = function(obj) {
        return obj instanceof OffsetClock ? this._baseClock.equals(obj._baseClock) && this._offset.equals(obj._offset) : !1;
      };
      _proto4.toString = function() {
        return "OffsetClock[" + this._baseClock + "," + this._offset + "]";
      };
      return OffsetClock;
    }(Clock), ZoneOffsetTransition = function() {
      function ZoneOffsetTransition(transition, offsetBefore, offsetAfter) {
        requireNonNull(transition, "transition");
        requireNonNull(offsetBefore, "offsetBefore");
        requireNonNull(offsetAfter, "offsetAfter");
        if (offsetBefore.equals(offsetAfter)) {
          throw new IllegalArgumentException("Offsets must not be equal");
        }
        if (0 !== transition.nano()) {
          throw new IllegalArgumentException("Nano-of-second must be zero");
        }
        this._transition = transition instanceof LocalDateTime ? transition : LocalDateTime.ofEpochSecond(transition, 0, offsetBefore);
        this._offsetBefore = offsetBefore;
        this._offsetAfter = offsetAfter;
      }
      ZoneOffsetTransition.of = function(transition, offsetBefore, offsetAfter) {
        return new ZoneOffsetTransition(transition, offsetBefore, offsetAfter);
      };
      var _proto = ZoneOffsetTransition.prototype;
      _proto.instant = function() {
        return this._transition.toInstant(this._offsetBefore);
      };
      _proto.toEpochSecond = function() {
        return this._transition.toEpochSecond(this._offsetBefore);
      };
      _proto.dateTimeBefore = function() {
        return this._transition;
      };
      _proto.dateTimeAfter = function() {
        return this._transition.plusSeconds(this.durationSeconds());
      };
      _proto.offsetBefore = function() {
        return this._offsetBefore;
      };
      _proto.offsetAfter = function() {
        return this._offsetAfter;
      };
      _proto.duration = function() {
        return Duration.ofSeconds(this.durationSeconds());
      };
      _proto.durationSeconds = function() {
        return this._offsetAfter.totalSeconds() - this._offsetBefore.totalSeconds();
      };
      _proto.isGap = function() {
        return this._offsetAfter.totalSeconds() > this._offsetBefore.totalSeconds();
      };
      _proto.isOverlap = function() {
        return this._offsetAfter.totalSeconds() < this._offsetBefore.totalSeconds();
      };
      _proto.isValidOffset = function(offset) {
        return this.isGap() ? !1 : this._offsetBefore.equals(offset) || this._offsetAfter.equals(offset);
      };
      _proto.validOffsets = function() {
        return this.isGap() ? [] : [this._offsetBefore, this._offsetAfter];
      };
      _proto.compareTo = function(transition) {
        return this.instant().compareTo(transition.instant());
      };
      _proto.equals = function(other) {
        return other === this ? !0 : other instanceof ZoneOffsetTransition ? this._transition.equals(other._transition) && this._offsetBefore.equals(other.offsetBefore()) && this._offsetAfter.equals(other.offsetAfter()) : !1;
      };
      _proto.hashCode = function() {
        return this._transition.hashCode() ^ this._offsetBefore.hashCode() ^ this._offsetAfter.hashCode() >>> 16;
      };
      _proto.toString = function() {
        return "Transition[" + (this.isGap() ? "Gap" : "Overlap") + " at " + this._transition.toString() + this._offsetBefore.toString() + " to " + this._offsetAfter + "]";
      };
      return ZoneOffsetTransition;
    }(), SystemDefaultZoneRules = function(_ZoneRules) {
      function SystemDefaultZoneRules() {
        return _ZoneRules.apply(this, arguments) || this;
      }
      _inheritsLoose(SystemDefaultZoneRules, _ZoneRules);
      var _proto = SystemDefaultZoneRules.prototype;
      _proto.isFixedOffset = function() {
        return !1;
      };
      _proto.offsetOfInstant = function(instant) {
        instant = (new Date(instant.toEpochMilli())).getTimezoneOffset();
        return ZoneOffset.ofTotalMinutes(-1 * instant);
      };
      _proto.offsetOfEpochMilli = function(epochMilli) {
        epochMilli = (new Date(epochMilli)).getTimezoneOffset();
        return ZoneOffset.ofTotalMinutes(-1 * epochMilli);
      };
      _proto.offsetOfLocalDateTime = function(localDateTime) {
        localDateTime = 1000 * localDateTime.toEpochSecond(ZoneOffset.UTC);
        var offsetInMinutesBeforePossibleTransition = (new Date(localDateTime)).getTimezoneOffset();
        localDateTime = (new Date(localDateTime + 60000 * offsetInMinutesBeforePossibleTransition)).getTimezoneOffset();
        return ZoneOffset.ofTotalMinutes(-1 * localDateTime);
      };
      _proto.validOffsets = function(localDateTime) {
        return [this.offsetOfLocalDateTime(localDateTime)];
      };
      _proto.transition = function() {
        return null;
      };
      _proto.standardOffset = function(instant) {
        return this.offsetOfInstant(instant);
      };
      _proto.daylightSavings = function() {
        this._throwNotSupported();
      };
      _proto.isDaylightSavings = function() {
        this._throwNotSupported();
      };
      _proto.isValidOffset = function(dateTime, offset) {
        return this.offsetOfLocalDateTime(dateTime).equals(offset);
      };
      _proto.nextTransition = function() {
        this._throwNotSupported();
      };
      _proto.previousTransition = function() {
        this._throwNotSupported();
      };
      _proto.transitions = function() {
        this._throwNotSupported();
      };
      _proto.transitionRules = function() {
        this._throwNotSupported();
      };
      _proto._throwNotSupported = function() {
        throw new DateTimeException("not supported operation");
      };
      _proto.equals = function(other) {
        return this === other || other instanceof SystemDefaultZoneRules ? !0 : !1;
      };
      _proto.toString = function() {
        return "SYSTEM";
      };
      return SystemDefaultZoneRules;
    }(ZoneRules), SystemDefaultZoneId = function(_ZoneId) {
      function SystemDefaultZoneId() {
        var _this = _ZoneId.call(this) || this;
        _this._rules = new SystemDefaultZoneRules();
        return _this;
      }
      _inheritsLoose(SystemDefaultZoneId, _ZoneId);
      var _proto = SystemDefaultZoneId.prototype;
      _proto.rules = function() {
        return this._rules;
      };
      _proto.equals = function(other) {
        return this === other ? !0 : !1;
      };
      _proto.id = function() {
        return "SYSTEM";
      };
      return SystemDefaultZoneId;
    }(ZoneId), ZoneIdFactory = function() {
      function ZoneIdFactory() {
      }
      ZoneIdFactory.systemDefault = function() {
        return SYSTEM_DEFAULT_ZONE_ID_INSTANCE;
      };
      ZoneIdFactory.getAvailableZoneIds = function() {
        return ZoneRulesProvider.getAvailableZoneIds();
      };
      ZoneIdFactory.of = function(zoneId) {
        requireNonNull(zoneId, "zoneId");
        if ("Z" === zoneId) {
          return ZoneOffset.UTC;
        }
        if (1 === zoneId.length) {
          throw new DateTimeException("Invalid zone: " + zoneId);
        }
        if (StringUtil.startsWith(zoneId, "+") || StringUtil.startsWith(zoneId, "-")) {
          return ZoneOffset.of(zoneId);
        }
        if ("UTC" === zoneId || "GMT" === zoneId || "GMT0" === zoneId || "UT" === zoneId) {
          return new ZoneRegion(zoneId, ZoneOffset.UTC.rules());
        }
        if (StringUtil.startsWith(zoneId, "UTC+") || StringUtil.startsWith(zoneId, "GMT+") || StringUtil.startsWith(zoneId, "UTC-") || StringUtil.startsWith(zoneId, "GMT-")) {
          var offset = ZoneOffset.of(zoneId.substring(3));
          return 0 === offset.totalSeconds() ? new ZoneRegion(zoneId.substring(0, 3), offset.rules()) : new ZoneRegion(zoneId.substring(0, 3) + offset.id(), offset.rules());
        }
        return StringUtil.startsWith(zoneId, "UT+") || StringUtil.startsWith(zoneId, "UT-") ? (zoneId = ZoneOffset.of(zoneId.substring(2)), 0 === zoneId.totalSeconds() ? new ZoneRegion("UT", zoneId.rules()) : new ZoneRegion("UT" + zoneId.id(), zoneId.rules())) : "SYSTEM" === zoneId ? ZoneId.systemDefault() : ZoneRegion.ofId(zoneId);
      };
      ZoneIdFactory.ofOffset = function(prefix, offset) {
        requireNonNull(prefix, "prefix");
        requireNonNull(offset, "offset");
        if (0 === prefix.length) {
          return offset;
        }
        if ("GMT" === prefix || "UTC" === prefix || "UT" === prefix) {
          return 0 === offset.totalSeconds() ? new ZoneRegion(prefix, offset.rules()) : new ZoneRegion(prefix + offset.id(), offset.rules());
        }
        throw new IllegalArgumentException("Invalid prefix, must be GMT, UTC or UT: " + prefix);
      };
      ZoneIdFactory.from = function(temporal) {
        requireNonNull(temporal, "temporal");
        var obj = temporal.query(TemporalQueries.zone());
        if (null == obj) {
          throw new DateTimeException("Unable to obtain ZoneId from TemporalAccessor: " + temporal + ", type " + (null != temporal.constructor ? temporal.constructor.name : ""));
        }
        return obj;
      };
      return ZoneIdFactory;
    }(), SYSTEM_DEFAULT_ZONE_ID_INSTANCE = null, isInit = !1;
    if (!isInit) {
      isInit = !0;
      YearConstants.MIN_VALUE = -999999;
      YearConstants.MAX_VALUE = 999999;
      Duration.ZERO = new Duration(0, 0);
      ChronoUnit.NANOS = new ChronoUnit("Nanos", Duration.ofNanos(1));
      ChronoUnit.MICROS = new ChronoUnit("Micros", Duration.ofNanos(1000));
      ChronoUnit.MILLIS = new ChronoUnit("Millis", Duration.ofNanos(1000000));
      ChronoUnit.SECONDS = new ChronoUnit("Seconds", Duration.ofSeconds(1));
      ChronoUnit.MINUTES = new ChronoUnit("Minutes", Duration.ofSeconds(60));
      ChronoUnit.HOURS = new ChronoUnit("Hours", Duration.ofSeconds(3600));
      ChronoUnit.HALF_DAYS = new ChronoUnit("HalfDays", Duration.ofSeconds(43200));
      ChronoUnit.DAYS = new ChronoUnit("Days", Duration.ofSeconds(86400));
      ChronoUnit.WEEKS = new ChronoUnit("Weeks", Duration.ofSeconds(604800));
      ChronoUnit.MONTHS = new ChronoUnit("Months", Duration.ofSeconds(2629746));
      ChronoUnit.YEARS = new ChronoUnit("Years", Duration.ofSeconds(31556952));
      ChronoUnit.DECADES = new ChronoUnit("Decades", Duration.ofSeconds(315569520));
      ChronoUnit.CENTURIES = new ChronoUnit("Centuries", Duration.ofSeconds(3155695200));
      ChronoUnit.MILLENNIA = new ChronoUnit("Millennia", Duration.ofSeconds(31556952E3));
      ChronoUnit.ERAS = new ChronoUnit("Eras", Duration.ofSeconds(31556952 * (YearConstants.MAX_VALUE + 1)));
      ChronoUnit.FOREVER = new ChronoUnit("Forever", Duration.ofSeconds(MathUtil.MAX_SAFE_INTEGER, 999999999));
      ChronoField.NANO_OF_SECOND = new ChronoField("NanoOfSecond", ChronoUnit.NANOS, ChronoUnit.SECONDS, ValueRange.of(0, 999999999));
      ChronoField.NANO_OF_DAY = new ChronoField("NanoOfDay", ChronoUnit.NANOS, ChronoUnit.DAYS, ValueRange.of(0, 864E11 - 1));
      ChronoField.MICRO_OF_SECOND = new ChronoField("MicroOfSecond", ChronoUnit.MICROS, ChronoUnit.SECONDS, ValueRange.of(0, 999999));
      ChronoField.MICRO_OF_DAY = new ChronoField("MicroOfDay", ChronoUnit.MICROS, ChronoUnit.DAYS, ValueRange.of(0, 864E8 - 1));
      ChronoField.MILLI_OF_SECOND = new ChronoField("MilliOfSecond", ChronoUnit.MILLIS, ChronoUnit.SECONDS, ValueRange.of(0, 999));
      ChronoField.MILLI_OF_DAY = new ChronoField("MilliOfDay", ChronoUnit.MILLIS, ChronoUnit.DAYS, ValueRange.of(0, 864E5 - 1));
      ChronoField.SECOND_OF_MINUTE = new ChronoField("SecondOfMinute", ChronoUnit.SECONDS, ChronoUnit.MINUTES, ValueRange.of(0, 59));
      ChronoField.SECOND_OF_DAY = new ChronoField("SecondOfDay", ChronoUnit.SECONDS, ChronoUnit.DAYS, ValueRange.of(0, 86399));
      ChronoField.MINUTE_OF_HOUR = new ChronoField("MinuteOfHour", ChronoUnit.MINUTES, ChronoUnit.HOURS, ValueRange.of(0, 59));
      ChronoField.MINUTE_OF_DAY = new ChronoField("MinuteOfDay", ChronoUnit.MINUTES, ChronoUnit.DAYS, ValueRange.of(0, 1439));
      ChronoField.HOUR_OF_AMPM = new ChronoField("HourOfAmPm", ChronoUnit.HOURS, ChronoUnit.HALF_DAYS, ValueRange.of(0, 11));
      ChronoField.CLOCK_HOUR_OF_AMPM = new ChronoField("ClockHourOfAmPm", ChronoUnit.HOURS, ChronoUnit.HALF_DAYS, ValueRange.of(1, 12));
      ChronoField.HOUR_OF_DAY = new ChronoField("HourOfDay", ChronoUnit.HOURS, ChronoUnit.DAYS, ValueRange.of(0, 23));
      ChronoField.CLOCK_HOUR_OF_DAY = new ChronoField("ClockHourOfDay", ChronoUnit.HOURS, ChronoUnit.DAYS, ValueRange.of(1, 24));
      ChronoField.AMPM_OF_DAY = new ChronoField("AmPmOfDay", ChronoUnit.HALF_DAYS, ChronoUnit.DAYS, ValueRange.of(0, 1));
      ChronoField.DAY_OF_WEEK = new ChronoField("DayOfWeek", ChronoUnit.DAYS, ChronoUnit.WEEKS, ValueRange.of(1, 7));
      ChronoField.ALIGNED_DAY_OF_WEEK_IN_MONTH = new ChronoField("AlignedDayOfWeekInMonth", ChronoUnit.DAYS, ChronoUnit.WEEKS, ValueRange.of(1, 7));
      ChronoField.ALIGNED_DAY_OF_WEEK_IN_YEAR = new ChronoField("AlignedDayOfWeekInYear", ChronoUnit.DAYS, ChronoUnit.WEEKS, ValueRange.of(1, 7));
      ChronoField.DAY_OF_MONTH = new ChronoField("DayOfMonth", ChronoUnit.DAYS, ChronoUnit.MONTHS, ValueRange.of(1, 28, 31), "day");
      ChronoField.DAY_OF_YEAR = new ChronoField("DayOfYear", ChronoUnit.DAYS, ChronoUnit.YEARS, ValueRange.of(1, 365, 366));
      ChronoField.EPOCH_DAY = new ChronoField("EpochDay", ChronoUnit.DAYS, ChronoUnit.FOREVER, ValueRange.of(Math.floor(365.25 * YearConstants.MIN_VALUE), Math.floor(365.25 * YearConstants.MAX_VALUE)));
      ChronoField.ALIGNED_WEEK_OF_MONTH = new ChronoField("AlignedWeekOfMonth", ChronoUnit.WEEKS, ChronoUnit.MONTHS, ValueRange.of(1, 4, 5));
      ChronoField.ALIGNED_WEEK_OF_YEAR = new ChronoField("AlignedWeekOfYear", ChronoUnit.WEEKS, ChronoUnit.YEARS, ValueRange.of(1, 53));
      ChronoField.MONTH_OF_YEAR = new ChronoField("MonthOfYear", ChronoUnit.MONTHS, ChronoUnit.YEARS, ValueRange.of(1, 12), "month");
      ChronoField.PROLEPTIC_MONTH = new ChronoField("ProlepticMonth", ChronoUnit.MONTHS, ChronoUnit.FOREVER, ValueRange.of(12 * YearConstants.MIN_VALUE, 12 * YearConstants.MAX_VALUE + 11));
      ChronoField.YEAR_OF_ERA = new ChronoField("YearOfEra", ChronoUnit.YEARS, ChronoUnit.FOREVER, ValueRange.of(1, YearConstants.MAX_VALUE, YearConstants.MAX_VALUE + 1));
      ChronoField.YEAR = new ChronoField("Year", ChronoUnit.YEARS, ChronoUnit.FOREVER, ValueRange.of(YearConstants.MIN_VALUE, YearConstants.MAX_VALUE), "year");
      ChronoField.ERA = new ChronoField("Era", ChronoUnit.ERAS, ChronoUnit.FOREVER, ValueRange.of(0, 1));
      ChronoField.INSTANT_SECONDS = new ChronoField("InstantSeconds", ChronoUnit.SECONDS, ChronoUnit.FOREVER, ValueRange.of(-9007199254740991, 9007199254740991));
      ChronoField.OFFSET_SECONDS = new ChronoField("OffsetSeconds", ChronoUnit.SECONDS, ChronoUnit.FOREVER, ValueRange.of(-64800, 64800));
      (function() {
        LocalTime.HOURS = [];
        for (var hour = 0; 24 > hour; hour++) {
          LocalTime.of(hour, 0, 0, 0);
        }
        LocalTime.MIN = LocalTime.HOURS[0];
        LocalTime.MAX = new LocalTime(23, 59, 59, 999999999);
        LocalTime.MIDNIGHT = LocalTime.HOURS[0];
        LocalTime.NOON = LocalTime.HOURS[12];
        LocalTime.FROM = createTemporalQuery("LocalTime.FROM", function(temporal) {
          return LocalTime.from(temporal);
        });
      })();
      (function() {
        DAY_OF_QUARTER = new DAY_OF_QUARTER_FIELD();
        QUARTER_OF_YEAR = new QUARTER_OF_YEAR_FIELD();
        WEEK_OF_WEEK_BASED_YEAR = new WEEK_OF_WEEK_BASED_YEAR_FIELD();
        WEEK_BASED_YEAR = new WEEK_BASED_YEAR_FIELD();
        WEEK_BASED_YEARS = new Unit("WeekBasedYears", Duration.ofSeconds(31556952));
        QUARTER_YEARS = new Unit("QuarterYears", Duration.ofSeconds(7889238));
        IsoFields.DAY_OF_QUARTER = DAY_OF_QUARTER;
        IsoFields.QUARTER_OF_YEAR = QUARTER_OF_YEAR;
        IsoFields.WEEK_OF_WEEK_BASED_YEAR = WEEK_OF_WEEK_BASED_YEAR;
        IsoFields.WEEK_BASED_YEAR = WEEK_BASED_YEAR;
        IsoFields.WEEK_BASED_YEARS = WEEK_BASED_YEARS;
        IsoFields.QUARTER_YEARS = QUARTER_YEARS;
        LocalDate.prototype.isoWeekOfWeekyear = function() {
          return this.get(IsoFields.WEEK_OF_WEEK_BASED_YEAR);
        };
        LocalDate.prototype.isoWeekyear = function() {
          return this.get(IsoFields.WEEK_BASED_YEAR);
        };
      })();
      (function() {
        TemporalQueries.ZONE_ID = createTemporalQuery("ZONE_ID", function(temporal) {
          return temporal.query(TemporalQueries.ZONE_ID);
        });
        TemporalQueries.CHRONO = createTemporalQuery("CHRONO", function(temporal) {
          return temporal.query(TemporalQueries.CHRONO);
        });
        TemporalQueries.PRECISION = createTemporalQuery("PRECISION", function(temporal) {
          return temporal.query(TemporalQueries.PRECISION);
        });
        TemporalQueries.OFFSET = createTemporalQuery("OFFSET", function(temporal) {
          return temporal.isSupported(ChronoField.OFFSET_SECONDS) ? ZoneOffset.ofTotalSeconds(temporal.get(ChronoField.OFFSET_SECONDS)) : null;
        });
        TemporalQueries.ZONE = createTemporalQuery("ZONE", function(temporal) {
          var zone = temporal.query(TemporalQueries.ZONE_ID);
          return null != zone ? zone : temporal.query(TemporalQueries.OFFSET);
        });
        TemporalQueries.LOCAL_DATE = createTemporalQuery("LOCAL_DATE", function(temporal) {
          return temporal.isSupported(ChronoField.EPOCH_DAY) ? LocalDate.ofEpochDay(temporal.getLong(ChronoField.EPOCH_DAY)) : null;
        });
        TemporalQueries.LOCAL_TIME = createTemporalQuery("LOCAL_TIME", function(temporal) {
          return temporal.isSupported(ChronoField.NANO_OF_DAY) ? LocalTime.ofNanoOfDay(temporal.getLong(ChronoField.NANO_OF_DAY)) : null;
        });
      })();
      (function() {
        DayOfWeek.MONDAY = new DayOfWeek(0, "MONDAY");
        DayOfWeek.TUESDAY = new DayOfWeek(1, "TUESDAY");
        DayOfWeek.WEDNESDAY = new DayOfWeek(2, "WEDNESDAY");
        DayOfWeek.THURSDAY = new DayOfWeek(3, "THURSDAY");
        DayOfWeek.FRIDAY = new DayOfWeek(4, "FRIDAY");
        DayOfWeek.SATURDAY = new DayOfWeek(5, "SATURDAY");
        DayOfWeek.SUNDAY = new DayOfWeek(6, "SUNDAY");
        DayOfWeek.FROM = createTemporalQuery("DayOfWeek.FROM", function(temporal) {
          return DayOfWeek.from(temporal);
        });
        ENUMS = [DayOfWeek.MONDAY, DayOfWeek.TUESDAY, DayOfWeek.WEDNESDAY, DayOfWeek.THURSDAY, DayOfWeek.FRIDAY, DayOfWeek.SATURDAY, DayOfWeek.SUNDAY];
      })();
      (function() {
        Instant.MIN_SECONDS = -31619119219200;
        Instant.MAX_SECONDS = 31494816403199;
        Instant.EPOCH = new Instant(0, 0);
        Instant.MIN = Instant.ofEpochSecond(Instant.MIN_SECONDS, 0);
        Instant.MAX = Instant.ofEpochSecond(Instant.MAX_SECONDS, 999999999);
        Instant.FROM = createTemporalQuery("Instant.FROM", function(temporal) {
          return Instant.from(temporal);
        });
      })();
      (function() {
        LocalDate.MIN = LocalDate.of(YearConstants.MIN_VALUE, 1, 1);
        LocalDate.MAX = LocalDate.of(YearConstants.MAX_VALUE, 12, 31);
        LocalDate.EPOCH_0 = LocalDate.ofEpochDay(0);
        LocalDate.FROM = createTemporalQuery("LocalDate.FROM", function(temporal) {
          return LocalDate.from(temporal);
        });
      })();
      (function() {
        LocalDateTime.MIN = LocalDateTime.of(LocalDate.MIN, LocalTime.MIN);
        LocalDateTime.MAX = LocalDateTime.of(LocalDate.MAX, LocalTime.MAX);
        LocalDateTime.FROM = createTemporalQuery("LocalDateTime.FROM", function(temporal) {
          return LocalDateTime.from(temporal);
        });
      })();
      (function() {
        Year.MIN_VALUE = YearConstants.MIN_VALUE;
        Year.MAX_VALUE = YearConstants.MAX_VALUE;
        PARSER$2 = (new DateTimeFormatterBuilder()).appendValue(ChronoField.YEAR, 4, 10, SignStyle.EXCEEDS_PAD).toFormatter();
        Year.FROM = createTemporalQuery("Year.FROM", function(temporal) {
          return Year.from(temporal);
        });
      })();
      Month.JANUARY = new Month(1, "JANUARY");
      Month.FEBRUARY = new Month(2, "FEBRUARY");
      Month.MARCH = new Month(3, "MARCH");
      Month.APRIL = new Month(4, "APRIL");
      Month.MAY = new Month(5, "MAY");
      Month.JUNE = new Month(6, "JUNE");
      Month.JULY = new Month(7, "JULY");
      Month.AUGUST = new Month(8, "AUGUST");
      Month.SEPTEMBER = new Month(9, "SEPTEMBER");
      Month.OCTOBER = new Month(10, "OCTOBER");
      Month.NOVEMBER = new Month(11, "NOVEMBER");
      Month.DECEMBER = new Month(12, "DECEMBER");
      var MONTHS = [Month.JANUARY, Month.FEBRUARY, Month.MARCH, Month.APRIL, Month.MAY, Month.JUNE, Month.JULY, Month.AUGUST, Month.SEPTEMBER, Month.OCTOBER, Month.NOVEMBER, Month.DECEMBER];
      (function() {
        PARSER$1 = (new DateTimeFormatterBuilder()).appendValue(ChronoField.YEAR, 4, 10, SignStyle.EXCEEDS_PAD).appendLiteral("-").appendValue(ChronoField.MONTH_OF_YEAR, 2).toFormatter();
        YearMonth.FROM = createTemporalQuery("YearMonth.FROM", function(temporal) {
          return YearMonth.from(temporal);
        });
      })();
      (function() {
        PARSER = (new DateTimeFormatterBuilder()).appendLiteral("--").appendValue(ChronoField.MONTH_OF_YEAR, 2).appendLiteral("-").appendValue(ChronoField.DAY_OF_MONTH, 2).toFormatter();
        MonthDay.FROM = createTemporalQuery("MonthDay.FROM", function(temporal) {
          return MonthDay.from(temporal);
        });
      })();
      Period.ofDays(0);
      ZoneOffset.MAX_SECONDS = 18 * LocalTime.SECONDS_PER_HOUR;
      ZoneOffset.UTC = ZoneOffset.ofTotalSeconds(0);
      ZoneOffset.MIN = ZoneOffset.ofTotalSeconds(-ZoneOffset.MAX_SECONDS);
      ZoneOffset.MAX = ZoneOffset.ofTotalSeconds(ZoneOffset.MAX_SECONDS);
      (function() {
        ZonedDateTime.FROM = createTemporalQuery("ZonedDateTime.FROM", function(temporal) {
          return ZonedDateTime.from(temporal);
        });
      })();
      SYSTEM_DEFAULT_ZONE_ID_INSTANCE = new SystemDefaultZoneId();
      ZoneId.systemDefault = ZoneIdFactory.systemDefault;
      ZoneId.getAvailableZoneIds = ZoneIdFactory.getAvailableZoneIds;
      ZoneId.of = ZoneIdFactory.of;
      ZoneId.ofOffset = ZoneIdFactory.ofOffset;
      ZoneId.from = ZoneIdFactory.from;
      ZoneOffset.from = ZoneIdFactory.from;
      ZoneId.SYSTEM = SYSTEM_DEFAULT_ZONE_ID_INSTANCE;
      ZoneId.UTC = ZoneOffset.ofTotalSeconds(0);
      IsoChronology.INSTANCE = new IsoChronology("IsoChronology");
      (function() {
        DateTimeFormatter.ISO_LOCAL_DATE = (new DateTimeFormatterBuilder()).appendValue(ChronoField.YEAR, 4, 10, SignStyle.EXCEEDS_PAD).appendLiteral("-").appendValue(ChronoField.MONTH_OF_YEAR, 2).appendLiteral("-").appendValue(ChronoField.DAY_OF_MONTH, 2).toFormatter(ResolverStyle.STRICT).withChronology(IsoChronology.INSTANCE);
        DateTimeFormatter.ISO_LOCAL_TIME = (new DateTimeFormatterBuilder()).appendValue(ChronoField.HOUR_OF_DAY, 2).appendLiteral(":").appendValue(ChronoField.MINUTE_OF_HOUR, 2).optionalStart().appendLiteral(":").appendValue(ChronoField.SECOND_OF_MINUTE, 2).optionalStart().appendFraction(ChronoField.NANO_OF_SECOND, 0, 9, !0).toFormatter(ResolverStyle.STRICT);
        DateTimeFormatter.ISO_LOCAL_DATE_TIME = (new DateTimeFormatterBuilder()).parseCaseInsensitive().append(DateTimeFormatter.ISO_LOCAL_DATE).appendLiteral("T").append(DateTimeFormatter.ISO_LOCAL_TIME).toFormatter(ResolverStyle.STRICT).withChronology(IsoChronology.INSTANCE);
        DateTimeFormatter.ISO_INSTANT = (new DateTimeFormatterBuilder()).parseCaseInsensitive().appendInstant().toFormatter(ResolverStyle.STRICT);
        DateTimeFormatter.ISO_OFFSET_DATE_TIME = (new DateTimeFormatterBuilder()).parseCaseInsensitive().append(DateTimeFormatter.ISO_LOCAL_DATE_TIME).appendOffsetId().toFormatter(ResolverStyle.STRICT).withChronology(IsoChronology.INSTANCE);
        DateTimeFormatter.ISO_ZONED_DATE_TIME = (new DateTimeFormatterBuilder()).append(DateTimeFormatter.ISO_OFFSET_DATE_TIME).optionalStart().appendLiteral("[").parseCaseSensitive().appendZoneId().appendLiteral("]").toFormatter(ResolverStyle.STRICT).withChronology(IsoChronology.INSTANCE);
        DateTimeFormatter.BASIC_ISO_DATE = (new DateTimeFormatterBuilder()).appendValue(ChronoField.YEAR, 4, 10, SignStyle.EXCEEDS_PAD).appendValue(ChronoField.MONTH_OF_YEAR, 2).appendValue(ChronoField.DAY_OF_MONTH, 2).toFormatter(ResolverStyle.STRICT).withChronology(IsoChronology.INSTANCE);
        DateTimeFormatter.ISO_OFFSET_DATE = (new DateTimeFormatterBuilder()).parseCaseInsensitive().append(DateTimeFormatter.ISO_LOCAL_DATE).appendOffsetId().toFormatter(ResolverStyle.STRICT).withChronology(IsoChronology.INSTANCE);
        DateTimeFormatter.ISO_OFFSET_TIME = (new DateTimeFormatterBuilder()).parseCaseInsensitive().append(DateTimeFormatter.ISO_LOCAL_TIME).appendOffsetId().toFormatter(ResolverStyle.STRICT).withChronology(IsoChronology.INSTANCE);
        DateTimeFormatter.ISO_ORDINAL_DATE = (new DateTimeFormatterBuilder()).appendValue(ChronoField.YEAR, 4, 10, SignStyle.EXCEEDS_PAD).appendLiteral("-").appendValue(ChronoField.DAY_OF_YEAR).toFormatter(ResolverStyle.STRICT);
        DateTimeFormatter.ISO_WEEK_DATE = (new DateTimeFormatterBuilder()).appendValue(ChronoField.YEAR, 4, 10, SignStyle.EXCEEDS_PAD).appendLiteral("-W").appendValue(ChronoField.ALIGNED_WEEK_OF_YEAR).appendLiteral("-").appendValue(ChronoField.DAY_OF_WEEK).toFormatter(ResolverStyle.STRICT);
        DateTimeFormatter.PARSED_EXCESS_DAYS = createTemporalQuery("PARSED_EXCESS_DAYS", function(temporal) {
          return temporal instanceof DateTimeBuilder ? temporal.excessDays : Period.ZERO;
        });
        DateTimeFormatter.PARSED_LEAP_SECOND = createTemporalQuery("PARSED_LEAP_SECOND", function(temporal) {
          return temporal instanceof DateTimeBuilder ? temporal.leapSecond : !1;
        });
      })();
      ReducedPrinterParser.BASE_DATE = LocalDate.of(2000, 1, 1);
      DateTimeFormatterBuilder.CompositePrinterParser = CompositePrinterParser;
      DateTimeFormatterBuilder.PadPrinterParserDecorator = PadPrinterParserDecorator;
      DateTimeFormatterBuilder.SettingsParser = SettingsParser;
      DateTimeFormatterBuilder.CharLiteralPrinterParser = StringLiteralPrinterParser;
      DateTimeFormatterBuilder.StringLiteralPrinterParser = StringLiteralPrinterParser;
      DateTimeFormatterBuilder.CharLiteralPrinterParser = CharLiteralPrinterParser;
      DateTimeFormatterBuilder.NumberPrinterParser = NumberPrinterParser;
      DateTimeFormatterBuilder.ReducedPrinterParser = ReducedPrinterParser;
      DateTimeFormatterBuilder.FractionPrinterParser = FractionPrinterParser;
      DateTimeFormatterBuilder.OffsetIdPrinterParser = OffsetIdPrinterParser;
      DateTimeFormatterBuilder.ZoneIdPrinterParser = ZoneIdPrinterParser;
      (function() {
        OffsetDateTime.MIN = LocalDateTime.MIN.atOffset(ZoneOffset.MAX);
        OffsetDateTime.MAX = LocalDateTime.MAX.atOffset(ZoneOffset.MIN);
        OffsetDateTime.FROM = createTemporalQuery("OffsetDateTime.FROM", function(temporal) {
          return OffsetDateTime.from(temporal);
        });
      })();
      (function() {
        OffsetTime.MIN = OffsetTime.ofNumbers(0, 0, 0, 0, ZoneOffset.MAX);
        OffsetTime.MAX = OffsetTime.ofNumbers(23, 59, 59, 999999999, ZoneOffset.MIN);
        OffsetTime.FROM = createTemporalQuery("OffsetTime.FROM", function(temporal) {
          return OffsetTime.from(temporal);
        });
      })();
    }
    var ToNativeJsConverter = function() {
      function ToNativeJsConverter(temporal, zone) {
        if (temporal instanceof LocalDate) {
          zone = null == zone ? ZoneId.systemDefault() : zone, temporal = temporal.atStartOfDay(zone);
        } else if (temporal instanceof LocalDateTime) {
          zone = null == zone ? ZoneId.systemDefault() : zone, temporal = temporal.atZone(zone);
        } else if (temporal instanceof ZonedDateTime) {
          temporal = null == zone ? temporal : temporal.withZoneSameInstant(zone);
        } else {
          throw new IllegalArgumentException("unsupported instance for convert operation:" + temporal);
        }
        this.instant = temporal.toInstant();
      }
      var _proto = ToNativeJsConverter.prototype;
      _proto.toDate = function() {
        return new Date(this.instant.toEpochMilli());
      };
      _proto.toEpochMilli = function() {
        return this.instant.toEpochMilli();
      };
      return ToNativeJsConverter;
    }(), NativeJsTemporal = function(_TemporalAccessor) {
      function NativeJsTemporal(date, zone) {
        void 0 === zone && (zone = ZoneId.systemDefault());
        var _this = _TemporalAccessor.call(this) || this;
        _this._zone = zone;
        if (date instanceof Date) {
          return _this._epochMilli = date.getTime(), _assertThisInitialized(_this);
        }
        if ("function" === typeof date.toDate && date.toDate() instanceof Date) {
          return _this._epochMilli = date.toDate().getTime(), _assertThisInitialized(_this);
        }
        assert(!1, "date must be either a javascript date or a moment");
        return _this;
      }
      _inheritsLoose(NativeJsTemporal, _TemporalAccessor);
      var _proto = NativeJsTemporal.prototype;
      _proto.query = function(_query) {
        requireNonNull(_query, "query");
        return _query === TemporalQueries.localDate() ? LocalDate.ofInstant(Instant.ofEpochMilli(this._epochMilli), this._zone) : _query === TemporalQueries.localTime() ? LocalTime.ofInstant(Instant.ofEpochMilli(this._epochMilli), this._zone) : _query === TemporalQueries.zone() ? this._zone : _TemporalAccessor.prototype.query.call(this, _query);
      };
      _proto.get = function(field) {
        return this.getLong(field);
      };
      _proto.getLong = function(field) {
        requireNonNull(field, "field");
        if (field instanceof ChronoField) {
          switch(field) {
            case ChronoField.NANO_OF_SECOND:
              return 1000000 * MathUtil.floorMod(this._epochMilli, 1000);
            case ChronoField.INSTANT_SECONDS:
              return MathUtil.floorDiv(this._epochMilli, 1000);
          }
          throw new UnsupportedTemporalTypeException("Unsupported field: " + field);
        }
        return field.getFrom(this);
      };
      _proto.isSupported = function(field) {
        return field === ChronoField.INSTANT_SECONDS || field === ChronoField.NANO_OF_SECOND;
      };
      return NativeJsTemporal;
    }(TemporalAccessor), _ = {assert:assert$1, DateTimeBuilder, DateTimeParseContext, DateTimePrintContext, MathUtil, StringUtil, StringBuilder}, jsJodaExports = {_, convert, nativeJs, ArithmeticException, DateTimeException, DateTimeParseException, IllegalArgumentException, IllegalStateException, UnsupportedTemporalTypeException, NullPointerException, Clock, DayOfWeek, Duration, Instant, LocalDate, LocalTime, LocalDateTime, OffsetTime, OffsetDateTime, Month, MonthDay, Period, Year, YearConstants, 
    YearMonth, ZonedDateTime, ZoneOffset, ZoneId, ZoneRegion, ZoneOffsetTransition, ZoneRules, ZoneRulesProvider, ChronoLocalDate, ChronoLocalDateTime, ChronoZonedDateTime, IsoChronology, ChronoField, ChronoUnit, IsoFields, Temporal, TemporalAccessor, TemporalAdjuster, TemporalAdjusters, TemporalAmount, TemporalField, TemporalQueries, TemporalQuery, TemporalUnit, ValueRange, DateTimeFormatter, DateTimeFormatterBuilder, DecimalStyle, ResolverStyle, SignStyle, TextStyle}, use = function(jsJoda) {
      var used = [];
      return function(fn) {
        ~used.indexOf(fn) || (fn(jsJoda), used.push(fn));
        return jsJoda;
      };
    }(jsJodaExports);
    jsJodaExports.use = use;
    exports.ArithmeticException = ArithmeticException;
    exports.ChronoField = ChronoField;
    exports.ChronoLocalDate = ChronoLocalDate;
    exports.ChronoLocalDateTime = ChronoLocalDateTime;
    exports.ChronoUnit = ChronoUnit;
    exports.ChronoZonedDateTime = ChronoZonedDateTime;
    exports.Clock = Clock;
    exports.DateTimeException = DateTimeException;
    exports.DateTimeFormatter = DateTimeFormatter;
    exports.DateTimeFormatterBuilder = DateTimeFormatterBuilder;
    exports.DateTimeParseException = DateTimeParseException;
    exports.DayOfWeek = DayOfWeek;
    exports.DecimalStyle = DecimalStyle;
    exports.Duration = Duration;
    exports.IllegalArgumentException = IllegalArgumentException;
    exports.IllegalStateException = IllegalStateException;
    exports.Instant = Instant;
    exports.IsoChronology = IsoChronology;
    exports.IsoFields = IsoFields;
    exports.LocalDate = LocalDate;
    exports.LocalDateTime = LocalDateTime;
    exports.LocalTime = LocalTime;
    exports.Month = Month;
    exports.MonthDay = MonthDay;
    exports.NullPointerException = NullPointerException;
    exports.OffsetDateTime = OffsetDateTime;
    exports.OffsetTime = OffsetTime;
    exports.Period = Period;
    exports.ResolverStyle = ResolverStyle;
    exports.SignStyle = SignStyle;
    exports.Temporal = Temporal;
    exports.TemporalAccessor = TemporalAccessor;
    exports.TemporalAdjuster = TemporalAdjuster;
    exports.TemporalAdjusters = TemporalAdjusters;
    exports.TemporalAmount = TemporalAmount;
    exports.TemporalField = TemporalField;
    exports.TemporalQueries = TemporalQueries;
    exports.TemporalQuery = TemporalQuery;
    exports.TemporalUnit = TemporalUnit;
    exports.TextStyle = TextStyle;
    exports.UnsupportedTemporalTypeException = UnsupportedTemporalTypeException;
    exports.ValueRange = ValueRange;
    exports.Year = Year;
    exports.YearConstants = YearConstants;
    exports.YearMonth = YearMonth;
    exports.ZoneId = ZoneId;
    exports.ZoneOffset = ZoneOffset;
    exports.ZoneOffsetTransition = ZoneOffsetTransition;
    exports.ZoneRegion = ZoneRegion;
    exports.ZoneRules = ZoneRules;
    exports.ZoneRulesProvider = ZoneRulesProvider;
    exports.ZonedDateTime = ZonedDateTime;
    exports._ = _;
    exports.convert = convert;
    exports.nativeJs = nativeJs;
    exports.use = use;
    Object.defineProperty(exports, "__esModule", {value:!0});
  });
};

//# sourceMappingURL=module$node_modules$$js_joda$core$dist$js_joda.js.map
