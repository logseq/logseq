(ns frontend.state.search
  "Search and reactive query state accessors."
  (:require [frontend.state :as state]))

(def set-search-mode! state/set-search-mode!)
(def set-search-result! state/set-search-result!)
